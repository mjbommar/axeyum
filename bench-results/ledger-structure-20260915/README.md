# Ledger structure — does the ledger show a dispatch order worth deriving? (in progress)

**2026-09-15, lane LEDGER-STRUCTURE.** Phase 3 sizing for Phase 4 of
`docs/plan/dispatch-and-instrumentation-2026-09-15.md`: fill the outcome
ledger (ADR-2102, ADR-2105) over the seven Tier 1 pinned 200-file lists on
commit `db31113fa`, then ask, per feature class and per division, whether one
route decides a large majority of what gets decided while a DIFFERENT route is
tried first. Sweep is running; this file is a placeholder committed early per
this repository's rule, and will be replaced with the full tables and verdict.
