# axeyum-verify-macros

Proc-macro implementation companion for `axeyum-verify`. The parent crate
re-exports the public attributes and documents the admitted Rust surface,
bounded semantics, witness replay, and result contract:
[`axeyum-verify`](../axeyum-verify/README.md).

```sh
cargo test -p axeyum-verify-macros
```
