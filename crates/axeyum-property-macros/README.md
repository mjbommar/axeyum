# axeyum-property-macros

Proc-macro implementation companion for `axeyum-property`. Consumers should
depend on and import the derive re-export from
[`axeyum-property`](../axeyum-property/README.md), which owns the public typed
property and counterexample contract.

```sh
cargo test -p axeyum-property-macros
```
