//! Exact polynomial EXTREMUM (ADR-0603 row 3): the Extreme Value Theorem on
//! the decidable fragment — placeholder, being built out in this lane.

use axeyum_ir::Rational;

/// Placeholder marker so the module compiles while the certificate lands.
#[must_use]
pub fn _placeholder(p: &[Rational]) -> usize {
    p.len()
}
