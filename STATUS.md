# Status moved to PLAN.md

The canonical project status, ordered work queue, blockers, and resume protocol
are all in [`PLAN.md`](PLAN.md). Update that file only.

This compatibility pointer exists because historical documents and external
links refer to `STATUS.md`. It must not acquire a second current-focus section,
phase table, changelog, or task queue.

The pre-consolidation status journal is preserved immutably at Git revision
`803c08439` as blob `0f169eddfabb190f45428090ffb057e8289bf355`.
