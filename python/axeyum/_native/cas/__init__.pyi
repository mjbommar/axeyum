# This file is automatically generated by pyo3_stub_gen
# ruff: noqa: E501, F401, F403, F405

from axeyum import _native
import builtins
import enum
import fractions
import typing
from . import certify
__all__ = [
    "Assumptions",
    "CasError",
    "Certainty",
    "CertifiedIntegral",
    "DefiniteIntegral",
    "Expr",
    "Gf2Error",
    "LimitPoint",
    "Matrix",
    "Monomial",
    "MultiPoly",
    "MvPoly",
    "Rational",
    "RealInterval",
    "Sign",
    "ZeroTest",
    "apart",
    "apply_initial_conditions",
    "approximate_real_roots",
    "argument",
    "cancel",
    "certify",
    "characteristic_polynomial",
    "coeff",
    "collect",
    "companion_matrix",
    "conjugate",
    "content",
    "count_real_roots",
    "cyclotomic_polynomial",
    "definite_integrate",
    "definite_sum",
    "degree",
    "diagonalize",
    "discriminant",
    "divergence",
    "dsolve_bernoulli",
    "dsolve_euler_cauchy",
    "dsolve_exact",
    "dsolve_first_order_linear",
    "dsolve_homogeneous",
    "dsolve_inhomogeneous",
    "dsolve_separable",
    "eigenvalues",
    "eigenvectors",
    "equal",
    "evalf",
    "evaluate_trig",
    "expand",
    "expand_log",
    "expand_trig",
    "factor",
    "factor_expr",
    "finite_product",
    "gosper_sum",
    "gradient",
    "hessian",
    "imaginary_part",
    "infinite_sum",
    "integrate",
    "is_irreducible",
    "jacobian",
    "laplacian",
    "leading_coeff",
    "limit",
    "logcombine",
    "matrix_rank",
    "minimal_polynomial",
    "modulus",
    "normalize",
    "nsimplify",
    "poly_div",
    "poly_gcd",
    "poly_lcm",
    "primitive_part",
    "prove_derivative",
    "rationalize",
    "real_part",
    "real_root_intervals",
    "residue",
    "resultant",
    "rewrite_exp",
    "sample_standard_deviation",
    "series",
    "series_at",
    "simplify",
    "simplify_radicals",
    "simplify_under_assumptions",
    "solve",
    "solve_linear_system",
    "solve_polynomial_inequality",
    "solve_polynomial_system",
    "solve_recurrence",
    "standard_deviation",
    "sum_polynomial",
    "trace",
    "trigsimp",
    "wronskian",
]

@typing.final
class Assumptions:
    r"""
    A set of sign assumptions about named variables.
    
    Every builder returns a **new** context, mirroring the Rust consuming
    builder; nothing here mutates in place.
    """
    def __new__(cls) -> Assumptions:
        r"""
        An empty assumption context.
        """
    def positive(self, var: builtins.str) -> Assumptions:
        r"""
        This context plus `var > 0`.
        """
    def negative(self, var: builtins.str) -> Assumptions:
        r"""
        This context plus `var < 0`.
        """
    def nonnegative(self, var: builtins.str) -> Assumptions:
        r"""
        This context plus `var >= 0`.
        """
    def nonzero(self, var: builtins.str) -> Assumptions:
        r"""
        This context plus `var != 0`.
        """
    def sign_of(self, expr: Expr) -> Sign:
        r"""
        The sign this context establishes for `expr`.
        """
    def is_positive(self, expr: Expr) -> builtins.bool:
        r"""
        Whether this context establishes `expr > 0`.
        """
    def is_nonnegative(self, expr: Expr) -> builtins.bool:
        r"""
        Whether this context establishes `expr >= 0`.
        """
    def is_nonzero(self, expr: Expr) -> builtins.bool:
        r"""
        Whether this context establishes `expr != 0`.
        """
    def __repr__(self) -> builtins.str: ...

class CasError(_native.AxeyumError):
    r"""
    A CAS artifact was malformed, or a checker discharged no obligation.
    """
    ...

@typing.final
class CertifiedIntegral:
    r"""
    An antiderivative that carries its own proof.
    
    The `certificate` is `equal(d(antiderivative)/dvar, integrand)` — a
    first-class [`ZeroTest`], not a bool, so a caller can inspect the witness
    rather than trust the flag.
    """
    @property
    def antiderivative(self) -> Expr:
        r"""
        The computed antiderivative `F` (up to `+ C`).
        """
    @property
    def certificate(self) -> ZeroTest:
        r"""
        The differentiate-and-check certificate.
        """
    def is_certified(self) -> builtins.bool:
        r"""
        Whether the certificate decided the obligation as an exact equality.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class DefiniteIntegral:
    r"""
    A definite integral, with the antiderivative and certificate it came from.
    """
    @property
    def value(self) -> Expr:
        r"""
        The evaluated value `F(b) - F(a)`.
        """
    @property
    def antiderivative(self) -> Expr:
        r"""
        The antiderivative used.
        """
    @property
    def certificate(self) -> ZeroTest:
        r"""
        The certificate carried over from the indefinite integral.
        """
    def is_certified(self) -> builtins.bool:
        r"""
        Whether the underlying antiderivative was certified.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class Expr:
    @staticmethod
    def int(n: builtins.int) -> Expr:
        r"""
        An exact integer constant.
        """
    @staticmethod
    def rat(num: builtins.int, den: builtins.int = 1) -> Expr:
        r"""
        An exact rational constant `num / den`.
        
        # Errors
        
        Raises `ValueError` when `den` is zero. The Rust `CasExpr::rat` *panics*
        there; this goes through `Rational::checked_new` instead.
        """
    @staticmethod
    def var(name: builtins.str) -> Expr:
        r"""
        A named variable.
        """
    @staticmethod
    def zero() -> Expr:
        r"""
        The constant `0`.
        """
    @staticmethod
    def one() -> Expr:
        r"""
        The constant `1`.
        """
    @staticmethod
    def imaginary_unit() -> Expr:
        r"""
        The imaginary unit `i`.
        """
    def pow(self, exp: builtins.int) -> Expr:
        r"""
        `self ** exp` for a non-negative integer `exp`.
        
        The exponent is a `u32`: the fragment has no negative or symbolic powers.
        """
    def nth_root(self, q: builtins.int) -> Expr:
        r"""
        The principal `q`-th root `self ** (1/q)`.
        """
    def polygamma(self, n: builtins.int) -> Expr:
        r"""
        The polygamma function of order `n`.
        """
    def bessel_j(self, n: builtins.int) -> Expr:
        r"""
        The Bessel function of the first kind, order `n`.
        """
    def bessel_i(self, n: builtins.int) -> Expr:
        r"""
        The modified Bessel function of the first kind, order `n`.
        """
    def ln(self) -> Expr:
        r"""
        Natural logarithm.
        
        The Rust builder consumes `self`, so this clones.
        """
    def exp(self) -> Expr:
        r"""
        Exponential.
        
        The Rust builder consumes `self`, so this clones.
        """
    def sin(self) -> Expr:
        r"""
        Sine.
        
        The Rust builder consumes `self`, so this clones.
        """
    def cos(self) -> Expr:
        r"""
        Cosine.
        
        The Rust builder consumes `self`, so this clones.
        """
    def tan(self) -> Expr:
        r"""
        Tangent.
        
        The Rust builder consumes `self`, so this clones.
        """
    def atan(self) -> Expr:
        r"""
        Arctangent.
        
        The Rust builder consumes `self`, so this clones.
        """
    def sqrt(self) -> Expr:
        r"""
        Principal square root.
        
        The Rust builder consumes `self`, so this clones.
        """
    def cbrt(self) -> Expr:
        r"""
        Principal cube root.
        
        The Rust builder consumes `self`, so this clones.
        """
    def airy_ai(self) -> Expr:
        r"""
        The Airy function `Ai`.
        
        The Rust builder consumes `self`, so this clones.
        """
    def airy_bi(self) -> Expr:
        r"""
        The Airy function `Bi`.
        
        The Rust builder consumes `self`, so this clones.
        """
    def lambert_w(self) -> Expr:
        r"""
        The principal branch of the Lambert `W` function.
        
        The Rust builder consumes `self`, so this clones.
        """
    def erf(self) -> Expr:
        r"""
        The error function.
        
        The Rust builder consumes `self`, so this clones.
        """
    def gamma(self) -> Expr:
        r"""
        The gamma function.
        
        The Rust builder consumes `self`, so this clones.
        """
    def digamma(self) -> Expr:
        r"""
        The digamma function.
        
        The Rust builder consumes `self`, so this clones.
        """
    def factorial(self) -> Expr:
        r"""
        The factorial, as `Gamma(x + 1)`.
        
        The Rust builder consumes `self`, so this clones.
        """
    def si(self) -> Expr:
        r"""
        The sine integral `Si`.
        
        The Rust builder consumes `self`, so this clones.
        """
    def ci(self) -> Expr:
        r"""
        The cosine integral `Ci`.
        
        The Rust builder consumes `self`, so this clones.
        """
    def ei(self) -> Expr:
        r"""
        The exponential integral `Ei`.
        
        The Rust builder consumes `self`, so this clones.
        """
    def li(self) -> Expr:
        r"""
        The logarithmic integral `li`.
        
        The Rust builder consumes `self`, so this clones.
        """
    def abs(self) -> Expr:
        r"""
        Absolute value.
        
        The Rust builder consumes `self`, so this clones.
        """
    def sign(self) -> Expr:
        r"""
        The sign function.
        
        The Rust builder consumes `self`, so this clones.
        """
    def floor(self) -> Expr:
        r"""
        The floor function.
        
        The Rust builder consumes `self`, so this clones.
        """
    def ceiling(self) -> Expr:
        r"""
        The ceiling function.
        
        The Rust builder consumes `self`, so this clones.
        """
    def differentiate(self, var: builtins.str) -> Expr:
        r"""
        The exact derivative with respect to `var`. Total: always succeeds.
        """
    def differentiate_n(self, var: builtins.str, n: builtins.int) -> Expr:
        r"""
        The `n`-th derivative with respect to `var`.
        """
    def substitute(self, var: builtins.str, replacement: Expr) -> Expr:
        r"""
        `self` with every occurrence of `var` replaced by `replacement`.
        """
    def eval(self, env: dict) -> typing.Optional[fractions.Fraction]:
        r"""
        Exact evaluation under `env`, or `None`.
        
        `None` means an unbound variable or an `i128` overflow — an honest
        undecided, never an error.
        
        # Errors
        
        Raises `ValueError` when a binding is not a rational.
        """
    def variables(self) -> builtins.list[builtins.str]:
        r"""
        The set of variables this expression mentions, sorted.
        """
    def __add__(self, other: Expr  |  builtins.int  |  fractions.Fraction) -> Expr: ...
    def __radd__(self, other: Expr  |  builtins.int  |  fractions.Fraction) -> Expr: ...
    def __sub__(self, other: Expr  |  builtins.int  |  fractions.Fraction) -> Expr: ...
    def __rsub__(self, other: Expr  |  builtins.int  |  fractions.Fraction) -> Expr: ...
    def __mul__(self, other: Expr  |  builtins.int  |  fractions.Fraction) -> Expr: ...
    def __rmul__(self, other: Expr  |  builtins.int  |  fractions.Fraction) -> Expr: ...
    def __truediv__(self, other: Expr  |  builtins.int  |  fractions.Fraction) -> Expr: ...
    def __rtruediv__(self, other: Expr  |  builtins.int  |  fractions.Fraction) -> Expr: ...
    def __neg__(self) -> Expr: ...
    def __pow__(self, exponent: builtins.int, modulo: typing.Optional[typing.Any] = None) -> Expr:
        r"""
        `self ** exp`; the three-argument (modular) form is not defined here.
        
        # Errors
        
        Raises `TypeError` when a modulus is supplied.
        """
    def __eq__(self, other: typing.Any) -> builtins.bool: ...
    def __hash__(self) -> builtins.int: ...
    def __str__(self) -> builtins.str:
        r"""
        The rendered expression. **Not** parseable back into an `Expr`.
        """
    def __repr__(self) -> builtins.str: ...

class Gf2Error(CasError):
    r"""
    A GF(2) budget, shape, or certificate-structure refusal.
    """
    ...

@typing.final
class LimitPoint:
    r"""
    The point a limit is taken at.
    """
    @staticmethod
    def finite(value: builtins.int  |  fractions.Fraction  |  Rational) -> LimitPoint:
        r"""
        A finite rational point.
        
        # Errors
        
        Raises `ValueError` when `value` is not an exact rational.
        """
    @staticmethod
    def pos_infinity() -> LimitPoint:
        r"""
        `x -> +infinity`.
        """
    @staticmethod
    def neg_infinity() -> LimitPoint:
        r"""
        `x -> -infinity`.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class Matrix:
    r"""
    A dense matrix of expressions.
    """
    @property
    def rows(self) -> builtins.int:
        r"""
        The number of rows.
        """
    @property
    def cols(self) -> builtins.int:
        r"""
        The number of columns.
        """
    @staticmethod
    def from_rows(rows: typing.Sequence[typing.Sequence[Expr]]) -> typing.Optional[Matrix]:
        r"""
        A matrix from a list of rows, or `None` when the rows are ragged.
        
        # Errors
        
        Propagates the per-element extraction error.
        """
    @staticmethod
    def identity(n: builtins.int) -> Matrix:
        r"""
        The `n x n` identity.
        """
    @staticmethod
    def zeros(rows: builtins.int, cols: builtins.int) -> Matrix:
        r"""
        An all-zero matrix.
        """
    def get(self, row: builtins.int, col: builtins.int) -> typing.Optional[Expr]:
        r"""
        The entry at `(row, col)`, or `None` when out of range.
        """
    def transpose(self) -> Matrix:
        r"""
        The transpose.
        """
    def add(self, other: Matrix) -> typing.Optional[Matrix]:
        r"""
        `self + other`, or `None` on a shape mismatch.
        """
    def sub(self, other: Matrix) -> typing.Optional[Matrix]:
        r"""
        `self - other`, or `None` on a shape mismatch.
        """
    def mul(self, other: Matrix) -> typing.Optional[Matrix]:
        r"""
        `self @ other`, or `None` on a shape mismatch.
        """
    def pow(self, exponent: builtins.int) -> typing.Optional[Matrix]:
        r"""
        `self ** exponent`, or `None`.
        """
    def determinant(self) -> typing.Optional[Expr]:
        r"""
        The determinant, or `None`.
        """
    def rref(self) -> typing.Optional[Matrix]:
        r"""
        The reduced row echelon form, or `None`.
        """
    def null_space(self) -> typing.Optional[builtins.list[Matrix]]:
        r"""
        A basis of the null space, or `None`.
        """
    def __eq__(self, other: typing.Any) -> builtins.bool: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class Monomial:
    r"""
    A monomial: a product of variable powers, with no coefficient.
    """
    @staticmethod
    def one() -> Monomial:
        r"""
        The empty monomial `1`.
        """
    @staticmethod
    def from_powers(factors: typing.Sequence[tuple[builtins.str, builtins.int]]) -> Monomial:
        r"""
        A monomial from `[(variable, exponent), ...]`.
        
        # Errors
        
        Propagates the per-element extraction error.
        """
    def total_degree(self) -> builtins.int:
        r"""
        The sum of the exponents.
        """
    def exponent_of(self, var: builtins.str) -> builtins.int:
        r"""
        The exponent of `var`, or `0`.
        """
    def powers(self) -> builtins.list[tuple[builtins.str, builtins.int]]:
        r"""
        `[(variable, exponent), ...]`, collected to an owned list.
        
        The Rust iterator borrows the monomial, so it cannot cross the boundary
        lazily (inventory §0.3).
        """
    def __eq__(self, other: typing.Any) -> builtins.bool: ...
    def __hash__(self) -> builtins.int: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class MultiPoly:
    r"""
    The canonical polynomial normal form [`crate::cas::normalize`] produces.
    
    Reachable only through `normalize` and through a [`crate::cas::expr::ZeroTest`]
    witness: the Rust type has no public constructor from terms.
    """
    @staticmethod
    def zero() -> MultiPoly:
        r"""
        The zero normal form.
        """
    def is_zero(self) -> builtins.bool:
        r"""
        Whether this is the zero polynomial.
        """
    def to_univariate(self, var: builtins.str) -> typing.Optional[builtins.list[fractions.Fraction]]:
        r"""
        The dense coefficient list in `var`, lowest degree first, or `None` when
        the polynomial is not univariate in `var`.
        
        # Errors
        
        Propagates any Python error raised while building the fractions.
        """
    def to_expr(self) -> Expr:
        r"""
        The same polynomial as an [`Expr`].
        """
    def eval(self, env: dict) -> typing.Optional[fractions.Fraction]:
        r"""
        Exact evaluation under `env`, or `None`.
        
        # Errors
        
        Raises `ValueError` when a binding is not an exact rational.
        """
    def __eq__(self, other: typing.Any) -> builtins.bool: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class MvPoly:
    r"""
    A multivariate polynomial over the exact rationals.
    """
    @staticmethod
    def zero() -> MvPoly:
        r"""
        The zero polynomial.
        """
    @staticmethod
    def constant(value: builtins.int  |  fractions.Fraction  |  Rational) -> MvPoly:
        r"""
        A constant polynomial.
        
        # Errors
        
        Raises `ValueError` when `value` is not an exact rational.
        """
    @staticmethod
    def var(name: builtins.str) -> MvPoly:
        r"""
        The polynomial `var`.
        """
    @staticmethod
    def from_terms(terms: typing.Sequence[tuple[Monomial, builtins.int  |  fractions.Fraction  |  Rational]]) -> typing.Optional[MvPoly]:
        r"""
        A polynomial from `[(Monomial, coefficient), ...]`, or `None` on
        `i128` overflow.
        
        # Errors
        
        Raises `ValueError` when a coefficient is not an exact rational.
        """
    def is_zero(self) -> builtins.bool:
        r"""
        Whether every coefficient is zero.
        """
    def term_count(self) -> builtins.int:
        r"""
        The number of nonzero terms.
        """
    def terms(self) -> builtins.list[tuple[Monomial, fractions.Fraction]]:
        r"""
        `[(Monomial, Fraction), ...]`, collected to an owned list.
        
        # Errors
        
        Propagates any Python error raised while building the fractions.
        """
    def variables(self) -> builtins.list[builtins.str]:
        r"""
        The variables this polynomial mentions, sorted.
        """
    def degree_in(self, var: builtins.str) -> builtins.int:
        r"""
        The highest exponent of `var`.
        """
    def total_degree(self) -> builtins.int:
        r"""
        The highest total degree of any term.
        """
    def leading_coeff(self, main_var: builtins.str) -> MvPoly:
        r"""
        The leading coefficient with respect to `main_var`, as a polynomial.
        """
    def add(self, other: MvPoly) -> typing.Optional[MvPoly]:
        r"""
        `self + other`, or `None` on `i128` coefficient overflow.
        """
    def neg(self) -> typing.Optional[MvPoly]:
        r"""
        `-self`, or `None` on overflow.
        """
    def sub(self, other: MvPoly) -> typing.Optional[MvPoly]:
        r"""
        `self - other`, or `None` on overflow.
        """
    def mul(self, other: MvPoly) -> typing.Optional[MvPoly]:
        r"""
        `self * other`, or `None` on overflow.
        """
    def pow(self, exp: builtins.int) -> typing.Optional[MvPoly]:
        r"""
        `self ** exp`, or `None` on overflow.
        """
    def derivative_in(self, var: builtins.str) -> typing.Optional[MvPoly]:
        r"""
        The partial derivative in `var`, or `None` on overflow.
        """
    def evaluate(self, assignment: dict) -> typing.Optional[fractions.Fraction]:
        r"""
        Exact evaluation under `assignment`, or `None` on an unbound variable or
        overflow.
        
        # Errors
        
        Raises `ValueError` when a binding is not an exact rational.
        """
    def divide(self, divisor: MvPoly) -> typing.Optional[tuple[MvPoly, MvPoly]]:
        r"""
        `(quotient, remainder)` of division by `divisor`, or `None`.
        """
    def divides(self, other: MvPoly) -> typing.Optional[builtins.bool]:
        r"""
        Whether `self` divides `other` exactly; `None` when undecided.
        """
    def exact_div(self, divisor: MvPoly) -> typing.Optional[MvPoly]:
        r"""
        `self / divisor` when the division is exact, else `None`.
        """
    def gcd(self, other: MvPoly) -> typing.Optional[MvPoly]:
        r"""
        The greatest common divisor, or `None`.
        """
    def squarefree(self, main_var: builtins.str) -> typing.Optional[builtins.list[tuple[MvPoly, builtins.int]]]:
        r"""
        The squarefree decomposition in `main_var` as `[(factor, multiplicity)]`,
        or `None`.
        """
    def to_expr(self) -> Expr:
        r"""
        The same polynomial as an [`Expr`].
        """
    @staticmethod
    def from_expr(expr: Expr) -> typing.Optional[MvPoly]:
        r"""
        The polynomial an expression denotes, or `None` when it is outside the
        polynomial fragment (or overflows).
        """
    def __add__(self, other: MvPoly) -> typing.Optional[MvPoly]:
        r"""
        `self + other`; **`None` on overflow**, mirroring `add`.
        """
    def __sub__(self, other: MvPoly) -> typing.Optional[MvPoly]:
        r"""
        `self - other`; **`None` on overflow**, mirroring `sub`.
        """
    def __mul__(self, other: MvPoly) -> typing.Optional[MvPoly]:
        r"""
        `self * other`; **`None` on overflow**, mirroring `mul`.
        """
    def __neg__(self) -> typing.Optional[MvPoly]:
        r"""
        `-self`; **`None` on overflow**, mirroring `neg`.
        """
    def __eq__(self, other: typing.Any) -> builtins.bool: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class Rational:
    r"""
    An exact rational number, normalized, with an `i128` numerator and
    denominator.
    
    Constructing one with a zero denominator raises `ValueError`:
    `axeyum_ir::Rational::new` panics there, so the binding goes through
    `checked_new` (inventory §0.6).
    """
    @property
    def numerator(self) -> builtins.int:
        r"""
        The normalized numerator (carries the sign).
        """
    @property
    def denominator(self) -> builtins.int:
        r"""
        The normalized denominator (always positive).
        """
    def __new__(cls, num: builtins.int, den: builtins.int = 1) -> Rational:
        r"""
        `Rational(num, den=1)`.
        """
    def is_integer(self) -> builtins.bool:
        r"""
        Whether the denominator is `1`.
        """
    def is_zero(self) -> builtins.bool:
        r"""
        Whether the value is exactly zero.
        """
    def to_fraction(self) -> fractions.Fraction:
        r"""
        The same value as a `fractions.Fraction`.
        
        # Errors
        
        Propagates any Python error raised while importing `fractions`.
        """
    @staticmethod
    def coerce(value: builtins.int  |  fractions.Fraction  |  Rational) -> Rational:
        r"""
        Builds a [`Rational`] from anything with `numerator`/`denominator` —
        a Python `int`, a `fractions.Fraction`, or another [`Rational`].
        
        # Errors
        
        Raises `ValueError` when the pair does not fit in `i128` or the
        denominator is zero, and `TypeError` for an object with no such fields.
        """
    def __repr__(self) -> builtins.str: ...
    def __str__(self) -> builtins.str: ...
    def __eq__(self, other: typing.Any) -> builtins.bool: ...
    def __hash__(self) -> builtins.int: ...

@typing.final
class RealInterval:
    r"""
    A real interval with rational or infinite endpoints.
    """
    @property
    def lower(self) -> typing.Optional[fractions.Fraction]:
        r"""
        The lower endpoint, or `None` for `-infinity`.
        
        # Errors
        
        Propagates any Python error raised while building the fraction.
        """
    @property
    def lower_closed(self) -> builtins.bool:
        r"""
        Whether the lower endpoint is included.
        """
    @property
    def upper(self) -> typing.Optional[fractions.Fraction]:
        r"""
        The upper endpoint, or `None` for `+infinity`.
        
        # Errors
        
        Propagates any Python error raised while building the fraction.
        """
    @property
    def upper_closed(self) -> builtins.bool:
        r"""
        Whether the upper endpoint is included.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class ZeroTest:
    r"""
    The result of a decidable zero test — the CAS's smallest certificate.
    
    `Certified` carries the *witness*: the difference `a - b` in canonical form.
    Re-normalizing that difference is the independent re-check, which is why the
    witness is exposed rather than folded into a bool. `Unknown` is an `i128`
    overflow — an honest undecided, never a wrong answer.
    """
    @property
    def kind(self) -> builtins.str:
        r"""
        `"certified"` or `"unknown"` — the variant tag.
        """
    @property
    def equal(self) -> typing.Optional[builtins.bool]:
        r"""
        Whether the two expressions were decided **equal**.
        
        `None` on `Unknown`: an undecided test is not a `False`.
        """
    @property
    def witness(self) -> typing.Optional[MultiPoly]:
        r"""
        The witness: the difference `a - b` in canonical form.
        
        `None` on `Unknown`. This is the certificate — re-normalize the
        difference yourself and confirm it agrees.
        """
    def is_decided(self) -> builtins.bool:
        r"""
        Whether the test decided at all (as opposed to overflowing).
        """
    def certainty(self) -> Certainty:
        r"""
        The trust tag for this answer.
        """
    def __eq__(self, other: typing.Any) -> builtins.bool: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class Certainty(enum.Enum):
    r"""
    How much trust an answer carries.
    
    `Certified` means a checkable witness is attached and the answer re-checks
    independently; the other two do not, and collapsing them would be the exact
    mistake this project treats as a defect.
    """
    Certified = ...
    r"""
    A checkable witness is attached.
    """
    DecidableUncertified = ...
    r"""
    A complete algorithm decided it, but emitted no witness.
    """
    Heuristic = ...
    r"""
    May fail to find a true answer; never asserts a false one.
    """

@typing.final
class Sign(enum.Enum):
    r"""
    The sign an [`Assumptions`] context can establish for an expression.
    """
    Positive = ...
    r"""
    Strictly positive.
    """
    Negative = ...
    r"""
    Strictly negative.
    """
    Zero = ...
    r"""
    Exactly zero.
    """
    Nonnegative = ...
    r"""
    Nonnegative, not further resolved.
    """
    Nonpositive = ...
    r"""
    Nonpositive, not further resolved.
    """
    Unknown = ...
    r"""
    Not determined.
    """

def apart(expr: Expr, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    Partial-fraction decomposition in `var`.
    """

def apply_initial_conditions(general: Expr, var: builtins.str, conditions: typing.Sequence[tuple[builtins.int, Expr, Expr]]) -> typing.Optional[Expr]:
    r"""
    Pins the free constants of a general solution to initial conditions given as
    `[(derivative_order, point, value), ...]`.
    
    # Errors
    
    Propagates the per-element extraction error.
    """

def approximate_real_roots(expr: Expr, var: builtins.str, width: builtins.int  |  fractions.Fraction  |  Rational) -> typing.Optional[builtins.list[fractions.Fraction]]:
    r"""
    Rational approximations of every real root, to within `width`.
    
    # Errors
    
    Raises `ValueError` when `width` is not an exact rational.
    """

def argument(expr: Expr) -> typing.Optional[Expr]:
    r"""
    The complex argument.
    """

def cancel(expr: Expr) -> typing.Optional[Expr]:
    r"""
    Cancels common factors in a quotient.
    """

def characteristic_polynomial(matrix: Matrix, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    The characteristic polynomial in `var`.
    """

def coeff(expr: Expr, var: builtins.str, n: builtins.int) -> typing.Optional[Expr]:
    r"""
    The coefficient of `var ** n`.
    """

def collect(expr: Expr, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    Collects terms by powers of `var`.
    """

def companion_matrix(poly: Expr, var: builtins.str) -> typing.Optional[Matrix]:
    r"""
    The companion matrix of a monic polynomial in `var`.
    """

def conjugate(expr: Expr) -> Expr:
    r"""
    The complex conjugate.
    """

def content(expr: Expr, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    The content (gcd of coefficients) in `var`.
    """

def count_real_roots(expr: Expr, var: builtins.str, lower: builtins.int  |  fractions.Fraction  |  Rational, upper: builtins.int  |  fractions.Fraction  |  Rational) -> typing.Optional[builtins.int]:
    r"""
    The exact number of distinct real roots in `[lower, upper]`.
    
    # Errors
    
    Raises `ValueError` when an endpoint is not an exact rational.
    """

def cyclotomic_polynomial(n: builtins.int, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    The `n`-th cyclotomic polynomial in `var`.
    """

def definite_integrate(expr: Expr, var: builtins.str, lower: Expr, upper: Expr) -> typing.Optional[DefiniteIntegral]:
    r"""
    A definite integral, with the antiderivative and certificate it came from.
    """

def definite_sum(f: Expr, var: builtins.str, lower: Expr, upper: Expr) -> typing.Optional[Expr]:
    r"""
    The definite sum of `f` over `var` from `lower` to `upper`.
    """

def degree(expr: Expr, var: builtins.str) -> typing.Optional[builtins.int]:
    r"""
    The degree of `expr` in `var`, or `None` when it is not a polynomial there.
    """

def diagonalize(matrix: Matrix, var: builtins.str) -> typing.Optional[tuple[Matrix, Matrix]]:
    r"""
    `(P, D)` with `A = P D P^-1`, or `None` when not diagonalizable.
    """

def discriminant(expr: Expr, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    The discriminant in `var`.
    """

def divergence(field: typing.Sequence[Expr], vars: typing.Sequence[builtins.str]) -> typing.Optional[Expr]:
    r"""
    The divergence of a vector field.
    
    # Errors
    
    Propagates the per-element extraction error.
    """

def dsolve_bernoulli(p: Expr, q: Expr, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    Solves a Bernoulli first-order ODE.
    """

def dsolve_euler_cauchy(coeffs: typing.Sequence[builtins.int  |  fractions.Fraction  |  Rational], var: builtins.str) -> typing.Optional[Expr]:
    r"""
    Solves the Euler-Cauchy equation with the given coefficients.
    
    # Errors
    
    Raises `ValueError` when a coefficient is not an exact rational.
    """

def dsolve_exact(m: Expr, n: Expr, xvar: builtins.str, yvar: builtins.str) -> typing.Optional[Expr]:
    r"""
    Solves an exact first-order ODE.
    """

def dsolve_first_order_linear(p: Expr, q: Expr, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    Solves `y' + p(var) y = q(var)`.
    """

def dsolve_homogeneous(char_coeffs: typing.Sequence[builtins.int  |  fractions.Fraction  |  Rational], var: builtins.str) -> typing.Optional[Expr]:
    r"""
    Solves a constant-coefficient homogeneous linear ODE.
    
    # Errors
    
    Raises `ValueError` when a coefficient is not an exact rational.
    """

def dsolve_inhomogeneous(char_coeffs: typing.Sequence[builtins.int  |  fractions.Fraction  |  Rational], forcing: Expr, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    Solves a constant-coefficient inhomogeneous linear ODE.
    
    # Errors
    
    Raises `ValueError` when a coefficient is not an exact rational.
    """

def dsolve_separable(f: Expr, g: Expr, xvar: builtins.str, yvar: builtins.str) -> typing.Optional[Expr]:
    r"""
    Solves a separable first-order ODE.
    """

def eigenvalues(matrix: Matrix, var: builtins.str) -> typing.Optional[builtins.list[Expr]]:
    r"""
    The eigenvalues, or `None`.
    """

def eigenvectors(matrix: Matrix, var: builtins.str) -> typing.Optional[builtins.list[tuple[Expr, builtins.list[Matrix]]]]:
    r"""
    `[(eigenvalue, [eigenvector, ...]), ...]`, or `None`.
    """

def equal(a: Expr, b: Expr) -> ZeroTest:
    r"""
    The decidable zero test `a == b`, with a re-checkable witness.
    
    The returned [`ZeroTest`] is the certificate, not a bool: `Certified` carries
    the canonical form of `a - b`, which an independent caller can re-normalize.
    """

def evalf(expr: Expr, bindings: dict) -> typing.Optional[builtins.float]:
    r"""
    Floating-point evaluation under `bindings`, or `None`.
    
    # Errors
    
    Propagates the per-entry extraction error.
    """

def evaluate_trig(expr: Expr) -> Expr:
    r"""
    Evaluates trig at exact special angles.
    """

def expand(expr: Expr) -> typing.Optional[Expr]:
    r"""
    Expands to a flat sum of monomials.
    """

def expand_log(expr: Expr) -> Expr:
    r"""
    Expands `ln` of products and powers.
    """

def expand_trig(expr: Expr) -> Expr:
    r"""
    Expands trig of sums and multiples.
    """

def factor(expr: Expr, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    Factors a univariate polynomial in `var`.
    """

def factor_expr(expr: Expr, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    Factors over the rationals, certified.
    """

def finite_product(f: Expr, var: builtins.str, lower: Expr, upper: Expr) -> typing.Optional[Expr]:
    r"""
    The finite product of `f` over `var` from `lower` to `upper`.
    """

def gosper_sum(expr: Expr, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    Gosper's indefinite hypergeometric summation.
    """

def gradient(expr: Expr, vars: typing.Sequence[builtins.str]) -> builtins.list[Expr]:
    r"""
    The gradient of `expr` with respect to `vars`.
    """

def hessian(f: Expr, vars: typing.Sequence[builtins.str]) -> typing.Optional[Matrix]:
    r"""
    The Hessian of `f` with respect to `vars`.
    """

def imaginary_part(expr: Expr) -> typing.Optional[Expr]:
    r"""
    The imaginary part.
    """

def infinite_sum(f: Expr, var: builtins.str, lower: Expr) -> typing.Optional[Expr]:
    r"""
    The infinite sum of `f` over `var` from `lower`.
    """

def integrate(expr: Expr, var: builtins.str) -> typing.Optional[CertifiedIntegral]:
    r"""
    An antiderivative that carries its own proof, or `None` when declined.
    
    The result's `certificate` is a [`ZeroTest`] over
    `d(antiderivative)/dvar - integrand`.
    """

def is_irreducible(expr: Expr, var: builtins.str) -> typing.Optional[builtins.bool]:
    r"""
    Whether `expr` is irreducible over the rationals in `var`; `None` when
    undecided.
    """

def jacobian(exprs: typing.Sequence[Expr], vars: typing.Sequence[builtins.str]) -> typing.Optional[Matrix]:
    r"""
    The Jacobian of `exprs` with respect to `vars`.
    
    # Errors
    
    Propagates the per-element extraction error.
    """

def laplacian(f: Expr, vars: typing.Sequence[builtins.str]) -> Expr:
    r"""
    The Laplacian of `f` with respect to `vars`.
    """

def leading_coeff(expr: Expr, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    The leading coefficient in `var`.
    """

def limit(expr: Expr, var: builtins.str, point: LimitPoint) -> typing.Optional[Expr]:
    r"""
    The limit of `expr` as `var` approaches `point`.
    """

def logcombine(expr: Expr) -> Expr:
    r"""
    Combines a sum of logarithms into one.
    """

def matrix_rank(matrix: Matrix) -> typing.Optional[builtins.int]:
    r"""
    The rank of a matrix, or `None`.
    """

def minimal_polynomial(matrix: Matrix, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    The minimal polynomial in `var`.
    """

def modulus(expr: Expr) -> typing.Optional[Expr]:
    r"""
    The complex modulus.
    """

def normalize(expr: Expr) -> typing.Optional[MultiPoly]:
    r"""
    The canonical polynomial normal form of `expr`, or `None` when it is outside
    the polynomial fragment (or overflows).
    """

def nsimplify(value: builtins.float, max_denominator: builtins.int) -> typing.Optional[Expr]:
    r"""
    A symbolic value matching the float `value`, or `None`.
    """

def poly_div(a: Expr, b: Expr, var: builtins.str) -> typing.Optional[tuple[Expr, Expr]]:
    r"""
    `(quotient, remainder)` of polynomial division in `var`.
    """

def poly_gcd(a: Expr, b: Expr, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    Polynomial gcd in `var`.
    """

def poly_lcm(a: Expr, b: Expr, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    Polynomial lcm in `var`.
    """

def primitive_part(expr: Expr, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    The primitive part in `var`.
    """

def prove_derivative(expr: Expr, var: builtins.str, claimed: Expr) -> ZeroTest:
    r"""
    Checks a claimed derivative directly, returning the certificate.
    """

def rationalize(x: builtins.float, max_denominator: builtins.int) -> typing.Optional[fractions.Fraction]:
    r"""
    The best rational approximation of `x` with denominator at most
    `max_denominator`.
    
    # Errors
    
    Propagates any Python error raised while building the fraction.
    """

def real_part(expr: Expr) -> typing.Optional[Expr]:
    r"""
    The real part.
    """

def real_root_intervals(expr: Expr, var: builtins.str) -> typing.Optional[builtins.list[tuple[fractions.Fraction, fractions.Fraction]]]:
    r"""
    Sturm-isolating intervals, one per distinct real root, ascending.
    
    # Errors
    
    Propagates any Python error raised while building the fractions.
    """

def residue(expr: Expr, var: builtins.str, point: builtins.int  |  fractions.Fraction  |  Rational) -> typing.Optional[Expr]:
    r"""
    The residue of `expr` at the rational `point`.
    
    # Errors
    
    Raises `ValueError` when `point` is not an exact rational.
    """

def resultant(a: Expr, b: Expr, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    The resultant in `var`.
    """

def rewrite_exp(expr: Expr) -> Expr:
    r"""
    Rewrites hyperbolic/trig heads as exponentials.
    """

def sample_standard_deviation(data: typing.Sequence[builtins.int  |  fractions.Fraction  |  Rational]) -> typing.Optional[Expr]:
    r"""
    The sample standard deviation of exact rational data.
    
    # Errors
    
    Raises `ValueError` when a datum is not an exact rational.
    """

def series(expr: Expr, var: builtins.str, order: builtins.int) -> typing.Optional[Expr]:
    r"""
    The Maclaurin series of `expr` to `order`.
    """

def series_at(expr: Expr, var: builtins.str, center: Expr, order: builtins.int) -> typing.Optional[Expr]:
    r"""
    The Taylor series of `expr` about `center` to `order`.
    """

def simplify(expr: Expr) -> Expr:
    r"""
    Heuristic simplification. Never returns a wrong form.
    """

def simplify_radicals(expr: Expr) -> Expr:
    r"""
    Pulls perfect powers out of radicals.
    """

def simplify_under_assumptions(expr: Expr, assumptions: Assumptions) -> Expr:
    r"""
    Simplification under sign assumptions.
    """

def solve(expr: Expr, var: builtins.str) -> typing.Optional[builtins.list[Expr]]:
    r"""
    The exact roots of `expr` in `var`, or `None`.
    """

def solve_linear_system(equations: typing.Sequence[Expr], vars: typing.Sequence[builtins.str]) -> typing.Optional[builtins.list[tuple[builtins.str, Expr]]]:
    r"""
    Solves a linear system, returning `[(variable, value), ...]`.
    
    # Errors
    
    Propagates the per-element extraction error.
    """

def solve_polynomial_inequality(expr: Expr, var: builtins.str, op: builtins.str) -> typing.Optional[builtins.list[RealInterval]]:
    r"""
    Solves `p(var) OP 0` over the reals as a union of disjoint intervals.
    
    `op` is one of `">"`, `">="`, `"<"`, `"<="`.
    
    # Errors
    
    Raises `ValueError` for an unrecognized `op`.
    """

def solve_polynomial_system(f: Expr, g: Expr, xvar: builtins.str, yvar: builtins.str) -> typing.Optional[builtins.list[tuple[Expr, Expr]]]:
    r"""
    Solves a bivariate polynomial system, returning `[(x, y), ...]`.
    """

def solve_recurrence(coefficients: typing.Sequence[builtins.int  |  fractions.Fraction  |  Rational], initial: typing.Sequence[builtins.int  |  fractions.Fraction  |  Rational], var: builtins.str) -> typing.Optional[Expr]:
    r"""
    Solves a constant-coefficient linear recurrence.
    
    # Errors
    
    Raises `ValueError` when a coefficient is not an exact rational.
    """

def standard_deviation(data: typing.Sequence[builtins.int  |  fractions.Fraction  |  Rational]) -> typing.Optional[Expr]:
    r"""
    The population standard deviation of exact rational data.
    
    # Errors
    
    Raises `ValueError` when a datum is not an exact rational.
    """

def sum_polynomial(expr: Expr, var: builtins.str) -> typing.Optional[Expr]:
    r"""
    The indefinite sum of a polynomial in `var`.
    """

def trace(matrix: Matrix) -> typing.Optional[Expr]:
    r"""
    The trace, or `None` for a non-square matrix.
    """

def trigsimp(expr: Expr) -> Expr:
    r"""
    Trigonometric simplification.
    """

def wronskian(functions: typing.Sequence[Expr], var: builtins.str) -> typing.Optional[Expr]:
    r"""
    The Wronskian of `functions` in `var`.
    
    # Errors
    
    Propagates the per-element extraction error.
    """

