# This file is automatically generated by pyo3_stub_gen
# ruff: noqa: E501, F401, F403, F405

import builtins
import typing
__all__ = [
    "ArtifactLimits",
    "FORMAT",
    "FrobeniusReduction",
    "Gf2BothVerdict",
    "Gf2Limits",
    "Gf2Poly",
    "Gf2Verdict",
    "HalfDegreeArtifact",
    "IndependentCheckLimits",
    "IrreducibilityCertificate",
    "RabinBezout",
    "STATEMENT",
    "VERSION",
    "certify_irreducible",
]

FORMAT: builtins.str
STATEMENT: builtins.str
VERSION: builtins.int
@typing.final
class ArtifactLimits:
    r"""
    Ceilings for parsing and validating an on-disk artifact.
    
    Defaults: `max_bytes=32 MiB`, `max_id_bytes=256`, `max_producer_bytes=256`,
    with the two checkers' own defaults.
    """
    @property
    def max_bytes(self) -> builtins.int:
        r"""
        Maximum serialized input size, in bytes.
        """
    @property
    def max_id_bytes(self) -> builtins.int:
        r"""
        Maximum identifier length, in bytes.
        """
    @property
    def max_producer_bytes(self) -> builtins.int:
        r"""
        Maximum producer-identity length, in bytes.
        """
    def __new__(cls, max_bytes: builtins.int = 33554432, max_id_bytes: builtins.int = 256, max_producer_bytes: builtins.int = 256, primary: typing.Optional[Gf2Limits] = None, independent: typing.Optional[IndependentCheckLimits] = None) -> ArtifactLimits:
        r"""
        Ceilings, defaulting to the Rust `ArtifactLimits::default()`.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class FrobeniusReduction:
    r"""
    One step of the Frobenius chain: `x ** (2 ** i) = quotient * f + remainder`.
    """
    @property
    def quotient(self) -> Gf2Poly:
        r"""
        The quotient multiplying the candidate polynomial.
        """
    @property
    def remainder(self) -> Gf2Poly:
        r"""
        The reduced residue.
        """
    def __new__(cls, quotient: Gf2Poly, remainder: Gf2Poly) -> FrobeniusReduction:
        r"""
        A reduction from its quotient and remainder.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class Gf2BothVerdict:
    r"""
    Both checkers' answers about one certificate.
    """
    @property
    def primary(self) -> Gf2Verdict:
        r"""
        The packed-word checker's answer.
        """
    @property
    def independent(self) -> Gf2Verdict:
        r"""
        The dense independent checker's answer.
        """
    @property
    def accepted(self) -> builtins.bool:
        r"""
        Whether **both** checkers accepted.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class Gf2Limits:
    r"""
    Resource ceilings for the packed-word GF(2) arithmetic.
    
    Defaults are `Gf2Limits::default()` verbatim: `max_input_degree=4_096`,
    `max_intermediate_degree=8_192`, `max_frobenius_steps=4_096`,
    `max_word_ops=50_000_000`.
    """
    @property
    def max_input_degree(self) -> builtins.int:
        r"""
        Maximum degree of a candidate presented for checking.
        """
    @property
    def max_intermediate_degree(self) -> builtins.int:
        r"""
        Maximum degree of an intermediate polynomial.
        """
    @property
    def max_frobenius_steps(self) -> builtins.int:
        r"""
        Maximum Frobenius squarings in one certificate.
        """
    @property
    def max_word_ops(self) -> builtins.int:
        r"""
        Approximate word-level work ceiling.
        """
    def __new__(cls, max_input_degree: builtins.int = 4096, max_intermediate_degree: builtins.int = 8192, max_frobenius_steps: builtins.int = 4096, max_word_ops: builtins.int = 50000000) -> Gf2Limits:
        r"""
        Ceilings, defaulting to the Rust `Gf2Limits::default()`.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class Gf2Poly:
    r"""
    A polynomial over GF(2), held as packed 64-bit words.
    """
    @staticmethod
    def from_exponents(exponents: typing.Sequence[builtins.int], limits: typing.Optional[Gf2Limits] = None) -> Gf2Poly:
        r"""
        A polynomial from the exponents whose coefficients are `1`.
        
        # Errors
        
        Raises `Gf2Error` when an exponent exceeds the degree ceiling.
        """
    @staticmethod
    def from_words(words: typing.Sequence[builtins.int]) -> Gf2Poly:
        r"""
        A polynomial from its packed 64-bit words, least significant first.
        """
    @staticmethod
    def zero() -> Gf2Poly:
        r"""
        The zero polynomial.
        """
    @staticmethod
    def one() -> Gf2Poly:
        r"""
        The constant `1`.
        """
    @staticmethod
    def x() -> Gf2Poly:
        r"""
        The polynomial `x`.
        """
    def degree(self) -> typing.Optional[builtins.int]:
        r"""
        The degree, or `None` for the zero polynomial.
        """
    def is_zero(self) -> builtins.bool:
        r"""
        Whether this is the zero polynomial.
        """
    def coefficient(self, exponent: builtins.int) -> builtins.bool:
        r"""
        The coefficient of `x ** exponent`.
        """
    def exponents(self) -> builtins.list[builtins.int]:
        r"""
        The exponents with coefficient `1`, ascending.
        """
    def words(self) -> builtins.list[builtins.int]:
        r"""
        The packed words.
        """
    def is_half_degree_shaped(self) -> builtins.bool:
        r"""
        Whether `f - x ** n` has degree at most `floor(n / 2)`.
        """
    def __eq__(self, other: typing.Any) -> builtins.bool: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class Gf2Verdict:
    r"""
    One checker's answer about a certificate.
    
    `accepted` is paired with the obligation counts the certificate carries, so a
    caller can tell a checker that re-derived a 100-step Frobenius chain from one
    that re-derived nothing.
    """
    @property
    def checker(self) -> builtins.str:
        r"""
        `"packed"` or `"independent"` — which implementation answered.
        """
    @property
    def accepted(self) -> builtins.bool:
        r"""
        Whether this checker accepted the certificate.
        """
    @property
    def reason(self) -> typing.Optional[builtins.str]:
        r"""
        Why it did not, when it did not.
        """
    @property
    def frobenius_steps(self) -> builtins.int:
        r"""
        Frobenius reductions the certificate carries, and this checker re-derived.
        """
    @property
    def bezout_obligations(self) -> builtins.int:
        r"""
        Bezout identities the certificate carries, one per distinct prime divisor
        of the degree.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class HalfDegreeArtifact:
    r"""
    A checked bounded witness together with the identity of the **untrusted**
    producer that made it.
    
    `producer` is the tier boundary made data: it records who to distrust, and it
    is part of the canonical bytes.
    """
    @property
    def id(self) -> builtins.str:
        r"""
        The artifact identifier.
        """
    @property
    def producer(self) -> builtins.str:
        r"""
        The untrusted producer's identity.
        """
    @property
    def certificate(self) -> IrreducibilityCertificate:
        r"""
        The certificate.
        """
    def __new__(cls, id: builtins.str, producer: builtins.str, certificate: IrreducibilityCertificate) -> HalfDegreeArtifact:
        r"""
        An artifact from an identifier, a producer identity, and a certificate.
        """
    def to_canonical_json(self, limits: typing.Optional[ArtifactLimits] = None) -> builtins.str:
        r"""
        The canonical JSON bytes. Validates before serializing: fail-closed.
        
        # Errors
        
        Raises `Gf2Error` when the artifact does not validate.
        """
    @staticmethod
    def from_canonical_json(text: builtins.str, limits: typing.Optional[ArtifactLimits] = None) -> HalfDegreeArtifact:
        r"""
        Parses and fully validates canonical JSON.
        
        # Errors
        
        Raises `Gf2Error` for oversized input, malformed JSON, a broken format
        invariant, or a certificate either checker rejects.
        """
    def validate(self, limits: typing.Optional[ArtifactLimits] = None) -> None:
        r"""
        Re-validates this artifact, fail-closed.
        
        # Errors
        
        Raises `Gf2Error` with the first invariant that failed.
        """
    def __eq__(self, other: typing.Any) -> builtins.bool: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class IndependentCheckLimits:
    r"""
    Resource ceilings for the dense independent checker.
    
    Defaults: `max_degree=4_096`, `max_coefficient_ops=500_000_000`.
    """
    @property
    def max_degree(self) -> builtins.int:
        r"""
        Maximum candidate degree.
        """
    @property
    def max_coefficient_ops(self) -> builtins.int:
        r"""
        Maximum coefficient reads and XORs.
        """
    def __new__(cls, max_degree: builtins.int = 4096, max_coefficient_ops: builtins.int = 500000000) -> IndependentCheckLimits:
        r"""
        Ceilings, defaulting to the Rust `IndependentCheckLimits::default()`.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class IrreducibilityCertificate:
    r"""
    A portable Rabin irreducibility certificate.
    """
    @property
    def frobenius(self) -> builtins.list[FrobeniusReduction]:
        r"""
        The Frobenius chain, in order.
        """
    @property
    def bezout(self) -> builtins.list[RabinBezout]:
        r"""
        The Bezout identities, one per distinct prime divisor of the degree.
        """
    @property
    def polynomial(self) -> Gf2Poly:
        r"""
        The polynomial whose irreducibility is witnessed.
        """
    @property
    def frobenius_steps(self) -> builtins.int:
        r"""
        The number of Frobenius reductions carried.
        """
    @property
    def bezout_prime_divisors(self) -> builtins.list[builtins.int]:
        r"""
        The distinct prime divisors of the degree the certificate covers.
        """
    def __new__(cls, polynomial: Gf2Poly, frobenius: typing.Sequence[FrobeniusReduction], bezout: typing.Sequence[RabinBezout]) -> IrreducibilityCertificate:
        r"""
        A certificate from its parts.
        
        The certificate is *portable data* — it is meant to arrive from an
        untrusted producer over a wire — so reconstructing one from its parts is
        part of the surface, not a back door. It is also what makes the two
        checkers falsifiable from Python: hand them an edited chain and watch
        both reject it.
        """
    def check_primary(self, limits: typing.Optional[Gf2Limits] = None) -> Gf2Verdict:
        r"""
        The packed-word checker's answer.
        
        # Errors
        
        Raises `Gf2Error` on a budget or shape refusal. A *rejected* certificate
        is not an error: it comes back as `accepted == False` with a reason.
        """
    def check_independent(self, limits: typing.Optional[IndependentCheckLimits] = None) -> Gf2Verdict:
        r"""
        The dense independent checker's answer.
        
        # Errors
        
        Raises `Gf2Error` on a budget or shape refusal.
        """
    def check_both(self, limits: typing.Optional[Gf2Limits] = None, independent_limits: typing.Optional[IndependentCheckLimits] = None) -> Gf2BothVerdict:
        r"""
        Both checkers. `accepted` is true only when **both** accept.
        
        # Errors
        
        Raises `Gf2Error` on a budget or shape refusal from either checker.
        """
    def __eq__(self, other: typing.Any) -> builtins.bool: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class RabinBezout:
    r"""
    Bezout evidence for one distinct prime divisor of the candidate degree.
    """
    @property
    def prime_divisor(self) -> builtins.int:
        r"""
        The distinct prime divisor of the candidate degree.
        """
    @property
    def polynomial_coefficient(self) -> Gf2Poly:
        r"""
        The coefficient of the candidate polynomial.
        """
    @property
    def frobenius_coefficient(self) -> Gf2Poly:
        r"""
        The coefficient of `r_(n/p) + x`.
        """
    def __new__(cls, prime_divisor: builtins.int, polynomial_coefficient: Gf2Poly, frobenius_coefficient: Gf2Poly) -> RabinBezout:
        r"""
        An identity from its prime divisor and the two coefficients.
        """
    def __repr__(self) -> builtins.str: ...

def certify_irreducible(polynomial: Gf2Poly, limits: typing.Optional[Gf2Limits] = None) -> typing.Optional[IrreducibilityCertificate]:
    r"""
    Produces an irreducibility certificate, or `None` for a **reducible**
    polynomial.
    
    `None` is a decided answer, not a decline.
    
    # Errors
    
    Raises `Gf2Error` for a zero or constant input and for typed degree,
    Frobenius-step, or work-limit refusals.
    """

