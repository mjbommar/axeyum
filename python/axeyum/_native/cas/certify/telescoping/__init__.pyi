# This file is automatically generated by pyo3_stub_gen
# ruff: noqa: E501, F401, F403, F405

from axeyum._native import cas
import builtins
import fractions
import typing
__all__ = [
    "CertificateDocument",
    "CheckOptions",
    "CheckReport",
    "ClosedFormReport",
    "FORMAT",
    "Factor",
    "HyperTerm",
    "Limits",
    "LinearForm",
    "TelescopingCertificate",
    "TelescopingOutcome",
    "VERSION",
    "Verdict",
    "binomial_factors",
    "factorial_factor",
    "zeilberger",
]

FORMAT: builtins.str
VERSION: builtins.int
@typing.final
class CertificateDocument:
    r"""
    A certificate together with the options and closed-form claim it ships with.
    """
    @property
    def id(self) -> builtins.str:
        r"""
        The document identifier.
        """
    @property
    def title(self) -> builtins.str:
        r"""
        The document title.
        """
    @property
    def certificate(self) -> TelescopingCertificate:
        r"""
        The certificate.
        """
    @property
    def options(self) -> CheckOptions:
        r"""
        The check options the document pins.
        """
    @property
    def closed_form(self) -> typing.Optional[HyperTerm]:
        r"""
        The claimed closed form, when the document carries one.
        """
    def __new__(cls, id: builtins.str, title: builtins.str, certificate: TelescopingCertificate, options: CheckOptions, closed_form: typing.Optional[HyperTerm] = None, base: builtins.int = 0, symbolic: builtins.bool = False) -> CertificateDocument:
        r"""
        A document from its parts.
        """
    def to_json(self) -> builtins.str:
        r"""
        The deterministic JSON rendering.
        """
    @staticmethod
    def from_json(text: builtins.str) -> CertificateDocument:
        r"""
        Parses a document from its deterministic JSON rendering.
        
        # Errors
        
        Raises `CasError` when the text is not a well-formed document.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class CheckOptions:
    r"""
    The sample points and window the checker replays over.
    """
    @property
    def window(self) -> tuple[builtins.int, builtins.int]:
        r"""
        The replay window `(low, high)`.
        """
    @property
    def min_ratio_samples(self) -> builtins.int:
        r"""
        The minimum number of shift-ratio samples the checker demands.
        """
    @property
    def samples(self) -> builtins.dict[builtins.str, builtins.list[builtins.int]]:
        r"""
        `{variable: [point, ...]}`.
        """
    @staticmethod
    def over(shift_var: builtins.str, points: typing.Sequence[builtins.int], window: tuple[builtins.int, builtins.int]) -> CheckOptions:
        r"""
        Options over `shift_var` sampled at `points`, replayed on `window`.
        """
    def with_(self, var: builtins.str, points: typing.Sequence[builtins.int]) -> CheckOptions:
        r"""
        These options with `var` additionally sampled at `points`.
        
        Spelled `with_` because `with` is a Python keyword; the Rust method is
        `CheckOptions::with`.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class CheckReport:
    r"""
    The counts an accepted telescoping certificate discharged.
    
    A zero count is the fail signal: this is exactly the report a checker that
    did nothing would produce, and it must be visible from Python.
    """
    @property
    def ratio_samples(self) -> builtins.int:
        r"""
        Shift-ratio identities re-derived.
        """
    @property
    def pointwise_samples(self) -> builtins.int:
        r"""
        Pointwise evaluations of the telescoping identity.
        """
    @property
    def certificate_poles_in_window(self) -> builtins.int:
        r"""
        Certificate poles found inside the replay window.
        """
    @property
    def recurrence_samples(self) -> builtins.int:
        r"""
        Recurrence evaluations.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class ClosedFormReport:
    r"""
    The evidence that a verified recurrence pins the sum to a claimed closed
    form.
    """
    @property
    def base(self) -> builtins.int:
        r"""
        The base index the identity is claimed from.
        """
    @property
    def base_cases(self) -> builtins.int:
        r"""
        Base cases checked by exact finite summation.
        """
    @property
    def leading_zeros(self) -> builtins.list[builtins.int]:
        r"""
        Integers at or above `base` where the leading coefficient vanishes.
        
        A nonempty list breaks the induction; the checker rejects rather than
        reports it, so a verified report always has this empty.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class Factor:
    r"""
    One factor of a hypergeometric term.
    """
    @property
    def kind(self) -> builtins.str:
        r"""
        `"Gamma"`, `"Power"`, or `"Poly"`.
        """
    @staticmethod
    def gamma(form: LinearForm, exponent: builtins.int) -> Factor:
        r"""
        `Gamma(form) ** exponent`. A negative exponent is a denominator.
        """
    @staticmethod
    def power(base: builtins.int  |  fractions.Fraction  |  cas.Rational, form: LinearForm) -> Factor:
        r"""
        `base ** form` for a nonzero rational `base`.
        
        # Errors
        
        Raises `ValueError` when `base` is not an exact rational.
        """
    @staticmethod
    def poly(poly: cas.MvPoly, exponent: builtins.int) -> Factor:
        r"""
        `poly ** exponent`. A negative exponent is a denominator.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class HyperTerm:
    r"""
    A product of [`Factor`]s: the hypergeometric summand.
    """
    @property
    def factors(self) -> builtins.list[Factor]:
        r"""
        The factors.
        """
    def __new__(cls, factors: typing.Sequence[Factor]) -> HyperTerm:
        r"""
        A term from its factors, in the order supplied.
        """
    def variables(self) -> builtins.list[builtins.str]:
        r"""
        Every variable the term mentions, sorted.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class Limits:
    r"""
    Search ceilings. Defaults are `Limits::classical()` verbatim:
    `max_order=2`, `max_certificate_degree=8`, `max_unknowns=400`,
    `max_poly_terms=4_000`, `max_dispersion=32`, `max_parameter_degree=6`.
    
    None of these is a degree *ansatz*: starving one makes the search **decline**,
    never mislead.
    """
    @property
    def max_order(self) -> builtins.int:
        r"""
        The largest recurrence order searched.
        """
    @property
    def max_certificate_degree(self) -> builtins.int:
        r"""
        The largest derived certificate degree searched.
        """
    @property
    def max_unknowns(self) -> builtins.int:
        r"""
        The largest linear system solved.
        """
    @property
    def max_poly_terms(self) -> builtins.int:
        r"""
        The largest intermediate polynomial admitted.
        """
    @property
    def max_dispersion(self) -> builtins.int:
        r"""
        The largest dispersion admitted.
        """
    @property
    def max_parameter_degree(self) -> builtins.int:
        r"""
        The largest parameter degree admitted.
        """
    def __new__(cls, max_order: builtins.int = 2, max_certificate_degree: builtins.int = 8, max_unknowns: builtins.int = 400, max_poly_terms: builtins.int = 4000, max_dispersion: builtins.int = 32, max_parameter_degree: builtins.int = 6) -> Limits:
        r"""
        Ceilings, defaulting to `Limits::classical()`.
        """
    @staticmethod
    def classical() -> Limits:
        r"""
        The classical binomial-identity ceilings.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class LinearForm:
    r"""
    An integer-linear form `sum(coefficient * variable) + constant`.
    """
    @property
    def constant(self) -> builtins.int:
        r"""
        The additive constant.
        """
    def __new__(cls, terms: typing.Sequence[tuple[builtins.str, builtins.int]], constant: builtins.int = 0) -> LinearForm:
        r"""
        A form from `[(variable, coefficient), ...]` and a constant.
        """
    def coefficient(self, var: builtins.str) -> builtins.int:
        r"""
        The coefficient of `var`, or `0`.
        """
    def variables(self) -> builtins.list[builtins.str]:
        r"""
        The variables this form mentions, sorted.
        """
    def to_poly(self) -> typing.Optional[cas.MvPoly]:
        r"""
        The same form as a polynomial, or `None` on overflow.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class TelescopingCertificate:
    r"""
    A creative-telescoping certificate.
    """
    @property
    def term(self) -> HyperTerm:
        r"""
        The summand.
        """
    @property
    def shift_var(self) -> builtins.str:
        r"""
        The parameter the recurrence is in.
        """
    @property
    def sum_var(self) -> builtins.str:
        r"""
        The summation variable.
        """
    @property
    def recurrence(self) -> builtins.list[cas.MvPoly]:
        r"""
        The recurrence coefficients, lowest shift first.
        """
    @property
    def certificate_numerator(self) -> cas.MvPoly:
        r"""
        The certificate's rational-function numerator.
        """
    @property
    def certificate_denominator(self) -> cas.MvPoly:
        r"""
        The certificate's rational-function denominator.
        """
    def order(self) -> builtins.int:
        r"""
        The recurrence order.
        """
    def check(self, options: CheckOptions) -> Verdict:
        r"""
        Independently re-derives this certificate, returning the verdict **with**
        its four report counts.
        """
    def check_closed_form(self, closed_form: HyperTerm, base: builtins.int, options: CheckOptions) -> ClosedFormReport:
        r"""
        Checks a claimed closed form against this certificate's recurrence.
        
        # Errors
        
        Raises `CasError` carrying every reason the claim was not established.
        """
    def __eq__(self, other: typing.Any) -> builtins.bool: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class TelescopingOutcome:
    r"""
    The outcome of a creative-telescoping search: `Found` or `Declined`.
    """
    @property
    def kind(self) -> builtins.str:
        r"""
        `"Found"` or `"Declined"`.
        """
    @property
    def certificate(self) -> typing.Optional[TelescopingCertificate]:
        r"""
        The certificate, when the search found one.
        
        It is **not** verified: run `check` before believing it.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class Verdict:
    r"""
    The verdict of the independent telescoping checker.
    """
    @property
    def kind(self) -> builtins.str:
        r"""
        `"Verified"` or `"Rejected"`.
        """
    @property
    def report(self) -> typing.Optional[CheckReport]:
        r"""
        The counts, on `Verified`.
        """
    @property
    def reasons(self) -> typing.Optional[builtins.list[builtins.str]]:
        r"""
        Every reason the certificate was rejected.
        """
    def is_verified(self) -> builtins.bool:
        r"""
        Whether the certificate re-derived.
        """
    def __repr__(self) -> builtins.str: ...

def binomial_factors(upper: LinearForm, lower: LinearForm, power: builtins.int) -> builtins.list[Factor]:
    r"""
    The three gamma factors of `binomial(upper, lower) ** power`.
    """

def factorial_factor(form: LinearForm, exponent: builtins.int) -> Factor:
    r"""
    `(form)! ** exponent`, i.e. `Gamma(form + 1) ** exponent`.
    """

def zeilberger(term: HyperTerm, shift_var: builtins.str, sum_var: builtins.str, limits: typing.Optional[Limits] = None) -> TelescopingOutcome:
    r"""
    Searches for a creative-telescoping certificate, smallest order first.
    
    The result is an **unchecked** search output; soundness lives in `check`.
    """

