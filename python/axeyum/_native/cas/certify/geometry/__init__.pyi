# This file is automatically generated by pyo3_stub_gen
# ruff: noqa: E501, F401, F403, F405

from axeyum._native import cas
from axeyum._native.cas.certify import groebner
import builtins
import fractions
import typing
__all__ = [
    "CheckOptions",
    "Condition",
    "Constraint",
    "DegenerateWitness",
    "FORMAT",
    "GenericWitness",
    "GeometryCertificate",
    "GeometryDecline",
    "GeometryProblem",
    "GeometryReport",
    "GeometryVerdict",
    "INVERSE_PREFIX",
    "ProofOutcome",
    "Pt",
    "VERSION",
    "centroid",
    "certify",
    "certify_any_route",
    "certify_by_linear_elimination",
    "collinear",
    "concyclic",
    "corpus",
    "det",
    "dist_sq",
    "dot",
    "equidistant",
    "frontier",
    "geometry_limits",
    "midpoint",
    "parallel",
    "perpendicular",
    "same_point",
]

FORMAT: builtins.str
INVERSE_PREFIX: builtins.str
VERSION: builtins.int
@typing.final
class CheckOptions:
    r"""
    How hard the numeric cross-check works. Defaults: `numeric_points=24`,
    `half_range=6`.
    """
    @property
    def numeric_points(self) -> builtins.int:
        r"""
        Integer points per conclusion at which to re-evaluate the identity.
        """
    @property
    def half_range(self) -> builtins.int:
        r"""
        Points are drawn from `-half_range ..= half_range`.
        """
    def __new__(cls, numeric_points: builtins.int = 24, half_range: builtins.int = 6) -> CheckOptions:
        r"""
        Options, defaulting to the Rust `CheckOptions::default()`.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class Condition:
    r"""
    A non-degeneracy condition: an identified polynomial that must **not** vanish.
    """
    @property
    def id(self) -> builtins.str:
        r"""
        The stable identifier.
        """
    @property
    def description(self) -> builtins.str:
        r"""
        The human-readable gloss.
        """
    @property
    def poly(self) -> cas.MvPoly:
        r"""
        The polynomial that must not vanish.
        """
    def __new__(cls, id: builtins.str, description: builtins.str, poly: cas.MvPoly) -> Condition:
        r"""
        A condition `poly != 0` with a stable identifier and a gloss.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class Constraint:
    r"""
    A hypothesis or conclusion: an identified polynomial equation `poly == 0`.
    """
    @property
    def id(self) -> builtins.str:
        r"""
        The stable identifier.
        """
    @property
    def description(self) -> builtins.str:
        r"""
        The human-readable gloss.
        """
    @property
    def poly(self) -> cas.MvPoly:
        r"""
        The polynomial that must vanish.
        """
    def __new__(cls, id: builtins.str, description: builtins.str, poly: cas.MvPoly) -> Constraint:
        r"""
        A constraint `poly == 0` with a stable identifier and a gloss.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class DegenerateWitness:
    r"""
    A configuration on a degeneracy locus that genuinely breaks the theorem.
    
    This is the certificate's **negative control**: a stated witness that does
    not in fact break the theorem makes the producer decline
    (`UnverifiedWitness`) rather than ship a decorative one.
    """
    @property
    def condition_id(self) -> builtins.str:
        r"""
        The condition this witness breaks.
        """
    @property
    def description(self) -> builtins.str:
        r"""
        The human-readable gloss.
        """
    @property
    def assignment(self) -> dict:
        r"""
        The real part of the assignment.
        
        # Errors
        
        Propagates any Python error raised while building the fractions.
        """
    @staticmethod
    def rational(condition_id: builtins.str, description: builtins.str, assignment: dict) -> DegenerateWitness:
        r"""
        A purely rational witness for the named condition.
        
        # Errors
        
        Raises `ValueError` when a coordinate is not an exact rational.
        """
    def is_gaussian(self) -> builtins.bool:
        r"""
        Whether any coordinate has a nonzero imaginary part.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class GenericWitness:
    r"""
    A generic configuration at which the theorem's identity is replayed.
    """
    @property
    def description(self) -> builtins.str:
        r"""
        The human-readable gloss.
        """
    @property
    def assignment(self) -> dict:
        r"""
        The assignment.
        
        # Errors
        
        Propagates any Python error raised while building the fractions.
        """
    def __new__(cls, description: builtins.str, assignment: dict) -> GenericWitness:
        r"""
        A generic configuration.
        
        # Errors
        
        Raises `ValueError` when a coordinate is not an exact rational.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class GeometryCertificate:
    r"""
    A checkable geometry certificate.
    """
    @property
    def id(self) -> builtins.str:
        r"""
        The theorem's identifier.
        """
    @property
    def title(self) -> builtins.str:
        r"""
        The theorem's title.
        """
    @property
    def statement(self) -> builtins.str:
        r"""
        The theorem's prose statement.
        """
    @property
    def coordinates(self) -> builtins.list[builtins.str]:
        r"""
        The coordinate names, in order.
        """
    @property
    def generators(self) -> builtins.list[cas.MvPoly]:
        r"""
        The generator list the identity is stated over.
        """
    @property
    def saturation_condition_ids(self) -> builtins.list[builtins.str]:
        r"""
        The identifiers of the conditions this proof saturated by.
        """
    @property
    def conclusion_cofactors(self) -> builtins.list[tuple[builtins.str, builtins.list[cas.MvPoly]]]:
        r"""
        `[(conclusion_id, [cofactor, ...]), ...]`.
        """
    def check(self, options: typing.Optional[CheckOptions] = None) -> GeometryVerdict:
        r"""
        Independently re-derives this certificate.
        
        Returns a [`GeometryVerdict`] carrying the five `GeometryReport` counts,
        not a bool: the counts are what make a `Verified` falsifiable, and a
        checker that discharges nothing must be visible as such.
        """
    def to_json(self) -> builtins.str:
        r"""
        The deterministic JSON rendering.
        """
    @staticmethod
    def from_json(text: builtins.str) -> GeometryCertificate:
        r"""
        Parses a certificate from its deterministic JSON rendering.
        
        # Errors
        
        Raises `CasError` when the text is not a well-formed certificate.
        """
    def __eq__(self, other: typing.Any) -> builtins.bool: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class GeometryDecline:
    r"""
    Why the geometry producer emitted no certificate.
    """
    @property
    def name(self) -> builtins.str:
        r"""
        The variant name: one of `Reduction`, `TooManyConditions`,
        `UnverifiedWitness`, `UndividableMultiplier`, `RefutedByOwnWitness`.
        """
    @property
    def reduction_reason(self) -> typing.Optional[groebner.DeclineReason]:
        r"""
        The inner cofactor-reduction reason, when the decline was a reduction.
        """
    def is_refuted_by_own_witness(self) -> builtins.bool:
        r"""
        Whether the producer's own negative control refuted the statement.
        
        `True` means **the theorem as stated is false** — not a resource limit,
        and never to be reported as "declined".
        """
    def is_ceiling(self) -> builtins.bool:
        r"""
        Whether a larger [`Limits`] could change the answer.
        
        Only a reduction decline can be a ceiling; every other variant is a
        statement about the theorem, not the budget.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class GeometryProblem:
    r"""
    A geometry theorem, stated in coordinates.
    """
    @property
    def id(self) -> builtins.str:
        r"""
        The stable identifier.
        """
    @property
    def title(self) -> builtins.str:
        r"""
        The theorem's title.
        """
    @property
    def statement(self) -> builtins.str:
        r"""
        The theorem's prose statement.
        """
    @property
    def hypotheses(self) -> builtins.list[Constraint]:
        r"""
        The hypotheses.
        """
    @property
    def nondegeneracy(self) -> builtins.list[Condition]:
        r"""
        The non-degeneracy conditions.
        """
    @property
    def conclusions(self) -> builtins.list[Constraint]:
        r"""
        The conclusions.
        """
    @property
    def degenerate_witnesses(self) -> builtins.list[DegenerateWitness]:
        r"""
        The degenerate witnesses (the negative controls).
        """
    @property
    def generic_witnesses(self) -> builtins.list[GenericWitness]:
        r"""
        The generic witnesses.
        """
    def __new__(cls, id: builtins.str, title: builtins.str, statement: builtins.str, hypotheses: typing.Sequence[Constraint], conclusions: typing.Sequence[Constraint], nondegeneracy: typing.Sequence[Condition] = [], coordinate_gloss: typing.Sequence[tuple[builtins.str, builtins.str]] = [], degenerate_witnesses: typing.Sequence[DegenerateWitness] = [], generic_witnesses: typing.Sequence[GenericWitness] = []) -> GeometryProblem:
        r"""
        States a theorem from its hypotheses, non-degeneracy conditions,
        conclusions and witnesses.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class GeometryReport:
    r"""
    The counts an accepted certificate discharged.
    
    Every one of these is exposed because a zero is the fail signal: a checker
    that verified nothing and a checker that verified everything must not look
    alike from Python.
    """
    @property
    def conclusions_checked(self) -> builtins.int:
        r"""
        Conclusions whose cofactor identity was re-expanded.
        """
    @property
    def degenerate_witnesses_checked(self) -> builtins.int:
        r"""
        Degenerate witnesses replayed.
        """
    @property
    def generic_witnesses_checked(self) -> builtins.int:
        r"""
        Generic witnesses replayed.
        """
    @property
    def numeric_points_checked(self) -> builtins.int:
        r"""
        Integer points at which the identity was re-evaluated.
        """
    @property
    def conditions_used(self) -> builtins.list[builtins.str]:
        r"""
        The non-degeneracy conditions the proof actually used.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class GeometryVerdict:
    r"""
    The verdict of the independent geometry checker.
    """
    @property
    def kind(self) -> builtins.str:
        r"""
        `"Verified"` or `"Rejected"`.
        """
    @property
    def report(self) -> typing.Optional[GeometryReport]:
        r"""
        The counts, on `Verified`.
        """
    @property
    def reason(self) -> typing.Optional[builtins.str]:
        r"""
        The rejection reason, on `Rejected`.
        """
    def is_verified(self) -> builtins.bool:
        r"""
        Whether the certificate re-derived.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class ProofOutcome:
    r"""
    The three-way result of attempting to certify a [`GeometryProblem`].
    """
    @property
    def kind(self) -> builtins.str:
        r"""
        `"Certified"`, `"NotInSaturatedIdeal"`, or `"Declined"`.
        """
    @property
    def certificate(self) -> typing.Optional[GeometryCertificate]:
        r"""
        The certificate, when one was produced.
        """
    @property
    def conclusion_id(self) -> typing.Optional[builtins.str]:
        r"""
        The identifier of a conclusion that did not reduce to zero.
        
        Present only on `NotInSaturatedIdeal`, which claims **nothing**: the
        theorem may need a condition nobody stated, or may be false.
        """
    @property
    def remainder(self) -> typing.Optional[cas.MvPoly]:
        r"""
        The nonzero remainder, on `NotInSaturatedIdeal`.
        """
    @property
    def decline(self) -> typing.Optional[GeometryDecline]:
        r"""
        The decline reason, on `Declined`.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class Pt:
    r"""
    A point with polynomial coordinates.
    """
    @property
    def x(self) -> cas.MvPoly:
        r"""
        The `x` coordinate.
        """
    @property
    def y(self) -> cas.MvPoly:
        r"""
        The `y` coordinate.
        """
    @staticmethod
    def free(name: builtins.str) -> Pt:
        r"""
        A free point `(name_x, name_y)`, its coordinates fresh variables.
        """
    @staticmethod
    def fixed(x: builtins.int  |  fractions.Fraction  |  cas.Rational, y: builtins.int  |  fractions.Fraction  |  cas.Rational) -> Pt:
        r"""
        A point at fixed rational coordinates.
        
        # Errors
        
        Raises `ValueError` when a coordinate is not an exact rational.
        """
    def sub(self, other: Pt) -> typing.Optional[Pt]:
        r"""
        `self - other`, or `None` on overflow.
        """
    def add(self, other: Pt) -> typing.Optional[Pt]:
        r"""
        `self + other`, or `None` on overflow.
        """
    def scale(self, factor: builtins.int  |  fractions.Fraction  |  cas.Rational) -> typing.Optional[Pt]:
        r"""
        `factor * self`, or `None` on overflow.
        
        # Errors
        
        Raises `ValueError` when `factor` is not an exact rational.
        """
    def __repr__(self) -> builtins.str: ...

def centroid(first: Pt, second: Pt, third: Pt) -> typing.Optional[Pt]:
    r"""
    The centroid of three points.
    """

def certify(problem: GeometryProblem, limits: typing.Optional[groebner.Limits] = None) -> ProofOutcome:
    r"""
    The Groebner-saturation route only.
    """

def certify_any_route(problem: GeometryProblem, limits: typing.Optional[groebner.Limits] = None) -> ProofOutcome:
    r"""
    The front door: tries every route and returns the three-way outcome.
    """

def certify_by_linear_elimination(problem: GeometryProblem, handover: typing.Optional[groebner.Limits] = None) -> ProofOutcome:
    r"""
    The linear-elimination route, with an optional Groebner handover.
    """

def collinear(first: Pt, second: Pt, third: Pt) -> typing.Optional[cas.MvPoly]:
    r"""
    Three points are collinear.
    """

def concyclic(first: Pt, second: Pt, third: Pt, fourth: Pt) -> typing.Optional[cas.MvPoly]:
    r"""
    Four points lie on a common circle.
    """

def corpus() -> builtins.list[GeometryProblem]:
    r"""
    The committed corpus of geometry theorems.
    """

def det(first: Pt, second: Pt) -> typing.Optional[cas.MvPoly]:
    r"""
    The determinant of the two coordinate vectors.
    """

def dist_sq(first: Pt, second: Pt) -> typing.Optional[cas.MvPoly]:
    r"""
    The squared distance between two points.
    """

def dot(first: Pt, second: Pt) -> typing.Optional[cas.MvPoly]:
    r"""
    The dot product of the two coordinate vectors.
    """

def equidistant(first: Pt, second: Pt, other_first: Pt, other_second: Pt) -> typing.Optional[cas.MvPoly]:
    r"""
    `|first - second|` equals `|other_first - other_second|`.
    """

def frontier() -> builtins.list[GeometryProblem]:
    r"""
    The frontier: theorems the producer does not yet certify.
    """

def geometry_limits() -> groebner.Limits:
    r"""
    The ceilings calibrated to the committed geometry corpus.
    """

def midpoint(first: Pt, second: Pt) -> typing.Optional[Pt]:
    r"""
    The midpoint of two points.
    """

def parallel(first: Pt, second: Pt, other_first: Pt, other_second: Pt) -> typing.Optional[cas.MvPoly]:
    r"""
    `first->second` is parallel to `other_first->other_second`.
    """

def perpendicular(first: Pt, second: Pt, other_first: Pt, other_second: Pt) -> typing.Optional[cas.MvPoly]:
    r"""
    `first->second` is perpendicular to `other_first->other_second`.
    """

def same_point(first: Pt, second: Pt) -> typing.Optional[builtins.list[cas.MvPoly]]:
    r"""
    The two coordinate equations saying that two points coincide.
    """

