# `typing.Any` in the generated `axeyum._native` stubs, with the reason for each.
#
# Generated stubs, hand-written reasons. `tools/check_stub_types.py` fails when a
# site here no longer has `Any` (the entry has to go, so the budget cannot be
# re-spent) and when an `Any` appears that is not here. The count can only fall.
#
# Every line is one site and one reason, separated by TWO SPACES. A site is
# `<module>.<qualname>(<parameter>)` or `<module>.<qualname> -> return`.
#
# The rule this file exists to enforce is the one in
# `docs/python-2026-08/10-quality-best-practices.md`: a stub type that cannot be
# derived stays `Any` and is listed -- a WRONG type is worse than none. Nothing
# below is a to-do; each is a signature whose Python type is decided at run time
# by another argument, by a sort, or by the caller.

axeyum._native.BvValue.__eq__ -> return  Returns a Python bool OR NotImplemented -- a bit-vector is not an int, and deferring lets the other operand decide.
axeyum._native.BvValue.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.FuncValue.default -> return  An IR `Value`. Its Python type is decided by the term's SORT at run time -- bool, int, str, fractions.Fraction, BvValue, ArrayValue, GenericArrayValue, DatatypeValue, UninterpretedValue or RealAlgebraicValue -- so no single annotation is both true and useful.
axeyum._native.GenericArrayValue.default -> return  An IR `Value`. Its Python type is decided by the term's SORT at run time -- bool, int, str, fractions.Fraction, BvValue, ArrayValue, GenericArrayValue, DatatypeValue, UninterpretedValue or RealAlgebraicValue -- so no single annotation is both true and useful.
axeyum._native.UninterpretedValue.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.cas.Expr.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.cas.Expr.__pow__(modulo)  CPython's third `pow()` argument. Any object may arrive; anything that is not None is refused with TypeError, because modular exponentiation is not defined on this fragment.
axeyum._native.cas.Matrix.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.cas.Monomial.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.cas.MultiPoly.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.cas.MvPoly.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.cas.Rational.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.cas.ZeroTest.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.cas.certify.geometry.GeometryCertificate.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.cas.certify.gf2.Gf2Poly.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.cas.certify.gf2.HalfDegreeArtifact.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.cas.certify.gf2.IrreducibilityCertificate.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.cas.certify.groebner.DeclineReason.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.cas.certify.sturm.Interval.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.cas.certify.telescoping.TelescopingCertificate.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.ir.Arena.bv_const(value)  Accepts a Python int or a BvValue of the same width; the width check is a run-time refusal, not something an annotation can carry.
axeyum._native.ir.Assignment.get -> return  An IR `Value`. Its Python type is decided by the term's SORT at run time -- bool, int, str, fractions.Fraction, BvValue, ArrayValue, GenericArrayValue, DatatypeValue, UninterpretedValue or RealAlgebraicValue -- so no single annotation is both true and useful.
axeyum._native.ir.Assignment.set(value)  The Python denotation of a value of the symbol's sort -- bool, int, fractions.Fraction, str or BvValue, chosen by the sort, and rejected with SortError when it does not match.
axeyum._native.ir.Constructor.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.ir.Datatype.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.ir.Func.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.ir.Sort.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.ir.SortRef.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.ir.Symbol.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.ir.Term.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.ir.TermNode.value -> return  An IR `Value`. Its Python type is decided by the term's SORT at run time -- bool, int, str, fractions.Fraction, BvValue, ArrayValue, GenericArrayValue, DatatypeValue, UninterpretedValue or RealAlgebraicValue -- so no single annotation is both true and useful.
axeyum._native.ir.bits.bv_value_to_lsb_bits(value)  Accepts a Python int or a BvValue of the same width; the width check is a run-time refusal, not something an annotation can carry.
axeyum._native.ir.bits.lsb_bits_to_bv_value -> return  An IR `Value`. Its Python type is decided by the term's SORT at run time -- bool, int, str, fractions.Fraction, BvValue, ArrayValue, GenericArrayValue, DatatypeValue, UninterpretedValue or RealAlgebraicValue -- so no single annotation is both true and useful.
axeyum._native.ir.bits.lsb_bits_to_value -> return  An IR `Value`. Its Python type is decided by the term's SORT at run time -- bool, int, str, fractions.Fraction, BvValue, ArrayValue, GenericArrayValue, DatatypeValue, UninterpretedValue or RealAlgebraicValue -- so no single annotation is both true and useful.
axeyum._native.ir.bits.value_to_lsb_bits(value)  The Python denotation of a value of `sort`; which Python type is accepted is decided by that argument.
axeyum._native.ir.bv.BitLowering.evaluate_root -> return  An IR `Value`. Its Python type is decided by the term's SORT at run time -- bool, int, str, fractions.Fraction, BvValue, ArrayValue, GenericArrayValue, DatatypeValue, UninterpretedValue or RealAlgebraicValue -- so no single annotation is both true and useful.
axeyum._native.ir.eval -> return  An IR `Value`. Its Python type is decided by the term's SORT at run time -- bool, int, str, fractions.Fraction, BvValue, ArrayValue, GenericArrayValue, DatatypeValue, UninterpretedValue or RealAlgebraicValue -- so no single annotation is both true and useful.
axeyum._native.ir.fp.FloatFormat.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.ir.well_founded_default -> return  An IR `Value`. Its Python type is decided by the term's SORT at run time -- bool, int, str, fractions.Fraction, BvValue, ArrayValue, GenericArrayValue, DatatypeValue, UninterpretedValue or RealAlgebraicValue -- so no single annotation is both true and useful.
axeyum._native.kernel.ExprId.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.kernel.LevelId.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.kernel.Lit.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.kernel.Lit.nat(value)  Read through `str()` and parsed as an arbitrary-precision decimal, so an int and its decimal spelling are both accepted and neither is the whole type.
axeyum._native.kernel.Lit.value -> return  An int for a `nat` literal and a str for a `str` literal; `.kind` says which.
axeyum._native.kernel.NameId.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.kernel.Prelude.__getattr__ -> return  Attribute access over a prelude's declarations: the value is whatever the named field holds, which the class does not know statically.
axeyum._native.kernel.Prelude.__getitem__ -> return  Item access over a prelude's declarations; same as `__getattr__`.
axeyum._native.producers.DeclineReason.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.producers.ImportLimits.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.producers.import_candidate_statement_ndjson(source)  A filesystem path (str, bytes or os.PathLike) or in-memory NDJSON bytes; `resolve_source` decides which and raises TypeError otherwise.
axeyum._native.producers.import_statement_ndjson(source)  A filesystem path (str, bytes or os.PathLike) or an in-memory bytes buffer; `resolve_source` decides which and raises TypeError otherwise.
axeyum._native.smt.ScriptTerm.__eq__(other)  A rich-comparison operand is any Python object; the body answers False or defers with NotImplemented rather than narrowing the type.
axeyum._native.smt.get_value -> return  One model value per `(get-value ...)` term, each typed by its own sort -- the same union as the IR evaluator returns.


# --- Q8 CAS long-tail (2026-08-24): gf2/stats report getters, algebraic/beta rationals, and __eq__ ---
axeyum._native.cas.AlgebraicReal.__eq__(other)  structural equality takes any Python object
axeyum._native.cas.AlgebraicReal.refine(width)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionConnectedAdamsTraceReport.candidate_absolute_bound -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionConnectedAdamsTraceReport.centered_fourth_moment -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionConnectedAdamsTraceReport.centered_second_moment -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionConnectedAdamsTraceReport.connected_adams_trace -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionConnectedAdamsTraceReport.fourth_cumulant_numerator -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionConnectedAdamsTraceReport.minimum_normalized_betti_ceiling -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllThreeDegreeSevenClosedForm.centered_fourth_moment -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllThreeDegreeSevenClosedForm.centered_second_moment -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllThreeDegreeSevenClosedForm.connected_adams_trace -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllThreeDegreeSevenClosedForm.field_order -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllThreeDegreeSevenClosedForm.fourth_cumulant_numerator -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllThreeDegreeSevenClosedForm.ordinary_class_count -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllThreeDegreeSevenClosedForm.ordinary_class_population -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllThreeDegreeSevenClosedForm.special_class_count -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllThreeDegreeSevenClosedForm.special_class_population -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllThreeDegreeSevenWittShiftedClosedForm.conductor_one_high_character_trace -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllThreeDegreeSevenWittShiftedClosedForm.conductor_two_high_character_trace -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllThreeDegreeSevenWittShiftedClosedForm.field_order -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllThreeDegreeSevenWittShiftedClosedForm.supported_coarse_mass -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllTwoDegreeFiveClosedForm.centered_fourth_moment -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllTwoDegreeFiveClosedForm.centered_second_moment -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllTwoDegreeFiveClosedForm.connected_adams_trace -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllTwoDegreeFiveClosedForm.field_order -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllTwoDegreeFiveClosedForm.fourth_cumulant_numerator -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllTwoDegreeFiveClosedForm.nonzero_subtrace_population -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionEllTwoDegreeFiveClosedForm.zero_subtrace_population -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionWittShiftedLayerTrace.identity_aggregate_mass -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionWittShiftedLayerTrace.parent_aggregate_mass -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionWittShiftedLayerTrace.signed_high_character_trace -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionWittShiftedLayerTrace.signed_spatial_layer -> return  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.BinaryExtensionWittShiftedTraceReport.aggregate_global_mass -> return  exact BigInt/BigRational report field crossing as a Python int/Fraction from a decimal string; typed newtype is a follow-up
axeyum._native.cas.BoolExpr.__eq__(other)  structural equality takes any Python object
axeyum._native.cas.ExtensionTraceHankelMinor.determinant -> return  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.Permutation.__eq__(other)  structural equality takes any Python object
axeyum._native.cas.RealSet.__eq__(other)  structural equality takes any Python object
axeyum._native.cas.RealSet.contains(x)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.RealSet.finite(points)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.RealSet.point(a)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.algebraic_real_roots(coeffs)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.beta(x)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.beta(y)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.certify.ansatz.AnsatzLimits.__eq__(other)  structural equality takes any Python object
axeyum._native.cas.certify.ansatz.LinearBlock.__eq__(other)  structural equality takes any Python object
axeyum._native.cas.certify.ansatz.cofactors_by_ansatz(generators)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.certify.ansatz.combination(cofactors)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.certify.ansatz.combination(generators)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.certify.ansatz.detect_linear_blocks(generators)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.certify.ansatz.eliminate(generators)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.certify.ansatz.eliminate_blocks(generators)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.certify.moments.CertifiedSquaredBinomialFallingMoment.__eq__(other)  structural equality takes any Python object
axeyum._native.cas.certify.moments.CertifiedSquaredBinomialMoment.__eq__(other)  structural equality takes any Python object
axeyum._native.cas.certify.moments.WzCertificate.__eq__(other)  structural equality takes any Python object
axeyum._native.cas.certify.sturm.SetInterval.__eq__(other)  structural equality takes any Python object
axeyum._native.cas.certify.sturm.SetInterval.closed_open(a)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.certify.sturm.SetInterval.closed_open(b)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.certify.sturm.SetInterval.open_closed(a)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.certify.sturm.SetInterval.open_closed(b)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.certify.sturm.SetInterval.point(a)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.covariance(xs)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.covariance(ys)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.exact_rational(value)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.gamma(x)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.gegenbauer(weight)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.generalized_laguerre(alpha)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.gram_schmidt(vectors)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.jacobi(alpha)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.jacobi(beta)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.lagrange_interpolation(points)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.mean(data)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.median(data)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.mode(data)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.newton_divided_differences(points)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.pade(series_coeffs)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.pade_fraction(series_coeffs)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.sample_variance(data)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
axeyum._native.cas.variance(data)  exact rational value, sort-decided at runtime; typed newtype is a follow-up
