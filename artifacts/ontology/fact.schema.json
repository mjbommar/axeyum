{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://axeyum.dev/ontology/fact.schema.json",
  "title": "Fact",
  "description": "A mathematical statement as a first-class object, independent of its status.\n\nWHY THIS EXISTS. Every other resource in this repository holds either a formal statement or a status, never both. A `claim` carries prose in `statement` and a CNF *generator recipe* in `formal`, so nothing downstream can reason with what it asserts. A kernel declaration is formal but exists only once proved, and links to no concept, ledger or evidence row. A curriculum node is a topic. A math-education concept is prose with a label. So there was no object that could hold `forall a b : Nat, a + b = b + a` AND say open / conjectured / computed / proved / refuted, with its dependencies and its evidence.\n\nWHAT IT UNLOCKS. A fact is the object the self-extension loop operates on: pick an `open` fact whose `depends_on` are all established, dispatch its `formal.statement` to the solver, reconstruct the result into the kernel, attach evidence, flip the status, record the axiom footprint. Each of those stages exists today and keeps its own incompatible record; this is the shared one.\n\nSELF-CONTAINMENT IS A REQUIREMENT. A fact must stand alone on `formal.statement` plus `status` plus `evidence`. `concept_refs` are provenance and gloss, never substance -- so a public repository works while a source corpus stays private, and nothing needs vendoring or republishing.\n\nRELATION TO `claim`. A claim is an *evidence row about an instance*: it says a specific value was computed and how to re-derive it. A fact is the *proposition*. Claims reference facts through `evidence[].supports`; they are not superseded by them.",
  "type": "object",
  "required": [
    "schema_version",
    "id",
    "title",
    "statement",
    "formal",
    "epistemic_status",
    "depends_on",
    "evidence",
    "provenance"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_version": { "type": "integer", "const": 1 },

    "id": {
      "type": "string",
      "pattern": "^F:[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Stable identifier, `F:` prefixed, e.g. `F:nat-add-comm`. Mirrors the `C:`/`TQ:` convention of the concept graph so the two can be cited side by side."
    },

    "title": { "type": "string", "minLength": 1 },

    "statement": {
      "type": "string",
      "minLength": 1,
      "description": "The proposition in prose, for a human. Never the machine-readable form -- that is `formal.statement`. Kept because a formal statement alone is not reviewable."
    },

    "formal": {
      "type": "object",
      "required": ["language", "statement", "fragment"],
      "additionalProperties": false,
      "description": "What is asserted, machine-readably. This is the field that distinguishes a fact from a claim: a claim's `formal` is a generator recipe, this is the proposition itself.",
      "properties": {
        "language": {
          "enum": ["smtlib2", "lean4", "lean4-surface", "axeyum-ir", "cas-term", "certificate-spec"],
          "description": "`smtlib2` is dispatchable to the solver; `axeyum-ir` is the internal term form.\n\n`cas-term` is the computer-algebra statement form: a `axeyum_cas::telescoping::HyperTerm` specification (a product of `Γ(linear form)^e`, `base^(linear form)` and `polynomial^e` factors) together with a summation and a claimed closed form, rendered as text. It exists because a definite hypergeometric identity is not expressible in any of the other four: it quantifies over ℕ so it is not `smtlib2`, and no kernel declaration for `Γ`, `∑` or binomial coefficients exists, so writing it as `lean4` would name a theorem the kernel cannot state. The distinction is load-bearing — the whole point of `cas-certificate` facts is that a `cas-term` statement can be settled for ALL `n` by a polynomial identity, and mislabelling the language would make the fact look dispatchable to a machine that cannot read it.\n\n`certificate-spec` is a checker-native proposition encoded as a versioned canonical JSON object. It is not automatically dispatchable and does not imply an SMT sort, a kernel term, or a CAS identity. A registered operation must name the exact format and checker before Autogenesis may execute it.\n\n`lean4` means Lean **kernel core**, as emitted by `Kernel::render_lean` — the form `nat_theorem_inventory` prints and the form a fact should normally carry. `lean4-surface` means source syntax intended for the elaborator.\n\nThese are NOT interchangeable and the difference is observed, not theoretical: Lean's elaborator inserts pending implicit arguments when a parenthesized application closes, so `(@Eq.refl α) a` fails where the flat `@Eq.refl α a` type-checks. A statement that is valid core can therefore be rejected as surface. One extraction lane had 19 core statements and 1 surface (an iteration the prelude cannot express) and no way to mark the difference except English prose in `notes`."
        },
        "statement": {
          "type": "string",
          "minLength": 1,
          "description": "The proposition in the named language. For `smtlib2`, an assertion whose NEGATION being unsat establishes the fact."
        },
        "fragment": {
          "type": "string",
          "description": "The theory that decides it, e.g. `QF_LIA`, `Nat`, `QF_NRA`, or `none` when no fragment we have decides it. Drives reachability queries."
        },
        "free_symbols": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Symbols the statement quantifies over or leaves free, so a consumer need not re-parse to know its shape."
        },
        "kernel_statement": {
          "type": "string",
          "minLength": 1,
          "description": "The kernel's OWN rendering of `kernel_theorem` -- `Kernel::render_lean` output, the form `nat_theorem_inventory` prints -- when that differs from `statement`.\n\nIt exists to give a lane somewhere to put the rendered type it has in hand at the moment it flips a fact to `proved`. Without such a place, nineteen `ml430` mirror facts had the rendered type written OVER `formal.statement`; because a mirror's top-level `statement` is only a prose reference by name ('the proposition declared as X in the pinned Mathlib v4.30 source'), those facts stopped carrying the proposition they claimed to mirror anywhere in the file. Nothing was false -- the theorems were real and axiom-free -- but the ledger stopped being self-contained for those rows, and self-containment is what lets a referee check 'we proved what Mathlib states' in one command instead of by git archaeology. See `docs/research/11-design-review/2026-08-29-nineteen-mirrors-lost-their-statement.md` and `scripts/check-mirror-statement-fidelity.py`.\n\nThe division is: `statement` is WHAT IS CLAIMED, in the vocabulary the claim was made in (for a mirror, the pinned upstream proposition verbatim); `kernel_statement` is HOW OUR KERNEL SPELLS IT. They are frequently different strings for the same proposition -- `Nat.Coprime` has no separate name in the Nat prelude and is spelled inline as `gcd _ _ = 1` -- and that difference is exactly what a reviewer wants to see, not a discrepancy to hide by overwriting one with the other.\n\nOnly meaningful alongside a `kernel_theorem` naming the declaration being rendered; `scripts/check-mirror-statement-fidelity.py` rejects it without one, ledger-wide (that rule is not scoped to mirrors, unlike the rest of that gate). Set it only when the rendering differs from `statement`; omit when they coincide."
        },
        "kernel_theorem": {
          "type": ["string", "null"],
          "description": "Which kernel-lean theorem this fact is FIRST-PARTY about, stated explicitly rather than left for a checker_command regex to guess. Every consumer that needs a fact's subject theorem (`create-autogenesis-chain-catalog.py`, `check-fact-depends-derived.py`, `create-autogenesis-snapshot.py`, all sharing one `theorem_of`) reads this key WHEN PRESENT, before falling back to extracting the first dotted theorem name mentioned in the fact's own evidence `checker_command`s.\n\nOmit the key entirely to ask for that fallback -- correct for the common case, where evidence mentions the fact's own theorem and nothing else that looks like one.\n\nSet it to a STRING when extraction would guess wrong or ambiguously: evidence commonly also names dependency/witness theorems (a `dependencies-X` grep listing what X's proof used, or a witness lemma cited to corroborate the argument), and a regex has no way to tell 'the fact's subject' from 'a theorem this fact's evidence happens to mention'.\n\nSet it explicitly to `null` -- present, not absent -- when the fact is genuinely NOT about one single kernel theorem: a package-level result (e.g. 'the whole Complex ring is axiom-free', bundling several laws; a categoricity fact whose evidence checks several related theorems together). `null` here is a deliberate assertion ('no single subject'), distinct from omitting the key ('infer conservatively'), and it is what stops two such package-level facts from silently colliding onto the same extracted name -- which happened: `F:complex-mul-assoc` and `F:complex-ring-constructed-axiom-free` both extracted to `Complex.mul_assoc` before this field existed."
        }
      }
    },

    "epistemic_status": {
      "enum": ["axiom", "proved", "computed", "empirical", "conjectured", "open", "refuted"],
      "description": "**What THIS system has established.** Deliberately the SAME vocabulary as `claim.schema.json` and the math-education concept graph, plus `refuted`. A fact can be refuted; a claim, being a record of something established, cannot.\n\nThis is a statement about our ledger, NOT about mathematics. `open` here means we have not established it, whoever else may have. See `external_status`."
    },

    "external_status": {
      "enum": ["proved", "refuted", "conjectured", "open", "unknown"],
      "description": "**What is known to mathematics, independent of us.** Optional; omit when nobody has looked, which is different from `unknown` (nobody has looked *and we checked*).\n\nWHY THIS EXISTS. Without it, `epistemic_status: open` does double duty for two unrelated situations: `Goldbach` (nobody has proved it) and `Fermat's Last Theorem` (proved in 1995; we simply have not imported it). Both are `open` to us and only one is open. That has two concrete costs: the self-extension loop consumes `open` facts, so it would burn its queue retrying settled theorems; and the headline open-count -- a number this project reports -- becomes meaningless.\n\nThe first extraction lane hit this immediately and REFUSED to extract Fermat's Last Theorem, correctly, on the grounds that every available status would have been a lie.\n\nTHE INTERESTING COMBINATION. `epistemic_status: proved`/`computed` together with `external_status: open`/`conjectured` is not a contradiction -- it is a **new result**, this project's whole point (e.g. R_4(5(x-y)=2z) = 625). The validator surfaces these rather than rejecting them."
    },

    "depends_on": {
      "type": "array",
      "items": { "type": "string", "pattern": "^F:[a-z0-9]+(-[a-z0-9]+)*$" },
      "description": "Other facts this one rests on. This is the DAG the build order comes from -- over STATEMENTS, not topics. May be empty for a foundational fact."
    },

    "proof_route": {
      "enum": ["kernel-lean", "imported-kernel-lean", "smt-term-level", "smt-clausal", "search-certificate", "cas-certificate", "none"],
      "description": "WHICH machine established this, because `axiom_footprint` is only meaningful — and only comparable — within a route.\n\nWHY THIS EXISTS. Two vocabularies were coexisting in `axiom_footprint` with nothing marking the difference: 17 facts carried `[]` (kernel route, axiom-free) and 14 carried `[\"axeyum-ir.bool-evaluator\", \"classical-two-valued-bool-semantics\"]` (SMT route, strings a lane had to invent because the field offered none). A reader comparing them would conclude the first group rests on less. That comparison is invalid — they are different trust bases, not different amounts of the same one.\n\nThe routes are NOT of equal strength and are not interchangeable. The logic prelude is intuitionistic and carries zero axioms of any trusted kind (measured), so excluded middle, double-negation elimination and Peirce's law are provable on `smt-term-level` and NOT obtainable on `kernel-lean` without a new axiom. A fact settled on one route is not thereby settled on the other, and a reconstruction pipeline that assumes otherwise hits an opaque wall on every classical tautology.\n\n`kernel-lean` — a proof term THIS project constructed, type-checked by our independent kernel. `imported-kernel-lean` — a proof term this project did NOT construct, translated from a pinned foreign export and admitted by our independent kernel. `smt-term-level` — exhaustive evaluation by the IR evaluator, trusting neither bit-blaster, CNF encoder nor SAT solver. `smt-clausal` — decided through CNF, with a DRAT/Alethe certificate. `search-certificate` — a combinatorial search whose result carries a replayable witness or cover. `cas-certificate` — an algebraic identity re-derived by exact polynomial arithmetic that shares no code with the search that found it (Gröbner cofactors `target = Σ cᵢ·gᵢ + r`; creative-telescoping / Wilf–Zeilberger certificates `Σ_j a_j(n)·F(n+j,k)/F(n,k) = R(n,k+1)·r(n,k) − R(n,k)`).\n\nWHY THIS IS NOT `search-certificate`. A witness or cover settles a FINITE statement: replaying it re-derives one instance, and a claim about all `n` needs one certificate per `n`. A CAS certificate is an identity in `ℚ(vars)`, so a single polynomial zero-test settles every instance at once — it is the only route here that can establish a universally quantified arithmetic statement without a kernel proof term. Conflating the two would make `axiom_footprint` incomparable in exactly the way this field exists to prevent, because their footprints differ in kind: a replayed witness assumes only the replay semantics, while an algebraic identity assumes characteristic zero, that the declared term specification denotes the intended summand, and whatever side conditions carry the identity from `ℚ(vars)` to the integers (for telescoping: that `R·F` has the same natural boundary as `F`). Those are real and `[]` is not available here.\n\nWHY `imported-kernel-lean` IS NOT `kernel-lean`. The type-checking is the same gate — `Kernel::add_declaration`, re-deriving the type from the proof term — so it is tempting to reuse the label. Two things differ, and both are the kind of difference `axiom_footprint` exists to keep visible.\n\nFirst, AUTHORSHIP. Every `kernel-lean` fact is a proposition this project constructed a proof of; that is the metric the project publishes and the number the self-extension loop is trying to raise. An imported theorem raises no such number, and a route that made the two indistinguishable would let the headline count be inflated by ingestion. The trap is specific: recording what Mathlib says does not make it checked here.\n\nSecond, TRUST BASE. `[]` on `kernel-lean` means a kernel environment admits no `Axiom`, `Opaque` or `Quotient`. An import can reach that state inside the imported environment and still rest on three things the constructed route does not: that the exporter faithfully rendered the source system's environment, that our translation of the wire format preserves meaning, and that the delivered bytes are the producer's intended export (format 3.1 has no footer, so completion is relative to the bytes handed over — the importer's own documentation says so). Those assumptions are real, so `[]` is not available on this route and the validator rejects it. Note also that the two systems' footprints are not written in the same vocabulary: for `Classical.em`, Lean's `#print axioms` reports three names while `Kernel::axiom_footprint` reports six, because our kernel classifies the whole quotient package as `Quotient` declarations. Ours is the more conservative reading, and it must not be silently reconciled to Lean's."
    },

    "axiom_footprint": {
      "type": "array",
      "items": { "type": "string" },
      "description": "What the establishing evidence rests on that was NOT itself proved. REQUIRED when `epistemic_status` is `proved`: an empty array means axiom-free and is a strictly stronger claim than an absent field. This is the metric the project publishes.\n\nONLY COMPARABLE WITHIN A `proof_route`. On `kernel-lean` these are kernel declaration names (`propext`, `Classical.choice`, `Quot.sound`) and `[]` means the environment admits no `Axiom`, `Opaque` or `Quotient` — a claim `Kernel::axiom_footprint` computes per theorem. On `smt-term-level` they name the semantic assumptions the evaluator makes; `[]` is NOT achievable there and the validator rejects it, because an empty list on that route would read as axiom-freedom the route cannot deliver."
    },

    "cas_substance": {
      "type": "object",
      "description": "What a `cas-certificate` fact's KERNEL obligation actually establishes (ADR-0622). Required on, and only on, a fact that `scripts/validate-facts.py` classifies as `kernel-reconstructed`. That classification asks which PACKAGE the checker runs, never what the kernel was asked to check, so without this block an obligation of the form `poly_expr(X) = 1 * poly_expr(X)` -- true of every polynomial -- moves the headline counter by exactly as much as a six-variable identity in which sixteen monomials from two independently-derived geometric predicates cancel to eight. Gated by `scripts/check-cas-substance.py`, which DERIVES the shape from the named certificate artifact and refuses a declaration that disagrees with it.",
      "additionalProperties": false,
      "required": ["shape", "certificate"],
      "properties": {
        "shape": {
          "enum": ["empty", "refl", "scale", "combination", "identity", "evaluation"],
          "description": "`empty`: nothing to reconstruct -- no coordinates, no generators, an already-empty conclusion polynomial; refused outright. `refl`: one generator carries a nonzero cofactor, that cofactor is the constant 1, and the generator is identical to the conclusion, so the obligation is X = 1*X and holds for EVERY polynomial. `scale`: one active generator whose cofactor must be distributed. `combination`: two or more active generators, so monomials contributed by distinct generators must cancel -- the shape that makes an identity specific to its configuration. `identity`: a symbolic ring identity at a free variable, not in cofactor form. `evaluation`: a closed obligation at concrete rationals. The first four are DERIVED from a certificate; the last two are declared, because their producers emit no certificate."
        },
        "certificate": {
          "type": ["string", "null"],
          "description": "Repository-relative path to the certificate artifact the shape is derived from, or null when the producer emits none -- in which case `derivation_declined_reason` must say so and the shape is self-reported."
        },
        "derivation_declined_reason": {
          "type": "string",
          "description": "Required when `certificate` is null: why no certificate artifact exists for this reconstruction."
        },
        "disclosure": {
          "type": "string",
          "description": "Required when the shape is non-discriminating (`empty` or `refl`): what the kernel obligation does NOT establish. Registration with disclosure is the honest outcome for a weak-but-real reconstruction; silence is not, and exclusion would drop real work."
        },
        "disclosure_axiom_key": {
          "type": "string",
          "description": "Required when the shape is non-discriminating: the key of the `axiom_footprint` entry carrying the same disclosure, so a reader meets it where the assumptions are listed and not only in a validator's summary."
        }
      }
    },

    "evidence": {
      "type": "array",
      "description": "Zero or more artifacts establishing the status. Deliberately the same shape as `claim.schema.json`'s evidence rows so one checker can walk both. A fact with `epistemic_status: open` carries an empty array -- and that is a statement, not an omission.",
      "items": {
        "type": "object",
        "required": ["id", "kind", "supports", "check_status"],
        "additionalProperties": true,
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "kind": {
            "enum": [
              "kernel-term",
              "witness-replay",
              "unsat-certificate",
              "cube-cover",
              "cube-tree-cover",
              "exhaustive-enumeration",
              "published-value-replication",
              "bound-citation",
              "instance-pin",
              "claim-ref"
            ],
            "description": "`kernel-term` is a proof term an independent kernel type-checks -- the strongest. `claim-ref` points at an existing `artifacts/claims/` entry, which is how a computed value becomes evidence for a proposition."
          },
          "supports": { "type": "string", "minLength": 1 },
          "check_status": { "enum": ["checked", "replay-only", "not-checked"] },
          "checkers": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Each INDEPENDENT re-derivation of this evidence, named. `check_status` alone cannot carry this: across the first 50 evidence rows in this ledger every single one said `checked`, so the field had no discriminating power at all, while one lane had in fact validated each result three separate ways — the producing solve, `Evidence::check` against a fresh parse, and an external z3 run — with all three collapsing into that one token.\n\nCross-oracle agreement is the strongest signal this repository has and it had nowhere to go but prose. List what actually re-derived the result, e.g. [\"producing-solve\", \"Evidence::check-fresh-parse\", \"z3-4.13\"]. Two entries that share an implementation are ONE check wearing two names; the value of the list is independence, not length."
          },
          "checker_command": { "type": "string" },
          "kernel_declaration": {
            "type": "string",
            "minLength": 1,
            "description": "Optional exact declaration ID for kernel-term evidence. This is the backwards-compatible identity bridge when the historical evidence id is not itself kernel-<fully-qualified-declaration>. It grants search linkage only; the checker and kernel remain authoritative."
          },
          "kernel_declarations": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": { "type": "string", "minLength": 1 },
            "description": "Optional exact declaration IDs when one kernel-term evidence row jointly checks multiple declarations. This plural binding takes precedence over kernel_declaration and the historical evidence id. It grants search linkage only; each named declaration must still resolve in the live kernel projection."
          },
          "checker_seconds": {
            "type": "integer",
            "minimum": 1,
            "description": "Measured typical wall-clock seconds for `checker_command` on a quiet machine. The replay gate gives such a row a proportionally larger budget instead of timing it out every run. Record what you MEASURED, not what you hope: this exists so an expensive-but-honest checker is distinguishable from a hung one, and inflating it hides real hangs."
          },
          "artifact": { "type": "string" },
          "notes": { "type": "string" }
        }
      }
    },

    "concept_refs": {
      "type": "array",
      "description": "Gloss and provenance only, NEVER substance. A fact must be intelligible without resolving these, so that a public repository works while a source corpus stays private.",
      "items": {
        "type": "object",
        "required": ["graph", "ref"],
        "additionalProperties": true,
        "properties": {
          "graph": { "type": "string" },
          "ref": { "type": "string" },
          "relation": { "type": "string" },
          "resolved": { "type": "boolean" }
        }
      }
    },

    "provenance": {
      "type": "object",
      "required": ["date"],
      "additionalProperties": true,
      "properties": {
        "date": { "type": "string" },
        "established_by": { "type": "string", "description": "What settled it: a lane, a solver route, an import, or a person." },
        "source": { "type": "string", "description": "Where the statement came from, if extracted rather than authored." },
        "prior_art": {
          "type": "array",
          "description": "Who established this outside axeyum. Required by the validator when `external_status` is settled AND we have not established it ourselves — i.e. when the literature is the only thing holding the fact up.",
          "items": {
            "type": "object",
            "additionalProperties": true,
            "properties": {
              "who": { "type": "string" },
              "what": { "type": "string" },
              "where": { "type": "string" },
              "year": { "type": "integer" },
              "attribution": {
                "type": "string",
                "description": "How the citation itself was obtained — first-class because a lane invented it as a convention and was right to. The honest common case is \"standard textbook attribution; this lane did not consult the primary source\", which is a materially weaker claim than having read the paper and must not be silently indistinguishable from it. This project has already cited unrefereed Zenodo self-deposits as though they were published literature."
              }
            }
          }
        }
      }
    },

    "notes": { "type": "string" },

    "supersedes": {
      "type": "array",
      "items": { "type": "string", "pattern": "^F:[a-z0-9]+(-[a-z0-9]+)*$" }
    },

    "equivalent_to": {
      "type": "array",
      "minItems": 1,
      "maxItems": 1,
      "items": { "type": "string", "pattern": "^F:[a-z0-9]+(-[a-z0-9]+)*$" },
      "description": "Deliberately NOT `supersedes` -- this fact is not obsolete and nothing here was replaced. It marks that this fact's `formal.statement` is a BYTE-IDENTICAL kernel proposition to the named fact (same `Kernel::render_lean` canonical type, verified by scripts/check-proposition-duplication.py against scripts/check-trust-closure.py's identity classes), proved either independently or by literally invoking the named fact's kernel theorem in this one's proof term. Both facts stay `proved`; neither is retracted (ADR-0542: no deletion of a settled fact, only an amendment). The field exists so a headline count can separate FACTS SETTLED (includes both members of a pair) from DISTINCT PROPOSITIONS ESTABLISHED (collapses each pair to its one non-`equivalent_to` member) -- see ADR-0790. Length-1 by construction: this project's duplicate identity classes are all size-2 (identity-map.tsv), and a fact points at exactly the ONE class-mate the counting rule designates canonical, never at a whole class."
    }
  }
}
