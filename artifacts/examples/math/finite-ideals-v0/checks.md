# Checks

## `z6-even-ideal`

Expected result: `sat`.

The validator checks that `{0, 2, 4}` is an additive subgroup of `Z/6Z` and is
closed under left and right multiplication by every ring element.

## `principal-ideal-span-replay`

Expected result: `sat`.

The validator recomputes the finite two-sided ideal generated by `2` and checks
that it equals `{0, 2, 4}`.

## `mod-two-ring-hom-kernel-image`

Expected result: `sat`.

The validator checks that reduction modulo `2` preserves zero, one, addition,
and multiplication, then recomputes kernel and image.

## `quotient-ring-replay`

Expected result: `sat`.

The validator checks that the even and odd cosets partition `Z/6Z`, then
verifies quotient addition and multiplication from representatives.

## `qf-uf-quotient-ring-representative-alethe`

Expected result: `unsat`.

The SMT-LIB artifact checks the congruence obligation behind quotient-ring
well-definedness: if `0` and `2` are the same quotient class, and `1` and `3`
are the same quotient class, then quotient addition on those representative
pairs must agree. The row links that fixed EUF contradiction to checked
QF_UF/Alethe evidence.

## `bad-ideal-rejected`

Expected result: `unsat`.

The validator rejects `{0, 2}` because it is not additively closed:
`2 + 2 = 4`, and `4` is not in the subset. This row is exact finite replay; it
does not own the proof object.

## `qf-uf-bad-ideal-additive-closure`

Expected result: `unsat`.

The SMT-LIB artifact records the fixed membership conflict behind the replayed
bad ideal: `2` and `2` are present, `2 + 2 = 4`, `4` is absent, and the
malformed row claims `in_subset(add(2,2)) = present`. The row links that fixed
EUF contradiction to checked QF_UF/Alethe evidence.

## `general-ideal-theory-lean-horizon`

Expected result: `not-run`.

Ideal correspondence theorems, prime and maximal ideals, localization,
Noetherian ideal theory, and algebraic-geometry spectra belong in future Lean
resources. The finite rows above are exact table replay checks only.
