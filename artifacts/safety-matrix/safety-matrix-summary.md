# Safety matrix census (S0)

Generated by `scripts/gen-safety-matrix.py`. Do not hand-edit.

Ledger: 2368 facts, 2182 `proved`. Every proved fact appears
exactly once in `safety-matrix.tsv`.

A `yes` means the protection is PRESENT for this row, not that it is
strong; `checker_fanout_max` says how many other facts share its widest
checker.

## Protection coverage

| protection | proved facts with it | share |
|---|---:|---:|
| `kernel_theorem` | 2012 / 2182 | 92.2% |
| `per_theorem_footprint` | 65 / 2182 | 3.0% |
| `env_footprint` | 1914 / 2182 | 87.7% |
| `circularity` | 14 / 2182 | 0.6% |
| `semantic_falsification` | 101 / 2182 | 4.6% | **UPPER BOUND — 8 demonstrated.** Counts facts whose evidence names a semantic control, not facts whose control was shown to discriminate.
| `mutation_control` | 15 / 2182 | 0.7% |
| `independent_replay` | 7 / 2182 | 0.3% |
| `coverage_bearing_checker` | 1988 / 2182 | 91.1% |

## Centrally-enforced coverage (NOT the same measurement)

The columns above are PER-FACT EVIDENCE: this fact's own record
exercises the protection. A gate that enforces the same protection on
every merge, for facts that cite nothing, moves none of them. The two
are different questions and neither dominates -- per-fact evidence is
self-describing and survives the fact being copied elsewhere; central
coverage is stronger in practice and far cheaper. ADR-0795.

A gate earns a column here only by publishing, machine-readably, the
PER-FACT SET it reached. A headline number confers nothing on any
member, and `protection_count` above deliberately excludes these: a
fact is not better protected because somebody else measured it.

| coverage | proved facts | share | published per-fact set |
|---|---:|---:|---|
| `exact_statement` | 2180 / 2182 | 99.9% | `artifacts/ontology/settled-fact-statement-pins.json` `pins[].fact_id` (S1, ADR-0763) |

Gates that enforce a protection this census names and publish NO
machine-readable per-fact set, so they cannot be credited here. Each
row is a finding about the GATE, not about the facts; the missing
column is what the gate would have to emit (ADR-0795).

| protection | gate | publishes | what it would have to emit |
|---|---|---|---|
| `circularity`, `per_theorem_footprint` | `scripts/check-trust-closure.py` (S2) | `subjects` / `unresolved` counts | the `subjects.resolved` fact-id set it already builds and discards |
| `semantic_falsification` | `scripts/check-semantic-control-fixtures.py` (S3) | `census.load_bearing_facts` count in `fixture-pack.json` | the `load_bearing` map keyed by fact id, which it already computes |
| `independent_replay` | `real_lean_replay_census` (S4, ADR-0760) | declaration names Lean's kernel admitted | the fact-to-declaration join, so a NAME grade becomes a FACT grade |
| `mutation_control` | `scripts/check-statement-identity-mutations.py` (S1) | one ledger-wide pass/fail | nothing -- it is not a per-fact protection and should not be read as one |

S3's own artifact already states the direction that matters: its
`semantic_falsification` figure is an UPPER bound, counting facts with a
semantic evidence row rather than facts whose control was shown to
discriminate. Measured 2026-08-30: evidence 95, demonstrated 8.

## Protections per fact

| protections held | facts |
|---:|---:|
| 0 | 83 |
| 1 | 98 |
| 2 | 38 |
| 3 | 1919 |
| 4 | 35 |
| 5 | 9 |

## Checker fan-out

2379 distinct `checker_command`s cover 2182 proved facts.

| facts sharing one command | commands |
|---:|---:|
| 1 | 2305 |
| 2 | 26 |
| 3 | 7 |
| 4 | 10 |
| 5 | 5 |
| 7 | 2 |
| 8 | 1 |
| 9 | 1 |
| 10 | 3 |
| 13 | 1 |
| 14 | 1 |
| 16 | 2 |
| 19 | 1 |
| 24 | 1 |
| 28 | 1 |
| 30 | 1 |
| 38 | 1 |
| 43 | 1 |
| 64 | 1 |
| 76 | 1 |
| 117 | 1 |
| 125 | 1 |
| 129 | 1 |
| 138 | 1 |
| 295 | 1 |
| 347 | 1 |
| 468 | 1 |

Largest fan-outs:

| facts | command |
|---:|---|
| 468 | `cargo run -q --release -p axeyum-lean-kernel --example nat_axiom_inventory -- --include-constructed --requi...` |
| 347 | `cargo run -q --release -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat` |
| 295 | `cargo run -q -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat` |
| 138 | `cargo run -q --release -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free rat` |
| 129 | `cargo run -q --release -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free integer` |
| 125 | `cargo run -q --release -p axeyum-lean-kernel --example nat_axiom_inventory -- --include-constructed --requi...` |
| 117 | `cargo run -q -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free rat` |
| 76 | `cargo run -q --release -p axeyum-lean-kernel --example nat_axiom_inventory -- --include-constructed --requi...` |

## Thin spots

- 219 / 2182 proved facts hold two protections or fewer.
- 194 / 2182 have no discriminating checker naming their own subject, where the subject is taken only from an EXPLICIT
  `formal.kernel_theorem` / `kernel_declaration` binding.
- of those, 0 would gain one if the ledger's regex
  fallback (`theorem_of`, whose own docstring calls it "demonstrably NOT
  reliable in general") were trusted. That gap is the size of the
  ledger's unbound-subject debt, not a protection.
- 2 / 2182 have no `formal.statement` drift pin.
- 2175 / 2182 have no independent Lean replay.
- 2081 / 2182 carry no semantic-falsification evidence row.
- 2167 / 2182 name no mutation or negative control.
- 2117 / 2182 have no PER-THEOREM axiom footprint check; their footprint evidence is the prelude-wide sweep.
- 51 / 2182 have NO checker of their own: every command they cite is shared with another fact.
- 17 / 2182 cite no `checker_command` at all.
- 2049 / 2182 carry an evidence row listing two or more named `checkers`, and 1364 of those name the PRODUCING run as one of
  them. The production is not a re-derivation of itself, so those rows
  are one check and one re-list, not two independent checks —
  `validate-facts.py` counts them toward its "re-derived by 2+
  independent checkers" line.
- 1965 evidence rows across 1965 facts declare a semantic `kind` (`exhaustive-enumeration` / `instance-pin`) while their
  `supports` records an axiom footprint. Nothing was enumerated. Read at
  face value the `kind` enum would over-report semantic falsification by
  1965 rows, so this census reads `supports`, not `kind`.

## By curation

| curation | facts | median protections | own-subject checker | statement pin |
|---|---:|---:|---:|---:|
| (unset) | 1128 | 3 | 934 | 1126 |
| curated | 15 | 3 | 15 | 15 |
| generated-unreviewed | 1039 | 3 | 1039 | 1039 |

## By proof route

| route | facts | median protections |
|---|---:|---:|
| cas-certificate | 41 | 1 |
| imported-kernel-lean | 5 | 2 |
| kernel-lean | 2102 | 3 |
| search-certificate | 7 | 2 |
| smt-clausal | 10 | 1 |
| smt-term-level | 17 | 1 |

