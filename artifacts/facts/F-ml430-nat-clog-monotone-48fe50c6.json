{
  "schema_version": 1,
  "id": "F:ml430-nat-clog-monotone-48fe50c6",
  "title": "Mathlib v4.30 source proposition Nat.clog_monotone",
  "statement": "The proposition declared as `Nat.clog_monotone` in the pinned Mathlib v4.30 source.",
  "formal": {
    "language": "lean4-surface",
    "statement": "∀ (b : ℕ), Monotone (Nat.clog b)",
    "fragment": "Nat",
    "kernel_theorem": "Nat.clog_monotone",
    "kernel_statement": "theorem Nat.clog_monotone : (((x0 : AxNat) -> ((x1 : AxNat) -> ((x2 : AxNat) -> ((x3 : AxNat.le x1 x2) -> AxNat.le (AxNat.clog x0 x1) (AxNat.clog x0 x2))))))"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "depends_on": [
    "F:ml430-nat-clog-mono-right-8d87a410"
  ],
  "evidence": [
    {
      "id": "kernel-Nat.clog_monotone",
      "kind": "kernel-term",
      "supports": "`Nat.clog_monotone` is admitted as a Theorem with EXACTLY the stated shape.",
      "check_status": "checked",
      "checkers": [
        "nat_theorem_inventory (renders the kernel type of every Theorem; matched on the exact name/type columns)",
        "axeyum-lean-kernel::Kernel::add_declaration (re-typechecks the proof term against the stated type on every build)"
      ],
      "checker_command": "test \"$(cargo run -q --release -p axeyum-lean-kernel --example nat_theorem_inventory 2>/dev/null | awk -F'\\t' '$1 == \"Nat.clog_monotone\" && $3 == \"((x0 : AxNat) -> ((x1 : AxNat) -> ((x2 : AxNat) -> ((x3 : AxNat.le x1 x2) -> AxNat.le (AxNat.clog x0 x1) (AxNat.clog x0 x2)))))\"' | wc -l)\" -ge 1",
      "kernel_declaration": "Nat.clog_monotone"
    },
    {
      "id": "footprint-Nat.clog_monotone",
      "kind": "exhaustive-enumeration",
      "supports": "axiom_footprint: [] for `Nat.clog_monotone`. The `nat` prelude's trusted surface is empty (no Axiom, no Opaque, no Quotient), which bounds every declaration in it, including `Nat.clog_monotone`.",
      "check_status": "checked",
      "checkers": [
        "Kernel::axiom_footprint",
        "nat_axiom_inventory --require-axiom-free (environment-wide trusted-surface enumeration, independent of the per-theorem footprint route)"
      ],
      "checker_command": "cargo run -q --release -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat"
    }
  ],
  "provenance": {
    "date": "2026-08-30",
    "established_by": "lane nat-log-mirrors (2026-08-30), new kernel construction",
    "source": "statement-only extraction of `Nat.clog_monotone` from Mathlib v4.30.0; no proof value was exposed",
    "prior_art": [
      {
        "who": "the Mathlib contributors",
        "what": "the theorem declaration `Nat.clog_monotone`",
        "where": "mathlib4 commit c5ea00351c28e24afc9f0f84379aa41082b1188f (v4.30.0)",
        "year": 2026,
        "attribution": "the proposition was read from the pinned statement-only inventory; the proof term and tactic trace were not consulted"
      }
    ]
  },
  "notes": "CLOSED 2026-08-30 by a native, axiom-free kernel construction (The same `Monotone`-unfolds-to-pointwise core rendering as `Nat.log_monotone`, specializing `Nat.clog_mono_right`. `crates/axeyum-lean-kernel/src/nat_prelude/log_clog_order.rs`.) This is a genuine library-composition increment, not a found pre-existing declaration. The external Mathlib theorem remains prior art; its proof term and tactic trace were not consulted. formal.statement remains the pinned Lean surface spelling, definitionally equivalent to the explicit core rendering checked by the evidence command.",
  "proof_route": "kernel-lean",
  "axiom_footprint": []
}
