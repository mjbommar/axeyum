{
  "schema_version": 1,
  "id": "F:ml430-nat-mod-mul-beaccbad",
  "title": "Mathlib v4.30 source proposition Nat.mod_mul",
  "statement": "The proposition declared as `Nat.mod_mul` in the pinned Mathlib v4.30 source.",
  "formal": {
    "language": "lean4-surface",
    "statement": "∀ {a b x : ℕ}, x % (a * b) = x % a + a * (x / a % b)",
    "fragment": "Nat",
    "kernel_theorem": "Nat.mod_mul"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "depends_on": [
    "F:nat-div-mod-exec",
    "F:nat-div-mod-unique",
    "F:nat-succ-pred-of-pos",
    "F:nat-one-le-mul",
    "F:nat-mul-assoc",
    "F:nat-left-distrib",
    "F:nat-add-assoc",
    "F:nat-add-comm",
    "F:nat-mul-succ",
    "F:nat-mul-le-mul-left",
    "F:nat-add-lt-add-left",
    "F:nat-lt-of-lt-of-le",
    "F:nat-zero-mul",
    "F:nat-mul-zero",
    "F:nat-add-zero",
    "F:nat-zero-add",
    "F:nat-mod-zero",
    "F:nat-zero-lt-succ",
    "F:ml430-nat-add-assoc-8c87a1f1",
    "F:ml430-nat-add-comm-56a2d614"
  ],
  "evidence": [
    {
      "id": "kernel-Nat.mod_mul",
      "kind": "kernel-term",
      "check_status": "checked",
      "checker_command": "test \"$(cargo run -q -p axeyum-lean-kernel --example nat_theorem_inventory -- mod_mul 2>/dev/null | grep -Ec '^Nat\\.mod_mul[[:space:]]')\" -ge 1",
      "supports": "∀ {a b x : ℕ}, x % (a * b) = x % a + a * (x / a % b)",
      "notes": "Declared in `nat_prelude/mod_mul_lemmas.rs`'s `declare_mod_mul_family`. Case-splits `a` (`cases_zero_succ`): at `a=0` the whole identity collapses via `zero_mul`/`mod_zero`/`add_zero` congruence, never evaluating `mod _ 0` beyond the one declared equation; at `a=succ apred`, case-splits `b`: at `b=0` the identity again collapses via `mul_zero`/`mod_zero` plus the canonical `x = a*(x/a)+x%a` decomposition (`div_mod_exec`) commuted; at `b=succ bpred` (both positive), `double_decompose` reconstructs `divMod (a*b) x ((x/a)/b) (x%a + a*(x/a%b))` directly from two `div_mod_exec` decompositions (`x` at `a`, then `x/a` at `b`) combined via `left_distrib`/`mul_assoc`/`add_assoc`/`add_comm`, and `div_mod_unique` against the canonical decomposition of `x` at `a*b` (positivity from `one_le_mul`) forces the remainder equality directly. `nat_theorem_inventory`'s rendered type for `Nat.mod_mul` matches this fact's `formal.statement` verbatim (`x0`=a, `x1`=b, `x2`=x)."
    },
    {
      "id": "footprint-Nat.mod_mul",
      "kind": "exhaustive-enumeration",
      "check_status": "checked",
      "checker_command": "cargo run -q -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat",
      "checkers": [
        "Kernel::axiom_footprint",
        "environment-wide trusted-surface enumeration"
      ],
      "supports": "axiom_footprint: [] -- the Nat prelude's trusted surface is empty",
      "notes": "`nat_axiom_inventory --require-axiom-free nat` enumerates the built Nat environment and exits non-zero unless it admits no Axiom, Opaque or Quotient declaration (measured: axiom=0 opaque=0 quotient=0). A theorem cannot depend on a trusted declaration the environment does not contain, so an empty trusted surface bounds Nat.mod_mul's own footprint by []. `nat_prelude_tests::every_nat_declaration_is_checked_and_axiom_free` additionally checks this theorem's own `Kernel::axiom_footprint` directly (via `theorem_names`, which now lists it)."
    }
  ],
  "provenance": {
    "date": "2026-08-29",
    "established_by": "not established in this ledger",
    "source": "statement-only extraction of `Nat.mod_mul` from Mathlib v4.30.0; no proof value was exposed",
    "prior_art": [
      {
        "who": "the Mathlib contributors",
        "what": "the theorem declaration `Nat.mod_mul`",
        "where": "mathlib4 commit c5ea00351c28e24afc9f0f84379aa41082b1188f (v4.30.0)",
        "year": 2026,
        "attribution": "the proposition was read from the pinned statement-only inventory; the proof term and tactic trace were not consulted"
      }
    ]
  },
  "notes": "Established 2026-08-29 in this ledger: a fresh local construction, not a transcription of Mathlib's proof (`the proof term and tactic trace were not consulted`, per provenance). The declared statement matches this fact's `formal.statement`; see the kernel-term evidence row for the exact rendered type comparison and route.",
  "proof_route": "kernel-lean",
  "axiom_footprint": []
}
