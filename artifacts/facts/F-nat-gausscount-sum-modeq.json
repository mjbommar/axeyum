{
  "schema_version": 1,
  "id": "F:nat-gausscount-sum-modeq",
  "title": "The two Gauss counting exponents sum to n*m modulo 2",
  "statement": "The congruence form of `Nat.gaussCount_sum_even`: for odd `pp = 2m+1` and odd `q = 2n+1` with `gcd q pp = 1`, `Nat.modEq 2 (gaussNegCount pp q m + gaussNegCount q pp n) (n*m)`. `Nat.modEq d a b := exists u v, a + d*u = b + d*v` is the BALANCED form (`modular.rs`), which is what makes this a corollary at `u := n*m`, `v := k` rather than a second proof: from `S + T = k + k` one has `S + 2*T = (S + T) + T = (k + k) + T` and `T + 2*k = T + (k + k)`, which differ by one `add_comm`. The same route `Nat.eisenstein_lemma_modEq` takes from `Nat.eisenstein_lemma`. `Kernel::axiom_footprint` is EMPTY. Admitted on the first attempt.",
  "formal": {
    "language": "lean4",
    "statement": "theorem Nat.gaussCount_sum_modEq : ((x0 : AxNat) -> ((x1 : AxNat) -> ((x2 : Eq.{1} AxNat (AxNat.gcd (AxNat.succ (AxNat.mul (AxNat.succ (AxNat.succ AxNat.zero)) x1)) (AxNat.succ (AxNat.mul (AxNat.succ (AxNat.succ AxNat.zero)) x0))) (AxNat.succ AxNat.zero)) -> AxNat.modEq (AxNat.succ (AxNat.succ AxNat.zero)) (AxNat.add (AxNat.gaussNegCount (AxNat.succ (AxNat.mul (AxNat.succ (AxNat.succ AxNat.zero)) x0)) (AxNat.succ (AxNat.mul (AxNat.succ (AxNat.succ AxNat.zero)) x1)) x0) (AxNat.gaussNegCount (AxNat.succ (AxNat.mul (AxNat.succ (AxNat.succ AxNat.zero)) x1)) (AxNat.succ (AxNat.mul (AxNat.succ (AxNat.succ AxNat.zero)) x0)) x1)) (AxNat.mul x1 x0))))",
    "fragment": "Nat",
    "free_symbols": [
      "x0",
      "x1",
      "x2"
    ],
    "kernel_theorem": "Nat.gaussCount_sum_modEq"
  },
  "epistemic_status": "proved",
  "proof_route": "kernel-lean",
  "depends_on": [
    "F:nat-add-assoc",
    "F:nat-add-comm",
    "F:nat-gausscount-sum-even",
    "F:nat-mul-comm",
    "F:nat-zero-add",
    "F:ml430-nat-add-assoc-8c87a1f1",
    "F:ml430-nat-add-comm-56a2d614"
  ],
  "axiom_footprint": [],
  "evidence": [
    {
      "id": "kernel-Nat.gaussCount_sum_modEq",
      "kind": "kernel-term",
      "supports": "Nat.gaussCount_sum_modEq is admitted by the trusted kernel gate with the type recorded in formal.statement.",
      "check_status": "checked",
      "checkers": [
        "producing-build (Kernel::add_declaration)",
        "theorem_dependency_inventory re-list"
      ],
      "checker_command": "cargo run -q --release -p axeyum-lean-kernel --example theorem_dependency_inventory -- Nat.gaussCount_sum_modEq 2>/dev/null | grep -cE '^Nat\\.gaussCount_sum_modEq[[:space:]]'",
      "checker_seconds": 14,
      "kernel_declaration": "Nat.gaussCount_sum_modEq",
      "notes": "Two independent failure modes, so the exit status depends on the finding rather than on the run completing: theorem_dependency_inventory exits non-zero when a NAMED filter matches nothing, and grep -c exits 1 printing 0 when the anchored line is absent. RUN WITH A NEGATIVE CONTROL and it was: the real name prints 1 with both pipeline stages at 0, a one-character typo prints 0 with both stages at 1. Anchored with [[:space:]], never \\t -- in a scripted (GNU) grep \\t is a literal t. grep -c rather than grep -q, which would SIGPIPE the producer under pipefail. --release is MANDATORY (the debug build SIGABRTs). Pass ONE name per invocation: this tool silently keeps only the FIRST name argument, and a PREBUILT copy reports a just-declared theorem ABSENT (ADR-1552 finding 5), which is why the command runs `cargo run` rather than the binary."
    },
    {
      "id": "footprint-Nat.gaussCount_sum_modEq",
      "kind": "exhaustive-enumeration",
      "supports": "axiom_footprint: [] -- the nat prelude's trusted surface is empty, which bounds Nat.gaussCount_sum_modEq.",
      "check_status": "checked",
      "checkers": [
        "Kernel::axiom_footprint",
        "environment-wide trusted-surface enumeration"
      ],
      "checker_command": "cargo run -q --release -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat",
      "checker_seconds": 12,
      "notes": "--require-axiom-free exits non-zero when the named prelude's trusted surface (Axiom + Opaque + Quotient) is not empty, and errors rather than silently passing for a prelude the run never built. A declaration cannot depend on a trusted declaration the environment does not contain, so an empty surface bounds every declaration in it. This is a whole-prelude bound, not a per-declaration measurement; the per-declaration figure is measured 0 by the axiom-footprint assertion in the module's test file."
    },
    {
      "id": "numeric-Nat.gaussCount_sum_modEq",
      "kind": "exhaustive-enumeration",
      "supports": "ADR-1557's check script verifies the congruence's two sides agree modulo 2 at every pair it sweeps, and that the witnesses u := n*m, v := k really satisfy the balanced equation.",
      "check_status": "checked",
      "checkers": [
        "adr-1557-quadratic-reciprocity-checks.py"
      ],
      "checker_command": "python3 docs/research/09-decisions/adr-1557-quadratic-reciprocity-checks.py",
      "checker_seconds": 3,
      "notes": "Exit status depends on the finding: a claim that fails, or a control that behaves other than as recorded, exits 1. Verified by mutating the script itself in a scratch copy with one uniquely-named file per mutant (so the stale-__pycache__ trap cannot report the previous mutant's result)."
    }
  ],
  "provenance": {
    "date": "2026-09-02",
    "curation": "curated",
    "established_by": "axeyum-lean-kernel build_nat_prelude (crates/axeyum-lean-kernel/src/nat_prelude/quadratic_reciprocity_count.rs)",
    "source": "formal.statement is taken verbatim from the kernel's own rendering (Kernel::render_lean of the admitted declaration type; the same string is pinned character for character in the module's test file). depends_on is the direct-theorem column of theorem_dependency_inventory intersected with this ledger's registered facts, so it is an INTERSECTION and not the full dependency set -- Nat.gcd_comm is a direct dependency of Nat.gaussCount_sum_even with no fact row and is therefore omitted. Nothing about the statement was hand transcribed; title, statement and the evidence notes were authored (lane quadratic-reciprocity-2)."
  },
  "notes": "Declared in `crates/axeyum-lean-kernel/src/nat_prelude/quadratic_reciprocity_count.rs`. ADR-1557. Declaration kind: theorem. A NAME-SEARCH NOTE worth keeping: `shape_search --name-like modEq` returns only `Int` declarations, because the `Nat` side spells its theorems `mod_eq_*` in lower case even when the constant they mention is `Nat.modEq`. Search for the CONSTANT. GRADED FAMILY (ADR-0603): row 1, the general constructive form over this kernel's own constructed naturals and integers -- and STRICTLY MORE GENERAL than the classical statement, which is about two distinct odd PRIMES, because only coprimality is assumed. Row 2 (a boundary refutation) is EMPTY BY PROOF for this family in the sense that matters: the statement is decidable at every concrete pair and no boundary case escapes it, so the row-2 work is the hypothesis-dropping refutation, which IS done numerically and inside the kernel at a named non-coprime witness (p = q = 3) with a recorded SURVIVOR (p = q = 5, where the two sides agree and the control separates nothing). It is not stated as a kernel theorem. Row 3 (a decidable-fragment exact form) is what the concrete instantiations are, at five prime pairs, but no closed-form decision procedure is declared. Row 4, the labeled Mathlib import (`Mathlib.NumberTheory.LegendreSymbol.QuadraticReciprocity`), is A SEPARATE LANE and is not attempted here; nothing in this row is an import.",
  "external_status": "proved"
}
