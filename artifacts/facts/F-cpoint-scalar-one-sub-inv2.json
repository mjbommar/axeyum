{
  "schema_version": 1,
  "id": "F:cpoint-scalar-one-sub-inv2",
  "title": "One minus one-half is one-half, for the constructed-plane scalar one-half",
  "statement": "1 - CPoint.Scalar.inv2 is CReal.Equiv-equivalent to CPoint.Scalar.inv2 (CPoint.Scalar.inv2 is the constructed real one-half used to build the midpoint operator).",
  "formal": {
    "language": "lean4",
    "statement": "theorem CPoint.Scalar.one_sub_inv2 : CReal.Equiv (CReal.add CReal.one (CReal.neg CPoint.Scalar.inv2)) CPoint.Scalar.inv2",
    "fragment": "CPoint",
    "kernel_theorem": "CPoint.Scalar.one_sub_inv2",
    "free_symbols": []
  },
  "epistemic_status": "proved",
  "proof_route": "kernel-lean",
  "depends_on": [
    "F:cpoint-scalar-twoposbound",
    "F:creal-add-assoc",
    "F:creal-add-comm",
    "F:creal-add-congr",
    "F:creal-add-neg",
    "F:creal-add-zero",
    "F:creal-equiv-refl",
    "F:creal-equiv-symm",
    "F:creal-equiv-trans",
    "F:creal-left-distrib",
    "F:creal-mul-comm",
    "F:creal-mul-inv-cancel",
    "F:creal-mul-one"
  ],
  "axiom_footprint": [],
  "evidence": [
    {
      "id": "kernel-CPoint.Scalar.one_sub_inv2",
      "kind": "kernel-term",
      "supports": "CPoint.Scalar.one_sub_inv2 is admitted by the trusted kernel gate with the type recorded in formal.statement.",
      "check_status": "checked",
      "checkers": [
        "producing-build (Kernel::add_declaration)",
        "theorem_dependency_inventory re-list"
      ],
      "checker_command": "cargo run -q --release -p axeyum-lean-kernel --example theorem_dependency_inventory -- CPoint.Scalar.one_sub_inv2 2>/dev/null | grep -cE '^CPoint\\.Scalar\\.one_sub_inv2[[:space:]]'",
      "checker_seconds": 10,
      "notes": "`build_cpoint_prelude` admits CPoint.Scalar.one_sub_inv2 through the trusted `Kernel::add_declaration` gate, which re-checks the proof term against the stated type, so a line in this tool's output for the exact name is a machine-checked proof having been admitted. `theorem_dependency_inventory` builds creal/complex/cpoint in one run; it exits non-zero for a named filter that matches nothing, and `grep -c` (never `-q`) independently asserts the exact tab-anchored line is present. `--release` is MANDATORY: building the constructed carriers recurses deep enough in a debug build to overflow the default thread stack (measured on this tree: release exits 0, debug SIGABRTs at 134)."
    },
    {
      "id": "footprint-CPoint.Scalar.one_sub_inv2",
      "kind": "exhaustive-enumeration",
      "supports": "axiom_footprint: [] -- the cpoint prelude's trusted surface is empty, which bounds CPoint.Scalar.one_sub_inv2",
      "check_status": "checked",
      "checkers": [
        "Kernel::axiom_footprint",
        "environment-wide trusted-surface enumeration"
      ],
      "checker_command": "cargo run -q --release -p axeyum-lean-kernel --example nat_axiom_inventory -- --include-constructed --require-axiom-free cpoint",
      "checker_seconds": 20,
      "notes": "`nat_axiom_inventory --include-constructed --require-axiom-free cpoint` builds the cpoint environment as its own group and reports `cpoint: axiom=0 opaque=0 quotient=0 total_trusted=0` (re-measured on this tree), covering Axiom, Opaque AND Quotient, not just Declaration::Axiom -- this bounds every individual cpoint theorem's footprint by []. `--include-constructed` is REQUIRED: without it the command exits 1 naming `cpoint` as known but not built this run rather than a silent pass, which is the standing trap of an empty/absent result reading as a strong negative when the tool was never pointed at the subject. `nat_axiom_inventory` now covers `cpoint` directly (re-verified on this tree: `--require-axiom-free cpoint` exits 0 with `cpoint: axiom=0 opaque=0 quotient=0 total_trusted=0`), so the footprint_closure_audit fallback some earlier CPoint facts used is not needed here. `--release` is MANDATORY for the same deep-recursion reason as `theorem_dependency_inventory` (measured on this tree: debug SIGABRTs at 134)."
    }
  ],
  "provenance": {
    "date": "2026-08-25",
    "established_by": "axeyum-lean-kernel build_cpoint_prelude",
    "source": "theorem name and dependency edges from theorem_dependency_inventory (builds creal/complex/cpoint); canonical type and top-level binder arity read via a standalone probe binary depending on axeyum-lean-kernel by path, calling only its public Kernel API (environment(), display_name(), render_lean(), axiom_footprint(), expr_node()) -- no in-tree example prints Complex/CPoint theorem types; crates/ source was not touched to produce this batch; the probe was built and run in the session scratchpad and deleted after use."
  },
  "notes": "depends_on keeps the sibling-mapped subset of CPoint.Scalar.one_sub_inv2's direct-edge set from theorem_dependency_inventory: a used theorem is declared only when it is itself a registered ledger fact (including new facts from this same batch).",
  "external_status": "proved"
}
