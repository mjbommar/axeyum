{
  "schema_version": 1,
  "id": "F:rat-mul-inv-cancel-of-ne-zero",
  "title": "Every nonzero rational has a multiplicative inverse",
  "statement": "For every rational a with a != 0, a * a^-1 = 1. Combined with the ring laws this is what makes ℚ a field, not merely a commutative ring.",
  "formal": {
    "language": "lean4",
    "statement": "theorem Rat.mul_inv_cancel_of_ne_zero : ((x0 : Rat) -> ((x1 : Not (Eq.{1} Rat x0 Rat.zero)) -> Eq.{1} Rat (Rat.mul x0 (Rat.inv x0)) Rat.one))",
    "fragment": "Rat",
    "kernel_theorem": "Rat.mul_inv_cancel_of_ne_zero",
    "free_symbols": [
      "x0",
      "x1"
    ]
  },
  "epistemic_status": "proved",
  "proof_route": "kernel-lean",
  "depends_on": [
    "F:rat-lt-trichotomy",
    "F:rat-mul-inv-cancel",
    "F:rat-mul-inv-cancel-of-neg"
  ],
  "axiom_footprint": [],
  "evidence": [
    {
      "id": "kernel-Rat.mul_inv_cancel_of_ne_zero",
      "kind": "kernel-term",
      "supports": "Rat.mul_inv_cancel_of_ne_zero is admitted by the trusted kernel gate with the type recorded in formal.statement.",
      "check_status": "checked",
      "checker_command": "cargo run -q --release -p axeyum-lean-kernel --example theorem_dependency_inventory -- Rat.mul_inv_cancel_of_ne_zero 2>/dev/null | grep -cE '^Rat\\.mul_inv_cancel_of_ne_zero[[:space:]]'",
      "notes": "`build_rat_prelude` admits Rat.mul_inv_cancel_of_ne_zero through the trusted `Kernel::add_declaration` gate, which re-checks the proof term against the stated type, so a line in this tool's output for the exact name is a machine-checked proof having been admitted. `theorem_dependency_inventory` exits non-zero for a named filter that matches nothing (a deleted theorem cannot read as a re-derived one), and `grep -c` (never `-q`) both avoids a SIGPIPE-under-pipefail false negative and independently asserts the exact tab-anchored line is present -- two ways the same absence would be caught. `--release` is MANDATORY: this tool now also builds `creal`/`complex`/`cpoint`, which recurse deep enough in a debug build to overflow the default thread stack (measured: release exits 0 with 1092 theorems, debug SIGABRTs at 134) -- the same resource-limit gotcha already documented for `prelude_theorem_inventory --include-constructed`."
    },
    {
      "id": "footprint-Rat.mul_inv_cancel_of_ne_zero",
      "kind": "exhaustive-enumeration",
      "supports": "axiom_footprint: [] -- the Rat prelude's trusted surface is empty",
      "check_status": "checked",
      "checkers": [
        "Kernel::axiom_footprint",
        "environment-wide trusted-surface enumeration"
      ],
      "checker_command": "cargo run -q -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free rat",
      "notes": "`nat_axiom_inventory` builds the Rat environment as its own group and reports `rat: axiom=0 opaque=0 quotient=0 total_trusted=0` (re-measured on this tree). That bounds every individual Rat theorem's footprint by [], since a theorem cannot depend on a trusted declaration the environment does not contain -- and the enumeration covers Axiom, Opaque AND Quotient, not just Declaration::Axiom, because Opaque has no proof body and Quotient admits Quot.sound. `--require-axiom-free <name>` is an error (not a silent pass/zero) for a prelude this run never built, which is what makes `rat` here a claim rather than an absence."
    }
  ],
  "concept_refs": [
    {
      "graph": "math-education",
      "ref": "C:multiplicative-inverse",
      "relation": "instance-of",
      "resolved": false
    }
  ],
  "provenance": {
    "date": "2026-08-25",
    "established_by": "axeyum-lean-kernel build_rat_prelude",
    "source": "see F:rat-add-comm for the extraction method (shared for the whole batch)."
  },
  "notes": "depends_on is empty because the direct kernel dependencies (Rat.lt_trichotomy, Rat.mul_inv_cancel, Rat.mul_inv_cancel_of_neg) are not themselves registered as facts in this batch; recording depends_on as a proper subset of the true dependency set is consistent with the schema (it need not be exhaustive) but should not be read as exhaustive here. This is the field-closing law: F:rationals-are-a-field-axiom-free (pre-existing) states the field as a bundle without pinning this specific admitted theorem name and type -- this fact does.",
  "external_status": "proved"
}
