{
  "schema_version": 1,
  "id": "F:ml430-nat-coprime-of-dvd-right-a640bd56",
  "title": "Mathlib v4.30 source proposition Nat.Coprime.of_dvd_right",
  "statement": "The proposition declared as `Nat.Coprime.of_dvd_right` in the pinned Mathlib v4.30 source.",
  "formal": {
    "language": "lean4-surface",
    "statement": "∀ {a b₁ b₂ : ℕ}, b₁ ∣ b₂ → a.Coprime b₂ → a.Coprime b₁",
    "fragment": "Nat",
    "kernel_theorem": "Nat.coprime_of_dvd_right",
    "kernel_statement": "theorem Nat.coprime_of_dvd_right : ((x0 : AxNat) -> ((x1 : AxNat) -> ((x2 : AxNat) -> ((x3 : AxNat.dvd x1 x2) -> ((x4 : Eq.{1} AxNat (AxNat.gcd x0 x2) (AxNat.succ AxNat.zero)) -> Eq.{1} AxNat (AxNat.gcd x0 x1) (AxNat.succ AxNat.zero))))))"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "proof_route": "kernel-lean",
  "depends_on": [
    "F:nat-dvd-gcd",
    "F:nat-dvd-trans",
    "F:nat-eq-one-of-dvd-one",
    "F:nat-gcd-dvd-left",
    "F:nat-gcd-dvd-right",
    "F:ml430-nat-dvd-gcd-e5184fc5"
  ],
  "axiom_footprint": [],
  "evidence": [
    {
      "id": "kernel-Nat.coprime_of_dvd_right",
      "kind": "kernel-term",
      "supports": "b1 divides b2, and gcd a b2 = 1, implies gcd a b1 = 1",
      "check_status": "checked",
      "checker_command": "test \"$(cargo run -q -p axeyum-lean-kernel --example nat_theorem_inventory -- coprime_of_dvd_right 2>/dev/null | grep -Ec '^Nat\\.coprime_of_dvd_right[[:space:]]')\" -ge 1",
      "notes": "`build_nat_prelude` admits this theorem through the trusted `Kernel::add_declaration` gate: `gcd a b1` divides `a` (`gcd_dvd_left`) and `b1` (`gcd_dvd_right`), and `b1` divides `b2` (the hypothesis), so `dvd_trans` gives `gcd a b1 | b2`; `dvd_gcd` combines those into `gcd a b1 | gcd a b2`, which is `1` by hypothesis, so `gcd a b1 | 1` and `eq_one_of_dvd_one` closes it -- the right-hand mirror of `coprime_of_dvd_left`. `nat_theorem_inventory` exits non-zero for a name that does not exist, and the `grep -c` requires the admitted declaration to be printed."
    },
    {
      "id": "footprint-Nat.coprime_of_dvd_right",
      "kind": "exhaustive-enumeration",
      "supports": "axiom_footprint: [] -- the Nat prelude's trusted surface is empty",
      "check_status": "checked",
      "checkers": [
        "Kernel::axiom_footprint",
        "environment-wide trusted-surface enumeration"
      ],
      "checker_command": "cargo run -q -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat",
      "notes": "`nat_axiom_inventory --require-axiom-free nat` enumerates the built Nat environment and exits non-zero unless it admits no Axiom, Opaque or Quotient declaration. A theorem cannot depend on a trusted declaration the environment does not contain, so an empty trusted surface bounds every individual theorem's footprint by []."
    }
  ],
  "concept_refs": [
    {
      "graph": "math-education",
      "ref": "C:greatest-common-divisor",
      "relation": "about",
      "resolved": false
    },
    {
      "graph": "math-education",
      "ref": "C:divisibility",
      "relation": "about",
      "resolved": false
    }
  ],
  "provenance": {
    "date": "2026-08-28",
    "established_by": "axeyum-lean-kernel build_nat_prelude, lane coprime-backlog",
    "source": "statement-only extraction of `Nat.Coprime.of_dvd_right` from Mathlib v4.30.0 (see prior_art), independently proved here as `Nat.coprime_of_dvd_right` and admitted through `Kernel::add_declaration`",
    "prior_art": [
      {
        "who": "the Mathlib contributors",
        "what": "the theorem declaration `Nat.Coprime.of_dvd_right`",
        "where": "mathlib4 commit c5ea00351c28e24afc9f0f84379aa41082b1188f (v4.30.0)",
        "year": 2026,
        "attribution": "the proposition was read from the pinned statement-only inventory; the proof term and tactic trace were not consulted, and this project's proof is independently constructed"
      }
    ]
  },
  "notes": "`Nat.Coprime` has no separate name in this kernel's Nat prelude (matching `Nat.coprime_of_lt_prime`'s established convention); it is spelled inline as `gcd _ _ = 1`, so `formal.statement` is the pinned Mathlib v4.30 proposition this fact mirrors; `formal.kernel_statement` is the actual kernel-core `render_lean` output for `Nat.coprime_of_dvd_right`, which spells the same proposition in the prelude's own vocabulary. `depends_on` is `theorem_dependency_inventory`'s reported direct theorem dependencies, restricted to the ones that already carry a fact ledger entry (all five do here)."
}
