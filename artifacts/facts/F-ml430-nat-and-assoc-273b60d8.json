{
  "schema_version": 1,
  "id": "F:ml430-nat-and-assoc-273b60d8",
  "title": "Mathlib v4.30 source proposition Nat.and_assoc",
  "statement": "The proposition declared as `Nat.and_assoc` in the pinned Mathlib v4.30 source.",
  "formal": {
    "language": "lean4-surface",
    "statement": "\u2200 (x y z : \u2115), x &&& y &&& z = x &&& (y &&& z)",
    "fragment": "Nat",
    "kernel_theorem": "Nat.land_assoc"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "depends_on": [
    "F:ml430-nat-land-assoc-ad4775b8",
    "F:ml430-nat-add-comm-56a2d614",
    "F:ml430-nat-and-le-left-6d04acb7",
    "F:nat-le-add-right",
    "F:nat-le-trans"
  ],
  "evidence": [
    {
      "id": "kernel-Nat.land_assoc",
      "kind": "kernel-term",
      "kernel_declaration": "Nat.land_assoc",
      "supports": "\u2200 (x y z : \u2115), x &&& y &&& z = x &&& (y &&& z)",
      "check_status": "checked",
      "checker_command": "test \"$(cargo run -q --release -p axeyum-lean-kernel --example nat_theorem_inventory -- land_assoc 2>/dev/null | grep -Ec '^Nat\\.land_assoc[[:space:]]')\" -ge 1",
      "notes": "`build_nat_prelude` admits `Nat.land_assoc` through the trusted `Kernel::add_declaration` gate. `nat_theorem_inventory`'s rendered type matches this fact's `formal.statement` up to variable naming. Anchored with `^Nat\\.land_assoc[[:space:]]` so a sibling name cannot satisfy this row; `grep -Ec` consumes the pipe so the tested count decides the exit status. Run `--release` -- the debug build of this inventory example SIGABRTs on stack depth (unrelated to this proof). Mathlib's `&&&` (`HAnd`) for `Nat` is the SAME function as `Nat.land` -- `Init.Data.Nat.Bitwise.Lemmas`'s `Nat.and_*` family and Mathlib's own `Nat.land_*` family (already reconciled: F:ml430-nat-land-comm-7e6ad72e, F:ml430-nat-land-assoc-ad4775b8) state the identical proposition over that one function under two different pinned declaration names -- reconciling this `and_*` mirror to our `Nat.land_*` theorem is honest under the mirror-flip criterion in CLAUDE.md: Mathlib's `def` (`Nat.land`, reached via the `&&&` notation) is the same function ours proves the property about."
    },
    {
      "id": "compute-Nat.land_assoc",
      "kind": "kernel-term",
      "kernel_declaration": "Nat.land_assoc",
      "supports": "land(land(3,7),5) = land(3,land(7,5)) = 1, with BOTH intermediate values nonzero (land(3,7)=3, land(7,5)=5) -- the fully-generic sub-case of the hard leaf.",
      "check_status": "checked",
      "checker_command": "cargo test -p axeyum-lean-kernel --lib nat_prelude::nat_prelude_tests::land_assoc_applies_at_a_nonzero_concrete_instance",
      "checkers": [
        "Kernel::infer",
        "Kernel::infer_in",
        "Kernel::def_eq"
      ]
    },
    {
      "id": "footprint-Nat.land_assoc",
      "kind": "exhaustive-enumeration",
      "supports": "axiom_footprint: [] -- the Nat prelude's trusted surface is empty",
      "check_status": "checked",
      "checker_command": "cargo run -q --release -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat",
      "checkers": [
        "Kernel::axiom_footprint",
        "environment-wide trusted-surface enumeration"
      ],
      "notes": "`nat_axiom_inventory --require-axiom-free nat` enumerates the built Nat environment and exits non-zero unless it admits no Axiom, Opaque or Quotient declaration (measured: axiom=0 opaque=0 quotient=0). A theorem cannot depend on a trusted declaration the environment does not contain, so an empty trusted surface bounds `Nat.land_assoc`'s footprint by []."
    }
  ],
  "provenance": {
    "date": "2026-08-30",
    "established_by": "lane draw9-first-theorems",
    "source": "statement-only extraction of `Nat.and_assoc` from Mathlib v4.30.0; no proof value was exposed",
    "prior_art": [
      {
        "who": "the Mathlib contributors",
        "what": "the theorem declaration `Nat.and_assoc`",
        "where": "mathlib4 commit c5ea00351c28e24afc9f0f84379aa41082b1188f (v4.30.0)",
        "year": 2026,
        "attribution": "the proposition was read from the pinned statement-only inventory; the proof term and tactic trace were not consulted"
      }
    ]
  },
  "notes": "Proved in Axeyum by reconciliation with the already-proved `Nat.land_assoc` (`nat_prelude/rec_agreement.rs`), which this ledger already reconciled to Mathlib's OWN `Nat.land_assoc` (F:ml430-nat-land-assoc-ad4775b8). This lane additionally reconciles `Init.Data.Nat.Bitwise.Lemmas`'s `Nat.and_assoc` -- a second pinned declaration name for the identical proposition -- to the SAME kernel theorem. No new proof was written.\n\n2026-08-30: marked as restating [[F:ml430-nat-land-assoc-ad4775b8]]. Mathlib carries this proposition under TWO declaration names -- the `Nat.and_*` family in `Init.Data.Nat.Bitwise.Lemmas` and the `Nat.land_*` family -- over the same function, so both mirrors resolve to the same kernel theorem and state the same thing up to bound-variable naming. Both stay `proved`; only the distinct-proposition count changes.",
  "proof_route": "kernel-lean",
  "axiom_footprint": [],
  "equivalent_to": [
    "F:ml430-nat-land-assoc-ad4775b8"
  ]
}