{
  "schema_version": 1,
  "id": "F:ml430-nat-fermatnumber-ne-one-91232d67",
  "title": "Mathlib v4.30 source proposition Nat.fermatNumber_ne_one",
  "statement": "The proposition declared as `Nat.fermatNumber_ne_one` in the pinned Mathlib v4.30 source.",
  "formal": {
    "language": "lean4-surface",
    "statement": "∀ (n : ℕ), n.fermatNumber ≠ 1",
    "fragment": "Nat",
    "kernel_theorem": "Nat.fermatNumber_ne_one",
    "kernel_statement": "theorem Nat.fermatNumber_ne_one : (((x0 : AxNat) -> ((x1 : Eq.{1} AxNat (AxNat.fermatNumber x0) (AxNat.succ AxNat.zero)) -> False)))"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "depends_on": ["F:nat-le-succ-succ", "F:nat-lt-irrefl", "F:nat-pow-pos", "F:nat-succ-le-succ", "F:nat-zero-le"],
  "evidence": [
    {
      "id": "kernel-Nat.fermatNumber_ne_one",
      "kind": "kernel-term",
      "supports": "`Nat.fermatNumber_ne_one` is admitted as a Theorem with EXACTLY the stated shape.",
      "check_status": "checked",
      "checkers": [
        "nat_theorem_inventory (renders the kernel type of every Theorem; matched on the exact name/type columns)",
        "axeyum-lean-kernel::Kernel::add_declaration (re-typechecks the proof term against the stated type on every build)"
      ],
      "checker_command": "test \"$(cargo run -q --release -p axeyum-lean-kernel --example nat_theorem_inventory 2>/dev/null | awk -F'\\t' '$1 == \"Nat.fermatNumber_ne_one\" && $3 == \"((x0 : AxNat) -> ((x1 : Eq.{1} AxNat (AxNat.fermatNumber x0) (AxNat.succ AxNat.zero)) -> False))\"' | wc -l)\" -ge 1",
      "kernel_declaration": "Nat.fermatNumber_ne_one"
    },
    {
      "id": "footprint-Nat.fermatNumber_ne_one",
      "kind": "exhaustive-enumeration",
      "supports": "axiom_footprint: [] for `Nat.fermatNumber_ne_one`. The `nat` prelude's trusted surface is empty (no Axiom, no Opaque, no Quotient), which bounds every declaration in it, including `Nat.fermatNumber_ne_one`.",
      "check_status": "checked",
      "checkers": [
        "Kernel::axiom_footprint",
        "nat_axiom_inventory --require-axiom-free (environment-wide trusted-surface enumeration, independent of the per-theorem footprint route)"
      ],
      "checker_command": "cargo run -q --release -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat"
    }
  ],
  "provenance": {
    "date": "2026-08-29",
    "established_by": "not established in this ledger",
    "source": "statement-only extraction of `Nat.fermatNumber_ne_one` from Mathlib v4.30.0; no proof value was exposed",
    "prior_art": [
      {
        "who": "the Mathlib contributors",
        "what": "the theorem declaration `Nat.fermatNumber_ne_one`",
        "where": "mathlib4 commit c5ea00351c28e24afc9f0f84379aa41082b1188f (v4.30.0)",
        "year": 2026,
        "attribution": "the proposition was read from the pinned statement-only inventory; the proof term and tactic trace were not consulted"
      }
    ]
  },
  "notes": "CLOSED 2026-08-30 by a native, axiom-free kernel construction (lane fermat-mirrors). `fermatNumber n` is defeq `succ (pow 2 (pow 2 n))` (`add x 1` collapses via `add`'s right-recursion on the concrete `1`), and `pow 2 (pow 2 n) > 0` by `pow_pos`, so `succ_le_succ` gives `1 < fermatNumber n`, hence `fermatNumber n ≠ 1` (`ne_of_lt`/`ne_symm`, `finite.rs`). `crates/axeyum-lean-kernel/src/nat_prelude/fermat_number_mirrors.rs::declare_fermatnumber_ne_one`. Verified symbolically (the theorem is admitted over a genuinely free `n`, checked by `Kernel::add_declaration` at prelude-build time) and at a concrete instance (`nat_prelude_tests.rs::fermat_number_mirrors_apply_at_free_and_concrete_instances_with_a_reflexive_negative_control`, n=0: `fermatNumber 0 = 3 != 1`). This is a genuine kernel construction, not a found pre-existing declaration. The external Mathlib theorem remains prior art; its proof term and tactic trace were not consulted. formal.statement remains the pinned Lean surface spelling, definitionally equivalent to the explicit core rendering checked by the evidence command.",
  "proof_route": "kernel-lean",
  "axiom_footprint": []
}
