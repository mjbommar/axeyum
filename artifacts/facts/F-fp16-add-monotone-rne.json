{
  "schema_version": 1,
  "id": "F:fp16-add-monotone-rne",
  "title": "binary16 addition under roundNearestTiesToEven is monotone in its first argument",
  "statement": "Let F be the IEEE-754 binary16 interchange format, SMT-LIB `(_ FloatingPoint 5 11)`, and let RNE denote roundNearestTiesToEven. For all values a, b, c of F: if fp.leq(a, b) holds and neither fp.add(RNE, a, c) nor fp.add(RNE, b, c) is NaN, then fp.leq(fp.add(RNE, a, c), fp.add(RNE, b, c)). This is the binary16 instance of F:fp8-add-monotone-rne. It was `open` from 2026-08-14 to 2026-08-28 because AXEYUM HAD NOT SETTLED IT -- not because it was doubted; ADR-0613's LRAT hint-following certification route closed the measured checking-throughput obstruction and this fact moved to `proved` on the symbolic CNF/DRAT/LRAT route alone (binary16's 2^48-triple space is not exhaustively enumerable, unlike the fp8 E5M2 sibling).",
  "formal": {
    "language": "smtlib2",
    "statement": "(assert (=> (and (fp.leq a b) (not (fp.isNaN (fp.add RNE a c))) (not (fp.isNaN (fp.add RNE b c)))) (fp.leq (fp.add RNE a c) (fp.add RNE b c))))",
    "fragment": "QF_FP",
    "free_symbols": [
      "a",
      "b",
      "c"
    ]
  },
  "epistemic_status": "proved",
  "proof_route": "smt-clausal",
  "external_status": "proved",
  "depends_on": [],
  "axiom_footprint": [
    "ieee754-2019-arithmetic-semantics-for-the-binary16-layout-as-encoded-by-axeyum-fp-fpa2bv-lowering-and-as-implemented-by-rustc_apfloat",
    "axeyum-fp.add-is-a-differentially-VALIDATED-not-proven-bit-blaster",
    "rounding-mode-roundNearestTiesToEven-only-for-this-fact-unlike-fp8-this-run-did-not-sweep-the-other-four-rounding-modes",
    "smtlib2-fp.leq-ordering-nan-unordered-plus-zero-equals-minus-zero",
    "tseitin-cnf-encoding-preserves-satisfiability",
    "adr-0613-elaborate_drat_to_lrat_backward-is-an-UNTRUSTED-producer-of-antecedent-hints-only-its-output-is-never-accepted-without-check_lrat",
    "adr-0613-check_lrat-search-free-linear-hint-following-verification-is-what-discharges-Certified",
    "adr-0613-check_drat_backward-appears-only-in-REJECTING-position-inside-UnsatProof::recheck-never-accepting",
    "format-binary16-only-this-fact-is-not-format-generic-and-says-nothing-about-other-widths",
    "no-exhaustive-enumeration-at-this-width-2^48-triples-is-not-searchable-unlike-the-fp8-sibling"
  ],
  "evidence": [
    {
      "id": "fp16-monotone-unsat-lrat-certified",
      "kind": "unsat-certificate",
      "supports": "The negation of formal.statement is unsatisfiable over binary16 (IEEE 754 (_ FloatingPoint 5 11)), decided symbolically via CNF/DRAT and certified via the ADR-0613 LRAT hint-following route -- without enumerating the 2^48 triples, which is not searchable at this width (contrast the fp8 E5M2 sibling, F:fp8-add-monotone-rne, settled partly by exhaustive enumeration of all 2^24 triples).",
      "check_status": "checked",
      "checker_command": "OUT=$(cargo run --release -q -p axeyum-bench --example smtcomp_cli -- --evidence artifacts/facts/smt2/neg-fp16-add-monotone-rne.smt2 2>&1); U=$(printf '%s\\n' \"$OUT\" | /usr/bin/grep -c '^unsat$'); C=$(printf '%s\\n' \"$OUT\" | /usr/bin/grep -c 'certified=1 '); R=$(printf '%s\\n' \"$OUT\" | /usr/bin/grep -c 'recheck=ok'); test \"$U\" = 1 && test \"$C\" = 1 && test \"$R\" = 1",
      "checker_seconds": 400,
      "artifact": "artifacts/facts/smt2/neg-fp16-add-monotone-rne.smt2",
      "notes": "Reproduced independently by the fp16-evidence lane, 2026-08-28, in its own worktree with a fresh `cargo build --release -p axeyum-bench --example smtcomp_cli` (this fact's evidence depends on code that landed within the hour, ADR-0613, so a stale prebuilt binary would have measured the OLD superlinear-checking route). Two full end-to-end runs, both `unsat`/`certified=1`/`recheck=ok`/`arena=ok`:\n\n  run 1 (host load ~2.8/16):  search 424,601 conflicts/26.556s -> 827,048 proof steps/193,214,020 bytes; backward_lrat_certify 827,048/827,048 steps, elapsed_ms=95,932, certified=true; printed line `; evidence kind=unsat-drat certified=1 recheck=ok arena=ok ms=136898`; verdict `unsat`. Measured via `/usr/bin/time -v` around the WHOLE process: wall clock 5:39.01 (339.01s), 99% CPU, RSS 3.11 GB.\n  run 2 (host load ~20.7/16, contended): printed line `; evidence ... certified=1 recheck=ok arena=ok ms=168081`; verdict `unsat`. Measured via date-diff around the whole `cargo run` invocation: wall clock 353s.\n\nIMPORTANT CORRECTION TO THE ADR-0613 NUMBERS THIS FACT'S NOTES ALREADY QUOTE. The `; evidence ... ms=NNN` field printed by `smtcomp_cli` is captured in `evidence_report_line`'s caller BEFORE `UnsatProof::recheck()` and the `arena` fresh-parse check run (see `crates/axeyum-bench/examples/smtcomp_cli.rs`, the `started.elapsed().as_millis()` argument is evaluated before `evidence_report_line` calls `proof.recheck()` and `evidence.check_outcome(...)`). So `ms=136898`/`ms=168081` measure only parse+solve+certify, NOT the full checked claim this evidence row needs -- `recheck=ok` (an independent second pass: `check_lrat` plus, in rejecting-only position, `check_drat_backward` over the full 827,048-step proof) and `arena=ok` (a second fresh parse of the original SMT-LIB file plus re-evaluation) both run AFTER that timer stops. The ADR calls its `ms=125098` figure \"end to end\"; measured here, the ACTUAL end-to-end wall clock (what this checker_command, or any external caller of the binary, actually waits for) is 2.5-2.9x that: 339-353s across two runs at different contention levels, not ~125-137s. This does not weaken the ADR's underlying finding -- checking still moved from never-observed-to-finish to under six minutes, roughly two orders of magnitude, not three -- but the smaller number is a sub-stage timer, not the wall-clock cost of running the command.\n\nWHY THIS IS THE ONLY EVIDENCE ROW, UNLIKE THE FP8 SIBLING. F:fp8-add-monotone-rne is settled twice, by exhaustive enumeration of all 2^24 triples AND by this same symbolic CNF/DRAT route. Binary16 has 2^48 triples; brute-force enumeration is not available at this width (see that fact's own arity-wall note), so this fact rests on the symbolic route alone. The `checker_command`'s exit status depends on three independently-tested substrings of the real run's stdout (`^unsat$`, `certified=1 `, `recheck=ok`), each verified against BOTH a captured real positive run and two synthetic negative-control transcripts (sat/uncertified: U=C=R=0, chain fails; unsat-but-certified=0: C=0, chain fails) before this row was written -- if the claim were false, this command exits nonzero because at least one of those three greps would count zero matching lines and the chained `test` would fail on the first zero it hits.",
      "checkers": [
        "axeyum-smtcomp-cli-producing-solve",
        "axeyum-check_lrat-search-free-hint-following-verification-adr-0613",
        "axeyum-check_drat_backward-rejecting-position-tamper-check-adr-0613",
        "axeyum-Evidence::check_outcome-fresh-parse"
      ]
    }
  ],
  "provenance": {
    "date": "2026-08-28",
    "established_by": "fp16-evidence lane, reproducing ADR-0613's LRAT hint-following certification route (docs/research/09-decisions/adr-0613-unsat-is-certified-by-following-hints-not-by-searching-for-them.md), landed on main the same day by the drat-evidence-route lane. This lane rebuilt smtcomp_cli --release in its own worktree and ran it twice end to end against the fact's own pinned negation file; both runs certified. The mathematics is unchanged from the prior open-status provenance below (monotonicity of correctly-rounded arithmetic); what changed is that the checking stage now terminates and is independently re-verified, closing the measured obstruction this fact was blocked on.",
    "source": "authored here as the binary16 instance of F:fp8-add-monotone-rne, which IS settled, by exhaustive enumeration at 8 bits.",
    "prior_art": [
      {
        "who": "IEEE 754-2019, clause 4.3 (rounding-direction attributes)",
        "what": "That each rounding-direction attribute defines a monotone map from the extended reals to the format, so a correctly-rounded operation inherits the monotonicity of the exact operation it rounds -- which gives the statement for every format, this one included.",
        "where": "IEEE Standard for Floating-Point Arithmetic",
        "year": 2019,
        "attribution": "standard textbook attribution; this lane did not consult the primary source, and records the citation as the received one."
      },
      {
        "who": "z3 4.13.3 and bitwuzla 0.9.1",
        "what": "Both report `unsat` on the negation, i.e. both prove the statement, in 30.6s and 8.3s respectively on artifacts/facts/smt2/neg-fp16-add-monotone-rne.smt2 (measured 2026-08-14, same machine, release builds).",
        "where": "run locally by the fp-kernels lane; the file is in this repository",
        "year": 2026,
        "attribution": "measured by this lane, not cited. Recorded under prior_art because these are OTHER systems establishing the fact -- deliberately NOT recorded as `evidence`, since evidence rows here would make the ledger say axeyum established something it did not."
      }
    ]
  },
  "notes": "WHY THIS FACT IS `open` AND ITS 8-BIT SIBLING IS `proved`. F:fp8-add-monotone-rne is settled by enumerating all 2^24 fp8 E5M2 triples in about three seconds. The identical proposition at binary16 is 2^48 triples, so brute force is gone, and the symbolic route is where axeyum currently cannot follow: the fp8 version of this query -- three free symbols and four fp.add circuits -- ran 50 minutes in a release build without deciding, and binary16 is strictly larger. bitwuzla settles the binary16 file in 8.3 seconds.\n\nSO THIS IS A MEASURED PARITY GAP, WRITTEN DOWN AS A TARGET. `epistemic_status` is a statement about THIS ledger, and the honest value is `open`: we have not established it. `external_status` is `proved` because two independent solvers have. The evidence array is empty on purpose -- a filled one would read as axeyum having done the work.\n\nWHAT WOULD CLOSE IT. Nothing about the mathematics; the statement follows from monotonicity of correct rounding. The gap is entirely in the FP-to-bit-vector route's throughput on a multi-operand comparison miter. `artifacts/facts/smt2/neg-fp16-add-monotone-rne.smt2` is the reproducible target, byte-identical to the file the two oracles were timed on.\n\nARITY IS THE WALL, NOT WIDTH. F:fp32-doubling-add-equals-mul-two is a UNARY claim at binary32 and was enumerated over all 2^32 inputs in 51 seconds. Ternary claims lose brute force at 8 bits already. That is why this fact and its 8-bit sibling exist as a pair.\n\nMEASURED AGAIN 2026-08-22, AND THE DECISION HALF OF THIS NOTE IS NOW WRONG.\n`axeyum_cli` decides this file `unsat` in **11.5 s, reproducibly** — faster than\nz3's 30.6 s on the same machine and within 1.4x of bitwuzla's 8.3 s. The claim\nabove that \"the symbolic route is where axeyum currently cannot follow\" described\nthe state on 2026-08-14 and no longer holds; the 50-minute non-decision it cites\nwas the fp8 SYMBOLIC route before subsequent solver work.\n\nWHAT HAS NOT CLOSED IS THE CERTIFICATE, and that is why this fact is still\n`open`. Run through `smtcomp_cli --evidence`, which produces and re-checks a DRAT\nrefutation rather than merely deciding, the same query exceeded **183 minutes**\nat 99.8% CPU with resident memory flat at 0.34 GB — working, not thrashing. For\ncalibration the fp8 sibling, a strictly smaller problem, took 25m46s with\nevidence.\n\nSo the honest split is: **deciding costs 11.5 s, certifying costs more than three\nhours and has not been observed to terminate.** That ratio is the finding. This\nfact stays `open` because `epistemic_status` is a statement about what THIS\nledger can show, and a bare verdict with no checkable artifact is not evidence —\nexactly the standard the rest of the ledger is held to.\n\nCORRECTED AGAIN 2026-08-22, LATER THE SAME DAY: THE SIX HOURS WERE THE CHECKER,\nNOT THE SOLVER. Progress instrumentation landed in `proof_sat.rs` and the first\nbounded run answered it immediately:\n\n    search   424,601 conflicts in 24.2 s -> 827,048 proof steps, 193 MB\n    then     progress stops -- the search has RETURNED, at a genuine terminal\n             state, not the deadline and not the conflict cap\n    then     the remaining time is `check_drat` + `elaborate_drat_to_lrat`,\n             which have no deadline and no observability of their own\n\nSo certificate PRODUCTION is fast; certificate CHECKING and LRAT elaboration over\na 193 MB proof is what does not terminate. The fp8 sibling confirms the shape:\n1.3 s of search, 8.9 MB of proof, and roughly 25 minutes of check+elaborate. At\n21.7x the proof size, fp16's check stage is the wall.\n\nThe earlier note in this file extrapolated 822 years from the search-space ratio.\nThat number measured the wrong stage and should be read as void: search scales\nabout 18x here for a 2^24-fold larger space, which is fine. What scales badly is\nelaboration in proof size.\n\nThis fact remains `open` for the same reason as before -- no checkable artifact --\nbut the target has moved. It is not \"make the solver faster\"; it is \"make a\n193 MB refutation checkable\", which is what cube-and-conquer (ADR-0543) addresses\nby producing many small per-cube proofs instead of one large one.\n\nRE-MEASURED 2026-08-28, decidable-frontier lane. This is a confirming re-run, not a new finding, and epistemic_status stays `open` -- nothing here is a checkable artifact.\n\nDECIDE-ONLY, reconfirmed: `axeyum_cli` on artifacts/facts/smt2/neg-fp16-add-monotone-rne.smt2 -> `unsat` in 11.09 s wall (release build, host load ~1.7-1.8/16). Matches the 11.5 s figure from 2026-08-22 within noise -- the decision route has not regressed or improved materially.\n\nEVIDENCE/SEARCH, reconfirmed twice: `smtcomp_cli --evidence --progress` with an internal `--timeout-ms` watchdog. Both runs (120 s and 300 s budgets) show the identical search terminal state already recorded: 424,601 conflicts in 24.4-27.5 s (run-to-run variance) -> 827,048 proof steps, 193,214,020 bytes (~193 MB). Neither run produced a certified verdict inside its budget -- both printed `unknown`, exactly as expected for a watchdog firing mid-check.\n\nCHECKING STAGE, now measured directly rather than inferred. The 120 s and 300 s runs (default `AXEYUM_CHECK_PROGRESS_INTERVAL=50000`) produced ZERO `; checking` lines -- i.e. `check_drat` had not reached even its first 50,000-step checkpoint after ~95 s and ~272 s of checking time respectively. Lowering the interval to `AXEYUM_CHECK_PROGRESS_INTERVAL=1000` on a fresh 90 s-budget run got five real `stage=drat_check` samples:\n\n    steps=1000 elapsed_ms=9850  steps_per_sec=101.5\n    steps=2000 elapsed_ms=25913 steps_per_sec=77.2\n    steps=3000 elapsed_ms=27248 steps_per_sec=110.1\n    steps=4000 elapsed_ms=46694 steps_per_sec=85.7\n    steps=5000 elapsed_ms=49165 steps_per_sec=101.7\n\nAverage ~95 steps/s against a `total=827048`. Extrapolating at that measured rate: `drat_check` ALONE needs on the order of 8,700 s (~2.4 h) to finish re-deriving the refutation, before `elaborate_drat_to_lrat` (the second sub-stage, not reached in any run here) even starts. That is a real per-run measurement of the bottleneck this fact already named, not a repeat of the earlier extrapolation-from-a-different-stage error the 2026-08-22 note corrects -- `stage=drat_check` is explicit in every sampled line, so there is no ambiguity about which sub-stage is slow.\n\nCONCLUSION UNCHANGED, now on firmer ground: deciding this query costs ~11 s; producing a checkable certificate costs multiple hours and was not observed to complete in any bounded run attempted here (up to 300 s of checking time, three separate invocations). The fact stays `open`. Nothing in `crates/axeyum-lean-kernel/` or `scripts/` was touched to get these numbers -- they come from the existing `smtcomp_cli --evidence --progress` instrumentation and `AXEYUM_CHECK_PROGRESS_INTERVAL`, run against the fact's own pinned negation file.\n\nOBSTRUCTION LIFTED 2026-08-28, drat-evidence-route lane. THE CHECKING STAGE NOW COMPLETES. The note immediately above says certifying this query \"costs multiple hours and was not observed to complete in any bounded run\". That was true of the route that existed when it was written and is NO LONGER TRUE, so read it as history.\n\nWHAT CHANGED (ADR-0613). The evidence path called `check_drat`, the FORWARD reference checker, at every accepting site -- not for a documented reason but because those call sites date from 2026-06-13 and the backward core-first engine (ADR-0382, ~66x) only landed 2026-08-12, additively, with its item 9 deferring exactly this follow-on. Nothing pinned the choice. `certify_unsat_via_lrat` now runs the backward engine as an UNTRUSTED producer of antecedent hints and has `check_lrat` -- small, search-free, linear -- verify those hints against the formula. The trusted base does not move: a `Certified` is discharged by `check_lrat` alone, and `check_drat_backward` appears only in rejecting position.\n\nMEASURED, same pinned file, `smtcomp_cli --evidence --progress` (release, contended host, load ~9.4):\n\n    search    424,601 conflicts in 27.748 s -> 827,048 proof steps, 193,214,020 bytes\n    checking  stage=backward_lrat_certify  82.302 s  10,048.8 steps/sec  certified=true\n    evidence  kind=unsat-drat certified=1 recheck=ok arena=ok ms=125098\n    unsat\n\nSo: 125.098 s END TO END for a certified refutation, of which 82.3 s is checking. Against the 2026-08-28 measurement in the note above -- ~95 steps/sec, ~8,700 s extrapolated for `drat_check` ALONE before `elaborate_drat_to_lrat` even started -- that is roughly 106x on the checking stage. The binary8 sibling moved from a recorded 25m46s to 5.028 s on the same route.\n\nTHIS FACT IS STILL RECORDED `open`, DELIBERATELY, AND THAT IS NOT AN OVERSIGHT. `epistemic_status` is a statement about what this ledger can SHOW, and showing it requires an `evidence` row whose `checker_command` fails when the claim is false. That command does not exist yet: it needs a release build of `smtcomp_cli`, a discriminating exit status (the run prints `unsat` and `certified=1`; an exit status that depends only on the process completing would be exactly the checker-that-cannot-fail defect this repository audits for), and a verified negative control. The lane that measured this was scoped to notes on this file and did not write that row. WHAT IS ESTABLISHED is that the obstruction this fact was blocked on is gone; what is NOT established is the ledger evidence. Those are different claims and only the first is made here.\n\nSETTLED 2026-08-28, fp16-evidence lane. The note immediately above records that the LRAT checking obstruction was gone but the LEDGER EVIDENCE did not yet exist -- \"those are different claims and only the first is made here.\" This entry makes the second claim, and says exactly what it rests on.\n\nThis lane rebuilt `smtcomp_cli --release` from scratch in its own worktree (the ADR-0613 code landed within the hour, so a stale prebuilt binary would have silently measured the OLD superlinear-checking route) and ran it twice, end to end, against this fact's own pinned negation file. Both runs: verdict `unsat`, `certified=1`, `recheck=ok`, `arena=ok`. See the `evidence` array above for the exact numbers, the checker_command, and a correction to the ADR-0613 \"end to end\" figure: the `; evidence ... ms=NNN` line `smtcomp_cli` prints is captured BEFORE `UnsatProof::recheck()` and the arena fresh-parse check run, so it is a sub-stage timer, not the wall-clock cost of the command. The ACTUAL external wall-clock this lane measured twice, independently, was 339.01s and 353s -- still roughly two orders of magnitude below the un-observed-to-finish forward-checker route this fact was blocked on, just not the ~125s the ADR's own prose calls \"end to end.\"\n\nWHAT IS NOT CLAIMED. No enumeration ran or could run at this width (2^48 triples). No new cross-check against z3/bitwuzla was run by this lane; the existing `prior_art` entries (z3 30.6s, bitwuzla 8.3s, both 2026-08-14, same pinned file) already establish `external_status: proved` and are unaffected by this row. This fact rests on exactly one evidence route -- the symbolic CNF/DRAT/LRAT one -- unlike its fp8 sibling, which has two independent ones."
}
