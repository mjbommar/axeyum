{
  "schema_version": 1,
  "id": "F:ml430-nat-dvd-of-lcm-left-dvd-d6b2407c",
  "title": "Mathlib v4.30 source proposition Nat.dvd_of_lcm_left_dvd",
  "statement": "The proposition declared as `Nat.dvd_of_lcm_left_dvd` in the pinned Mathlib v4.30 source.",
  "formal": {
    "language": "lean4-surface",
    "statement": "∀ {a b c : ℕ}, a.lcm b ∣ c → b ∣ c",
    "fragment": "Nat",
    "kernel_theorem": "Nat.dvd_of_lcm_left_dvd",
    "kernel_statement": "theorem Nat.dvd_of_lcm_left_dvd : ((x0 : AxNat) -> ((x1 : AxNat) -> ((x2 : AxNat) -> ((x3 : AxNat.dvd (AxNat.lcm x0 x1) x2) -> AxNat.dvd x1 x2))))"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "depends_on": [
    "F:nat-dvd-lcm-right",
    "F:nat-dvd-trans",
    "F:ml430-nat-dvd-lcm-right-18ab8e2f"
  ],
  "evidence": [
    {
      "id": "kernel-Nat.dvd_of_lcm_left_dvd",
      "kind": "kernel-term",
      "supports": "lcm a b divides c implies b divides c",
      "check_status": "checked",
      "checker_command": "test \"$(cargo run -q -p axeyum-lean-kernel --example nat_theorem_inventory -- dvd_of_lcm_left_dvd 2>/dev/null | grep -Ec '^Nat\\.dvd_of_lcm_left_dvd[[:space:]]')\" -ge 1",
      "notes": "`build_nat_prelude` admits this theorem through the trusted `Kernel::add_declaration` gate, which re-checks the constructed proof term against the stated type: `dvd_trans` composing `dvd_lcm_right : b | lcm a b` with the hypothesis `lcm a b | c`. `nat_theorem_inventory` exits non-zero for a name that does not exist, and the `grep -c` requires the admitted declaration to be printed."
    },
    {
      "id": "footprint-Nat.dvd_of_lcm_left_dvd",
      "kind": "exhaustive-enumeration",
      "supports": "axiom_footprint: [] -- the Nat prelude's trusted surface is empty",
      "check_status": "checked",
      "checkers": [
        "Kernel::axiom_footprint",
        "environment-wide trusted-surface enumeration"
      ],
      "checker_command": "cargo run -q -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat",
      "notes": "`nat_axiom_inventory --require-axiom-free nat` enumerates the built Nat environment and exits non-zero unless it admits no Axiom, Opaque or Quotient declaration. A theorem cannot depend on a trusted declaration the environment does not contain, so an empty trusted surface bounds every individual theorem's footprint by []."
    }
  ],
  "provenance": {
    "date": "2026-08-28",
    "established_by": "axeyum-lean-kernel build_nat_prelude, lane nat-gcd",
    "source": "statement-only extraction of the Mathlib proposition from Mathlib v4.30.0 (see prior_art), independently proved here as `Nat.dvd_of_lcm_left_dvd` and admitted through `Kernel::add_declaration`",
    "prior_art": [
      {
        "who": "the Mathlib contributors",
        "what": "the theorem declaration `Nat.dvd_of_lcm_left_dvd`",
        "where": "mathlib4 commit c5ea00351c28e24afc9f0f84379aa41082b1188f (v4.30.0)",
        "year": 2026,
        "attribution": "the proposition was read from the pinned statement-only inventory; the proof term and tactic trace were not consulted"
      }
    ]
  },
  "notes": "`Nat.Coprime` has no separate name in this kernel's Nat prelude (matching `Nat.coprime_of_lt_prime`'s established convention); it is spelled inline as `gcd _ _ = 1`, so `formal.statement` is the pinned Mathlib v4.30 proposition this fact mirrors; `formal.kernel_statement` is the actual kernel-core `render_lean` output for `Nat.dvd_of_lcm_left_dvd`, which spells the same proposition in the prelude's own vocabulary. `depends_on` restricts to entries that already carry a fact ledger entry.",
  "proof_route": "kernel-lean",
  "axiom_footprint": []
}
