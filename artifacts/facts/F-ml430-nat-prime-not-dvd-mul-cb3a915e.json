{
  "schema_version": 1,
  "axiom_footprint": [],
  "id": "F:ml430-nat-prime-not-dvd-mul-cb3a915e",
  "title": "Mathlib v4.30 source proposition Nat.Prime.not_dvd_mul",
  "statement": "The proposition declared as `Nat.Prime.not_dvd_mul` in the pinned Mathlib v4.30 source.",
  "formal": {
    "language": "lean4-surface",
    "statement": "∀ {p m n : ℕ}, Nat.Prime p → ¬p ∣ m → ¬p ∣ n → ¬p ∣ m * n",
    "fragment": "Nat"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "depends_on": ["F:nat-euclid-lemma", "F:ml430-nat-prime-dvd-or-dvd-4ae88221"],
  "evidence": [
    {
      "check_status": "checked",
      "checker_command": "test \"$(cargo run -q -p axeyum-lean-kernel --example nat_theorem_inventory -- prime_not_dvd_mul 2>/dev/null | grep -Ec '^Nat\\.prime_not_dvd_mul[[:space:]]')\" -ge 1",
      "id": "kernel-Nat.prime_not_dvd_mul",
      "kind": "kernel-term",
      "notes": "`build_nat_prelude` admits `Nat.prime_not_dvd_mul` through the trusted `Kernel::add_declaration` gate, which re-checks the proof term against the stated type, so producing this row at all is a machine-checked proof. `nat_theorem_inventory` exits non-zero for a name that does not exist, and the `grep -c` count (tested `-ge 1`, not piped into `grep -q`) requires the admitted declaration to actually be printed. Primality is spelled inline (`2 ≤ p ∧ ∀ d, d ∣ p → d = 1 ∨ d = p`, matching `coprime_of_lt_prime`'s and `euclid_lemma`'s own convention) rather than through a named `Nat.Prime` predicate, since this prelude has none.",
      "supports": "∀ {p m n : ℕ}, Nat.Prime p → ¬p ∣ m → ¬p ∣ n → ¬p ∣ m * n"
    },
    {
      "check_status": "checked",
      "checker_command": "cargo run -q -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat",
      "checkers": [
        "Kernel::axiom_footprint",
        "environment-wide trusted-surface enumeration"
      ],
      "id": "footprint-Nat.prime_not_dvd_mul",
      "kind": "exhaustive-enumeration",
      "notes": "`nat_axiom_inventory --require-axiom-free nat` enumerates the built Nat environment and exits non-zero unless it admits no Axiom, Opaque or Quotient declaration (measured: axiom=0 opaque=0 quotient=0). A theorem cannot depend on a trusted declaration the environment does not contain, so an empty trusted surface bounds every individual theorem's footprint by [].",
      "supports": "axiom_footprint: [] -- the Nat prelude's trusted surface is empty"
    }
  ],
  "provenance": {
    "date": "2026-08-18",
    "established_by": "axeyum-lean-kernel build_nat_prelude, lane nat-prime",
    "source": "statement-only extraction of `Nat.Prime.not_dvd_mul` from Mathlib v4.30.0; no proof value was exposed",
    "prior_art": [
      {
        "who": "the Mathlib contributors",
        "what": "the theorem declaration `Nat.Prime.not_dvd_mul`",
        "where": "mathlib4 commit c5ea00351c28e24afc9f0f84379aa41082b1188f (v4.30.0)",
        "year": 2026,
        "attribution": "the proposition was read from the pinned statement-only inventory; the proof term and tactic trace were not consulted"
      }
    ]
  },
  "proof_route": "kernel-lean",
  "notes": "Proved in Axeyum via `declare_prime_not_dvd_mul` (`nat_prelude/primes.rs`), the direct contrapositive of `euclid_lemma` (`bezout.rs`): assuming `dvd p (m*n)`, `euclid_lemma` splits it into `dvd p m or dvd p n`, and each branch applies directly to one of the two `Not` hypotheses. formal.statement is Lean surface syntax accepted as a proposition by the proof-free generated axiom module; it is not kernel-core render_lean output. depends_on projects only direct upstream theorem uses whose endpoints both survived review. Those edges are curriculum and leakage metadata and grant no dispatch, proof, or admission authority."
}
