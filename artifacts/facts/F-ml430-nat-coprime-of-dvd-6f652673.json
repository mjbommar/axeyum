{
  "schema_version": 1,
  "axiom_footprint": [],
  "id": "F:ml430-nat-coprime-of-dvd-6f652673",
  "title": "Mathlib v4.30 source proposition Nat.coprime_of_dvd'",
  "statement": "The proposition declared as `Nat.coprime_of_dvd'` in the pinned Mathlib v4.30 source.",
  "formal": {
    "language": "lean4-surface",
    "statement": "∀ {m n : ℕ}, (∀ (k : ℕ), Nat.Prime k → k ∣ m → k ∣ n → k ∣ 1) → m.Coprime n",
    "fragment": "Nat",
    "kernel_theorem": "Nat.coprime_of_forall_prime_dvd"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "depends_on": ["F:nat-dvd-trans", "F:nat-dvd-zero", "F:nat-exists-prime-dvd", "F:nat-gcd-dvd-left", "F:nat-gcd-dvd-right", "F:nat-le-antisymm", "F:nat-le-of-dvd", "F:nat-le-of-succ-le-succ", "F:nat-le-trans", "F:nat-lt-or-ge", "F:nat-not-succ-le-zero", "F:nat-one-le-of-dvd-pos", "F:nat-succ-pred-of-pos", "F:nat-two-le-succ-or-eq-one", "F:nat-zero-le", "F:nat-zero-mul", "F:ml430-nat-le-antisymm-79dccead", "F:ml430-nat-le-of-succ-le-succ-a180a72c", "F:ml430-nat-le-refl-fd7d9e15"],
  "evidence": [
    {
      "check_status": "checked",
      "checker_command": "test \"$(cargo run -q -p axeyum-lean-kernel --example nat_theorem_inventory -- coprime_of_forall_prime_dvd 2>/dev/null | grep -Ec '^Nat\\.coprime_of_forall_prime_dvd[[:space:]]')\" -ge 1",
      "id": "kernel-Nat.coprime_of_forall_prime_dvd",
      "kind": "kernel-term",
      "notes": "`build_nat_prelude` admits `Nat.coprime_of_forall_prime_dvd` through the trusted `Kernel::add_declaration` gate, which re-checks the proof term against the stated type, so producing this row at all is a machine-checked proof. `nat_theorem_inventory` exits non-zero for a name that does not exist, and the `grep -c` count (tested `-ge 1`, not piped into `grep -q`) requires the admitted declaration to actually be printed. Confirmed kernel type: `x0 x1, (x2, (2<=x2 /\\ forall x3, x3|x2 -> x3=1 or x3=x2) -> x2|x0 -> x2|x1 -> x2|1) -> gcd x0 x1 = 1`, matching `m n, (k, Prime k -> k|m -> k|n -> k|1) -> Coprime m n` with the prelude's own inline primality spelling (no `Prime`/`Nat.Prime` predicate exists here; matches this file's established convention). Named `Nat.coprime_of_forall_prime_dvd` rather than `coprime_of_dvd` because that name is already taken by the unrelated `Nat.Coprime.of_dvd` (`dvd a1 a2 -> dvd b1 b2 -> Coprime a2 b2 -> Coprime a1 b1`), confirmed by grep of the whole `nat_prelude.rs` field list before naming. Proved via `declare_coprime_of_forall_prime_dvd` (`nat_prelude/primes.rs`): trichotomy on `g := gcd m n` via `lt_or_ge` twice. `g < 1` (so `g = 0` via `le_of_succ_le_succ`+`zero_le`+`le_antisymm`) forces `m = n = 0` (`zero_mul` on the `dvd 0 _` witness), and applying the hypothesis at `k = 2` (`prime_two`, already in this file) then contradicts itself (`refute_dvd_one_against_prime`, an existing private helper this file already had for exactly this `dvd p one -> False` shape). `1 <= g` and `g < 2` gives `g = 1` directly (`le_of_succ_le_succ` + `le_antisymm`) -- the goal. `1 <= g` and `2 <= g`: `exists_prime_dvd` gives a prime factor `pw` of `g`, hence of `m` and `n` (`dvd_trans`), so the hypothesis gives `pw | 1`, refuted the same way. No decidable equality or classical choice beyond the two order trichotomies (`lt_or_ge`) already proved in this prelude.",
      "supports": "forall m n, (forall k, Nat.prime_condition k -> k | m -> k | n -> k | 1) -> Nat.gcd m n = 1"
    },
    {
      "check_status": "checked",
      "checker_command": "cargo run -q -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat",
      "checkers": [
        "Kernel::axiom_footprint",
        "environment-wide trusted-surface enumeration"
      ],
      "id": "footprint-Nat.coprime_of_forall_prime_dvd",
      "kind": "exhaustive-enumeration",
      "notes": "`nat_axiom_inventory --require-axiom-free nat` enumerates the built Nat environment and exits non-zero unless it admits no Axiom, Opaque or Quotient declaration (measured: axiom=0 opaque=0 quotient=0). A theorem cannot depend on a trusted declaration the environment does not contain, so an empty trusted surface bounds every individual theorem's footprint by [].",
      "supports": "axiom_footprint: [] -- the Nat prelude's trusted surface is empty"
    }
  ],
  "provenance": {
    "date": "2026-08-18",
    "established_by": "not established in this ledger",
    "source": "statement-only extraction of `Nat.coprime_of_dvd'` from Mathlib v4.30.0; no proof value was exposed",
    "prior_art": [
      {
        "who": "the Mathlib contributors",
        "what": "the theorem declaration `Nat.coprime_of_dvd'`",
        "where": "mathlib4 commit c5ea00351c28e24afc9f0f84379aa41082b1188f (v4.30.0)",
        "year": 2026,
        "attribution": "the proposition was read from the pinned statement-only inventory; the proof term and tactic trace were not consulted"
      }
    ]
  },
  "proof_route": "kernel-lean",
  "notes": "Proved in Axeyum via `declare_coprime_of_forall_prime_dvd` (`nat_prelude/primes.rs`), named `coprime_of_forall_prime_dvd` (not `coprime_of_dvd`, which already names the unrelated `Nat.Coprime.of_dvd`), spelling `Nat.Prime` inline as `prime_condition` per this prelude's established convention (no separate `Prime` predicate exists). Dispatched after `declare_succ_pred_of_pos`: the proof's `prime_two` helper (via `two_divisor_dichotomy`) needs it and is not available at the `declare_primes`-adjacent dispatch point the sibling `coprime_of_*` declarations use. formal.statement is Lean surface syntax accepted as a proposition by the proof-free generated axiom module; it is not kernel-core render_lean output. depends_on projects only direct upstream theorem uses whose endpoints both survived review. Those edges are curriculum and leakage metadata and grant no dispatch, proof, or admission authority."
}
