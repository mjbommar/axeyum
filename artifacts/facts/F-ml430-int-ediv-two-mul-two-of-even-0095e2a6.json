{
  "schema_version": 1,
  "id": "F:ml430-int-ediv-two-mul-two-of-even-0095e2a6",
  "title": "Mathlib v4.30 source proposition Int.ediv_two_mul_two_of_even",
  "statement": "The proposition declared as `Int.ediv_two_mul_two_of_even` in the pinned Mathlib v4.30 source.",
  "formal": {
    "language": "lean4-surface",
    "statement": "∀ {n : ℤ}, Even n → n / 2 * 2 = n",
    "fragment": "Int"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "depends_on": [
    "F:int-add-zero",
    "F:int-ediv-add-emod",
    "F:int-mul-comm",
    "F:ml430-nat-even-iff-024826e9"
  ],
  "evidence": [
    {
      "id": "kernel-Int.ediv_two_mul_two_of_even",
      "kind": "kernel-term",
      "supports": "The proposition declared as `Int.ediv_two_mul_two_of_even` in the pinned Mathlib v4.30 source.",
      "check_status": "checked",
      "checkers": [
        "Kernel::add_declaration (re-derives the type from the proof term)",
        "int_prelude_tests::every_int_declaration_is_checked_and_axiom_free",
        "int_prelude_tests::derived_laws_have_no_axiom_footprint"
      ],
      "checker_command": "cargo test -p axeyum-lean-kernel --lib int_prelude::",
      "notes": "`Int.ediv_two_mul_two_of_even` in `crates/axeyum-lean-kernel/src/int_prelude/parity.rs` (`declare_ediv_two_mul_two_of_even`). Mirrors Mathlib v4.30's `Int.ediv_two_mul_two_of_even` (`Mathlib/Algebra/Group/Int/Even.lean`) verbatim. From `Int.ediv_add_emod` at `b := 2`: `Even n` gives `n % 2 = 0` (a new case-split helper `even_implies_emod_zero`, through `Nat.even_iff_mod_two_eq_zero` in the `ofNat` branch and a contrapositive through `Nat.even_iff_odd_succ` in the `negSucc` branch), rewritten via `add_zero` then `mul_comm` to swap `2*(n/2)` into `(n/2)*2`."
    },
    {
      "id": "footprint-Int.ediv_two_mul_two_of_even",
      "kind": "exhaustive-enumeration",
      "supports": "axiom_footprint: [] -- this theorem's trusted closure is empty",
      "check_status": "checked",
      "checkers": [
        "Kernel::axiom_footprint",
        "environment-wide trusted-surface enumeration (theorem_axiom_footprint)"
      ],
      "checker_command": "test \"$(cargo run --release -q -p axeyum-lean-kernel --example theorem_axiom_footprint -- Int.ediv_two_mul_two_of_even 2>/dev/null | /usr/bin/grep -xFc $'integer\\tInt.ediv_two_mul_two_of_even\\t0\\t')\" -ge 1",
      "notes": "The matched row is `integer<TAB>Int.ediv_two_mul_two_of_even<TAB>0<TAB>` with an EMPTY trailing footprint column, so a nonzero footprint or an absent declaration both give a count of 0 and fail. Uses `/usr/bin/grep` explicitly (not the interactive `ugrep` alias) and ANSI-C-quoted literal tabs, per this repository's own grep-dialect gotcha. Run `--release`: in debug, building the full constructed environment stack-overflows, which is a resource limit and not a fact about this theorem."
    }
  ],
  "provenance": {
    "date": "2026-08-29",
    "established_by": "not established in this ledger",
    "source": "statement-only extraction of `Int.ediv_two_mul_two_of_even` from Mathlib v4.30.0; no proof value was exposed",
    "prior_art": [
      {
        "who": "the Mathlib contributors",
        "what": "the theorem declaration `Int.ediv_two_mul_two_of_even`",
        "where": "mathlib4 commit c5ea00351c28e24afc9f0f84379aa41082b1188f (v4.30.0)",
        "year": 2026,
        "attribution": "the proposition was read from the pinned statement-only inventory; the proof term and tactic trace were not consulted"
      }
    ]
  },
  "notes": "CLOSED via the `int-parity-two` lane, 2026-08-29, as `Int.ediv_two_mul_two_of_even` -- axiom-free, statement identical to Mathlib's (confirmed a same-definition mirror by reading the pinned Mathlib v4.30 source directly, not by inference from the name).\n\n--- PRE-CLOSURE RECORD, RETAINED AS HISTORY ---\n\nOpen in Axeyum. The external theorem declaration is prior art, not a locally constructed proof. formal.statement is a BYTE-IDENTICAL quotation of the pinned extractor's pretty-printed type. Re-elaborated as a proof-free axiom after `import Mathlib` against Mathlib c5ea00351c28 on s5 and ACCEPTED -- the same grade as the 214 nursery-v1 rows. Acceptance is syntax/type evidence about the statement, never proof evidence about the claim. Preregistered in artifacts/autogenesis/nursery-v2-extension.json, which carries the partition; that manifest, not this file, is the split authority. Screened against the mirror-divergence registry and the statable-here vocabulary before preregistration.",
  "proof_route": "kernel-lean",
  "axiom_footprint": []
}
