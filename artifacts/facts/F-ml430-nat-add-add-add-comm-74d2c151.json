{
  "schema_version": 1,
  "id": "F:ml430-nat-add-add-add-comm-74d2c151",
  "title": "Mathlib v4.30 source proposition Nat.add_add_add_comm",
  "statement": "The proposition declared as `Nat.add_add_add_comm` in the pinned Mathlib v4.30 source.",
  "formal": {
    "language": "lean4-surface",
    "statement": "∀ (a b c d : ℕ), a + b + (c + d) = a + c + (b + d)",
    "fragment": "Nat",
    "kernel_theorem": "Nat.add_add_add_comm"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "depends_on": ["F:ml430-nat-add-assoc-8c87a1f1", "F:ml430-nat-add-comm-56a2d614"],
  "evidence": [
    {
      "check_status": "checked",
      "checker_command": "test \"$(cargo run -q -p axeyum-lean-kernel --example nat_theorem_inventory -- add_add_add_comm 2>/dev/null | grep -Ec '^Nat\\.add_add_add_comm[[:space:]]')\" -ge 1",
      "id": "kernel-Nat.add_add_add_comm",
      "kind": "kernel-term",
      "notes": "`build_nat_prelude` admits `Nat.add_add_add_comm` through the trusted `Kernel::add_declaration` gate, declared in `nat_prelude/add_basics.rs`. `declare_add_add_add_comm` chains `add_assoc` twice and `add_comm` once (the same construction `div_mod_lemmas.rs`'s per-file private copy already used internally; this is the first PUBLIC `Nat.add_add_add_comm` this prelude has declared -- see that module doc's remark that the prelude had none). `nat_theorem_inventory`'s rendered type is `(x0..x3:AxNat)->Eq (add (add x0 x1) (add x2 x3)) (add (add x0 x2) (add x1 x3))`, matching this fact's `formal.statement`. `nat_theorem_inventory` exits non-zero for a name that does not exist, and the `grep -c` count (tested `-ge 1`, not piped into `grep -q`) requires the admitted declaration to actually be printed. Verified both ways: the real name greps to a count `-ge 1`; grepping a made-up name (`Nat.add_add_add_comm_bogus`) greps to `0`.",
      "supports": "∀ (a b c d : ℕ), a + b + (c + d) = a + c + (b + d)"
    },
    {
      "check_status": "checked",
      "checker_command": "cargo run -q -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat",
      "checkers": [
        "Kernel::axiom_footprint",
        "environment-wide trusted-surface enumeration"
      ],
      "kind": "exhaustive-enumeration",
      "notes": "`nat_axiom_inventory --require-axiom-free nat` enumerates the built Nat environment and exits non-zero unless it admits no Axiom, Opaque or Quotient declaration (measured: axiom=0 opaque=0 quotient=0). A theorem cannot depend on a trusted declaration the environment does not contain, so an empty trusted surface bounds every individual theorem's footprint by []. `nat_prelude_tests::every_nat_declaration_is_checked_and_axiom_free` additionally checks this theorem's own `Kernel::axiom_footprint` directly via `theorem_names`, and `the_build_is_deterministic`/`every_promised_name_is_admitted_with_the_expected_kind` (both re-run on every `nat_prelude::` sweep) confirm the declaration is admitted at the exact rendered type checked below.",
      "supports": "axiom_footprint: [] -- the Nat prelude's trusted surface is empty",
      "id": "footprint-Nat.add_add_add_comm"
    }
  ],
  "provenance": {
    "date": "2026-08-29",
    "established_by": "not established in this ledger",
    "source": "statement-only extraction of `Nat.add_add_add_comm` from Mathlib v4.30.0; no proof value was exposed",
    "prior_art": [
      {
        "who": "the Mathlib contributors",
        "what": "the theorem declaration `Nat.add_add_add_comm`",
        "where": "mathlib4 commit c5ea00351c28e24afc9f0f84379aa41082b1188f (v4.30.0)",
        "year": 2026,
        "attribution": "the proposition was read from the pinned statement-only inventory; the proof term and tactic trace were not consulted"
      }
    ]
  },
  "notes": "Established 2026-08-29 in this ledger by the `nat-add-basics` lane: a fresh local construction (`nat_prelude/add_basics.rs`), not a transcription of Mathlib's proof. The declared statement matches this fact's `formal.statement`; see the kernel-term evidence row for the exact rendered type comparison. `depends_on` is empty because the route builds directly on prelude machinery already declared earlier in `build_nat_prelude` (`add_zero`/`zero_add`/`add_comm`/`add_assoc`/`add_left_cancel`/`add_right_cancel`/`add_eq_zero`/`le_add_right`/`lt_or_eq_of_le`/`le_of_lt_succ`/`not_lt_zero`), not on another `ml430` fact. Re-elaborated as a proof-free axiom after `import Mathlib` against Mathlib c5ea00351c28 on s5 and ACCEPTED -- the same grade as the 214 nursery-v1 rows.",
  "proof_route": "kernel-lean",
  "axiom_footprint": []
}
