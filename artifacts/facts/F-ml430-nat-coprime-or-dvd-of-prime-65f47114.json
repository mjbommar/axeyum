{
  "schema_version": 1,
  "axiom_footprint": [],
  "id": "F:ml430-nat-coprime-or-dvd-of-prime-65f47114",
  "title": "Mathlib v4.30 source proposition Nat.coprime_or_dvd_of_prime",
  "statement": "The proposition declared as `Nat.coprime_or_dvd_of_prime` in the pinned Mathlib v4.30 source.",
  "formal": {
    "language": "lean4-surface",
    "statement": "∀ {p : ℕ}, Nat.Prime p → ∀ (i : ℕ), p.Coprime i ∨ p ∣ i",
    "fragment": "Nat",
    "kernel_theorem": "Nat.coprime_or_dvd_of_prime"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "depends_on": [
    "F:ml430-nat-prime-dvd-iff-not-coprime-77854741",
    "F:nat-eq-of-beq-eq-true",
    "F:nat-ne-of-beq-eq-false",
    "F:ml430-nat-eq-of-beq-eq-true-6ec35ee4"
  ],
  "evidence": [
    {
      "check_status": "checked",
      "checker_command": "test \"$(cargo run -q -p axeyum-lean-kernel --example nat_theorem_inventory -- coprime_or_dvd_of_prime 2>/dev/null | grep -Ec '^Nat\\.coprime_or_dvd_of_prime[[:space:]]')\" -ge 1",
      "id": "kernel-Nat.coprime_or_dvd_of_prime",
      "kind": "kernel-term",
      "notes": "`build_nat_prelude` admits `Nat.coprime_or_dvd_of_prime` through the trusted `Kernel::add_declaration` gate, which re-checks the proof term against the stated type, so producing this row at all is a machine-checked proof. `nat_theorem_inventory` exits non-zero for a name that does not exist, and the `grep -c` count (tested `-ge 1`, not piped into `grep -q`) requires the admitted declaration to actually be printed. Decides `beq (gcd p i) one` via a `Bool.rec` case split (fully constructive, not classical excluded middle -- `Bool` has exactly two constructors): the `true` branch gives `Coprime p i` directly (`eq_of_beq_eq_true`); the `false` branch gives `Not (Coprime p i)` (`ne_of_beq_eq_false`), which `prime_dvd_iff_not_coprime`'s reverse direction converts to `dvd p i`.",
      "supports": "∀ {p : ℕ}, Nat.Prime p → ∀ (i : ℕ), p.Coprime i ∨ p ∣ i"
    },
    {
      "check_status": "checked",
      "checker_command": "cargo run -q -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat",
      "checkers": [
        "Kernel::axiom_footprint",
        "environment-wide trusted-surface enumeration"
      ],
      "id": "footprint-Nat.coprime_or_dvd_of_prime",
      "kind": "exhaustive-enumeration",
      "notes": "`nat_axiom_inventory --require-axiom-free nat` enumerates the built Nat environment and exits non-zero unless it admits no Axiom, Opaque or Quotient declaration (measured: axiom=0 opaque=0 quotient=0). A theorem cannot depend on a trusted declaration the environment does not contain, so an empty trusted surface bounds every individual theorem's footprint by [].",
      "supports": "axiom_footprint: [] -- the Nat prelude's trusted surface is empty"
    }
  ],
  "provenance": {
    "date": "2026-08-18",
    "established_by": "axeyum-lean-kernel build_nat_prelude, lane nat-cascade",
    "source": "statement-only extraction of `Nat.coprime_or_dvd_of_prime` from Mathlib v4.30.0; no proof value was exposed",
    "prior_art": [
      {
        "who": "the Mathlib contributors",
        "what": "the theorem declaration `Nat.coprime_or_dvd_of_prime`",
        "where": "mathlib4 commit c5ea00351c28e24afc9f0f84379aa41082b1188f (v4.30.0)",
        "year": 2026,
        "attribution": "the proposition was read from the pinned statement-only inventory; the proof term and tactic trace were not consulted"
      }
    ]
  },
  "proof_route": "kernel-lean",
  "notes": "Proved in Axeyum via `declare_coprime_or_dvd_of_prime` (`nat_prelude/primes.rs`), decided constructively by a `Bool.rec` case split on `beq (gcd p i) one` (mirrors `totient.rs`'s private `bool_true_or_false` helper, duplicated locally in `primes.rs` since it is `fn`-private to its own module) combined with `prime_dvd_iff_not_coprime`. formal.statement is Lean surface syntax accepted as a proposition by the proof-free generated axiom module; it is not kernel-core render_lean output. depends_on projects only direct upstream theorem uses whose endpoints both survived review. Those edges are curriculum and leakage metadata and grant no dispatch, proof, or admission authority."
}
