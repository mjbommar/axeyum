{
  "schema_version": 1,
  "id": "F:algs-orderedring-zero-add",
  "title": "The setoid ordered ring's additive unit law is right-handed, so the left one is a theorem",
  "statement": "Let R be an AlgS.OrderedRing and a an element of R. Then 0 + a is equivalent to a. The record carries addZero (a + 0 ~ a) as a field and not this; it follows from addComm against addZero in two steps. It is needed once per summand by ADR-1631's Bernoulli computation, because every sum that reduces out of AlgS.OrderedRing.sumRange begins with a literal zero on the LEFT.",
  "formal": {
    "language": "lean4-surface",
    "statement": "theorem AlgS.OrderedRing.zero_add : forall (R : AlgS.OrderedRing) (a : R.carrier), R.equiv (R.add R.zero a) a",
    "fragment": "Prop",
    "kernel_theorem": "AlgS.OrderedRing.zero_add"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "proof_route": "kernel-lean",
  "depends_on": [],
  "axiom_footprint": [],
  "evidence": [
    {
      "id": "kernel-axiom-free",
      "kind": "kernel-term",
      "supports": "The declaration is admitted by the trusted kernel gate with an empty Kernel::axiom_footprint, together with every other declaration of ADR-1631's generic Bernoulli layer. The test asserts Environment::contains FIRST, because an absent name has an empty footprint too, and it pins its own name-list length, so a declaration silently dropped from the layer fails it.",
      "check_status": "checked",
      "checkers": [
        "producing-build (Kernel::add_declaration)",
        "binomial_s_declarations_are_axiom_free"
      ],
      "checker_command": "cargo test --release -p axeyum-lean-kernel --lib rat_prelude::binomial_s::binomial_s_tests::binomial_s_declarations_are_axiom_free -- --exact 2>&1 | grep -c '^test result: ok. 1 passed'",
      "checker_seconds": 60,
      "kernel_declaration": "AlgS.OrderedRing.zero_add",
      "notes": "grep -c, not grep -q: -q would SIGPIPE the producer under pipefail, and -c exits 1 printing 0 when the line is absent, so a suite that compiled to zero tests and exited 0 is caught rather than counted as a pass. --release is mandatory: the rational prelude SIGABRTs on the default debug thread stack."
    }
  ],
  "provenance": {
    "date": "2026-09-05",
    "established_by": "lane binomial-hoeffding (roadmap W3-12, ADR-1631)"
  },
  "notes": "Elementary, and recorded because it is a missing FIELD of a record whose field set was drawn from one development's needs -- the same lesson ADR-1616 recorded when it had to rebuild mul_le_mul_of_nonneg_right from mul_nonneg."
}
