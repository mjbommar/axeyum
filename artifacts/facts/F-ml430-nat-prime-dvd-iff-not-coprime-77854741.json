{
  "schema_version": 1,
  "id": "F:ml430-nat-prime-dvd-iff-not-coprime-77854741",
  "title": "Mathlib v4.30 source proposition Nat.Prime.dvd_iff_not_coprime",
  "statement": "The proposition declared as `Nat.Prime.dvd_iff_not_coprime` in the pinned Mathlib v4.30 source.",
  "formal": {
    "language": "lean4-surface",
    "statement": "∀ {p n : ℕ}, Nat.Prime p → (p ∣ n ↔ ¬p.Coprime n)",
    "fragment": "Nat",
    "kernel_theorem": "Nat.prime_dvd_iff_not_coprime",
    "kernel_statement": "theorem Nat.prime_dvd_iff_not_coprime : ((x0 : AxNat) -> ((x1 : AxNat) -> ((x2 : And (AxNat.le (AxNat.succ (AxNat.succ AxNat.zero)) x0) (((x2 : AxNat) -> ((x3 : AxNat.dvd x2 x0) -> Or (Eq.{1} AxNat x2 (AxNat.succ AxNat.zero)) (Eq.{1} AxNat x2 x0))))) -> Iff (AxNat.dvd x0 x1) (Not (Eq.{1} AxNat (AxNat.gcd x0 x1) (AxNat.succ AxNat.zero))))))"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "proof_route": "kernel-lean",
  "depends_on": [
    "F:nat-dvd-gcd",
    "F:nat-dvd-refl",
    "F:nat-gcd-dvd-left",
    "F:nat-gcd-dvd-right",
    "F:nat-le-of-dvd",
    "F:nat-le-of-succ-le-succ",
    "F:nat-le-refl",
    "F:nat-le-trans",
    "F:nat-not-succ-le-zero",
    "F:ml430-nat-le-of-succ-le-succ-a180a72c",
    "F:ml430-nat-le-refl-fd7d9e15",
    "F:ml430-nat-dvd-gcd-e5184fc5"
  ],
  "axiom_footprint": [],
  "evidence": [
    {
      "id": "kernel-Nat.prime_dvd_iff_not_coprime",
      "kind": "kernel-term",
      "supports": "for prime p (2 <= p and every divisor of p is 1 or p), p divides n iff gcd p n != 1",
      "check_status": "checked",
      "checker_command": "test \"$(cargo run -q -p axeyum-lean-kernel --example nat_theorem_inventory -- prime_dvd_iff_not_coprime 2>/dev/null | grep -Ec '^Nat\\.prime_dvd_iff_not_coprime[[:space:]]')\" -ge 1",
      "notes": "`build_nat_prelude` admits this theorem through the trusted `Kernel::add_declaration` gate. Forward: `dvd p n` plus `dvd p p` (`dvd_refl`) give `dvd p (gcd p n)` (`dvd_gcd`); if `gcd p n = 1` that transports to `dvd p 1`, forcing `p <= 1` (`le_of_dvd`) against primality's `2 <= p` conjunct -- contradiction via `le_trans`/`le_of_succ_le_succ`/`not_succ_le_zero`, the same numeral-contradiction shape `coprime_of_lt_prime` already uses, so `gcd p n != 1`. Reverse: `gcd p n` divides `p` (`gcd_dvd_left`), so primality's divisor clause forces `gcd p n = 1 or gcd p n = p`; the hypothesis rules out `= 1`, leaving `gcd p n = p`, and `dvd p n` follows from `gcd_dvd_right` transported along that equation. `Nat.Prime` has no separate name in this kernel; it is spelled inline as the same `2 <= p and every divisor is 1 or p` clause `coprime_of_lt_prime` already uses. `nat_theorem_inventory` exits non-zero for a name that does not exist, and the `grep -c` requires the admitted declaration to be printed."
    },
    {
      "id": "footprint-Nat.prime_dvd_iff_not_coprime",
      "kind": "exhaustive-enumeration",
      "supports": "axiom_footprint: [] -- the Nat prelude's trusted surface is empty",
      "check_status": "checked",
      "checkers": [
        "Kernel::axiom_footprint",
        "environment-wide trusted-surface enumeration"
      ],
      "checker_command": "cargo run -q -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat",
      "notes": "`nat_axiom_inventory --require-axiom-free nat` enumerates the built Nat environment and exits non-zero unless it admits no Axiom, Opaque or Quotient declaration. A theorem cannot depend on a trusted declaration the environment does not contain, so an empty trusted surface bounds every individual theorem's footprint by []."
    }
  ],
  "concept_refs": [
    {
      "graph": "math-education",
      "ref": "C:prime-number",
      "relation": "about",
      "resolved": false
    },
    {
      "graph": "math-education",
      "ref": "C:greatest-common-divisor",
      "relation": "about",
      "resolved": false
    }
  ],
  "provenance": {
    "date": "2026-08-28",
    "established_by": "axeyum-lean-kernel build_nat_prelude, lane coprime-backlog",
    "source": "statement-only extraction of `Nat.Prime.dvd_iff_not_coprime` from Mathlib v4.30.0 (see prior_art), independently proved here as `Nat.prime_dvd_iff_not_coprime` and admitted through `Kernel::add_declaration`",
    "prior_art": [
      {
        "who": "the Mathlib contributors",
        "what": "the theorem declaration `Nat.Prime.dvd_iff_not_coprime`",
        "where": "mathlib4 commit c5ea00351c28e24afc9f0f84379aa41082b1188f (v4.30.0)",
        "year": 2026,
        "attribution": "the proposition was read from the pinned statement-only inventory; the proof term and tactic trace were not consulted, and this project's proof is independently constructed"
      }
    ]
  },
  "notes": "`Nat.Prime` and `Nat.Coprime` have no separate names in this kernel's Nat prelude (matching `Nat.coprime_of_lt_prime`'s established convention); primality is spelled inline as `2 <= p and every divisor of p is 1 or p` and coprimality as `gcd _ _ = 1`, so `formal.statement` is the pinned Mathlib v4.30 proposition this fact mirrors; `formal.kernel_statement` is the actual kernel-core `render_lean` output for `Nat.prime_dvd_iff_not_coprime`, which spells the same proposition in the prelude's own vocabulary. `depends_on` is `theorem_dependency_inventory`'s reported direct theorem dependencies, restricted to the ones that already carry a fact ledger entry (all nine do here)."
}
