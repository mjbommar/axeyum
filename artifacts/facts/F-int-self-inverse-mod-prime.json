{
  "schema_version": 1,
  "id": "F:int-self-inverse-mod-prime",
  "title": "[generated] kernel theorem Int.self_inverse_mod_prime (integer prelude, axiom-free, prose not curated)",
  "statement": "MECHANICALLY GENERATED, UNREVIEWED PROSE -- this sentence deliberately makes NO mathematical characterisation of the theorem. What is asserted, and all that is asserted, is this: the kernel declaration `Int.self_inverse_mod_prime` is a `Declaration::Theorem` admitted into the environment by `build_int_prelude` through the trusted `Kernel::add_declaration` gate, which re-derives its type from its proof term; its type is recorded verbatim in `formal.statement`; and its axiom footprint, as computed by `Kernel::axiom_footprint`, is empty. The authoritative content of this fact is `formal.statement`. A human-readable characterisation of what Int.self_inverse_mod_prime SAYS has not been supplied, because a generator cannot supply one honestly -- see `notes`.",
  "formal": {
    "language": "lean4",
    "statement": "theorem Int.self_inverse_mod_prime : ((x0 : Int) -> ((x1 : Int) -> ((x2 : And (AxNat.le (AxNat.succ (AxNat.succ AxNat.zero)) (Int.natAbs x0)) (((x2 : AxNat) -> ((x3 : AxNat.dvd x2 (Int.natAbs x0)) -> Or (Eq.{1} AxNat x2 (AxNat.succ AxNat.zero)) (Eq.{1} AxNat x2 (Int.natAbs x0)))))) -> ((x3 : Int.lt Int.zero x0) -> ((x4 : Int.le Int.one x1) -> ((x5 : Int.le x1 (Int.sub x0 Int.one)) -> ((x6 : Int.ModEq x0 (Int.mul x1 x1) Int.one) -> Or (Int.ModEq x0 x1 Int.one) (Int.ModEq x0 x1 (Int.sub x0 Int.one)))))))))",
    "fragment": "Int",
    "free_symbols": [
      "x0",
      "x1",
      "x2",
      "x3",
      "x4",
      "x5",
      "x6"
    ],
    "kernel_theorem": "Int.self_inverse_mod_prime"
  },
  "epistemic_status": "proved",
  "proof_route": "kernel-lean",
  "depends_on": [
    "F:int-add-assoc",
    "F:int-add-comm",
    "F:int-add-neg",
    "F:int-add-zero",
    "F:int-dvd-refl",
    "F:int-euclid-lemma",
    "F:int-left-distrib",
    "F:int-modeq-iff-dvd",
    "F:int-modeq-symm",
    "F:int-modeq-trans",
    "F:int-mul-comm",
    "F:int-mul-one",
    "F:int-mul-sub",
    "F:int-neg-one-mul",
    "F:logic-iff-mp",
    "F:logic-iff-mpr",
    "F:int-mul-neg",
    "F:ml430-int-add-mul-66aa025b"
  ],
  "axiom_footprint": [],
  "evidence": [
    {
      "id": "kernel-Int.self_inverse_mod_prime",
      "kind": "kernel-term",
      "supports": "Int.self_inverse_mod_prime is admitted by the trusted kernel gate with the type recorded in formal.statement.",
      "check_status": "checked",
      "checkers": [
        "producing-build (Kernel::add_declaration)",
        "theorem_dependency_inventory re-list"
      ],
      "checker_command": "cargo run -q --release -p axeyum-lean-kernel --example theorem_dependency_inventory -- Int.self_inverse_mod_prime 2>/dev/null | grep -cE '^Int\\.self_inverse_mod_prime[[:space:]]'",
      "checker_seconds": 10,
      "kernel_declaration": "Int.self_inverse_mod_prime",
      "notes": "Two independent failure modes, so the exit status depends on the finding rather than on the run completing: theorem_dependency_inventory exits non-zero when a NAMED filter matches nothing, and grep -c exits 1 printing 0 when the anchored line is absent. Anchored with [[:space:]], never \\t -- in a scripted (GNU) grep \\t is a literal t. grep -c rather than grep -q, which would SIGPIPE the producer under pipefail. --release is MANDATORY: this tool builds creal/complex/cpoint, which overflow the default debug thread stack."
    },
    {
      "id": "footprint-Int.self_inverse_mod_prime",
      "kind": "exhaustive-enumeration",
      "supports": "axiom_footprint: [] -- the integer prelude's trusted surface is empty, which bounds Int.self_inverse_mod_prime.",
      "check_status": "checked",
      "checkers": [
        "Kernel::axiom_footprint",
        "environment-wide trusted-surface enumeration"
      ],
      "checker_command": "cargo run -q --release -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free integer",
      "checker_seconds": 12,
      "notes": "--require-axiom-free exits non-zero when the named prelude's trusted surface (Axiom + Opaque + Quotient) is not empty, and errors rather than silently passing for a prelude the run never built. A declaration cannot depend on a trusted declaration the environment does not contain, so an empty integer surface bounds every declaration in it, including Int.self_inverse_mod_prime. This is a whole-prelude bound, not a per-declaration measurement; the per-declaration figure is the footprint column of kernel_declaration_projection, measured 0 for this row."
    }
  ],
  "provenance": {
    "date": "2026-08-27",
    "curation": "generated-unreviewed",
    "generated_by": "scripts/gen-kernel-facts.py",
    "established_by": "axeyum-lean-kernel build_int_prelude (crates/axeyum-lean-kernel/src/int_prelude/)",
    "source": "Derived mechanically from the unfiltered emit of `cargo run -q --release -p axeyum-lean-kernel --example kernel_declaration_projection`, which prints one TSV row per declaration whose fields are (prelude, kind, display name, axiom-footprint size, direct type declarations, direct declarations, direct theorems, Kernel::render_lean(declaration.ty())). formal.statement is that last field verbatim; depends_on is the direct-theorem column intersected with this ledger's registered facts; axiom_footprint is the footprint-size column, cross-checked by the whole-prelude nat_axiom_inventory run recorded in the second evidence row. No field was hand-transcribed and no prose was authored."
  },
  "notes": "NO CURATED COMMENTARY EXISTS FOR THIS FACT. Its absence means nobody has looked, NOT that there is nothing to say. A hand-written fact in this ledger typically records what a generator cannot invent -- that a bound is loose and does not pin a sign, that the global version of a statement is false for an arbitrary witness, that a hypothesis is domain-restricted, which of several attempted slices finally closed a gap. None of that has been assessed here. `provenance.curation` is `generated-unreviewed`; a lane that enriches this fact must flip it to `curated`, which `gen-kernel-facts.py --audit` enforces by re-deriving this prose and requiring a byte-identical match while the marker says generated. `external_status` is deliberately ABSENT rather than guessed: whether mathematics-at-large knows this statement is a judgement about the literature, and the schema reads an absent `external_status` as 'nobody has looked', which is exactly the case. Source: crates/axeyum-lean-kernel/src/int_prelude/."
}
