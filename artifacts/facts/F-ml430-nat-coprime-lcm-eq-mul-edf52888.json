{
  "schema_version": 1,
  "id": "F:ml430-nat-coprime-lcm-eq-mul-edf52888",
  "title": "Mathlib v4.30 source proposition Nat.Coprime.lcm_eq_mul",
  "statement": "The proposition declared as `Nat.Coprime.lcm_eq_mul` in the pinned Mathlib v4.30 source.",
  "formal": {
    "language": "lean4-surface",
    "statement": "∀ {m n : ℕ}, m.Coprime n → m.lcm n = m * n",
    "fragment": "Nat",
    "kernel_theorem": "Nat.coprime_lcm_eq_mul"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "proof_route": "kernel-lean",
  "axiom_footprint": [],
  "depends_on": ["F:ml430-nat-gcd-mul-lcm-b7217ace", "F:nat-one-mul"],
  "evidence": [
    {
      "check_status": "checked",
      "checker_command": "test \"$(cargo run -q --release -p axeyum-lean-kernel --example nat_theorem_inventory -- coprime_lcm_eq_mul 2>/dev/null | grep -Ec '^Nat\\.coprime_lcm_eq_mul[[:space:]]')\" -ge 1",
      "id": "kernel-Nat.coprime_lcm_eq_mul",
      "kind": "kernel-term",
      "notes": "This mirror was ALREADY PROVED in this ledger under a different name before the ml430 extraction landed: `Nat.coprime_lcm_eq_mul` (nat_prelude/lcm.rs, `declare_coprime_lcm_eq_mul`) states `gcd a b = 1 -> lcm a b = a * b`, which IS Mathlib's `Nat.Coprime.lcm_eq_mul` -- this codebase never gives `Coprime` its own predicate, spelling it `gcd _ _ = 1` directly everywhere (see nat_prelude/coprime_lemmas.rs's module doc for the same convention). `nat_theorem_inventory`'s rendered type for `Nat.coprime_lcm_eq_mul` is `((x0 : AxNat) -> ((x1 : AxNat) -> ((x2 : Eq.{1} AxNat (AxNat.gcd x0 x1) (AxNat.succ AxNat.zero)) -> Eq.{1} AxNat (AxNat.lcm x0 x1) (AxNat.mul x0 x1))))`, matching this fact's `formal.statement` (`m.Coprime n -> m.lcm n = m*n`) verbatim once `Coprime` is unfolded to `gcd = 1`. No new Rust was written for this fact; `draw11-theorems-b` lane confirmed the existing declaration's rendered type and wrote this evidence. `nat_theorem_inventory` exits non-zero for a name that does not exist, and the `grep -c` count (tested `-ge 1`, not piped into `grep -q`) requires the admitted declaration to actually be printed.",
      "supports": "∀ {m n : ℕ}, m.Coprime n → m.lcm n = m * n"
    },
    {
      "check_status": "checked",
      "checker_command": "cargo run -q --release -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat",
      "checkers": [
        "Kernel::axiom_footprint",
        "environment-wide trusted-surface enumeration"
      ],
      "kind": "exhaustive-enumeration",
      "notes": "`nat_axiom_inventory --require-axiom-free nat` enumerates the built Nat environment and exits non-zero unless it admits no Axiom, Opaque or Quotient declaration (measured 2026-08-31: axiom=0 opaque=0 quotient=0). A theorem cannot depend on a trusted declaration the environment does not contain, so an empty trusted surface bounds `Nat.coprime_lcm_eq_mul`'s footprint by [].",
      "supports": "axiom_footprint: [] -- the Nat prelude's trusted surface is empty",
      "id": "footprint-Nat.coprime_lcm_eq_mul"
    }
  ],
  "provenance": {
    "date": "2026-08-29",
    "established_by": "not established in this ledger",
    "source": "statement-only extraction of `Nat.Coprime.lcm_eq_mul` from Mathlib v4.30.0; no proof value was exposed",
    "prior_art": [
      {
        "who": "the Mathlib contributors",
        "what": "the theorem declaration `Nat.Coprime.lcm_eq_mul`",
        "where": "mathlib4 commit c5ea00351c28e24afc9f0f84379aa41082b1188f (v4.30.0)",
        "year": 2026,
        "attribution": "the proposition was read from the pinned statement-only inventory; the proof term and tactic trace were not consulted"
      }
    ]
  },
  "notes": "Closed 2026-08-31 by lane draw11-theorems-b: `formal.statement` is a byte-identical quotation of the pinned extractor's pretty-printed Mathlib type, and Axeyum's own construction is `Nat.coprime_lcm_eq_mul` (nat_prelude/lcm.rs), already proved before this ml430 row was ever preregistered -- this codebase spells `Coprime` as `gcd _ _ = 1` directly rather than as its own predicate, so the two are the same proposition once that spelling is unfolded. No new Rust was needed; this fact's status flip is the whole task. Preregistered in artifacts/autogenesis/nursery-v2-extension.json, which carries the partition; that manifest, not this file, is the split authority."
}
