{
  "schema_version": 1,
  "id": "F:ml430-nat-clog-pos-00852cb8",
  "title": "Mathlib v4.30 source proposition Nat.clog_pos",
  "statement": "The proposition declared as `Nat.clog_pos` in the pinned Mathlib v4.30 source.",
  "formal": {
    "language": "lean4-surface",
    "statement": "∀ {b n : ℕ}, 1 < b → 1 < n → 0 < Nat.clog b n",
    "fragment": "Nat",
    "kernel_theorem": "Nat.clog_pos",
    "kernel_statement": "theorem Nat.clog_pos : (((x0 : AxNat) -> ((x1 : AxNat) -> ((x2 : AxNat.lt (AxNat.succ AxNat.zero) x0) -> ((x3 : AxNat.lt (AxNat.succ AxNat.zero) x1) -> AxNat.lt AxNat.zero (AxNat.clog x0 x1))))))"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "depends_on": ["F:ml430-nat-ble-eq-true-of-le-5ce4ac2e", "F:nat-not-succ-le-zero", "F:nat-zero-lt-succ"],
  "evidence": [
    {
      "id": "kernel-Nat.clog_pos",
      "kind": "kernel-term",
      "supports": "`Nat.clog_pos` is admitted as a Theorem with EXACTLY the stated shape.",
      "check_status": "checked",
      "checkers": [
        "nat_theorem_inventory (renders the kernel type of every Theorem; matched on the exact name/type columns)",
        "axeyum-lean-kernel::Kernel::add_declaration (re-typechecks the proof term against the stated type on every build)"
      ],
      "checker_command": "test \"$(cargo run -q --release -p axeyum-lean-kernel --example nat_theorem_inventory 2>/dev/null | awk -F'\\t' '$1 == \"Nat.clog_pos\" && $3 == \"((x0 : AxNat) -> ((x1 : AxNat) -> ((x2 : AxNat.lt (AxNat.succ AxNat.zero) x0) -> ((x3 : AxNat.lt (AxNat.succ AxNat.zero) x1) -> AxNat.lt AxNat.zero (AxNat.clog x0 x1)))))\"' | wc -l)\" -ge 1",
      "kernel_declaration": "Nat.clog_pos"
    },
    {
      "id": "footprint-Nat.clog_pos",
      "kind": "exhaustive-enumeration",
      "supports": "axiom_footprint: [] for `Nat.clog_pos`. The `nat` prelude's trusted surface is empty (no Axiom, no Opaque, no Quotient), which bounds every declaration in it, including `Nat.clog_pos`.",
      "check_status": "checked",
      "checkers": [
        "Kernel::axiom_footprint",
        "nat_axiom_inventory --require-axiom-free (environment-wide trusted-surface enumeration, independent of the per-theorem footprint route)"
      ],
      "checker_command": "cargo run -q --release -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat"
    }
  ],
  "provenance": {
    "date": "2026-08-30",
    "established_by": "lane nat-log-mirrors (2026-08-30), new kernel construction",
    "source": "statement-only extraction of `Nat.clog_pos` from Mathlib v4.30.0; no proof value was exposed",
    "prior_art": [
      {
        "who": "the Mathlib contributors",
        "what": "the theorem declaration `Nat.clog_pos`",
        "where": "mathlib4 commit c5ea00351c28e24afc9f0f84379aa41082b1188f (v4.30.0)",
        "year": 2026,
        "attribution": "the proposition was read from the pinned statement-only inventory; the proof term and tactic trace were not consulted"
      }
    ]
  },
  "notes": "CLOSED 2026-08-30 by a native, axiom-free kernel construction (Case-splits on the value `n` (`clog`'s fuel and value are diagonal, so this one split gives both the succ-shaped fuel the unfolding needs and the succ-shaped value the guard needs); `n = 0` is refuted by `Lt 1 0` (`Le 2 0`) via `not_succ_le_zero`. At `n = succ n'` both guard cuts are already known true from the hypotheses, so two direct `bool_transport`s at the known evidence (no case split needed) reduce `clog b n` to a `succ`, positive by `zero_lt_succ`. `crates/axeyum-lean-kernel/src/nat_prelude/log_clog_order.rs`.) This is a genuine library-composition increment, not a found pre-existing declaration. The external Mathlib theorem remains prior art; its proof term and tactic trace were not consulted. formal.statement remains the pinned Lean surface spelling, definitionally equivalent to the explicit core rendering checked by the evidence command.",
  "proof_route": "kernel-lean",
  "axiom_footprint": []
}
