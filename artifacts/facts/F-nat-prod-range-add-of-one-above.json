{
  "schema_version": 1,
  "id": "F:nat-prod-range-add-of-one-above",
  "title": "Extending a product past its support changes nothing",
  "statement": "If a function is constantly 1 at and above a point k, then its product over [0, k+j) equals its product over [0, k), for every j. The induction goes on j rather than on the bound because Nat.add recurses on its right argument, so add k (succ m) is succ-shaped for symbolic k.",
  "formal": {
    "language": "lean4",
    "statement": "theorem Nat.prodRange_add_of_one_above : ((x0 : ((x0 : AxNat) -> AxNat)) -> ((x1 : AxNat) -> ((x2 : AxNat) -> ((x3 : ((x3 : AxNat) -> ((x4 : AxNat.le x1 x3) -> Eq.{1} AxNat (x0 x3) (AxNat.succ AxNat.zero)))) -> Eq.{1} AxNat (AxNat.prodRange x0 (AxNat.add x1 x2)) (AxNat.prodRange x0 x1)))))",
    "fragment": "Nat",
    "kernel_theorem": "Nat.prodRange_add_of_one_above"
  },
  "epistemic_status": "proved",
  "proof_route": "kernel-lean",
  "external_status": "proved",
  "depends_on": ["F:nat-le-add-right", "F:nat-mul-one"],
  "axiom_footprint": [],
  "evidence": [
    {
      "id": "nat-prod-range-add-of-one-above-1",
      "kind": "kernel-term",
      "supports": "Nat.prodRange_add_of_one_above is admitted by the trusted kernel gate with the type recorded in formal.statement.",
      "check_status": "checked",
      "checker_command": "out=$(cargo run -q -p axeyum-lean-kernel --example nat_theorem_inventory -- prodRange_add_of_one_above 2>&1) && test \"$(printf \"%s\\n\" \"$out\" | grep -cE '^Nat\\.prodRange_add_of_one_above.7.\\(\\(x0 : \\(\\(x0 : AxNat\\) -> AxNat\\)\\) -> \\(\\(x1 : AxNat\\) -> \\(\\(x2 : AxNat\\) -> \\(\\(x3 : \\(\\(x3 : AxNat\\) -> \\(\\(x4 : AxNat\\.le x1 x3\\) -> Eq\\.\\{1\\} AxNat \\(x0 x3\\) \\(AxNat\\.succ AxNat\\.zero\\)\\)\\)\\) -> Eq\\.\\{1\\} AxNat \\(AxNat\\.prodRange x0 \\(AxNat\\.add x1 x2\\)\\) \\(AxNat\\.prodRange x0 x1\\)\\)\\)\\)\\)$')\" = 1",
      "notes": "`build_nat_prelude` admits this theorem only through the trusted kernel gate, so a successful build IS the type-check. The pattern pins the ARITY and the FULL rendered type, not the theorem's presence: `scripts/new-fact.py` rejected a name-only pattern as population-only, because it catches the row disappearing but not the statement changing inside a row that is still there.",
      "kernel_declaration": "Nat.prodRange_add_of_one_above"
    },
    {
      "id": "footprint-nat-prod-range-add-of-one-above",
      "kind": "instance-pin",
      "supports": "axiom_footprint: [] -- the Nat environment admits no trusted declaration",
      "check_status": "checked",
      "checker_command": "cargo run -q -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat",
      "notes": "Reports `nat: axiom=0 opaque=0 quotient=0 total_trusted=0` over the FULL trusted surface (Axiom, Opaque and Quotient), not `Declaration::Axiom` alone, and exits non-zero unless the surface is empty. The enumeration is per-environment rather than per-theorem; it bounds this theorem's footprint because a proof cannot depend on a trusted declaration the environment does not contain. Read directly from `Kernel::axiom_footprint` via `theorem_axiom_footprint`, this theorem's row prints footprint size 0 and an empty axiom column, and all 904 `nat` rows do. (Count the tool's OWN summary line, not `grep -c '^nat'` -- the summary starts with `nat:` too, so the naive count is 905.)"
    }
  ],
  "concept_refs": [],
  "provenance": {
    "date": "2026-09-02",
    "established_by": "axeyum-lean-kernel build_nat_prelude",
    "source": "theorem name and canonical type read directly via nat_theorem_inventory, which prints render_lean of the admitted type; declared by `nat_prelude/multiset_prod.rs` (lane nat-factorization)."
  },
  "notes": "Stated with the bound as `add k j` rather than as a hypothesis `Le k n`, because `Nat.add` recurses on its RIGHT argument: `add k (succ m)` is `succ`-shaped for symbolic `k`, so the induction goes on `j` and needs no `le_dest`. A caller holding `Le k n` instead has to produce the `j` itself. The hypothesis is `Le k i`, i.e. AT and above `k` -- a function that is 1 only STRICTLY above `k` does not satisfy it, and the statement would be false for one."
}
