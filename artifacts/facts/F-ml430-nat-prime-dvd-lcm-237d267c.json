{
  "schema_version": 1,
  "id": "F:ml430-nat-prime-dvd-lcm-237d267c",
  "title": "Mathlib v4.30 source proposition Nat.Prime.dvd_lcm",
  "statement": "The proposition declared as `Nat.Prime.dvd_lcm` in the pinned Mathlib v4.30 source.",
  "formal": {
    "language": "lean4-surface",
    "statement": "∀ {p a b : ℕ}, Nat.Prime p → (p ∣ a.lcm b ↔ p ∣ a ∨ p ∣ b)",
    "fragment": "Nat",
    "kernel_theorem": "Nat.prime_dvd_lcm_iff"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "depends_on": [
    "F:nat-euclid-lemma",
    "F:nat-gcd-mul-lcm",
    "F:nat-dvd-lcm-left",
    "F:nat-dvd-lcm-right",
    "F:ml430-nat-dvd-lcm-left-c83bcebc",
    "F:ml430-nat-dvd-lcm-right-18ab8e2f",
    "F:ml430-nat-dvd-mul-left-of-dvd-200e20a4",
    "F:ml430-nat-gcd-mul-lcm-b7217ace",
    "F:ml430-nat-prime-dvd-or-dvd-4ae88221",
    "F:nat-dvd-trans"
  ],
  "evidence": [
    {
      "check_status": "checked",
      "checker_command": "test \"$(cargo run -q -p axeyum-lean-kernel --example nat_theorem_inventory -- prime_dvd_lcm_iff 2>/dev/null | grep -Ec '^Nat\\.prime_dvd_lcm_iff[[:space:]]')\" -ge 1",
      "id": "kernel-Nat.prime_dvd_lcm_iff",
      "kind": "kernel-term",
      "notes": "`declare_prime_dvd_lcm_iff` (`nat_prelude/prime_dvd_factorial_lcm.rs`) admits `Nat.prime_dvd_lcm_iff` through the trusted `Kernel::add_declaration` gate. Pure algebra, no induction: `mp` uses `dvd_mul_left_of_dvd` to extend `p | lcm a b` to `p | (gcd a b * lcm a b)`, `gcd_mul_lcm` (`gcd a b * lcm a b = a * b`, unconditional) identifies that product with `a*b`, and `euclid_lemma` splits `p | a*b` into `p | a \\/ p | b`. `mpr` is `dvd_trans` through `dvd_lcm_left`/`dvd_lcm_right`, case-split over the `Or` -- primality is unused on this side, only carried because the statement's hypothesis wraps the whole `Iff`.",
      "supports": "∀ {p a b : ℕ}, Nat.Prime p → (p ∣ a.lcm b ↔ p ∣ a ∨ p ∣ b)"
    },
    {
      "check_status": "checked",
      "checker_command": "cargo run -q -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat",
      "checkers": [
        "Kernel::axiom_footprint",
        "environment-wide trusted-surface enumeration"
      ],
      "id": "footprint-Nat.prime_dvd_lcm_iff",
      "kind": "exhaustive-enumeration",
      "notes": "`nat_axiom_inventory --require-axiom-free nat` enumerates the built Nat environment and exits non-zero unless it admits no Axiom, Opaque or Quotient declaration (measured: axiom=0 opaque=0 quotient=0). A theorem cannot depend on a trusted declaration the environment does not contain, so an empty trusted surface bounds every individual theorem's footprint by [].",
      "supports": "axiom_footprint: [] -- the Nat prelude's trusted surface is empty"
    }
  ],
  "provenance": {
    "date": "2026-08-29",
    "established_by": "not established in this ledger",
    "source": "statement-only extraction of `Nat.Prime.dvd_lcm` from Mathlib v4.30.0; no proof value was exposed",
    "prior_art": [
      {
        "who": "the Mathlib contributors",
        "what": "the theorem declaration `Nat.Prime.dvd_lcm`",
        "where": "mathlib4 commit c5ea00351c28e24afc9f0f84379aa41082b1188f (v4.30.0)",
        "year": 2026,
        "attribution": "the proposition was read from the pinned statement-only inventory; the proof term and tactic trace were not consulted"
      }
    ]
  },
  "notes": "Proved in Axeyum via `declare_prime_dvd_lcm_iff` (`nat_prelude/prime_dvd_factorial_lcm.rs`). formal.statement is a BYTE-IDENTICAL quotation of the pinned extractor's pretty-printed type; the admitted kernel declaration's rendered type (`nat_theorem_inventory`) matches it verbatim, modulo `Coprime a b` being spelled `gcd a b = 1` (this prelude has no separate `Coprime` name) and primality being spelled inline rather than through a `Prime` predicate. Preregistered in artifacts/autogenesis/nursery-v2-extension.json, which carries the partition; that manifest, not this file, is the split authority. depends_on projects only direct upstream theorem uses whose endpoints both survived review; those edges are curriculum and leakage metadata and grant no dispatch, proof, or admission authority.",
  "axiom_footprint": [],
  "proof_route": "kernel-lean"
}
