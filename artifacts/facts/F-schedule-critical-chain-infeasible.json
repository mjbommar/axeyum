{
  "schema_version": 1,
  "id": "F:schedule-critical-chain-infeasible",
  "title": "A five-constraint critical chain against a delivery deadline, refuted in the Lean kernel",
  "statement": "There are no real start times s_t03, s_t06, s_t09, s_t12 with s_t03 >= 6, s_t06 >= s_t03 + 5, s_t09 >= s_t06 + 6, s_t12 >= s_t09 + 3 and s_t12 + 4 <= 20. In project terms: if the long-lead material for t03 lands on day 6 and the chain t03 -> t06 -> t09 -> t12 runs 5, 6 and 3 days with a 4-day final task, delivery cannot happen by day 20. These five constraints are the measured irreducible core of the 60-row schedule in artifacts/instances/infeasibility/schedule-deadline.smt2.",
  "formal": {
    "language": "smtlib2",
    "statement": "(assert (not (exists ((s_t03 Real) (s_t06 Real) (s_t09 Real) (s_t12 Real)) (and (>= s_t03 6.0) (>= s_t06 (+ s_t03 5.0)) (>= s_t09 (+ s_t06 6.0)) (>= s_t12 (+ s_t09 3.0)) (<= (+ s_t12 4.0) 20.0)))))",
    "fragment": "QF_LRA",
    "free_symbols": [
      "s_t03",
      "s_t06",
      "s_t09",
      "s_t12"
    ]
  },
  "epistemic_status": "proved",
  "proof_route": "kernel-lean",
  "axiom_footprint": [
    "False",
    "Eq.{u}",
    "Eq.refl.{u}",
    "Eq.rec.{u._1, u}",
    "Real",
    "Real.add",
    "Real.neg",
    "Real.zero",
    "Real.one",
    "Real.le",
    "Real.lt",
    "Real.le_refl",
    "Real.lt_irrefl",
    "Real.lt_of_lt_of_le",
    "Real.le_of_lt",
    "Real.add_le_add",
    "Real.add_comm",
    "Real.add_assoc",
    "Real.add_zero",
    "Real.add_neg",
    "Real.zero_lt_one",
    "axeyum.reconstruct.lra.x._0",
    "axeyum.reconstruct.lra.x._1",
    "axeyum.reconstruct.lra.x._3",
    "axeyum.reconstruct.lra.x._5",
    "axeyum.reconstruct.lra.hyp._2",
    "axeyum.reconstruct.lra.hyp._4",
    "axeyum.reconstruct.lra.hyp._6",
    "axeyum.reconstruct.lra.hyp._7",
    "axeyum.reconstruct.lra.hyp._8"
  ],
  "depends_on": [],
  "evidence": [
    {
      "id": "schedule-chain-kernel-term",
      "kind": "kernel-term",
      "supports": "The five constraints are jointly unsatisfiable, by a kernel-checked proof term of False.",
      "check_status": "checked",
      "checker_command": "cargo run --release -q -p axeyum-solver --features full --example infeasibility_farkas_lean -- artifacts/instances/infeasibility/schedule-deadline.smt2 --require-kernel",
      "artifact": "artifacts/instances/infeasibility/schedule-deadline.smt2",
      "checkers": [
        "axeyum-farkas-certificate-verify-from-scratch",
        "axeyum-lean-kernel-infer-plus-def-eq-False",
        "z3-4.13.3-independent-agreement"
      ],
      "notes": "Ran 2026-08-14. `lra_farkas_certificate` on the five core rows returns five atoms with multipliers ALL EQUAL TO 1 -- the textbook negative-cycle refutation, 6+5+6+3 = 20 against a ceiling of 16, excess 4 -- and `FarkasCertificate::verify()` re-derives it from scratch in exact rationals (nonnegative multipliers, at least one positive, every variable cancels, surviving constant relation unsatisfiable), sharing no code with the Fourier-Motzkin elimination that found it. `reconstruct_lra_proof` then builds a Lean kernel term whose `infer` succeeds and whose inferred type is `def_eq` to `False`. The example ASSERTS that the term rests on exactly 5 hypothesis axioms, one per core row, and exits 1 otherwise. `--require-kernel` makes the whole route a ratchet: if the reconstruction ever declines this query, the command fails rather than reporting a stop."
    },
    {
      "id": "schedule-chain-module-shape",
      "kind": "instance-pin",
      "supports": "axiom_footprint: the 30 axioms the kernel module actually rests on.",
      "check_status": "checked",
      "checker_command": "cargo run --release -q -p axeyum-solver --features full --example infeasibility_farkas_lean -- artifacts/instances/infeasibility/schedule-deadline.smt2 --require-kernel",
      "checkers": [
        "axeyum-lean-kernel-render-lean-module-declaration-count"
      ],
      "notes": "The rendered module declares 30 axioms: 21 ordered-field prelude (the `Real` carrier, its operations, and the order/additive laws), 4 variable axioms (one abstract `Real` per start time), and 5 hypothesis axioms (one per core row, in the canonical `le L zero` form). This route IS NOT AXIOM-FREE and cannot presently be: `crates/axeyum-lean-kernel/src/arith_prelude.rs` declares `Real` and every field law through `declare_axiom`, with no theorems and no inductives, so `[]` here would be a lie the validator would have accepted (it only rejects `[]` off the kernel-lean route). CONTRAST WITH THE FACADE: `prove_unsat_to_lean_module` on the SAME five rows routes to `ProofFragment::LraDpll` and emits a 21-line STRUCTURAL SHIM -- `axiom prop : P`, `axiom hyp : Not P`, `theorem _ : False := hyp prop` -- which kernel-checks, is sorry-free, and contains no arithmetic whatsoever. Calling that 'the Farkas proof reached the kernel' would be false, so the example prints and labels both. SIZE: the real term is 5,099,060 bytes. The prelude has no numerals, so the constant 20 reconstructs as a 20-fold `Real.add Real.one` chain and every cancellation is an explicit Eq-rewrite; a 5-row explanation becomes a 5 MB proof term."
    }
  ],
  "provenance": {
    "date": "2026-08-14",
    "established_by": "axeyum lane `infeasibility`, 2026-08-14: crates/axeyum-solver/examples/infeasibility_farkas_lean.rs",
    "source": "the measured irreducible core of an instance authored by this lane (scripts/gen-infeasibility-instances.py)"
  },
  "notes": "WHAT A `theorem _ : False` MEANS HERE, since it reads alarmingly out of context. The module declares the five constraints as hypothesis axioms and derives `False` FROM THEM; that is a refutation of their conjunction, i.e. exactly the proposition in `formal.statement`, and the kernel environment is discarded with them. `formal.language` is `smtlib2` and not `lean4` for this reason: the rendered `theorem infeasible : False` carries no content without its hypothesis axioms, so quoting it as the formal statement would be uninformative at best. SCOPE: this is the largest linear-arithmetic reconstruction this repository has run. Measured: all fourteen call sites of `reconstruct_lra_proof` outside this lane's example live in crates/axeyum-solver/src/reconstruct/tests.rs and pass an assertion slice of length 1 (a negative test), 2, or 3. This is 5 constraints over 4 variables with non-unit integer constants, and it went through `try_general_farkas` unchanged."
}
