{
  "schema_version": 1,
  "id": "F:ml430-nat-and-one-is-mod-d861e96b",
  "title": "Mathlib v4.30 source proposition Nat.and_one_is_mod",
  "statement": "The proposition declared as `Nat.and_one_is_mod` in the pinned Mathlib v4.30 source.",
  "formal": {
    "language": "lean4-surface",
    "statement": "∀ (x : ℕ), x &&& 1 = x % 2",
    "fragment": "Nat"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "depends_on": ["F:ml430-nat-and-comm-7525d05a", "F:nat-land-zero-left", "F:nat-mod-eq-self-of-lt", "F:nat-one-mul", "F:nat-zero-add", "F:nat-zero-mod"],
  "evidence": [
    {
      "id": "kernel-Nat.land_one_is_mod",
      "kind": "kernel-term",
      "kernel_declaration": "Nat.land_one_is_mod",
      "supports": "∀ (x : ℕ), x &&& 1 = x % 2",
      "check_status": "checked",
      "checker_command": "test \"$(cargo run -q --release -p axeyum-lean-kernel --example nat_theorem_inventory -- land_one_is_mod 2>/dev/null | grep -Ec '^Nat\\.land_one_is_mod[[:space:]]')\" -ge 1",
      "notes": "`build_nat_prelude` admits `Nat.land_one_is_mod` (`nat_prelude/land_low_bit.rs`) through the trusted `Kernel::add_declaration` gate. `nat_theorem_inventory`'s rendered type is `(x0 : AxNat) -> Eq AxNat (AxNat.land x0 (succ zero)) (AxNat.mod x0 (succ (succ zero)))`, matching this fact's `formal.statement` up to variable naming and numeral rendering. Anchored with `^Nat\\.land_one_is_mod[[:space:]]` so a sibling name cannot satisfy this row; `grep -Ec` consumes the pipe so the tested count decides the exit status. Run `--release` -- the debug build of this inventory example SIGABRTs on stack depth (unrelated to this proof). Mathlib's `&&&` for `Nat` is the SAME function as `Nat.land`, so reconciling this mirror against a NEW `Nat.land_one_is_mod` theorem is honest under the mirror-flip criterion in CLAUDE.md."
    },
    {
      "id": "compute-Nat.land_one_is_mod",
      "kind": "kernel-term",
      "kernel_declaration": "Nat.land_one_is_mod",
      "supports": "land(5, 1) = 1 (5 odd) and land(4, 1) = 0 (4 even) -- a DISCRIMINATING pair, since a wrong parity direction would fail at least one of the two.",
      "check_status": "checked",
      "checker_command": "cargo test -p axeyum-lean-kernel --lib nat_prelude::nat_prelude_tests::land_one_is_mod_applies_at_concrete_instances",
      "checkers": [
        "Kernel::infer",
        "Kernel::def_eq"
      ]
    },
    {
      "id": "footprint-Nat.land_one_is_mod",
      "kind": "exhaustive-enumeration",
      "supports": "axiom_footprint: [] -- the Nat prelude's trusted surface is empty",
      "check_status": "checked",
      "checker_command": "cargo run -q --release -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat",
      "checkers": [
        "Kernel::axiom_footprint",
        "environment-wide trusted-surface enumeration"
      ],
      "notes": "`nat_axiom_inventory --require-axiom-free nat` enumerates the built Nat environment and exits non-zero unless it admits no Axiom, Opaque or Quotient declaration (measured: axiom=0 opaque=0 quotient=0). A theorem cannot depend on a trusted declaration the environment does not contain, so an empty trusted surface bounds `Nat.land_one_is_mod`'s footprint by []."
    }
  ],
  "provenance": {
    "date": "2026-08-30",
    "established_by": "lane draw9-second-theorems",
    "source": "statement-only extraction of `Nat.and_one_is_mod` from Mathlib v4.30.0; no proof value was exposed",
    "prior_art": [
      {
        "who": "the Mathlib contributors",
        "what": "the theorem declaration `Nat.and_one_is_mod`",
        "where": "mathlib4 commit c5ea00351c28e24afc9f0f84379aa41082b1188f (v4.30.0)",
        "year": 2026,
        "attribution": "the proposition was read from the pinned statement-only inventory; the proof term and tactic trace were not consulted"
      }
    ]
  },
  "notes": "Proved in Axeyum by a NEW kernel theorem, `Nat.land_one_is_mod` (`nat_prelude/land_low_bit.rs`), needing no induction at all: `land_comm` first swaps the operand order so the fuel-supplying slot becomes the LITERAL `1` rather than the symbolic `x` (`land x 1 = land 1 x`), and then a single unfold of `landAux`'s succ-row lands its own recursive call at the LITERAL fuel `0` (the predecessor of `1`), which collapses by `refl` regardless of the other two arguments -- no fuel-sufficiency bookkeeping is needed because the fuel was never symbolic to begin with. What remained was two ordinary arithmetic simplifications (`Nat.zero_add`, `Nat.one_mul`) plus `Nat.mod_eq_self_of_lt` to evaluate `mod 1 2 = 1`. Mathlib's `&&&` for `Nat` is `Nat.land`, so reconciling this `and_one_is_mod` mirror to `Nat.land_one_is_mod` is honest under the mirror-flip criterion in CLAUDE.md.",
  "proof_route": "kernel-lean",
  "axiom_footprint": []
}
