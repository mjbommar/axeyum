{
  "id": "F:nat-exists-prime-gt",
  "statement": "For every natural number n there is a prime p with n < p. Equivalently, there are infinitely many primes.",
  "formal": {
    "language": "lean4",
    "statement": "theorem Nat.exists_prime_gt : ((n : AxNat) -> Exists.{1} AxNat (fun (p : AxNat) => And (AxNat.lt n p) (And (AxNat.le (AxNat.succ (AxNat.succ AxNat.zero)) p) ((d : AxNat) -> ((hd : AxNat.dvd d p) -> Or (Eq.{1} AxNat d (AxNat.succ AxNat.zero)) (Eq.{1} AxNat d p))))))",
    "fragment": "Nat",
    "kernel_theorem": "Nat.exists_prime_gt",
    "free_symbols": [
      "n",
      "p"
    ]
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "proof_route": "kernel-lean",
  "axiom_footprint": [],
  "depends_on": [
    "F:nat-dvd-add",
    "F:nat-dvd-add-right-cancel-of-pos",
    "F:nat-dvd-factorial-of-le",
    "F:nat-exists-prime-dvd",
    "F:nat-le-add-right",
    "F:nat-le-total",
    "F:nat-le-trans",
    "F:nat-lt-or-eq-of-le",
    "F:nat-not-dvd-one-of-two-le",
    "F:nat-one-le-factorial"
  ],
  "evidence": [
    {
      "id": "kernel-Nat.exists_prime_gt",
      "kind": "kernel-term",
      "supports": "For every natural number n there is a prime p with n < p.",
      "check_status": "checked",
      "checker_command": "test \"$(cargo run -q -p axeyum-lean-kernel --example nat_theorem_inventory -- exists_prime_gt 2>/dev/null | grep -xFc 'Nat.exists_prime_gt\t3\t((x0 : AxNat) -> Exists.{1} AxNat (fun (x1 : AxNat) => And (AxNat.lt x0 x1) (And (AxNat.le (AxNat.succ (AxNat.succ AxNat.zero)) x1) (((x2 : AxNat) -> ((x3 : AxNat.dvd x2 x1) -> Or (Eq.{1} AxNat x2 (AxNat.succ AxNat.zero)) (Eq.{1} AxNat x2 x1)))))))')\" -ge 1",
      "notes": "`build_nat_prelude` admits this through the trusted `Kernel::add_declaration` gate, which re-checks the proof term against the stated type. Euclid's argument, done over N with no subtraction: take m = n! + 1, which is >= 2 because 1 <= n! (`one_le_factorial`), and let q be a prime dividing it (`exists_prime_dvd`). If q <= n then `dvd_factorial_of_le` gives q | n!, and with q | n!+1 `dvd_add_right_cancel_of_pos` yields q | 1, refuted by `not_dvd_one_of_two_le`. So n <= q, and the n = q case falls to the same contradiction after transporting n <= n along the equality, leaving n < q. The command matches the FULL RENDERED TYPE (grep -qxF), not the theorem name: lane `nat-prime-divisor` measured that a name-only checker survives a weakened statement -- spelling a primality bound `1 <= p` instead of `2 <= p` still type-checks, still admits, and is satisfied by p = 1. The kernel cannot see a weaker statement; only the type can."
    },
    {
      "id": "footprint-Nat.exists_prime_gt",
      "kind": "exhaustive-enumeration",
      "supports": "axiom_footprint: [] -- the Nat prelude's trusted surface is empty",
      "check_status": "checked",
      "checker_command": "cargo run -q -p axeyum-lean-kernel --example nat_axiom_inventory -- --require-axiom-free nat",
      "notes": "`theorem_axiom_footprint` reports `nat Nat.exists_prime_gt 0` and `integer Nat.exists_prime_gt 0`. `nat_axiom_inventory` bounds it by enumerating the whole trusted surface: a theorem cannot depend on a trusted declaration the environment does not contain. Counting `Declaration::Axiom` alone would not suffice -- `Opaque` has no proof body and `Quotient` admits `Quot.sound` -- so all three kinds are covered."
    }
  ],
  "schema_version": 1,
  "title": "There is no largest prime",
  "concept_refs": [
    {
      "graph": "math-education",
      "ref": "C:infinitude-of-primes",
      "relation": "instance-of",
      "resolved": false
    },
    {
      "graph": "math-education",
      "ref": "C:prime-number",
      "relation": "about",
      "resolved": false
    }
  ],
  "provenance": {
    "date": "2026-08-14",
    "established_by": "not established in this ledger",
    "source": "proposition extracted from the S:number strand of the math-education concept graph; statement authored here, nothing copied",
    "prior_art": [
      {
        "who": "Euclid",
        "what": "Elements, Book IX, Proposition 20",
        "year": -300,
        "attribution": "standard textbook attribution; this lane did not consult the primary source"
      }
    ]
  },
  "notes": "`open` here means NOT ESTABLISHED IN THIS LEDGER; it is not mathematically open, Euclid settled it. It is recorded because it is a live target rather than a curiosity: the prelude already carries `Nat.not_dvd_one_add_mul_of_two_le`, which is exactly the step `no a >= 2 divides 1 + a*t` -- the contradiction at the end of Euclid's argument. 2026-08-17: BOTH missing ingredients have now landed and this fact is one slice from closing. `Nat.factorial` is declared (computation rules definitional, like `Nat.pow`) together with `Nat.dvd_factorial_of_le : (d n : AxNat) -> AxNat.le 1 d -> AxNat.le d n -> AxNat.dvd d (AxNat.factorial n)`, so the number that every positive `d <= n` divides exists in the kernel and `1 + n!` is a witness with no divisor in `[2, n]`. The second ingredient is `F:nat-exists-prime-dvd`: `Nat.exists_prime_dvd : (m : AxNat) -> AxNat.le 2 m -> Exists p, (2 <= p AND forall d, d | p -> d = 1 OR d = p) AND p | m`, admitted axiom-free. Note it did NOT go through `lt_well_founded` as this note previously predicted -- strong induction on `m` would have to DECIDE primality of `m`, a bounded universal quantification whose constructive decision is itself a bounded search, so the search (`Nat.least_divisor_search`) is done directly by ordinary `Nat.rec` on the bound and returns the LEAST divisor >= 2, which is prime for free. WHAT REMAINS is exactly two steps, both small: (1) `1 <= Nat.factorial n` -- factorial is positive, by induction with `one_le_mul` at the successor -- which is what makes `2 <= 1 + n!` and so lets `exists_prime_dvd` be applied to it at all; and (2) the assembly. The assembly: take `p` prime with `p | 1 + n!`; if `p <= n` then `dvd_factorial_of_le` gives `p | n!`, `add_comm` reshapes `1 + n!` to `n! + 1`, `dvd_add_right_cancel_of_pos` yields `p | 1`, and `not_dvd_one_of_two_le` refutes it; so `le_total` leaves `n <= p`, and `lt_or_eq_of_le` turns that into `n < p` once `n = p` is ruled out by the same contradiction. Every lemma named in that paragraph is already admitted axiom-free. formal.statement is written in the SAME core rendering the kernel emits for its own theorems, and uses ONLY symbols `build_nat_prelude` actually declares (checked against the 119-theorem inventory dumped on 2026-08-14), so it can be dispatched without inventing vocabulary. Primality is spelled out inline because the prelude has no `Prime` predicate. Binders carry readable names rather than the kernel's `x0, x1, ...`; that is a pretty-printing difference only, and any admitted proof would render with positional names."
}
