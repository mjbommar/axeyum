{
  "schema_version": 1,
  "id": "F:ml430-nat-bitwise-swap-7175e90e",
  "title": "Mathlib v4.30 source proposition Nat.bitwise_swap",
  "statement": "The proposition declared as `Nat.bitwise_swap` in the pinned Mathlib v4.30 source.",
  "formal": {
    "language": "lean4-surface",
    "statement": "∀ {f : Bool → Bool → Bool}, Nat.bitwise (Function.swap f) = Function.swap (Nat.bitwise f)",
    "fragment": "Nat"
  },
  "epistemic_status": "proved",
  "external_status": "proved",
  "proof_route": "kernel-lean",
  "axiom_footprint": [],
  "depends_on": [
    "F:ml430-nat-bitwise-bit-4c4b28a8"
  ],
  "evidence": [
    {
      "id": "reconciliation-Nat.bitwise_swap",
      "kind": "kernel-term",
      "kernel_declaration": "Nat.bitwise_swap",
      "supports": "∀ {f : Bool → Bool → Bool}, Nat.bitwise (Function.swap f) = Function.swap (Nat.bitwise f)",
      "check_status": "checked",
      "checkers": [
        "axeyum-lean-kernel",
        "axeyum-lean-import/checked-proposition-compatibility"
      ],
      "artifact": "F:nat-bitwise-swap",
      "notes": "Registers an independently constructed native theorem (`F:nat-bitwise-swap`, `Nat.bitwise_swap` in `nat_prelude/bitwise.rs`) whose proposition matches this proof-free imported goal up to the standard, funext-avoiding restatement this kernel uses throughout (this kernel has no `funext`, so a `Function.swap`-level function equality is stated pointwise: `forall f m n, bitwise (swap f) m n = bitwise f n m` is the applied form of `bitwise (swap f) = swap (bitwise f)`). Our `Nat.bitwise` genuinely IS Mathlib's own general combinator (not a specialization), so the flip is honest in the strongest sense this repository's mirror-flip criterion recognizes: Mathlib's `def Nat.bitwise` and ours are the same function, and this closes the same theorem about it, restated in a form this kernel can actually state."
    }
  ],
  "provenance": {
    "date": "2026-08-18",
    "established_by": "not established in this ledger",
    "source": "statement-only extraction of `Nat.bitwise_swap` from Mathlib v4.30.0; no proof value was exposed",
    "prior_art": [
      {
        "who": "the Mathlib contributors",
        "what": "the theorem declaration `Nat.bitwise_swap`",
        "where": "mathlib4 commit c5ea00351c28e24afc9f0f84379aa41082b1188f (v4.30.0)",
        "year": 2026,
        "attribution": "the proposition was read from the pinned statement-only inventory; the proof term and tactic trace were not consulted"
      }
    ]
  },
  "notes": "Proved in Axeyum by reconciliation with the independently constructed native theorem `F:nat-bitwise-swap` (`Nat.bitwise_swap`) -- see that fact's `notes` for the full construction (unlike `bitwise_comm`, no commutativity hypothesis on `f` is needed: `swap f` beta-reduces directly to `f` with its arguments exchanged, so every site `bitwise_comm` needed a propositional `Bool` congruence for is pure defeq here). The external theorem declaration remains prior art, not the source of this proof; formal.statement is Lean surface syntax accepted as a proposition by the proof-free generated axiom module, not kernel-core render_lean output. depends_on projects only direct upstream theorem uses whose endpoints both survived review; that edge is curriculum and leakage metadata and grants no dispatch, proof, or admission authority -- this fact's own proof route does NOT go through `F:ml430-nat-bitwise-bit-4c4b28a8`, which remains open."
}
