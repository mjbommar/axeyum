# Verify capability scoreboard

Generated by `cargo run -p axeyum-verify --example measure_verify`. A construction-known corpus through `verify_program`; loop programs are cross-checked against the warm `check_program_loop` route. **DISAGREE = 0** is the soundness floor (a verdict contradicting the known label, or a warm-vs-unroll disagreement).

## Headline

- **14 cases**: 7 bugs found, 7 verified, 0 unknown.
- **DISAGREE = 0** (soundness floor).
- **Lean-certified: 4/7** verified results carry a kernel-checkable Lean module (the trusted-checking moat; the rest are re-checked in-process but outside the Lean reconstructor's fragment).

## Per case

| Case | Class | Expected | Outcome | Lean |
|---|---|---|---|---|
| add-overflow | overflow | bug | bug-found | — |
| mask-safe | overflow | safe | verified | no |
| signed-add-overflow | overflow | bug | bug-found | — |
| sub-underflow | overflow | bug | bug-found | — |
| div-by-zero | div0 | bug | bug-found | — |
| assert-violation | assert | bug | bug-found | — |
| assert-safe | assert | safe | verified | yes |
| wrapping-add-no-overflow | wrapping | safe | verified | no |
| wrapping-not-monotone | wrapping | bug | bug-found | — |
| saturating-sub-no-underflow | saturating | safe | verified | no |
| min-clamp-safe | minmax | safe | verified | yes |
| max-floor-safe | minmax | safe | verified | yes |
| loop-assert-bug | loop | bug | bug-found | — |
| loop-safe | loop | safe | verified | yes |

## Warm-BMC vs unroll scaling (safe deep loop)

A safe counter loop decided at increasing unwind bounds by both routes (both must stay `verified`/`safe` — agreement is the soundness check). Times are a single wall-clock run, indicative not tuned. **Caveat:** not pure apples-to-apples — the unroll route (`verify_program`) also produces a re-checked evidence certificate + a Lean-reconstruction attempt, while the warm route currently returns only the decision (a cert on the warm route is a follow-up). Even so, the warm `bounded_model_check` is genuinely incremental across depths and scales far better here. The EVM store-chain result remains the opposite after ADR-0087: candidate-triggered transitive summaries improve the depth-32 warm row 2.75x, but direct `ite` folding is still faster.

| Unwind bound | unroll t µs | warm-BMC t µs | agree |
|---|---|---|---|
| 2 | 71312 | 1888 | yes |
| 4 | 141425 | 3385 | yes |
| 6 | 199131 | 5111 | yes |
| 8 | 297834 | 6478 | yes |
