# Formal Mathematics Tour — machine-readable knowledge graph.
#
# Authoritative node/edge list for the curriculum. Per-node prose lives in the
# sibling markdown files (path = "<area-folder>/<id>.md"); this file is the
# graph (prerequisites = edges) plus the axeyum decidability/testability
# metadata. See README.md.
#
# Invariants (validated by axeyum-scenarios::mathtour tests, which mirror this
# graph): every `prerequisites` id exists; the graph is acyclic; every
# `unlocks` is the inverse of some `prerequisites`.
#
# Field legend:
#   layer         0 foundations · 1 number systems · 2 structures · 3 destinations
#   decidability  decidable | computable | bounded | undecidable
#                   decidable  — a complete decision procedure (e.g. propositional)
#                   computable — answer is computed then checked (e.g. gcd)
#                   bounded    — only finite/fixed instances are decided
#                   undecidable— general case is Lean-horizon, not a benchmark
#   axeyum_theory the theory whose testable fragment this node maps to
#   family        the axeyum-scenarios Family that exercises it ("" = not yet)
#   status        covered | planned | lean-horizon
#   kernel_decls  MEASURED count of axeyum-lean-kernel declarations (all kinds)
#                 attributed to this node -- a SECOND, INDEPENDENT axis
#
# `status`/`family` are the SCENARIO axis and nothing else. `Status::Covered`
# in crates/axeyum-scenarios/src/mathtour.rs is defined as "has a self-checking
# exercise family today", and a test enforces exactly that; `LeanHorizon` means
# "primarily a proof-reconstruction target, not a benchmark". Neither value
# says anything about what the Lean-core kernel has PROVED, and reading them as
# if they did the two axes disagree in opposite directions (ADR-1075):
# `calculus` is `lean-horizon` with 349 kernel declarations, `linear-algebra`
# is `covered` with 81.
#
# `probability` (ADR-1082) is the first node added past the original 23: the
# same class of measurement found 47 `Rat` declarations (distributions,
# expectation, variance, covariance with Cauchy-Schwarz, Markov, Chebyshev,
# the weak law of large numbers) with no node to attribute to, filed under
# `rationals` only for want of a better bucket. It is `status = "planned"`
# -- the first node to use that value -- because the content is
# bounded/computable (not a proof-reconstruction target) but no `Family`
# scenario exists for it yet. See
# DEPTH-PROPOSAL-number-theory-and-linear-algebra.md `S3`.
#
# `kernel_decls` is that second axis, stated separately
# so neither can be mistaken for the other. It is a measurement, not a
# judgement:
#
#   cargo run --release -p axeyum-lean-kernel \
#     --example kernel_declaration_projection > /tmp/proj.tsv
#   python3 scripts/measure-curriculum-kernel-coverage.py /tmp/proj.tsv \
#     --expect-attributed 2483 --require-node probability
#
# Measured 2026-08-31 over 2,615 distinct declarations, every one axiom-free
# (2,483 attribute to a node; the 132 that do not are the same 30 legacy
# `AxReal` axioms, the 94-declaration string package, and 8 newly-landed
# declarations -- `Nat`/`Int`/`Rat`/`Complex`/`CPoint` the carrier inductives
# themselves, plus `Max.max`/`Min.min`/`Squarefree`/`instMinNat` -- not yet in
# any bucket). This total moved from the 2,586/2,454 an earlier run of this
# same command measured hours earlier the same day, purely from other lanes'
# commits landing in between (`Int.euler_totient_theorem`, ADR-1110; `Rat.det`
# at general `n`, ADR-1120) -- re-measure rather than trust either number; the
# script's attribution table is auditable and its residual is printed.
#
# The `Rat.det`/`matSkip`/`matMinor`/`altSign`/`matInv2*` declarations ADR-1120
# landed were themselves briefly mis-measured: the script's `linear-algebra`
# pattern named `det2`/`det3` literally and did not cover the new general-`n`
# names, so they fell through to the `rationals` catch-all -- 22 declarations,
# the exact failure this file's own comment already warns about, recurring on
# the rung the depth proposal below names as the keystone. Fixed in the same
# commit that re-measured this table.

[meta]
title = "Formal Mathematics Tour"
description = "Backward-derived prerequisite DAG from calculus / number theory / linear algebra to foundations, with axeyum's testable fragment per node."
adr = "adr-0033-double-duty-educational-artifacts"
note = "docs/research/08-planning/formal-mathematics-tour.md"

# ---------------------------------------------------------------------------
# Layer 0 — Foundations (the "bridge")
# ---------------------------------------------------------------------------

[[node]]
id = "propositional-logic"
title = "Propositional Logic"
layer = 0
area = "foundations"
prerequisites = []
unlocks = ["predicate-logic", "proof-methods"]
decidability = "decidable"
axeyum_theory = "Bool / SAT"
family = "Logic"
status = "covered"
kernel_decls = 64
summary = "Boolean variables, connectives, validity and satisfiability."

[[node]]
id = "predicate-logic"
title = "Predicate Logic"
layer = 0
area = "foundations"
prerequisites = ["propositional-logic", "sets"]
unlocks = ["proof-methods"]
decidability = "bounded"
axeyum_theory = "Quantifiers (finite domain)"
family = "Predicate"
status = "covered"
kernel_decls = 3
summary = "Quantifiers over a domain; validity is undecidable in general, decidable over finite domains."

[[node]]
id = "proof-methods"
title = "Proof Methods"
layer = 0
area = "foundations"
prerequisites = ["propositional-logic", "predicate-logic"]
unlocks = ["induction"]
decidability = "bounded"
axeyum_theory = "Refutation (negate-and-decide)"
family = "ProofMethods"
status = "covered"
kernel_decls = 0
summary = "Direct, contrapositive, and contradiction proofs; refutation is exactly what a solver does."

[[node]]
id = "induction"
title = "Mathematical Induction"
layer = 0
area = "foundations"
prerequisites = ["proof-methods", "naturals"]
unlocks = ["number-theory"]
decidability = "bounded"
axeyum_theory = "LIA / BV (base + step instances)"
family = "Induction"
status = "covered"
kernel_decls = 18
summary = "The induction schema is undecidable; individual base/step obligations are decidable."

[[node]]
id = "sets"
title = "Sets"
layer = 0
area = "foundations"
prerequisites = ["propositional-logic"]
unlocks = ["relations-and-functions", "predicate-logic"]
decidability = "bounded"
axeyum_theory = "BV / enumeration (finite sets)"
family = "Sets"
status = "covered"
kernel_decls = 6
summary = "Membership, subset, union/intersection; finite-set identities are exhaustively checkable."

[[node]]
id = "relations-and-functions"
title = "Relations & Functions"
layer = 0
area = "foundations"
prerequisites = ["sets"]
unlocks = ["cardinality", "groups", "linear-algebra"]
decidability = "bounded"
axeyum_theory = "BV / EUF (finite domains)"
family = "Relation"
status = "covered"
kernel_decls = 29
summary = "Relations, equivalence, functions; properties (injective/surjective) decidable on finite domains."

[[node]]
id = "cardinality"
title = "Cardinality"
layer = 0
area = "foundations"
prerequisites = ["relations-and-functions"]
unlocks = []
decidability = "bounded"
axeyum_theory = "Counting / enumeration"
family = ""
status = "lean-horizon"
kernel_decls = 19
summary = "Counting and bijections. The kernel constructs Nat.countRange with split/union-inter/complement/permutation-invariance and a point-change lemma; infinite cardinality has no carrier here and stays Lean-horizon."

# ---------------------------------------------------------------------------
# Layer 1 — Number systems
# ---------------------------------------------------------------------------

[[node]]
id = "naturals"
title = "Natural Numbers (Peano)"
layer = 1
area = "number-systems"
prerequisites = ["sets"]
unlocks = ["integers", "induction"]
decidability = "bounded"
axeyum_theory = "LIA / BV"
family = "NumberSystem"
status = "covered"
kernel_decls = 516
summary = "Peano successor arithmetic; concrete arithmetic decidable, the induction axiom is not."

[[node]]
id = "integers"
title = "Integers"
layer = 1
area = "number-systems"
prerequisites = ["naturals"]
unlocks = ["rationals", "divisibility-and-euclid", "rings"]
decidability = "computable"
axeyum_theory = "LIA / BV"
family = "NumberSystem"
status = "covered"
kernel_decls = 193
summary = "Signed arithmetic and ordering; linear integer facts decided by simplex + branch-and-bound."

[[node]]
id = "rationals"
title = "Rational Numbers"
layer = 1
area = "number-systems"
prerequisites = ["integers"]
unlocks = ["reals", "fields", "probability"]
decidability = "computable"
axeyum_theory = "LRA (exact rationals)"
family = "Rational"
status = "covered"
kernel_decls = 204
summary = "Exact fractions; linear rational facts decided by the exact-rational simplex."

[[node]]
id = "reals"
title = "Real Numbers"
layer = 1
area = "number-systems"
prerequisites = ["rationals"]
unlocks = ["sequences-and-limits", "calculus", "complex"]
decidability = "bounded"
axeyum_theory = "LRA / NRA (real-closed fields)"
family = "RealAlgebra"
status = "covered"
kernel_decls = 201
summary = "Ordered complete field; the elementary (algebraic) theory is decidable (Tarski), completeness is not."

[[node]]
id = "complex"
title = "Complex Numbers"
layer = 1
area = "number-systems"
prerequisites = ["reals"]
unlocks = []
decidability = "bounded"
axeyum_theory = "NRA over pairs"
family = ""
status = "lean-horizon"
kernel_decls = 263
summary = "Pairs of constructed reals with field structure. The kernel has the Complex ring and the CPoint plane (collinearity, circles, Varignon); complex ANALYSIS -- holomorphy, contour integration -- is untouched and stays Lean-horizon."

# ---------------------------------------------------------------------------
# Layer 2 — Core structures & tools
# ---------------------------------------------------------------------------

[[node]]
id = "divisibility-and-euclid"
title = "Divisibility & the Euclidean Algorithm"
layer = 2
area = "structures"
prerequisites = ["integers"]
unlocks = ["modular-arithmetic", "number-theory"]
decidability = "computable"
axeyum_theory = "BV / LIA + GCD test"
family = "NumberTheory"
status = "covered"
kernel_decls = 153
summary = "Divisibility, gcd, Bézout's identity, unique factorization; gcd/Bézout are compute-and-check."

[[node]]
id = "modular-arithmetic"
title = "Modular Arithmetic & Congruences"
layer = 2
area = "structures"
prerequisites = ["divisibility-and-euclid"]
unlocks = ["number-theory", "fields"]
decidability = "bounded"
axeyum_theory = "BV / LIA"
family = "NumberTheory"
status = "covered"
kernel_decls = 104
summary = "Congruences mod n, inverses, CRT; modular identities exhaustively checkable over fixed moduli."

[[node]]
id = "groups"
title = "Groups"
layer = 2
area = "structures"
prerequisites = ["relations-and-functions"]
unlocks = ["rings"]
decidability = "bounded"
axeyum_theory = "BV / enumeration (finite groups)"
family = "Algebra"
status = "covered"
kernel_decls = 10
summary = "Closure/associativity/identity/inverse; finite-group axiom checks via Cayley tables."

[[node]]
id = "rings"
title = "Rings"
layer = 2
area = "structures"
prerequisites = ["groups", "integers"]
unlocks = ["fields", "polynomials"]
decidability = "bounded"
axeyum_theory = "BV / enumeration"
family = "Algebra"
status = "covered"
kernel_decls = 0
summary = "Two operations with distributivity; finite-ring identities checkable; ℤ as the motivating ring."

[[node]]
id = "fields"
title = "Fields"
layer = 2
area = "structures"
prerequisites = ["rings", "rationals", "modular-arithmetic"]
unlocks = ["linear-algebra", "polynomials"]
decidability = "bounded"
axeyum_theory = "LRA (ℚ) / BV (𝔽ₚ)"
family = "Algebra"
status = "covered"
kernel_decls = 0
summary = "Rings with multiplicative inverses; ℚ and 𝔽ₚ give concrete, checkable arithmetic."

[[node]]
id = "polynomials"
title = "Polynomials"
layer = 2
area = "structures"
prerequisites = ["rings", "fields"]
unlocks = ["calculus", "linear-algebra"]
decidability = "computable"
axeyum_theory = "BV (fixed-degree) / NRA"
family = "Polynomial"
status = "covered"
kernel_decls = 16
summary = "Polynomial arithmetic, identities, roots, factoring; fixed-degree identities are decidable."

[[node]]
id = "sequences-and-limits"
title = "Sequences & Limits"
layer = 2
area = "structures"
prerequisites = ["reals"]
unlocks = ["calculus"]
decidability = "bounded"
axeyum_theory = "LRA / NRA"
family = ""
status = "lean-horizon"
kernel_decls = 33
summary = "Convergence and limits. The ε–N definition is NOT a horizon: the kernel declares CReal.Converges, CReal.Cauchy and CReal.limit with uniqueness, squeeze, the algebra of limits, and Bishop completeness (limitSeq). What stays open is non-constructive limit reasoning, which this carrier cannot state."

[[node]]
id = "counting"
title = "Counting & Combinatorics"
layer = 2
area = "structures"
prerequisites = ["sets", "naturals"]
unlocks = ["number-theory", "probability"]
decidability = "computable"
axeyum_theory = "LIA / enumeration"
family = "Counting"
status = "covered"
kernel_decls = 66
summary = "Permutations, combinations, the pigeonhole principle; finite counting identities are checkable."

# ---------------------------------------------------------------------------
# Layer 3 — Destinations
# ---------------------------------------------------------------------------

[[node]]
id = "number-theory"
title = "Number Theory"
layer = 3
area = "destinations"
prerequisites = ["divisibility-and-euclid", "modular-arithmetic", "induction", "counting"]
unlocks = []
decidability = "bounded"
axeyum_theory = "BV / LIA"
family = "NumberTheory"
status = "covered"
kernel_decls = 108
summary = "Primes, factorization, congruences, Diophantine equations; bounded instances are self-checkable."

[[node]]
id = "linear-algebra"
title = "Linear Algebra"
layer = 3
area = "destinations"
prerequisites = ["fields", "relations-and-functions", "polynomials"]
unlocks = []
decidability = "computable"
axeyum_theory = "BV (fixed-size) / LRA / NRA"
family = "LinearAlgebra"
status = "covered"
kernel_decls = 81
summary = "Vector spaces, matrices, determinants, eigenvalues; fixed-size identities over ℚ are decidable. The determinant is now proved at general n (ADR-1120, a cofactor recursion over the dimension bound), not just 2x2/3x3; eigenvalues remain unbuilt."

[[node]]
id = "calculus"
title = "Calculus"
layer = 3
area = "destinations"
prerequisites = ["reals", "sequences-and-limits", "polynomials"]
unlocks = []
decidability = "bounded"
axeyum_theory = "NRA"
family = ""
status = "lean-horizon"
kernel_decls = 349
summary = "Limits, derivatives, integrals, series. ε–δ is NOT a horizon: the kernel has HasDerivativeOn with an explicit modulus, sum/product/chain/power rules, CReal.integral over uniformly continuous functions with additivity and integration by parts, the fundamental theorem (integral_eq_antideriv_diff), Rolle, the mean value theorem, IVT with an exact root, EVT, supOn, uniform convergence and the Weierstrass M-test, exp/sin/cos and sqrt. It is `lean-horizon` in the SCENARIO sense only -- a reconstruction target rather than a bounded benchmark family."

[[node]]
id = "probability"
title = "Probability"
layer = 3
area = "destinations"
prerequisites = ["rationals", "counting"]
unlocks = []
decidability = "computable"
axeyum_theory = "LRA (exact rational, finite distributions)"
family = ""
status = "planned"
kernel_decls = 47
summary = "Finite probability over exact rationals: distributions, expectation, variance/covariance with Cauchy-Schwarz, Markov's and Chebyshev's inequalities, and the weak law of large numbers. ADR-1082: a Spivak-shaped spine built entirely over Rat.sumRange, `planned` because no self-checking scenario Family exists for it yet even though the content is bounded/computable, not Lean-horizon."
