# Roadmap and plan — from the inventory and gap analysis (2026-09-09)

This distills [`docs/solver-inventory-2026-09/`](../solver-inventory-2026-09/00-README.md)
(what we have, and what is actually wired), the nine reference-solver files in
this folder, and [10-gap-analysis.md](10-gap-analysis.md) into an ordered plan.

It is written backwards from **exit criteria**. Every item names the observation
that would show it is done, and that observation is one a referee can re-run.
Items without a falsifiable exit criterion were left out on purpose.

Ordering rule: **value today ÷ cost to close**, then dependencies. That rule puts
checker fixes first (a checker that cannot fail makes the whole ledger
unfalsifiable), routing second (the engines exist), and algorithm work last.

## What the evidence says, in one paragraph

We are not behind on capability in the way the crate count suggests. We are
behind on **integration**: a warm incremental SAT engine, five inprocessing
passes, a machine-independent work meter, and both halves of stable/focused mode
switching all exist in the tree and are not reached by a default solve. On
evidence we lead the field by more than we have said — four reference solvers
produce no proofs, Z3 removed interpolation and ships a bit-vector proof checker
whose six functions all `return true` — but our own external-checker gates skip
and pass, so we cannot currently *demonstrate* that lead. The real algorithmic
gaps are specific and mostly in the SAT layer: equivalent-literal substitution,
a gate-structure interface to CNF, and propagation-based local search.

## Phase 0 — make the checkers able to fail (days)

Rationale: the repository's own rule. At N lanes the ledger is the product, and
every claim in Phases 1–3 will be *measured* through these gates. They must be
trustworthy before anything else is.

| # | Item | Do | Exit criterion (falsifiable) |
|---|---|---|---|
| 0.1 | `minus_simplify` — **source done, EXECUTION NOT**: the name is present at `alethe.rs:802` and the list is 180, but `carcara_checked_rules_parity.rs:119-129` still skips green because `references/carcara` is absent, so the derivation-from-the-clone this row asks for has never run. Remaining work is one `fetch-references.sh` on a gate host ([ADR-1903](../research/09-decisions/adr-1903-proof-format-target-reverified.md) §3) | Add the name to `CARCARA_CHECKED_RULES` (`crates/axeyum-cnf/src/alethe.rs:868` region). Carcara aliases it at `carcara/src/checker/shared.rs:292`. | The pinned list has 180 entries and equals the set of Carcara's `get_rule` match-arm strings at clone `6624ea80c`; a test derives that set from the clone rather than a literal. |
| 0.2 | A Carcara gate that runs Carcara — **DONE**, `scripts/check-carcara-gate.sh` (`AXEYUM_REQUIRE_Z3`-shaped default-fail, a build-free `--self-check` negative control, a 40-invocation floor, registered at `scripts/check.sh:1306-1307` and `justfile:1375-1376`); see [ADR-1903](../research/09-decisions/adr-1903-proof-format-target-reverified.md) §2 | `scripts/check-carcara-gate.sh` modeled on `check-lean-gate.sh`; `AXEYUM_REQUIRE_CARCARA=1` mode in `tests/carcara_crosscheck.rs`. Copy cvc5's shape (`references/cvc5/test/regress/cli/run_regression.py:312-335`): check exit status **and** `"valid" in stdout`, and pass `--allowed-rules` naming our three in-tree-only rules explicitly. | Under `AXEYUM_REQUIRE_CARCARA=1`, a proof with one rule renamed to `hole` **fails** the gate; with the binary absent the gate **fails**, not skips. Register the gate in `just check`. |
| 0.3 | drat-trim exit contract | Audit every recipe that invokes drat-trim; require the `s VERIFIED` grep that `check-claim-certificates.py:1227` already does. drat-trim has 17 `exit (0)` sites incl. MEMOUT and TIMEOUT (`drat-trim.c:905`). | A deliberately truncated proof reports failure from every recipe; `exit 0` alone never counts as a pass. |
| 0.4 | Support matrix behavioral probes | `SUPPORT_MATRIX` (19 rows) and `CAPABILITIES` (105) are hand-written literals dispatch never reads. Add a probe per row that runs a one-line query through the front door and asserts the claimed verdict class. | Flipping any row's claimed logic support makes exactly one probe fail. 12 of 19 matrix rows currently have no probe; `CAPABILITIES` has none. |
| 0.5 | Trust ledger derived, not typed | `ALL_TRUST_IDS` (15) and `is_certified` are hand-written. Derive the certified set from which `Evidence` variants carry a checked certificate. | Adding a `TrustId` without an evidence route fails a test; the ledger markdown regenerates from the derived set. |
| 0.6 | Documentation drift | Fix the ten drifted docs and two ADRs listed in the gap analysis §7, and `CLAUDE.md:319` (axeyum-fp deps) and the varisat sentence (splr ships DRAT; varisat remains the only Rust SAT solver with LRAT). Update `07-strings-and-regex.md` to include `str.update` and `seq.*`. | `docs/internals/cnf-and-sat.md` no longer says RAT is rejected; `support-matrix.md` column count matches source; ADR-0009 no longer claims a real incremental façade until 1.1 lands. |

| 0.7 | **The z3 differential fuzzes skip-and-pass** — **DONE 2026-09-10, see [ADR-1901](../research/09-decisions/adr-1901-z3-demotion-reverified.md) §4.** `AXEYUM_REQUIRE_Z3` now exists (`tests/common_z3/mod.rs`, `96da7e741`), 13 suites consume it, `scripts/check-z3-differential-gate.sh` carries a negative control, and `fleet-hosts.md` records the binary as a per-host capability naming s2 and s6 as absent. THE ROW TEXT BELOW IS STALE and is kept only as the record of the defect.** | 14 test files hardcode `/usr/bin/z3` and `return` when it is absent — one comment says outright "the test is a no-op pass". There is **no `AXEYUM_REQUIRE_Z3`**, though `AXEYUM_REQUIRE_LEAN`, `_ABC` and `_CARCARA` all exist. These fuzzes are the ONLY checks comparing our verdicts against an independent solver. | Same fix as 0.2: an `AXEYUM_REQUIRE_Z3=1` mode under which an absent binary FAILS. Blast radius unmeasured — `fleet-hosts.md` records z3 as a requirement, never as a per-host capability, so we do not know how many CI/dev hosts have been running these green-and-empty. |

## Phase 1 — wire what already exists (weeks)

Rationale: the top of the gap analysis. No new algorithms; each item has an
engine with tests already in tree.

| # | Item | Do | Exit criterion |
|---|---|---|---|
| 1.1a | **Warm engine in the CEGAR loops** — **DONE.** | The lazy-ROW and extensionality CEGAR loops hold one `IncrementalBvSolver` across rounds (`abv.rs` `RowEngine`, routed from `auto.rs:dispatch_pure_qf_abv`). **Corrected 2026-09-09:** there are SEVEN `SatBvBackend::new()` sites in `auto.rs`, not five (`:3993 :4735 :4853 :5563 :6672 :6689 :7250`); only `:5563` was a warmable refinement loop. `abv.rs` has none — it receives a backend from its caller. | **The original criterion was defective and is replaced.** "Assert clause count is monotone" CANNOT FAIL: a fresh engine at round *n*+1 encodes the whole larger working set, so its counts are monotone too — measured byte-identical, `[673, 1143, 2099, 2800]`, warm and rebuilt. The load-bearing assertion is `encodes_each_assertion_once` (`warm_asserted == working_final`): 18 vs 18 warm, 56 vs 18 rebuilt. Corpus verdicts identical. |
| 1.1b | **Warm `Solver` façade** — **BLOCKED, and correctly so.** | Route `Solver::check`/`solve_smtlib` through the warm engine and make `push`/`pop` real scopes. | **Prerequisite: an incremental-script corpus (new item 2.9).** 0 of 1,101 committed `.smt2` files contain `(push`, only 4 have two `check-sat`s, and `corpus_regression.rs:149` skips scoped scripts outright — so a façade leaking an assertion across a `pop` (a wrong `unsat`) would leave every committed gate green. Three further blockers: `Solver<B>` cannot ask a backend whether it is the pure-Rust BV path (needs a defaulted `warm_bv_engine_equivalent()` on `SolverBackend`); `Solver::assert` takes no arena; `smtlib.rs run_session` calls the full dispatcher per `check-sat`. ADR-0009's Status correction STAYS until this lands. |
| 1.2 | **Inprocessing on by default** — **MEASURED, DECIDED AGAINST.** | Stays `false`. Frontier at a comparable pinned frame: `bv_reduction` **35 → 26** (baseline 30) — PROGRESS off, REGRESSION on, scales within 1%, reproduced across a 33-commit gap. Corpus **137 → 130/131 agree**: 6-7 decided files become 2000 ms timeouts, none gained, wall time 17.4s → 35.0s. **0 DISAGREE both arms** — nothing became wrong; the cost is the objection. | Measurement note: `docs/research/03-measurements/inprocessing-default-flip-2026-09-09.md`. The certificate half was already met (117/117 inprocessed `unsat` check against the ORIGINAL formula), so evidence is not the blocker. **Correction 2026-09-10 (item 3.2, SUPERSEDED by T1):** 3.2 said "the lever is one integer at `ticks.rs:337`, not the feed". **It is neither.** `TickValve`/`TickEffort`/`bootstrap_reference` have ZERO references in `crates/axeyum-solver` — the call site has its OWN admission policy built on `axeyum_ir::budget`, a second implementation of the same CaDiCaL idea — so both the feed and the constant are inert. Setting the constant to **0**, the most discriminating value in its domain, gives **0** differences over 60 deterministic fixtures against a positive control that fires **255** times. And the gate that IS routed cannot refuse: its size arm needs `B < R` (2000 < 2) and its wall arm needs under **45.3 ms** of remaining budget on the corpus's largest file. Routed and swept end to end, 2,000,000 decides 186/200 and 10,000,000 decides 185; no arm of six beats the shipping configuration's 186, which reproduces to an identical file set across two runs. `docs/research/03-measurements/tick-valve-bootstrap-reference-2026-09-10.md` |
| 1.3 | **One inprocessing pipeline** — **DECIDED, see ADR-1810.** | **Corrected 2026-09-09:** the shipping path is the PROOF-CARRYING one. `ReductionLink` (`axeyum-cnf/src/reduction_link.rs:156`, same crate as `inprocess.rs`) has, since ADR-1780 on 2026-09-08, made `sat_bv_backend.rs:2812` check its `unsat` against the ORIGINAL formula; `inprocess.rs` cannot express the `compact()` renumbering. The shipping SEQUENCING survives and moves into `axeyum-cnf`. | The old criterion ("the other's file is gone") is the wrong shape — no file disappears. Replacement from ADR-1810: `sat_bv_backend.rs` contains zero calls to the seven pass-sequencing names, the 117 `unsat` files on the 200-file QF_BV parity list still report `ProofCoverage::Original`, and all 200 verdicts are unchanged. |
| 1.4 | **One preprocessing pipeline** — **DECIDED, see ADR-1811.** | `preprocess.rs` (8 rounds; `real_div_zeros`, no function carry) vs `auto::preprocess_reduce` (1 round; carries BOTH). **Corrected 2026-09-09:** the original row said "neither is a superset" — false, the front door is the superset (`auto.rs:2335`, `:2344`). ADR-1811 makes `preprocess.rs` the one home, parameterized over the solve call, and lands at cap 1. | A fixture with a `/0` witness AND a function interpretation replays through the merged path. Three negative controls: delete each witness loop in turn, require exactly one different test to die each time. Raising the cap above 1 is a SEPARATE gated step — `reduction_shrinks_encoding`'s rows (`auto.rs:2209-2232`) were measured at one round. |
| 1.5 | **The tick valve** — **BUILT BUT UNROUTED; the feed is moot until it has a consumer (T1, 2026-09-10).** | `TickValve` in `axeyum-cnf` admits each inprocessing pass on a per-mille slice of accrued search ticks, refuses below a formula-scaled threshold, and backs off exponentially — CaDiCaL's `SET_EFFORT_LIMIT` + `Delay`. | **The original criterion was partly cosmetic and is replaced.** I asked that `span_log.rs:75` "no longer say 'No ticks'" — but that sentence is TRUE: `SearchCounters` are filled only by the `count_search` variants and nothing in `axeyum-solver` calls them, so rewording it would be a doc change dressed as an implementation. What was met: an 8-round schedule is byte-identical across runs with a 7.1× wall-time spread, and contains granted, refused and backed-off decisions. What is OUTSTANDING: **the feed** — a solver-side lane must route the SAT solve through `solve_with_drat_proof_counted` and wrap the existing observer. Until then no span carries a tick and the valve is off the shipping path. | **Correction 2026-09-10 (item 3.2, SUPERSEDED by T1):** `bootstrap_reference` does not act either. The valve is routed NOWHERE the solver can reach, so neither knob is connected — measured, not inferred (0 differences at the constant's most extreme value; positive control 255). It IS mis-set — 1.69x below its own re-derived window, which reproduces to the unit — but recalibrating it belongs to whenever the valve acquires a consumer, which ADR-1810 already scopes to in-search inprocessing. Routing it today approximates `cnf_inprocessing: false`, which is already the default. `docs/research/03-measurements/tick-valve-bootstrap-reference-2026-09-10.md` |
| 1.6 | **Stable/focused switching** | Expose the `proof_sat.rs` EMA restart and `phase_policy.rs` target/best-phase machinery outside `#[cfg(test)]`; add the mode switch on a tick budget with quadratic interval growth, starting focused (`restart.cpp:19-84`). | `use_ema_restart` has a production setter; a curated corpus shows the mode schedule in the span log; ratchets non-regressing. |
| 1.7 | **Portfolio default** | `portfolio::FusedGroup` needs `AXEYUM_PORTFOLIO_WORKERS ≥ 2`; default is 1. Commit `613bc3f35` measured two workers faster on decided files. Make it default where the measurement holds. | Default run on that slice reproduces the +6/−0 result; verdicts deterministic under the race (the divergence at `auto.rs:2890-2894` is either removed or shown verdict-neutral). |
| 1.8 | **Route trace for quantified inputs** — **DONE (recording), ONE ATTRIBUTION DEFECT OPEN (diagnosed 2026-09-10).** | The quantified ladder records now, and lanes read it all day. But `q:mbqi` reads **0 ms on every file** while MBQI's real seconds are charged to `q:uf-fmf-full`. Mechanism, read from `auto.rs`: in the terminal arm, `prove_unsat_by_mbqi` runs and records NOTHING; `find_uf_finite_model` then runs; `UF_FMF_FULL` is recorded, and that segment spans BOTH; `MBQI` is recorded afterwards, by which point the segment since the last record is ~0. So `bound_by=q:uf-fmf-full` means "MBQI plus the finder", never the finder alone. | **This is not a typo — the two attributions want opposite orderings.** The late `MBQI` record is deliberate and its comment says why: it makes MBQI the trail's LAST WORD instead of a declined probe. Timing wants the record immediately after the pass; last-word wants it last. Recording twice double-counts. Exit: decide whether `RouteTrace` should separate a segment's COST from a rung's VERDICT, then fix — and re-check every published clock share, because at least one already had to be withdrawn over this (lane U1's 56.2% became 52.4% once MBQI's seconds were removed from a total labelled "cannot produce `unsat`"). `docs/research/03-measurements/where-the-uf-clock-goes-2026-09-10.md`. |

## Phase 2 — cheap, well-bounded engineering (weeks)

| # | Item | Do | Exit criterion |
|---|---|---|---|
| 2.1 | **SCC / equivalent-literal substitution** | The cheapest CaDiCaL pass (`decompose.cpp`) and the one every other pass feeds. Tarjan over the binary implication graph, substitute representatives, emit RUP for each replaced clause. | A curated instance with *k* equivalence classes shrinks by the expected variable count; DRAT accepted; the pass runs under the 1.5 valve. |
| 2.2 | **Keep gate structure across the CNF boundary** | CaDiCaL spends 7,925 lines (`congruence.cpp`) recovering AND/XOR/ITE that Tseitin destroyed; we still have it in the AIG and discard it at `tseitin_encode`. Carry a gate table beside the CNF so `xor_extract.rs` reads it instead of re-mining clauses. **Size this as an interface, not an algorithm.** | `xor_extract` finds the same XOR set from the table as from clause mining on the XOR curated corpus, in a fraction of the time; the mining path becomes a fallback for foreign CNF. |
| 2.3 | **The `i128` boundary** — **MEASURED, deliberately NOT opened.** | Opening `narrow()` turns a sound `unknown` into a PANIC. Measured: a 131-var doubling chain has an exact vertex at 2^130 that the boundary discards; the evaluator then cannot replay it (`ArithmeticOverflow { op: "real_mul" }`) because `Rational`'s `checked_*` family computes exactly then DEMOTES; and forcing `narrow` open leaves the corpus tally byte-identical, so nothing committed exercises this and no gate would catch the panic. | **My prerequisite was wrong.** This row said "the evaluator already handles `Value::WideInt`" — true but insufficient: `Value::Real` carries a `Rational`, not a `WideInt`, and has NO wide path. Real order: (1) a wide-real path in `eval.rs`; (2) the model-path panic sites (`auto.rs:2537`, `:2570`, `smtlib.rs:3904` — `get-model` cannot print a wide model today, `eval.rs:772`, `:776`); (3) the certificate residue (`alethe_lra.rs:944`, `reconstruct/arithmetic.rs:4778`); (4) THEN delete two lines in `narrow`. |
| 2.3b | **The wide-witness replay check is VACUOUS** — **NEW, found by 2.3.** | A correct 2^130 and a deliberately corrupt 2^131 produce the identical `ArithmeticOverflow`. Sound (declines to `unknown`, never a wrong `sat`) but zero discrimination. | Pinned as `the_replay_check_cannot_distinguish_a_correct_wide_witness_from_a_corrupt_one`. No claim that "the replay catches a corrupted wide witness" may be inherited from today's code. Closing 2.3 must make this test's premise false and flip its assertion. |
| 2.4 | **ABC as a bit-blasting cross-check** | Our AIGER export is ASCII (`aig/lib.rs:636`); ABC reads binary only (`giaAiger.c:1980`). One hop through `aigtoaig`. Add an optional gate: export, convert, `cec` against a second lowering. | `just abc-crosscheck` fails on a deliberately mis-lowered operator (one negative control), passes on the curated set. Optional dependency; skip-and-**fail** under `AXEYUM_REQUIRE_ABC=1`. |
| 2.5 | **Vendor the string corpus** | cvc5's strings/seq regression is 583 files (150 `QF_SLIA`, 74 `QF_S`); we vendored 20, zero `QF_SLIA`. Vendor the rest with `:status`; add the missing operators to the `:status` sweep. | `corpus/regression/cvc5/qf_slia/` exists; the sweep's file count rises accordingly; every new file's verdict is either matched or explicitly `unknown` — never wrong. | **CORRECTED 2026-09-10:** of the 66 vendored files **20 decide, 9 are `unknown`, and 37 the parser cannot read**  — not the 57 first reported. The per-file READMEs marked parse-skipped rows as "decided (agrees)"; `corpus_regression` buckets a parse failure as `Eval::Skip`, not `Eval::Agree`. 0 DISAGREE either way, so nothing became wrong — this is a coverage overstatement. The 16→47 operator figure counts operators PRESENT in the corpus, not operators we decide. |
| 2.6 | **Differential fuzz where none exists** | Difference logic runs *first* in the ladder and has no oracle fuzz; nor do pure `QF_LIA`, `int_real_relax`, `lia_gcd`, `bmc`/`imc`/`pdr`. | A `--features z3` suite per route; each generates the route's degenerate case (Hard Rule) and reports a nonzero test count. |
| 2.7 | **Retire or wire the eleven test-only modules** | `abduct`, `enums`, `faithfulness`, `horn`, `hypothesis_min`, `imc_lia`, `lex_reconstruct`, `pb`, `pdr_lia`, `records`, `toy_bv_vm`. Per module: dispatch it, move it to an example, or delete it. ADR for the deletions. | Module-level measurement (`11-wiring-and-integration.md` method) reports 0 test-only modules, or each survivor has a recorded reason. |
| 2.9 | **An incremental-script corpus** — **NEW, prerequisite for 1.1b.** | Today 0 of 1,101 committed `.smt2` files use `(push`, so every push/pop path in this solver is ungated. Vendor or generate scoped scripts with `:status`, and stop `corpus_regression.rs:149` from skipping them silently. | A script that asserts under `push`, pops, and re-checks is in the sweep; deliberately leaking that assertion across the `pop` makes it fail. Until this exists, no warm-façade work can be gated. |
| 2.10 | **Int-linear replays against the eliminated form** — **FIXED 2026-09-10 (`cbe3d6ef4`), and the recorded status was WRONG: the witness exists and reaches the public front door.** | `auto.rs:2836`, `:2894`, `:3088-3093` replay the model against `lin`, the `!divmod_*`-eliminated form, NOT the original assertions, and return it for the original query. `Model` has no `int_div_zero` component, so a chosen `(div x 0)` value structurally cannot be carried, while `eval.rs:830-846` pins `div a 0 = 0`. This is the SOUND-1 shape one theory over. | **DONE, and the status above was wrong** (SOUND-2, `f1ebb338e`; note: `docs/research/03-measurements/2026-09-10-sound-2-model-certificate-narrowing.md`). A witness exists and reaches the public front door: `(= x 3) ∧ (= (div x 0) 5)` returned `Sat` with model `x=3, !divmod_0=5`, which replays `false` at assertion #1 and leaks an internal symbol. SOUND-1 was right about the VERDICT — `sat` is correct (SMT-LIB leaves `div`-by-zero free) and the congruence `unsat`s are unchanged — and nobody had checked the CERTIFICATE. Fixed by the first exit criterion: replay against the ORIGINAL assertions and decline to `unknown`. Capability cost measured at zero. Follow-up that restores `sat`: give `Model`/`Assignment`/`eval` an integer division-at-zero component mirroring `real_div_zero` (needs an ADR). |
| 2.11 | **Eleven sites emit a narrower `Model` than they replayed** — **FIXED 2026-09-10 (`cbe3d6ef4`): eleven verified, THIRTEEN fixed, and this row was wrong in four places.** | `aufbv.rs:126`, `combined.rs:237`, `abv.rs:263`/`:11091`, `lia.rs:145`, `ufbv_online.rs:3168`, `datatype_native.rs:665`, `nia_linearize.rs:1837`, `lazy_bv.rs:323`, `pbls.rs:1151`, `incremental.rs:7616`. Most drop `real_div_zero` in fragments where a real `/0` should not arise — structural, not currently exploitable. | **DONE** (SOUND-2, `76f7f54ae`; audit `d78a911cf`). Fixed at all eleven plus two unlisted siblings (`abv.rs:6633`, `incremental.rs:7637`) — NOT with guard 2. The note about the two blind sites is correct, so those three `Model`→`Model` sites narrow via `Model::retain_symbols` (clone-and-retain: no component CAN be lost, including future ones), and the ten `Assignment`→`Model` sites use `Model::carry_assignment_components`. Row corrections: `incremental.rs:7616` already carried `real_div_zero`; FIVE sites also dropped `functions` (the `9b259f7c2` shape), which the row did not mention. Per-site mutation: 6 of 13 sites have a test that dies alone, 7 do not — the seven are inline in large functions and need extraction first. |
| 2.8 | **Lean-kernel-checked interpolants** — **DONE for 6 of 7.** | `dispatch_interpolant` routes through a new `dispatch_interpolant_certified`; six `certify_*` helpers attach the certificate AFTER the plain rung produced the interpolant, so the term never changes and a decline leaves `certificate: None`. The seventh is deliberately out: `propositional_interpolant_certified`'s DRAT lives in the CNF variable space and does not cover the lift to terms. | Six `*_certified` names have a `src/` caller (0 before). Unwiring any rung flips exactly one row of the pinned list in `tests/dispatch_interpolant_certified.rs`. Confirmed: all eight interpolators DO enforce Craig symbol containment — with a control showing conditions 1+2 do not imply it. |
| 2.8b | **Two certified rungs are unreachable through the ladder** — **NEW, found by 2.8.** | `certify_qf_bv` is SHADOWED: the ground-EUF rung sits above QF_BV and decides the canonical `x=y` / `x≠y` fixture, so the BV certificate never ships for that shape — while `capabilities.rs:236` advertises it without the caveat. `certify_uflia` is unreached for the same class of reason. | Either reorder the ladder so a BV-sorted equality partition reaches the BV rung, or state the caveat in the capability text. The measured per-rung list is pinned as a test, so any change fails loudly. |

## Phase 3 — the real algorithmic gaps (months, prioritize by corpus evidence)

Each of these should be gated by a measurement showing the gap costs us on a
public corpus *before* the work starts. Several may not survive that gate.

| # | Item | Evidence | Prerequisite measurement |
|---|---|---|---|
| 3.1 | **Propagation-based local search** — **MEASURED, DO NOT BUILD.** | Zero on both corpora. Committed: front door decides 19 of 20 satisfiable QF_BV, 18 in 0 ms; local search LOSES three of them. NAS (166 SMT-LIB-2024 `:status sat` QF_BV): **of the 43 we miss, local search decides 0**, and on 30 of those it performs **zero flips** — never reaching its own loop. 96 instances go bit-blasting's way, **zero go local search's way anywhere**. Flip rate median 1,175/s where competitive SLS runs 10⁵–10⁶. | `docs/research/03-measurements/local-search-gap-2026-09-10.md`. Reachability was the headline: `solve_local_search` has ONE `src/` caller (`preprocess.rs:111`), on cold lazy-ROW rounds only, while `auto.rs:5663` calls the warm entry — 0 reaches over 226 files, positive control fires 8×. **The gap is ~2 orders of magnitude in flip rate, not 68 invertibility functions** — the incremental memo already exists at 1.5×. **And Bitwuzla defaults `bv_solver` to BITBLAST** (`option.cpp:238-241`); `prop` is opt-in for them too. |
| 3.2 | **Remaining inprocessing passes** — **MEASURED, DO NOT BUILD — and the one-integer lever it named does not exist (T1, 2026-09-10).** | BVE is **76.4%** of inprocessing cost, subsume 21.5%, everything else under 2% — one pass dominates, so the lever is admission, not new passes. Addressable frontier is **9 files**, and the whole pipeline (2.54 M variables eliminated corpus-wide) converts **one**. Binary-implication graph: 3.73 M binary clauses over 3.82 M variables yield **4,362 substitutable variables, 0.114%**, nothing at all on 6 of 17 files — SCC is cheap and there is nothing to substitute. | `docs/research/03-measurements/remaining-inprocessing-passes-2026-09-10.md`. **`TickEffort::bootstrap_reference` (`ticks.rs:337`) is 1.69× BELOW its own useful window** `[3,375,600, 64,294,400)` — window and 98/195 both re-derived to the unit by T1, no disagreement. **But T1 then measured the follow-up and it is DO NOT BUILD as well:** the valve is routed nowhere the solver reaches, so the constant is dead configuration (0 differences at value 0; positive control 255); the gate that IS routed refuses on 0/195 and its work budget is vacuous on 191/195; and routed end to end it decides 186/200 at 2,000,000 but **185 at the 10,000,000 this note recommends**. Across SIX arms nothing beats the shipping configuration's 186, which reproduces EXACTLY (identical file sets) across two independent runs. Arming the real gate via `AXEYUM_BVE_BUDGET_MULTIPLE` at 500 and 100 makes it WORSE — both fall to `off`'s 184, losing the two files inprocessing gains. **The headline DO NOT BUILD stands and is strengthened; the cheap next task it named does not exist.** `docs/research/03-measurements/tick-valve-bootstrap-reference-2026-09-10.md` |
| 3.3 | **Word-level rewrite depth with levels** — **MEASURED, DO NOT BUILD.** | Of 25 candidate rules **14 remove ZERO AND gates** — `Aig::and` already folds constant literals, so a constant shift collapses the barrel shifter to wiring anyway. Two rules make the circuit BIGGER. Over 352,479 nodes on 123 real SMT-LIB instances there is **no head and no tail**: the four most frequent missing classes are 43,582 of the top five's 45,874 firings and **all four measure delta zero**; the two with a real win fire on 0.34% of nodes. | `docs/research/03-measurements/rewrite-depth-gap-2026-09-10.md`. **It also answers 3.9: the 43 misses are NOT rewrite-bound.** 17 of 43 cannot finish the rewriting we ALREADY run at 300 s/20 GB; on 19 the front door never reaches the solver; on the 26 that finish, rewriting **grows** the AIG 352.0M→355.2M. `ndist.b.24491.smt2` is 745 bytes and **six term nodes** with two 24,491-bit variables → 391,840 AND gates — a **bit-width** problem no rule touches. **The 296-vs-59 count compares different things: the comparable surface is 152.** Highest-value carve-out is not a rule — `BitLoweringMode::DemandSliced` is that win in the lowering and is defaulted off (`backend.rs:395`). |
| 3.4 | **LIA branching as SAT lemmas** — **MEASURED, DO NOT BUILD (completeness half closed).** | **0 of 911 benchmarks** across three populations are `unknown` because of `MAX_LIA_BNB_NODES`, and 131 are confirmed to have ENTERED branch-and-bound so the zero is a measurement, not a coverage gap. Mechanism: **97.7% of 308,160 integer decisions close in the Gomory round before BnB is entered**; when BnB runs it closes in a mean of 1.52 nodes. All BnB nodes across 382 files = 10,481, **21% of what the cap allows one call**. **The cap is unreachable, MEASURED not extrapolated:** a cap-forcing two-variable instance ran **71 minutes (4,250 s)** of continuous branch-and-bound without tripping the 50,000-node cap, worker at 87% CPU (observed live; the run was then STOPPED at ~4,500 s of its 9,000 s bound and wrote no log) — ~3× past what its own node-rate predicted, because node *k* solves a simplex over ~*k* rows. A stop that will not fire in an hour on the easiest possible input cannot cost a verdict at a 24 s budget. | `docs/research/03-measurements/lia-branching-cap-2026-09-10.md`. **The references replace the guarantee with nothing** — their non-termination is bounded by the user's wall clock, so a lemma redesign means deciding to stop offering a deterministic clock-free stop, which the proof and interpolant routes depend on. Revisit as SPEED, not completeness. **Separate fix owed:** only `LiaCounters::bnb_budget_exhausted` can distinguish cap from clock — two of three BnB routes drop the `UnknownReason` entirely. |
| 3.5 | **Instantiation strategies** — **MEASURED, DO NOT BUILD.** | Of 224 quantified files examined (163 declined, 92 attributable), **not one is blocked on any of cvc5's six strategies**. The settling line: **z3 refutes 18 of the 32 UF parity-loss files at the same 24 s budget using the SAME family we have** — E-matching + MBQI, none of cvc5's six — and **lane Q5 (2026-09-10) narrowed this further: `z3 smt.mbqi=false` decides the IDENTICAL 19 of 32, file for file, so the MBQI half is worth exactly ONE file and the gap is instance selection in E-matching alone**. We refute 0. Three arms rule out the cheap explanations: 8× ground ceiling → 0; refuter alone at 24 s → 0 (median wall 4.75 s); at 240 s → 0 (median 4.02 s). **A 10× budget does not move the median** — it stops early on its own. | `docs/research/03-measurements/instantiation-strategy-gap-2026-09-10.md`. Blocker on the 55 e-matching stops is **instance selection**, so the comparison point is cvc5's trigger-selection modes, not its strategies. **Two cheaper defects found:** 5 files abandoned in under a second with the full budget free — *all five are files z3 refutes*; and **56.2% of the wall clock on 30 declared-UNSAT files goes to SAT-side FMF rungs that cannot produce `unsat`**, via `UFBV_ONLINE_PROBE_SHARE` whose own doc says "unmeasured". Follow-up: install cvc5 and ablate per-option — every claim about its strategies here is inference, not head-to-head. |
| 3.6 | **Strings inference depth** — **MEASURED, DO NOT BUILD.** | Of the vendored files we do not decide, the overwhelming majority never reach a strings solver: they are declined at INGEST by the ADR-0029 admission test (`attempts=1 last=fd:parse`). No new inference kind moves a file the parser refuses. The handful that do reach the solver decline on the bounded-encoding gate, not a missing lemma. | `docs/research/03-measurements/strings-inference-gap-2026-09-10.md`. cvc5's 87 inference ids vs our 4 is a real gap and is NOT what costs us verdicts today — the parser is. Also corrected item 2.5's coverage claim: 20 of 66 decide, not 57. |
| 3.7 | **Interpolation strength and shape** — **MEASURED, DO NOT BUILD. The gate is still shut.** | Over every `Int` transition system committed: **systems=7, reach_imc=1, imc_rescues=1** — PDR decides 6 of 7 and the one that falls through is proven `Safe` by TODAY's interpolant. Quality binds on 0 of 7. **PDR consumes no interpolant at all** (`interpolant` appears 0× in `pdr.rs`/`pdr_lra.rs`/`pdr_lia.rs`), so only IMC is a consumer and `horn.rs` runs PDR first. | `docs/research/03-measurements/interpolation-consumer-gap-2026-09-10.md`. **Correction: item 2.7 did NOT open this gate** — it added the third of three families; `Real`→`imc_lra` and `Finite`→`imc` already routed. And `solve_horn` has no caller in `src/` **by design** (`horn.rs:164`): 0 of 1,179 committed `.smt2` and 0 of 84 SMT-LIB-2024 divisions are HORN — CHC ships with CHC-COMP. Strength parameterisation is LAST (OpenSMT's default IS McMillan, which we implement; its knob is `= delete` for integers). Exit: build a CHC front door, vendor CHC-COMP, re-run — the `#[ignore]`d probe IS the criterion. |
| 3.8 | **Store-pattern recognition** — **MEASURED, BUILD (scoped).** | Committed corpus: max depth 79, zero depth-correlated `unknown`s, 81 µs on the deepest — nothing to fix. Public `smtlib-2024` QF_ABV (15,148 files): depth to 1200; a 104-file `wchains` family pushes eager elimination to multi-million-node formulas, `unknown` from depth ~240-320, **23 GB RSS at depth 800**. But a 595-file KLEE family at the SAME depth solves in seconds — the driver is reads × depth, not depth. | `docs/research/03-measurements/array-store-chain-gap-2026-09-10.md`. **`eliminate_arrays` never returns `Unsupported` for a deep chain — it succeeds slowly**, so ADR-1814's existing lazy fallback never fires here and nothing protects this path. Build the STP-shaped cost predicate (item 4.2), NOT Boolector's lambda recognition. **Next measurement first:** force the lazy-ROW path on `wchains060-200`; if it is also slow, a cost predicate does not fix it. |

### Phase 3 addendum — the item the measurements actually surfaced

| # | Item | Evidence | Exit criterion |
|---|---|---|---|
| 3.10 | **We do not find instances z3 finds in 0.21 s with the same tools** — **ALL THREE ITEMS CLOSED 2026-09-10; see the evidence column.** | z3's refutations of the 18 need **2-10 instantiations** and land in **≤0.21 s**; we spend 24 s and get 0. On **13 of 17** the required substitution is a **Skolem-function application** — and we BUILD those terms (2-341 `!qskf` symbols per file). Term construction is not the failure; **instance selection is**. Not the clock either: at 120 s the ladder decides 0 of 32 and 24 return `unknown` before spending the budget. | `docs/research/03-measurements/why-z3-refutes-and-we-do-not-2026-09-10.md`. **ALL THREE ITEMS CLOSED 2026-09-10; this row's original text is kept below the line for provenance.** **(1) The routing defect is FIXED** (`318930806`): the gate was a SHAPE proxy for "the cheap route failed", false on 4 of 62 entries, and `smtlib.1116374` now decides `unsat` through the front door in 96 ms of a 748 ms slice — it was never short of time, it was never called. Pinned by `tests/quant_skolem_egraph_routing.rs` (an ASSERTED verdict; `corpus/regression/` skips `unknown` and would have stayed green), registered in both aggregate gates in `f7c716bcc` after it was found to be invoked by nothing. **(2) `smtlib.1098821` is ATTRIBUTED, not fixed:** its four universals are two inert nested registrations (`active=false`, no context — see the triggerless note), one that joins 44,537 tuples and admits 419 without refuting, and `universal[3]` which joins 2,937 and admits ZERO. That last is admission, and the admission census (`what-the-admission-filter-rejects`) attributes 53.3% of that class to a deferred pool released only `if admitted.is_empty()`. **(3) The e-graph question is ANSWERED and then CLOSED:** the required term is ABSENT on 10 of 16 attributable files, with 93% of its arguments already present — a construction gap one application wide (`does-the-required-instance-enter-our-egraph`) — and building it (`building-the-skolem-application`) moves the term 0→1 on f29 and the DECIDED COUNT NOT AT ALL, while costing a win. **Where the gap actually is now:** not the clock, not the ground ceiling, not construction, and not MBQI (`z3 smt.mbqi=false` decides the identical 19 of 32, so MBQI is worth ONE file). The slice decides 1 of 32 shipped, and 3 of 32 with the deferred-pool release armed, 0 losses across the 32 and the 24 wins. Next: the ground refutation check and clause selection — why an e-graph holding the refuting instance, admitted and certificate-checked, produces no conflict. |
| 3.9 | **Our bit-blasting misses 43 of 166 real satisfiable QF_BV instances at 10 s** — **MEASURED, and it is NOT encoding size. BUILD, three bounded items.** | **On a measurable part of this class we compute the right answer and discard it.** Nine instances — two of them real SMT-LIB files through the shipping front door — return `unknown` at a 10 s budget after spending **29-43 s**, and `sat` at 300 s after spending the **same** time: `sat_bv_backend.rs:327` re-reads the clock after `primary_sat_search` returns and throws the result away. The budget is not enforced during that search either — the CDCL core tests its deadline every `DEADLINE_CHECK_INTERVAL = 1_024` **conflicts** (`proof_sat.rs:62`, `:3453`) and the whole search here is **under 256 conflicts at 28-51 conflicts/second**, so it can never fire; measured overrun **1.15x-8.01x**. **Encoding size is refuted by our own data:** `bvwide-mul-w01024` has **8,891,930** CNF clauses and decides in 5.9 s while `bvwide-addcmp-w12288` has **430,038** — 20.7x fewer — and does not. Circuit is linear in the width (17.77 gates/bit at 64, 18.00 at 32,768), wall clock is ~`w^1.8`, cost per conflict ~`w^2.0`. **Admission over-refuses once, measurably:** `ABSOLUTE_CLAUSE_CEILING` (`sat_bv_backend.rs:2154`) refuses `bvwide-mul-w02048` on a *projected* 100,681,731 clauses whose real encoding is **35,609,626** — estimator **2.83x** pessimistic, instance **1.80x under** the cap, and it decides `sat` in 41.0 s once admitted. | `docs/research/03-measurements/why-43-satisfiable-qfbv-miss-2026-09-10.md`. **Gated:** `corpus/public-curated/non-incremental/QF_BV/smtlib-pspace-ndist/` (the whole 21-file `ndist.b` family, every one `:status sat`, 745 B each) plus a width-graduated generated corpus, ratcheted as a DECIDE-FRONTIER by `crates/axeyum-solver/tests/qfbv_width_frontier.rs`. `corpus/regression/` could not have gated this: it *skips* `unknown` and fails only on a wrong verdict. **Closes item 3.3's carve-out 1 as DO NOT BUILD:** `BitLoweringMode::DemandSliced` removes **ZERO** gates here — byte-identical AIG and CNF on both families — and costs **51.6x** more lowering time (516 ms vs 10 ms, 36% of wall clock). Every operator in this class is a conservative *barrier* in ADR-0157's own exact-propagation class, so there is nothing to slice; the off-by-default is not stale. **Answers the 19-that-never-reach-the-solver question: NO, not an admission gate** — `auto.rs:1953`, `:1958`, `:2408` are *timeouts*, and `node_budget`/`cnf_clause_budget` are never assigned in `auto.rs`/`smtlib.rs`/`solver.rs`. **No width threshold exists in the measured range** — at 300 s the whole real `ndist.b` family decides, to width 29,980; the ceiling is our clock, not the method. **Caveat:** item 3.1's 166-sample is NOT reproducible from its recipe (re-running gives 200, not 168), so the miss set was reconstructed by NAME from 3.3's tables (42 of 43) and only the 9 at ≤1 MB were measured. |

## Phase 4 — decisions to record, not code to write

**ALL FIVE ARE CLOSED.** 4.1, 4.2 and 4.3 were taken on 2026-09-09 by a first
Phase 4 lane (ADR-1813/1814/1815); 4.4 and 4.5 on 2026-09-10, together with an
independent re-verification of the first three (ADR-1901/1903) and a re-decision
of 4.2 after its pre-registered falsifier fired (ADR-1902). Do not re-open a row
without reading its ADR — this table's own text is superseded on four of five
rows and is preserved below only so the corrections have a subject.

| # | Decision | Inputs | Closed by |
|---|---|---|---|
| 4.1 | **The Z3 demotion path (ADR-0002).** Our three differential suites use exactly `Solver::new`, `set_params("timeout")`, `assert`, `check` — no tactics, cores, proofs, assumptions or push/pop anywhere in the workspace. The oracle we depend on is `smt_context` + `theory_lra` + exact simplex. | A replacement oracle needs only that surface. Candidates: cvc5, Yices2 (no proofs, but fast and exact). Decide and record. | **[ADR-1813](../research/09-decisions/adr-1813-the-z3-oracle-demotion-path.md)** — Z3 keeps its lane; Yices2 cannot cover strings/FP/datatypes (measured); cvc5 is an *additive* second oracle. Re-verified at `60fe8bdf2` by **[ADR-1901](../research/09-decisions/adr-1901-z3-demotion-reverified.md)**, which also corrects this row: **35** differential suites, not three, and assumptions ARE used once (`cnf_stream_bench.rs:331`). |
| 4.2 | **Eager vs lazy arrays.** Keep ADR-0010 (it buys a certificate nobody else produces) but adopt STP's staged rule (eager only when reads < 10 and expansion < 200) to remove the `MAX_ARRAY_EQ_INDEX_BITS = 8` refusal. | 3.8's measurement. | **[ADR-1814](../research/09-decisions/adr-1814-staged-eager-lazy-arrays.md)** declined, then **[ADR-1902](../research/09-decisions/adr-1902-array-cost-predicate-reopened.md)** superseded its commitment 2 once 3.8 fired its falsifier 1: **BUILD**, at `abv.rs:739` and *before* the admission rewrite, threshold gated on two measurements. This row's motivation is wrong — the cap is a route selector and deep chains never reach it. |
| 4.3 | **Proof format target.** cvc5's default is CPC/Eunoia, not Alethe; its Alethe printer translates 97 of 172 rules and holes the entire strings family. Carcara remains the checker for what we emit. | Decide whether to stay Alethe-only or add an Ethos route; do not do both without a consumer. | **[ADR-1815](../research/09-decisions/adr-1815-the-proof-format-target.md)** — stay Alethe-only; an Ethos route would have zero consumers. Re-verified by **[ADR-1903](../research/09-decisions/adr-1903-proof-format-target-reverified.md)**: item 0.2 has landed, item 0.1 has not (the parity derivation still skips). |
| 4.4 | **FP.** We eliminate at parse (ADR-0028, replay checks the same circuit); Bitwuzla/STP word-blast lazily via SymFPU. This is a fork, not a gap. | Revisit only if an FP corpus shows parse-time elimination losing on size. | **[ADR-1904](../research/09-decisions/adr-1904-fp-parse-time-elimination-is-a-fork-with-three-revisit-conditions.md)** — keep parse-time elimination, but the governing ADR is **0023**, not 0028, and the fork has **three** costs with three triggers; only this row's is about size, and the third (replay shares the circuit) has already fired once. |
| 4.5 | **Stop claiming exact arithmetic as a differentiator.** Z3's `lp::mpq` is bignum rational + δ, same as ours. | Remove from any comparison prose. | **[ADR-1905](../research/09-decisions/adr-1905-exact-arithmetic-is-parity-not-a-differentiator.md)** — done. A two-token sweep with a positive control found the claim surviving in **one** place, not several; the row is removed from `02-z3.md`. |

## What not to do

- **Do not size 2.2 as CaDiCaL's 7,925 lines.** They recover structure we never lost.
- **Do not build an FP theory solver because Bitwuzla has one** (4.4).
- **Do not flip `cnf_inprocessing` before 1.2's measurement.** The ratchets exist for this.
- **Do not add a reference solver as a dependency** for anything but oracle, differential, or cross-check duty (ADR-0002, ADR-1703). ABC in 2.4 is a cross-check.
- **Do not search for a thing by the name you have in mind.** Ten coordinator greps failed this way in the session that produced these documents (`exit(0)` vs `exit (0)`, a module name vs its exported function, a nested `carcara/carcara/`). Search for the string an author would have written, and run a positive control.

## Metrics to carry

Each phase reports against the existing instruments, not new ones:

| Metric | Instrument | Phase |
|---|---|---|
| Gates that can fail | The delete-one-guard, exactly-one-test-dies rule, per checker | 0 |
| Modules reachable from the default solve path | `11-wiring-and-integration.md` method, re-run | 1, 2.7 |
| Capability frontier | `progress_frontier` at a stated reference frame | 1.2, 1.6, 3.2 |
| Z3 parity | `bench-public-qfbv-sat-bv-compare` | 1.1, 3.1, 3.3 |
| Unsat routes with no evidence | count of `Evidence::Unsat(None)` construction sites (8 today) | ongoing |
| Trusted base | kernel `axiom_footprint`, never source text | unchanged |

## Sequencing

```
Phase 0  ──►  Phase 1.1–1.4  ──►  1.5–1.8  ──►  2.1–2.3  ──►  measure  ──►  Phase 3 (gated)
(days)        (routing)           (valve,       (SAT layer)                  (only what the
                                   modes)                                     measurement justifies)
Phase 4 decisions can be taken at any point; 4.1 and 4.5 now.
```

Phase 0 and Phase 1 fit in a small number of lanes with clear file ownership and
no shared allocation points beyond one ADR number each. Phase 3 should not
start until Phase 2's measurements say which items are worth it — several of
them may turn out to cost nothing on the corpora we actually run.
