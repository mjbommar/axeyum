# ADR-1670: The CAS keeps its `i128` fast path and gains a big-integer overflow fallback

Index-summary: CAS arbitrary precision — keep the `i128` normal form, decide overflowed zero-tests over `BigInt`; wave two priced by measurement
Index-status: proposed
Status: proposed
Date: 2026-09-05

## Context

DRAFT — measurements in progress (lane `cas-bignum`).

## Decision

DRAFT.

## Consequences

DRAFT.
