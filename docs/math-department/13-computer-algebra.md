# 13 — The computer algebra system, as a tool

Reviewer: the twelve chairs, each asked one question — *would you use
`axeyum-cas` to compute in your field, and what stops you?*
Verdict, 2026-09-05: **a deep single-variable calculus and elementary
number theory tool that overflows at 128 bits; every other chair finds
nothing to reach for**
Last measured: 2026-09-05 at `bb999de1f` (baseline `8f6c58420` in the table below; the close-out row in the log carries the re-measurement)

> "You built the one CAS whose answers can carry a proof, and then you
> built the parts of Mathematica a first-year calculus course uses. Where is
> the rest?"

This file is different from files 01 to 12. Those ask what the *library*
proves. This one asks what the *CAS* computes, judged as a computing tool by
the same twelve practitioners. It exists because the CAS roadmap under
[`docs/research/10-cas/`](../research/10-cas/README.md) was written
2026-07-20 to 07-23, its capability-parity push paused at wave 24 on
2026-07-22, and every CAS commit since 2026-08-26 has been trust work — the
[`cas-internal` residue](../research/09-decisions/adr-1617-exact-real-cost-and-cas-internal-residue-measured.md)
and the bridges that reduce it. That work is tracked in
[11-applied-and-computational.md](11-applied-and-computational.md) items 3
and 5 and in the roadmap rows W1-13 and W2-16. **This file tracks the other
axis: what the tool can compute at all.**

## What the CAS has today

Measured 2026-09-05 at `8f6c58420`; the commands are in *How to re-measure*.

| metric | value |
|---|---|
| source lines, `crates/axeyum-cas/src` | 80,851 |
| modules | 55 files plus 4 subdirectories |
| `pub fn` declarations | 691 (385 at column 0) |
| `#[test]` functions | 1,006 |
| `lib.rs` alone | 29,505 lines |
| `cas-certificate` ledger facts | 61: 16 kernel-reconstructed, 45 `cas-internal` (73.8%) |
| core coefficient arithmetic | `i128` rationals; overflow reported as `ZeroTest::Unknown` (`lib.rs:46`) |
| numeric evaluation | `evalf(expr, bindings: &[(&str, f64)]) -> Option<f64>` (`lib.rs:9903`) |

The surface, by area, is in the
[capability table](../research/10-cas/README.md#implemented): differentiation,
canonical forms and a decidable zero-test with sound transcendental folds;
factorization over ℚ (Berlekamp–Zassenhaus) and 𝔽ₚ; partial fractions;
Horowitz–Ostrogradsky rational integration plus a large elementary and
special-function integration table; series, Laurent and residues; limits
including L'Hôpital, squeeze and exponential dominance; Gosper and
Zeilberger summation; linear constant-coefficient and first-order ODEs;
Laplace, Fourier series and z-transforms; exact ℚ linear algebra through
Jordan form, Smith and Hermite forms, QR and Cholesky; Sturm real-root
isolation and real algebraic numbers; Gröbner bases over ℚ with cofactor
certificates; an elementary number-theory bundle through discrete logs,
Pell and continued fractions; plane geometry with a Nullstellensatz
certifier; GF(2) polynomial and tensor machinery; SOS, Lyapunov and barrier
certificates; exact descriptive statistics; permutations; boolean algebra.

Of the July roadmap's prioritized top 15, fourteen have code behind them.
The unbuilt ones are Lazard–Rioboo–Trager and Risch (zero files mention
either), CAD, and Meijer-G.

## What each chair would say

One line each, every absence re-checked against the crate on 2026-09-05
with the probe in *How to re-measure*, and paired with a positive control
(`smith_normal_form`, `laplace_transform`, `gosper_sum`, `count_real_roots`
each found by the same query shape).

| # | chair | would they use it | what stops them |
|---|---|---|---|
| 01 | number theory | for elementary work, yes | no Gaussian integers, no quadratic or number fields, no ideal factorization, no two-squares or Cornacchia, no elliptic curves; `i128` overflows on any real computation |
| 02 | constructive analysis | no | series are compute-only with no remainder bound; `evalf` returns a double and no enclosure |
| 03 | classical analysis | for one-variable calculus, yes | no Fourier transform, no asymptotic expansion past a leading term, no change of variables in multiple integrals, no PDE methods |
| 04 | algebra | no | Gröbner over ℚ only; factorization univariate plus two bivariate special forms; no ℚ(α) arithmetic beyond one real root; no finite-group computation beyond a single permutation; no Galois groups |
| 05 | geometry | for the rational plane, yes | no conics, no 3D, no projective coordinates, no isometries as objects |
| 06 | topology | no | nothing, and nothing expected, except that Smith normal form exists and simplicial homology is one module away |
| 07 | combinatorics | for sequences and triangles, yes | no formal power series object, no generating-function algebra, no recurrence guessing, no graph algorithms, no posets or tableaux |
| 08 | probability | for descriptive statistics, yes | no named distributions, no moment generating functions, no convolution of independent variables, no estimators or tests |
| 09 | category theory | no | nothing to ask for, and they would say so |
| 10 | logic | no | a proof-carrying CAS with no real quantifier elimination; Sturm and polynomial inequality solving exist, so the univariate existential fragment is close |
| 11 | applied | for exact rational work, yes | arbitrary precision and validated numerics absent; no published parity benchmark against SymPy with a denominator |
| 12 | the chair | — | the capability table's *certified* column is prose; no gate counts which of the 691 public functions carry a certificate |

## The Next Ten, in priority order

Ranked by how many chairs an item serves, then by what it unblocks. Items 1
and 2 are prerequisites for most of the rest being usable at scale; item 10
is what makes any of it citable.

- [~] **1. Arbitrary-precision core.** *Overflowed zero-tests decide over the unbounded ring (ADR-1670); wave two added the unbounded witness, the six folds, binary `pow` and the radical soundness fix; ADR-1710 designs `axeyum-arith` and slices 1 and 3 have landed (Dyadic, radix certificates, modular rings; ℤ[x]/ℚ[x], Sturm, Bézout certificates, four of five CAS copies migrated); wave three (2026-09-06) canonicalized constant radicals of every root index and measured `exp` as too costly under the current fallback entry; wave four (2026-09-06) keyed transcendental atoms on a canonical rational form, closing three wrong refutations; slice 2 moved enclosure rounding onto `Dyadic` (ℚ(√61) regulator to thirty digits in 58 ms); the fallback's overflow-versus-fragment distinction, `exp`, and the kernel numeral bridge remain.* `CasExpr` coefficients are `i128` and
      overflow reports `Unknown`. Chairs 01, 02, 03, 07, 11 hit this wall
      within minutes of real use. `num-bigint` is already a dependency and is
      used in eight modules (`telescoping`, `ratint`, `real_algebraic`,
      `mvpoly/big`, `sos/*`, …), so this is a migration with a measured cost
      curve, not a design.
- [x] **2. Validated numerics.** *Landed 2026-09-05; wave two added rational powers, `erf`, `Gamma`, Bessel and Krawczyk roots; wave three (2026-09-06) added the integral-defined heads, Euler's constant, symbolic exponents and certified quadrature; complex arguments, asymptotic tails past magnitude 20 and improper integrals remain.* Replace the `f64` `evalf` with rational
      interval enclosures to a requested precision, with interval Newton for
      root enclosures. `interval_arith.rs` exists and is not wired in. Serves
      02, 03, 11, and answers the computable-analysis seat's W1-12 complaint
      without running series inside the kernel.
- [~] **3. Formal power series and generating functions as an object.** *Object and Berlekamp–Massey landed; wave two added radius of convergence, finite-n asymptotics and P-recursive guessing; wave three (2026-09-05) made the radius exact for any factor of degree ≤ 6; transcendental singularities, multivariate series and the exact asymptotic amplitude remain.*
      Radius of convergence, composition, coefficient extraction, coefficient
      asymptotics, recurrence guessing from initial terms. The private
      `series.rs::Series` struct becomes public. Serves 07, 02, and the
      asymptotics 03 asked for.
- [~] **4. Algebraic extensions and algebraic number theory.** *ℚ(α), ℤ[i], two squares and units landed; wave two added ideals, splitting, class numbers and primitive elements; wave three made the imaginary class group a certified group; wave four (2026-09-06) added real quadratic class numbers by form cycles and the regulator as an enclosure; real-case composition, non-maximal orders, higher degree, Galois groups and multivariate factorization open.* Arithmetic in
      ℚ(α) over a minimal polynomial, then ℤ[i] and quadratic fields with
      ideal factorization, two squares, Pell as a unit computation.
      Multivariate factorization follows. Serves 01 and 04, and unblocks the
      July roadmap's own Lazard–Rioboo–Trager item.
- [~] **5. Finite group computation.** *Schreier–Sims landed; wave two added conjugacy classes, Sylow subgroups and counts, invariants and normality; wave three added certified isomorphism testing and presentations; wave four (2026-09-06) added finite matrix groups over 𝔽ₚ through certified actions; matrix groups over ℚ and ℤ, representation theory and the bounds remain.* Permutation groups with Schreier–Sims,
      subgroups, cosets, orbits and stabilizers, Cayley tables from
      presentations. Serves 04, and gives 05 its transformation groups.
- [~] **6. Geometry beyond the rational plane.** *Conics, 3D, homogeneous coordinates and isometries landed; wave two gave the corpus a search-cost field and a joint-scope block search; wave three (2026-09-06) combines elimination identities so a power of the stated condition is inverted, and the tetrahedron medians certify in a second; wave four (2026-09-06) certifies Pascal on the parabola with no side condition and affine Desargues on three falsifiable conditions; Pascal on an arbitrary conic, projective Desargues at infinity, and per-conclusion decompositions in a joint block remain.* Conics as quadratic forms,
      3D points and planes, homogeneous coordinates, isometries as maps. The
      cofactor certifier in `geometry_certify.rs` extends to all of these, so
      the certified route comes free. Serves 05.
- [~] **7. Real quantifier elimination.** *Univariate fragment landed; wave two added the `BigRational` engine, DNF and one bivariate projection step; wave three decides irrational cell boundaries in ℚ(α); wave four (2026-09-06) lifts to three variables with one existential and decides `∀x∃y` and `∃x∀y`; field towers, adjacency across levels and further alternation open.* The univariate existential fragment
      over Sturm and the existing inequality solver first, then
      low-dimensional CAD. The July roadmap deprioritized it for a weak
      certificate; sample-point certificates are checkable. Serves 10, 11, 03.
- [x] **8. Simplicial homology over ℤ.** *Landed 2026-09-05; waves two and three added coefficients, cohomology, induced maps with torsion, persistence over ℚ and 𝔽ₚ, and a fast integer matrix path; wave four (2026-09-06) added relative homology with the long exact sequence and the cup product; Steenrod squares and homotopy invariants remain.* Boundary matrices into the existing
      `smith_normal_form`. The cheapest new subject on this list and the only
      thing 06 would use.
- [~] **9. Symbolic probability.** *Eight distributions landed; wave two fixed the machinery for Poisson, Geometric moments and `Normal(μ, 1)`; wave three (2026-09-05) certifies symbolic-parameter Exponential, Uniform and Normal mgfs under recorded hypotheses; wave four (2026-09-06) closes Normal with symbolic variance and Geometric with symbolic p under recorded hypotheses; Gaussians with a linear term or finite bounds, index-in-denominator geometric summands, and symbolic-parameter convolution remain.* Named distributions with exact mean,
      variance and moment generating function through the existing
      `laplace_transform`; convolution of independent sums; the moment
      inequalities in symbolic form. Serves 08.
- [x] **10. A gated trust registry and a parity benchmark.** *Registry landed 2026-09-05 and now reads 120 certified; the SymPy parity corpus landed the same day, found one soundness defect on its first run, and has grown to 128 entries covering every wave (126 agree, 2 expected declines); since 2026-09-06 a checker fails when its three ledgers drift.* A script that
      derives, per public function, whether its result carries a certificate,
      ratcheted like `check-cas-internal-residue.py`. Beside it, a SymPy
      parity corpus with ground truth independent of this repository, in the
      style of
      [`docs/plan/cas-smt-capability-2026-08-12/`](../plan/cas-smt-capability-2026-08-12/README.md).
      Serves 12 and 11.

**What is deliberately not on this list.** Kernel bridges for the existing
certificates (the `cas-internal` residue, W1-13 and W2-16) — those are the
*trust* axis and are tracked in file 11 and the roadmap. Measure theory,
topology carriers, and category theory — not CAS-shaped. Risch — behind item
4, and the July roadmap's own sequencing (LRT first) still holds.

## The blocker

**None of a mathematical kind. Two of an engineering kind, and one of
discipline.**

- **`i128` in the core.** Every module that needed more precision grew its
  own `BigRational` path beside the core, which is why eight modules carry
  bignum code and the zero-test does not. Item 1 removes the reason for the
  duplication.
- **`lib.rs` is 29,505 lines.** Items 3 through 9 each want a module; landing
  them into `lib.rs` would make the next audit's job harder than this one's.
  Land each as its own file with its checker beside it.
- **Every new function ships with its certificate or its `uncertified`
  label** (the crate's standing rule, [`10-cas/README.md`](../research/10-cas/README.md#standing-rules-for-this-initiative-inherited-non-negotiable)).
  Item 10 is what makes that rule measurable instead of promised.

## Progress log

| date | change | evidence |
|---|---|---|
| 2026-09-05 | File created. Baseline: 80,851 lines, 55 modules, 691 `pub fn`, 1,006 tests, `i128` core, `f64` `evalf`; residue 61/16/45. Ten absence probes run with four positive controls; the two single-file hits (`gaussian_int`, `schreier`) were a test name and an Artin–Schreier doc comment, not capability. Capability-parity work has been paused since wave 24 (2026-07-22); all CAS commits since 2026-08-26 are trust work. | `8f6c58420`; `python3 scripts/check-cas-internal-residue.py --report` |
| 2026-09-05 | **Item 3 landed, first slice** (lane `cas-fps`, merged `3e6dd2b46`): `fps.rs`, a truncated ℚ[[x]] over `BigRational` with inverse, composition, compositional reversion, exact rational-function expansion, recurrence unrolling, Berlekamp–Massey guessing (recovers Fibonacci, Lucas, Padovan; declines on the primes) and a certified rational generating function. Three `TruncationIdentity` certificates plus a `RecurrenceCertificate`; 18 guards, each killing exactly one test after the lane found five that rejected through each other on its first sweep. Reused `series_coefficients` (so no transcendental expansion was duplicated) and re-derived the reversion recurrence over bignums because the i128 one overflows on Catalan. Radius of convergence deliberately not shipped: the rational case needs complex roots and Sturm certifies only real ones. | `fps::` 48 passed; crate sweep 989 passed, 0 failed; clippy exit 0 |
| 2026-09-05 | **Item 10 landed, first half** (lane `cas-trust-registry`, merged `a23a289ec`): `scripts/check-cas-trust-registry.py` scans every `pub fn` with a brace-aware scanner that skips `#[cfg(test)]` bodies, derives the certificate vocabulary from the source rather than a list, classifies each function certified / checker / uncertified, and ratchets the certified set with six guards each killed by exactly one control. Registered in `check.sh`, the justfile and `mutation_controls.py`. **The number chair 12 asked for: 698 public functions at the lane's base, 34 certified (4.9%), 26 checkers, 638 uncertified; on merged main with `fps` accepted, 730 / 41 / 29 / 660.** Of the README capability table's 28 rows, 25 claim a certificate for functions that classify uncertified: the column was a self-check discipline claim, not a certificate object. The scanner found and fixed two of its own bugs against the live crate (impl-of-pub-type pass never wired; a `;` inside `Option<[T; 2]>` silently dropped the next signature) and now matches a whole-crate regex 698/698. The gate refused the seven new certified `fps` functions on first run, as designed; accepted with `--write`. `check-aggregate-scope.sh` exits 1 before and after, byte-identical divergence list, pre-existing. | `python3 scripts/check-cas-trust-registry.py` OK floor 41; `mutation_controls.py cas-trust-registry` 6 of 6 killed by exactly one; `artifacts/measurements/cas-trust-registry-2026-09-05.md` |
| 2026-09-05 | **Item 9 landed, first slice** (lane `cas-probability`, merged `2b431eb34`): `probability.rs`, eight named distributions whose mass, mean, variance and mgf each carry a certificate naming the route that decided it, or an honest `Uncertified(reason)`. Convolution tables certified; Poisson+Poisson proved for all k by `prove_wz_sum`, a stronger certificate than the single Poisson's own total mass, which declines. **Three machinery gaps the lane measured rather than assumed, each a follow-up for the CAS itself:** Poisson moments decline because `λᵏ/k!` is not Gosper-summable and `infinite_sum` does not know the exponential series; Geometric mean and variance decline because `limit` cannot cancel a `k/k` factor the telescoped antidifference carries; `Normal(0,1)` declines because `integrate_gaussian` requires a perfect-square coefficient, so only variance 1/2 certifies. Every symbolic-parameter continuous mgf declines. | `probability::` 24 passed; clippy exit 0; 4 guards mutation-checked |
| 2026-09-05 | **Item 8 landed** (lane `cas-homology`, merged `449fc1399`): `homology.rs`, exact integer Betti numbers and torsion for a simplicial complex via the existing Smith form, with a certificate whose `verify` rebuilds every boundary matrix and re-derives every claim; eleven guard conditions each killing exactly one test; verified on the standard triangulations through the 9-vertex Klein bottle. **The finding that outranks the feature: `smith_normal_form` never terminated on the boundary matrix of any 3-cycle**, so the shipped normal form hung on any complex with a triangle in it, and `bareiss_determinant` panicked on the empty matrix. Both fixed with regression tests in `normalforms.rs` and `matrix.rs`. The topology chair now has one thing to reach for. Wall clock not measured; the module doc says so. | `homology::` 22 passed; crate sweep 966 passed, 0 failed; clippy exit 0 |
| 2026-09-05 | **Item 2 landed** (lane `cas-enclosure`, merged `e86eb5a1f`): `enclosure.rs`, certified interval enclosures with exact `BigRational` endpoints to a requested precision over a binding box, for rational arithmetic, integer powers, `sqrt`, `exp`, `ln`, `sin`, `cos`, `atan` and π, each step of the evidence re-derived by `verify` from the head, inputs and truncation order alone; `enclose_root` refines a Sturm-isolated root with the isolation itself re-checked. Thirteen guards each kill exactly one test. Measured, ADVISORY on a host at load 29: π to 500 bits in 20 ms to produce, 15 ms to verify. The constructive-analysis chair's first complaint (a double and no enclosure) is answered for this head set; `gamma`, Bessel, `erf` and non-integer powers still decline, and `evalf` is unchanged. | `enclosure::` 34 passed, 2 doctests; clippy exit 0; hygiene PASS in the lane |
| 2026-09-05 | **Item 5 landed, first slice** (lane `cas-permgroup`, merged `399f294c9`): `permgroup.rs`, deterministic Schreier–Sims over the existing `Permutation` type with order, membership, orbits, stabilizers, cosets, Cayley tables, centre and derived subgroup, every result carrying a certificate that re-derives the claim; S₈ handled without enumeration. One of five guards was measured redundant and is documented as such rather than claimed load-bearing. Sylow, presentations and isomorphism testing remain open. | `permgroup::` 25 passed; clippy exit 0; 4 guards each kill exactly one test |
| 2026-09-05 | **Item 7 landed, first slice** (lane `cas-qe`, merged `0bcb59f09`): `qe.rs`, one-variable one-quantifier real arithmetic over polynomial atoms decided by a sign-invariant cell decomposition, a `true` carrying an exact sample point and a `false` carrying the whole decomposition with a Sturm recount proving no root was omitted; nine guards each kill exactly one test. Two honest notes from the lane: the registry did not see the module until the certificate got its own `verify`, which is the right fix and not a workaround; and `x² − 10³⁰` returns `Unknown` because the reused root isolation is still `i128`, which is exactly the wall item 1 is removing. Multivariate and CAD remain open. | `qe::` 20 passed; clippy exit 0; cost profile not measured |
| 2026-09-05 | **Item 4 landed, first slice** (lane `cas-numberfield`, merged `b8ebdb38a`): `numberfield.rs`, exact arithmetic in ℚ(α) with certified inverse, norm, trace and element minimal polynomial; ℤ[i] with Gaussian factorization; sums of two squares both ways with a re-checked Pratt refutation; units of ℤ[√d] from continued fractions, minimality exhaustively certified below b = 10000 and honestly labelled above. Eleven guards each kill exactly one test. **The lane corrected this file's own brief**: the famous d = 61 number is the Pell solution, not the fundamental unit, which is 29718 + 3805√61 and whose square it is; both ship. Ideal factorization, class numbers, Galois groups and multivariate factorization remain open. The lane also found the crate's rustdoc red under `-D warnings` from three links in `homology.rs` and `probability.rs`, fixed the same day. | `numberfield::` 65 passed; crate sweep 1103 passed; clippy exit 0 |
| 2026-09-05 | **Item 1 landed, first slice, and the design is decided** (lane `cas-bignum`, merged `72a5a68d3`, ADR-1670 Proposed): `equal` retries an i128-overflowed normal form over the existing `BigPoly` under an explicit work budget, so `(x+1)^100` squared, Catalan-sized generating-function products and rational-function splits go Unknown → certified and a false identity at degree 160 goes Unknown → refuted, with two negative controls pinning that nothing which decided before changed in verdict or witness. **The measurement overturned the brief's framing**: with the algorithm held fixed, the unbounded ring costs 1.09× at degree 8 and 0.94× at degree 64, because `Rational::checked_mul` runs a gcd per coefficient and the integer ring runs none. So the decision is `Rational` at the API boundary and unbounded arithmetic underneath, not a fast path with a slow fallback; wholesale migration is priced at 71 public signatures and rejected as migrating the wrong thing. Remaining asymmetry, wave two's first job: equalities always decide, inequalities only when the difference still fits i128, because the witness type is still bounded. Five guards mutation-checked, the budget guard demonstrably firing (29.6 s against a 3–5 s baseline). Also this day: the crate's rustdoc was red under `-D warnings` from ten private-item links across four new modules; fixed in `5687d229e`. | crate sweep 1052 passed in the lane; clippy exit 0; registry unchanged at its floor |
| 2026-09-05 | **Item 6 landed, first slice** (lane `cas-geometry-beyond`, merged `bb999de1f`): `geometry_beyond.rs`, conics as quadratic forms with a certified conic through five points, 3D points, planes, lines and spheres, homogeneous coordinates with join and meet, and isometries with a distance-preservation certificate decided symbolically. Three theorems certified through the existing cofactor route and added to the corpus with artifacts (tetrahedron circumcentre, polar-is-tangent, tetrahedron medians); the artifacts suite the ledger pins goes 12 → 13. **Two findings**: the medians search costs 769 s because the certifier's linear-block detector is scoped per conclusion, so its unit test is `#[ignore]` with the reason and the theorem is re-checked from the artifact instead; and Pascal and Desargues decline under a small bounded search, recorded as declines rather than claimed. A certificate with zero negative coverage got its forgery test. | `geometry_beyond::` 31 passed, 1 ignored; artifacts 13 passed; clippy exit 0 |
| 2026-09-05 | **Close-out of the first pass over the Next Ten.** Ten lanes dispatched (five Opus, five Sonnet), ten merged; every item has code on main except the parity-corpus half of item 10. Re-measured at `bb999de1f`: 98,586 lines, 63 files plus 4 subdirectories, 975 `pub fn` (430 at column 0), 1,296 tests, `lib.rs` 30,311 lines, trust registry **78 certified** (34 when the gate was built), residue unchanged at 16 reconstructed / 45 `cas-internal`. Three lanes stalled on the close-out waiting for background jobs and were resumed with foreground instructions; one had its gates taken over by the coordinator. Two coordination incidents with peer sessions (a merge during another session's push; another session's commit absorbing an unresolved merge) were recovered with nothing lost and now have guards. Follow-ups the lanes surfaced, each a candidate row for the next pass: the exponential series in `infinite_sum` (Poisson moments), the `k/k` cancellation in `limit` (Geometric moments), the perfect-square precondition in `integrate_gaussian` (`Normal(0,1)`), the bounded witness type in `ZeroTest` (inequalities past i128, ADR-1670 wave two), the per-conclusion scoping of the certifier's linear-block detector (3D concurrency theorems), and Cauchy-bound overflow in the i128 root isolation (`qe` at 10³⁰). | `python3 scripts/check-cas-trust-registry.py`: OK 78 certified (floor 78), 59 checker, 845 uncertified |
| 2026-09-05 | **Post-merge hang found and fixed** (`17aef565e`): the geometry-beyond merge added `tetrahedron-medians-concurrent` to the corpus, and a pre-existing corpus-walking test in `geometry_certify.rs` reran Buchberger on it, so the debug crate sweep sat for an hour with one core spinning and no failure, the exact hazard that test's own comment predicts. Bisected from the prebuilt binary run single-threaded with a log because the debugger cannot attach under this host's ptrace policy; the entry joins the test's skip list with the reason. Two things this teaches the next pass: a corpus-walking test with a literal skip list turns every expensive addition into a hang rather than a failure, so the entry type should carry its search cost and the test should read it; and a lane's "0.65 s" measured only its own module, so the merge-time gate must be the whole-crate sweep, which is now what the coordinator runs. | route-selector test 4.18 s; crate sweep 1228 passed, 0 failed, 6 ignored; artifacts 13 passed; clippy exit 0; workspace check exit 0 |
| 2026-09-05 | **Second pass, docs** (lane `cas-docs-refresh`, merged `67a254659`): the July roadmap under `docs/research/10-cas/` is now marked historical with dated banners and points here; its README header is re-measured and its capability table's Certified column is restated per row as certificate object / self-check / uncertified so the registry can falsify it (2 rows agreed as written, 3 earned self-check on reading the code, 20 are uncertified). The parity handoff's stale "not integrated" sentence is corrected. **Registry follow-up found**: `CertifiedGosperSum` and `CofactorOutcome` are real certificates the suffix rule misses; the vocabulary derivation should also admit any type with a `verify`/`check` method or a `Certified*` prefix. | `check-links.sh` all links ok; no Rust touched |
| 2026-09-05 | **Item 7, wave two** (lane `cas-qe-2`, merged `969f795d7`): a `BigRational` Sturm engine (`qe_big.rs`) removes the i128 wall inside `qe`, so `x² − 10³⁰` and `x² − 10⁶⁰` decide with exact samples and the legacy route, kept under `cfg(test)`, agrees on the whole first-slice corpus; disjunctions become a first-class object with per-disjunct refutations (`qe_dnf.rs`); and the first bivariate step lands (`qe_bivariate.rs`): `∃y` over a conjunction at total degree ≤ 4 by projection through the Sylvester engine, a sign-invariant x-cell decomposition, and a re-derived fibre certificate per cell. Eight guards, zero mutation survivors. **Named declines that are the next bridge**: a point cell with an irrational boundary needs ℚ(α) arithmetic, which `numberfield.rs` now has; a projection where two atoms share a y-factor is a genuine delineability failure. Correction to the brief: the public `resultant` returns a constant, so the lane called the bivariate Sylvester engine beneath it. | `qe` 63 passed; crate sweep 1271 passed in the lane; clippy and rustdoc exit 0; cost at degree 4 under 1 ms, at 10⁶⁰ 85 ms (ADVISORY) |
| 2026-09-05 | **Item 3, wave two** (lane `cas-fps-2`, merged `234eadd32`): `fps_analytic.rs` gives the radius of convergence of a rational generating function exactly wherever every denominator factor is real-rooted or quadratic (root moduli via `f(t)f(−t)` and `√(c/a)`, composed and Sturm-isolated, with a Bézout pair witnessing coprimality so a cancelled pole cannot understate it) and a proved LOWER bound otherwise: `1/(1+x²)` now gives exactly 1, `Φ₅` honestly gives 1/2 where the truth is 1. Coefficient asymptotics are labelled finite-n evidence with the exponent derived, not fitted. `fps.rs` gains P-recursive guessing that recovers factorial, Catalan, central binomial and Motzkin and declines on the primes. 26 guards each kill exactly one test, after the sweep found one guard no test covered. **What remains for item 3**: the modulus polynomial of a general complex-rooted factor (a resultant construction), transcendental singularities, multivariate series. | `fps` 63 and `fps_analytic` 41 passed; clippy and rustdoc exit 0; ratchet 84 certified |
| 2026-09-05 | **Item 10, second half landed** (lane `cas-parity-corpus`, merged `bf1c8f46d`): a 71-entry SymPy parity corpus with ground truth that imports nothing from this repository (75 claims under SymPy 1.14.0, 32 without it), every identity paired with a near-miss control, 12 entries expected to decline, and a harness that classifies each answer's trust from its return type and exits nonzero on a wrong answer; registered in check.sh, the justfile's `check` recipe and as `bench-cas-parity` (about 300 ms). **Its first run found a soundness defect**: `equal(√2·√3, √6)` returns `Certified { equal: false }` because the zero-test keys distinct radicals as independent atoms, so the label claims a refutation the normal form never established. It sits in a `known_defect` tier whose disagreement the harness asserts persists, so the fix (with lane `cas-witness`, in the radical fold) will force its reclassification; the aggregate gate stays green meanwhile. Three corpus design errors surfaced only by running it and are corrected in the README. First-run tally: agree 70, decline 0, known defect 1; trust certified 54, uncertified 11, unknown 6. | harness exit 0 in the lane; `ground_truth.py` 32 claims 0 failed on main; scope check clean for these two steps |
| 2026-09-05 | **Item 5, wave two** (lane `cas-permgroup-2`, merged `09f915f31`): conjugacy classes, Sylow p-subgroups with their conjugate counts (both Sylow congruences checked before the recount), an `Invariants` tuple with `distinguish` documented as inconclusive on agreement, and `is_normal`, `normal_closure`, `quotient_order`, each certified by recomputation; S₄, A₅ and the D₄ versus Q₈ separation verified against an independent brute-force reference before coding. Every test under 5 s single-threaded. **Still open for item 5**: isomorphism testing proper, presentations, matrix groups. | `permgroup` 50 passed; clippy and rustdoc exit 0; ratchet 92 certified |
| 2026-09-05 | **Item 4, wave two** (lane `cas-numberfield-2`, merged `65ea377a3`): `numberfield_ideals.rs`, the ring of integers of any quadratic field against one relation `ω² = tω + n`, ideals as canonical Hermite triples, prime splitting by Dedekind's criterion cross-checked against the Kronecker symbol, ideal factorization within the i128 factorizer's reach, class numbers of negative discriminants below 10⁷ whose verifier recounts by a different traversal, and a primitive element for ℚ(α, β) with α and β written back in θ. The ℤ[√−5] non-unique-factorization example runs end to end. A 43-site mutation sweep removed one genuinely redundant guard, added three forgeries, and reordered three guard sets so none was unreachable. **First-slice defect fixed**: ℚ(i) was not constructible because `QuadraticField::new` refused d = −1. **Still open for item 4**: composition of forms (the class group is a set, not a group), real quadratic fields, rings of integers past degree 2, Galois groups, multivariate factorization. | `numberfield` 135 passed, slowest 0.04 s; crate sweep 1298 passed in the lane; clippy and rustdoc exit 0 |
| 2026-09-05 | **Item 9, wave two: the machinery gaps** (lane `cas-sum-gaps`, merged `b2cb7ee39`): `infinite_sum` recognizes the exponential series with both reconstruction steps decided by `equal` and near-misses declining; `integrate_gaussian` carries an irrational `√a`, which needed `simplify_radicals` to split `√(c·u)` and `prove_derivative` to retry under surd normalization; nine `probability.rs` claims flip to certified (Poisson with symbolic λ including the mgf, Geometric moments, `Normal(μ, 1)`). **Gap (b) did not reproduce**: Gosper's geometric path is pole-free and `limit` cancels `k/k` on the live crate, so the Geometric decline was the summand's spelling, now pinned by a test. Two fail-closed self-checks survive mutation and are recorded as such. **Still uncertified in item 9**: every symbolic-parameter continuous mgf and Exponential with symbolic λ (`to_univariate` needs a concrete coefficient), Geometric with symbolic p (convergence needs a concrete ratio). | crate sweep 1234 passed in the lane; `cas-summation-and-gaussian` 6 of 8 guards kill; clippy and rustdoc exit 0 |
| 2026-09-05 | **Item 6, wave two, and a correction to this file** (lane `cas-certifier-scope`, merged `632ae2769`): corpus entries now carry their search cost and the corpus-walking test derives its skip set from them and fails in 15 s with the entry's id, replacing the literal list added at `17aef565e`; the linear-block detector searches over a theorem's conclusions jointly with the determinant filter inside the search, which moves two plane theorems (centroid-divides-medians, parallelogram-diagonals-bisect) from Buchberger to a millisecond route with byte-identical certificates. **The diagnosis recorded in the item 6 row, the hang-fix row and the close-out row above was wrong**: per-conclusion scoping is a real limitation but is not what keeps the tetrahedron medians slow. Measured exhaustively (400,000 subsystems): every 3×3 block solves the medians in microseconds, and none has a determinant licensed by the non-coplanarity condition, because with median directions u, v the coefficient matrix is two rank-two skew matrices and every minor is a coordinate of u or v times a component of u × v; inverting the first factor would certify a weaker theorem, which the certifier rightly refuses. **What comes next for item 6**: a mechanism that combines several elimination identities so a power of the stated condition, not any single determinant, is what gets inverted; Pascal and Desargues still decline promptly for a residue on the collinearity conclusion. | geometry 68 passed, 1 ignored; artifacts 13 unchanged byte for byte; 7 guards each killed; clippy, rustdoc, hygiene, registry green in the lane |
| 2026-09-05 | **Item 1, wave two** (lane `cas-witness`, merged `68226ffea`): `ZeroTest::CertifiedBig` carries an unbounded witness and appears only where `Unknown` was returned before, so no prior verdict changed; all six fold passes have unbounded twins, so inequalities and atom-bearing identities at overflow scale decide, and `exp` and atom-bearing refutations decline with tested reasons; `recheck_zero_test` lets a caller re-derive the difference and refuse a wrong witness; `MultiPoly::pow` is binary at 2.97× on degree 64 (the ADR predicted 2.3×). **The parity corpus's radical finding was eight wrong-refuted identities, not one**, from `√4 = 2` to `|x|·|y| = |xy|`; constant radicands now canonicalize by their squarefree part at intake, constant radicals fold together in both rings, and a refutation whose difference still multiplies distinct radical, abs or root atoms declines into `Unknown`. The corpus entry was reclassified to core by the harness's own rule, and a second entry (Poisson total mass) was reclassified the same day because the summation lane made it certify. Twenty-one exhaustive `ZeroTest` matches gained an arm, six of them outside the crate. **What remains for item 1**: `exp` in the unbounded ring, the cross-family radical case `√2·∛2` (flagged, not fixed), the ADR's off-by-one on the degree-131 wall, and the resource-limited `(x+1)^4096`. | crate sweep 1451 passed in the lane; clippy, rustdoc, workspace check, hygiene green; parity harness 71 agree, 0 disagree on merged main |
| 2026-09-05 | **Item 8, wave two** (lane `cas-homology-2`, merged `ad4e8adee`): homology over 𝔽₂ and ℚ with the universal-coefficient identity as a guard against the ℤ answer, integer cohomology from an independently re-derived transposed Smith form with the cohomology UCT as a guard, induced maps on H_k(ℚ) of simplicial vertex maps certified by rank and a from-scratch rebuild, and persistent homology over 𝔽₂ with the reduction re-run and the Euler characteristic checked at every step. ℝP² reads (1,1,1) over 𝔽₂ and (1,0,0) over ℚ; the degree-2 circle map induces multiplication by 2. One guard has no forgery test and is named as a gap. **What comes next for item 8**: cohomology with other coefficients, induced maps over ℤ and 𝔽₂, persistence over other fields, and the cofactor unimodularity ceiling on larger complexes. | `homology` 51 passed in 2.7 s; clippy and rustdoc exit 0; ratchet 107 certified |
| 2026-09-05 | **Item 2, wave two** (lane `cas-enclosure-2`, merged `716f4e57a`): rational powers by Newton with the AM–GM bracket and by exp(ln), tested to agree; `erf`, `Gamma` (exact at integers and half-integers, Stirling-enveloped elsewhere, so Γ(1/3) encloses) and Bessel `J_n`, each with an exactly computed index from which its tail bound is valid; and multivariate root enclosure by the Krawczyk operator whose verifier re-derives the strict inclusion, twelve guards each killing exactly one test. **A performance finding every consumer of computed intervals will hit**: exact-rational series kernels cost quadratically in the denominator size; outward dyadic rounding of kernel arguments (sound, loses only accuracy) took Γ(1/3) at precision 100 from 26 s to 1.5 s. **What remains for item 2**: `Si`, `Ci`, `Ei` and the other integral-defined heads, `pow` with a symbolic exponent, and Krawczyk over more than a handful of variables. | `enclosure` 84 passed, largest test 1.3 s debug; clippy, rustdoc, hygiene exit 0 |
| 2026-09-05 | **Item 3, wave three** (lane `cas-fps-3`, merged `5a57ca9d3`): the exact radius of convergence for any denominator factor of degree ≤ 6 by the pairwise-product resultant, with the proof that its smallest positive root is the squared minimum modulus outright, so the brief's spurious-root rejection was unnecessary; Φ₅ and Φ₇ give exactly 1, and the lower-bound label now needs a degree-7 factor, a measured cost cap rather than a gap. A `BigRational` Sturm chain backs the i128 one where it declines, tested to agree on 54 pairs. **What remains for item 3**: raising the degree cap is a measurement; transcendental singularities and multivariate series are still out; the asymptotic amplitude `C` is still sampled, not exact. | `fps` 122 passed, slowest 1.25 s debug; crate sweep 1507 passed in the lane; clippy, rustdoc, hygiene green |
| 2026-09-05 | **Item 4, wave three** (lane `cas-numberfield-3`, merged `b73f5db48`): composition of forms checked on the ideal side over ℤ rather than by re-running the formula, the class group as a certified finite abelian group (table, identity, inverses, orders, invariant factors), the form–ideal correspondence both ways, and principality as a certificate with a checkable reduction, which closes the item wave two left uncertified: (2, 1+√−5) is certified non-principal. D = −84 gives ℤ/2 × ℤ/2 and D = −95 gives ℤ/8; the group axioms run before the recomposition so a table forgery hits a reachable guard, and the lane found that at h ≤ 4 no forgery survives the axioms at all, so the recomposition guard is tested at h = 8. **What remains for item 4**: real quadratic class groups (regulator), non-maximal orders, genus theory, degree > 2, and h > 200. | `numberfield` 207 passed in 0.33 s; crate sweep 1500 and workspace check green in the lane; clippy and rustdoc exit 0; ratchet 115 certified |
| 2026-09-05 | **Item 7, wave three** (lane `cas-qe-3`, merged `ecbb080c6`): irrational cut points are now point cells decided in ℚ(α) with exact Sturm chains and sign determination, the modulus split on demand (D5) so no factorization over ℚ is needed and soundness never rests on irreducibility; the bivariate step emits a quantifier-free union of maximal intervals with algebraic endpoints, checked four independent ways; an atom with a repeated y-factor is handled by a pseudo-division square-free fallback instead of refused. Sixteen guards, four of which survived the first mutation pass until adversarial certificates were written. **Correction to this file's brief**: `numberfield.rs` was not the bridge, because its field constructor demands an irreducible modulus from the i128 factorizer; on-demand splitting is the sound route. **What remains for item 7**: lifting to a third variable (no cell-to-plane phase), quantifier alternation, two atoms sharing a y-factor, transcendental atoms; irrational boundaries cost about 60× rational ones. | `qe` 94 passed, slowest 3.1 s debug; clippy, rustdoc and workspace check exit 0; ratchet 117 certified |
| 2026-09-05 | **Item 10, wave two** (lane `cas-trust-2`, merged `aaa931f81`): the registry's vocabulary admits `Certified*`-prefixed types and tightens the verify/check rule to a `self` receiver returning `Result` or `bool`, with negative fixtures, so three more real certificates reclassify (Gosper and the squared-binomial moments); `CofactorOutcome` meets neither rule and is reported, not forced. The parity corpus grows 71 → 119 with at least three entries per second- and third-pass module and independent ground truth: 119 agree, 0 disagree, 0 known defects; trust split 99 certified, 14 uncertified, 6 unknown. **Finding**: the JSON ledger still tiered the radical entry `known_defect` after the harness had reclassified it, so the two ledgers had diverged; fixed, and id-set parity is now checked programmatically. **What comes next for item 10**: a rule for evidence enums like `CofactorOutcome`, and corpus growth as the fourth and fifth waves land. | registry OK floor 120 (10 guards each killed by exactly one control); `ground_truth.py` 75 claims 0 failed on main; harness on main pending |
| 2026-09-05 | **Cross-cutting: the shared exact-arithmetic crate is designed** (lane `arith-design`, merged `77fbcdf14`, ADR-1710 Proposed, `docs/research/10-cas/axeyum-arith-design.md`): reading every implementation rather than grepping found eight univariate polynomial layers, six Sturm chains with two normalization conventions, three extended Euclids that do not know of each other, five `pow_mod`s and six exact Gauss–Jordan routines, most of them written this very day by lanes that could not see each other's private copies. The kernel finding matters for item 1: the accelerated `Lit::Nat` path needs both operands to be literals already and no prelude builds one, so it never fires on prelude terms; a `RadixCertificate` (now implemented, the Rust twin of `ofDigits_digits`) is the crate's half of a numeral bridge, and the other half is a prelude lemma of the `digits_succ` shape. Ecosystem, cited: `num-bigint` stays (malachite is LGPL and `deny.toml` refuses it; `num-bigint` 0.5 is pinned out by `num-rational`; dashu deferred pending a benchmark that does not exist yet). `crates/axeyum-arith` lands with `Dyadic` and `Radix` implemented and tested and the rest signature-only; nothing depends on it yet. **Migration order**: workspace deps, `Dyadic` into enclosures, `ModRing`, `ZPoly`, `QPoly` plus Sturm in five sub-slices, Bézout, `PA = LD⁻¹U`, Hensel, `AlgebraicNumber`. | `axeyum-arith` 25 tests, check and clippy exit 0, links and hygiene green in the lane |
| 2026-09-05 | **Item 8, wave three, and a correction to the brief** (lane `cas-homology-3`, merged `bc259ebc9`): the unimodularity ceiling was not Bareiss (it handled a 588×588 transform in seconds) but `Matrix::mul`'s symbolic path inside the Smith product certification; an integer fast path with symbolic fallback takes a 196-vertex grid torus from over a minute to about 13 s in release. Induced maps on integer homology including torsion (the identity on ℝP² inducing 1 on ℤ/2 caught a row-range indexing bug), persistence over ℚ and 𝔽ₚ (ℝP²'s torsion bar appears over 𝔽₂ only), S⁰ through S⁴ and the Möbius band as fixtures, and the previously untested guard got its forgery. Three fixtures (3-torus, dunce hat, an antipodal covering map) were descoped rather than hand-built without an independent check. **Honest gap**: the formal mutation tool was not run; each forgery test states the guard it isolates. **What remains for item 8**: the cup product, relative homology, the Smith reduction's own cost (about 6 s of the 13). | `homology` 68 passed, every test under 0.07 s (one release-only ignored); clippy and rustdoc exit 0; ratchet 125 certified |
| 2026-09-05 | **Item 9, wave three** (lane `cas-symbolic-mgf`, merged `d759eadff`, post-merge fixes `ef0ceb201`): the integration route carries a symbolic coefficient of the integration variable, proves the by-parts antiderivative, and records only what the boundary assumed, so `Trust::CertifiedUnder(hypotheses)` certifies a smaller statement rather than a weaker one: Exponential with symbolic λ (mass, mean, variance, mgf λ/(λ−t) under λ − t > 0), Uniform's mgf under t ≠ 0 with the removable singularity excluded rather than claimed, and Normal's mgf by completing the square onto a shifted-mass certificate. A dropped hypothesis is refused by tag comparison; two guards that mutation showed unfalsifiable through the public route got seams. The lane found the crate doctest red since the witness merge (fixed) and `ConditionalIntegral` invisible to the registry (fixed, floor 126). **Still declining, with the failing input recorded**: Normal with symbolic variance (no √a to build), Geometric with symbolic p (Gosper declines before any convergence question). | crate sweep 1581 passed in the lane; `probability` 37; clippy, rustdoc, registry green; corpus 123 entries |
| 2026-09-06 | **Item 7, wave four** (lane `cas-qe-4`, merged `ab7a5a088`): a bounded three-variable CAD with one existential at total degree ≤ 3, lifting each x-cell's sample to plane cells and deciding the z-fibre over ℚ(β) when the y-sample is algebraic, with Collins' projection theorem applied twice and stated; and the first alternations, `∀x∃y` certified by the interval union being the single interval (−∞, ∞) and `∃x∀y` by complementing an inner DNF existential. The tower ℚ(α)(β) is declined by name, and every other cell still decides. Measured: cost follows the projection set's shape, not algebraicity. Twelve guards singly falsifiable; 37 more fault variants have forged tests but were not individually deleted, and the report says so. **What remains for item 7**: the field tower, adjacency across levels, a fourth variable, a second alternation, and the lift's cylinder as an input to alternation. | `qe` 162 passed, slowest 1.7 s debug; clippy, rustdoc, workspace check, hygiene green in the lane |
| 2026-09-06 | **Item 4, wave four** (lane `cas-numberfield-4`, merged `6c864e9f6`): real quadratic fields — indefinite reduction and ρ-cycles give h⁺, and h by three routes that must agree (a unit-free principal-cycle test, the certified unit's norm sign, an ideal-side count), all under a millisecond at D = 10⁴; the regulator as a certified enclosure whose checker rebuilds the subject from (d, a, b). 28 of 28 guards singly falsifiable. **Corrections to this file's brief**: 79 is not a discriminant (D = 316), and the regulator's cost lives in `enclosure`'s `sqrt` and `ln` kernels, which lack the outward dyadic rounding that fixed Γ, so d = 61 reaches 19 digits, not 30 — a named follow-up for item 2. **What remains for item 4**: composition in the real case (indefinite reduction of an arbitrary form), the unit index for d ≡ 1 mod 4, non-maximal orders, degree > 2. | `numberfield` 247 passed; crate sweep 1746 in the lane; clippy and rustdoc exit 0 |
| 2026-09-06 | **Item 6, wave three: the named mechanism landed** (lane `cas-elim-combine`, merged `ec716a889`): several elimination identities are combined by rounds so a power of the stated non-degeneracy condition, not any single block determinant, is what gets inverted; the tetrahedron medians reach Δ² in seven rounds and certify on the linear route in 1.1 s release instead of 769 s on Buchberger, with the checker unchanged (it already divides powers out) and every other artifact byte-identical. The route declines in under 30 ms on the twelve theorems that do not need it. Pascal and Desargues still decline on the collinearity residue, and a regression the route briefly caused on them is bounded away. **Honest costs**: the medians debug test takes 9 to 15 s, over the 5 s budget; one self-check is killed by no test and is labelled so. **What remains for item 6**: Pascal and Desargues need linear algebra to settle the conclusion outright, which it does not; a wider candidate scope; cheaper large-determinant GCDs. | geometry 75 passed; artifacts 14; clippy, rustdoc, registry floor 126, hygiene green in the lane |
| 2026-09-06 | **Item 5, wave three** (lane `cas-permgroup-3`, merged `0db8a5b03`): `isomorphism(G, H)` decides isomorphism up to order 2,000 by invariants first and then a backtracking search over generator images pruned by element order and class size, accepting only when every Schreier relator holds and the image has full order, so an exhausted search is a re-derivable non-isomorphism proof; `presentation()` derives relators from the BSGS and certifies them by a bounded Todd–Coxeter enumeration closing at exactly |G|, documented as the module's one weaker trust level because the table replays the same engine. Mutation found one redundant guard and one guard nothing isolated (the order cross-check, now pinned by a D₄ fixture where three dropped relators close at 16 instead of 8); a Todd–Coxeter bug was fixed on the way. **What remains for item 5**: matrix groups, presentations and isomorphism past the bounds, and the two guards not mutation-checked for lack of time. | `permgroup` 72 passed, slowest 3.1 s; clippy and rustdoc exit 0; ratchet 138 certified |
| 2026-09-06 | **Cross-cutting: ADR-1710 migration slices 0 and 2** (lane `arith-slice-1`, merged `8fa1b9340`): the `num-*` crates and `axeyum-arith` are workspace dependencies at the versions already resolved (Cargo.lock changed by one edge, no version bump); `PlainModRing` and `PowModCertificate` land in `axeyum-arith` with a chain-replaying verifier and four singly-falsifiable guards; the two integer `pow_mod`s in `ntheory.rs` delegate to it behind 200-triple differential corpora. **Correction to the design inventory**: the "five copies" were three — the `gfp.rs` and `factor_int.rs` routines exponentiate polynomials modulo a polynomial over 𝔽ₚ, a different structure, documented at both sites; the oracle copy stays independent. Also found: the legacy code's own domain is moduli below 2¹²⁷. **Next slices**: `Dyadic` into the enclosure modules once enclosure-3 merges, then `ZPoly` and the five-way Sturm consolidation. | `axeyum-arith` 29 and `ntheory` 78 passed; clippy, rustdoc, workspace check and wasm exit 0 in the lane |
| 2026-09-06 | **Item 2, wave three** (lane `cas-enclosure-3`, merged `e9f47afc5`): twelve more heads (`Si`, `Ci`, `Ei`, `li`, `Shi`, `Chi`, both Fresnel integrals, `asin`, `acos`, `asinh`, `acosh`) plus Euler's constant, each with an exact remainder whose validity index is computed rather than assumed and each checked against 30 cited digits; symbolic exponents on positive bases; and certified definite integrals by Simpson with a fourth-derivative error term, box rule as the sound fallback. Fifteen guards each kill exactly one test after one control was found to be decoration and repaired. The lane ran the workspace check because it added public enum variants. **What remains for item 2**: complex arguments, asymptotic bounds past magnitude 20 (declined rather than claimed), improper and multivariate integrals, and the outward dyadic rounding in the `sqrt` and `ln` kernels that the real-quadratic regulator needs. | `enclosure` 129 passed; clippy, rustdoc and workspace check exit 0 |
| 2026-09-06 | **Item 8, wave four** (lane `cas-homology-4`, merged `a1c5c7c76`): relative homology of a pair with the long exact sequence checked as a rank identity at every node, and the cup product over 𝔽₂ and ℚ by the Alexander–Whitney diagonal, which separates the torus from S¹ ∨ S¹ ∨ S² where Betti numbers cannot and confirms ℝP²'s 𝔽₂ generator squares to nonzero. The Smith pivot change is recorded as a measured negative result (7.02 s to 6.91 s, within noise, because boundary entries are already ±1). **Honest gap**: one guard mutation-checked, eleven not, named in the lane report; the coordinator's merge-time sweep covers them or records which survive. **What remains for item 8**: Steenrod squares, simplicial maps between pairs, homotopy invariants (the dunce hat is still a point to this module), and the Smith reduction's own iteration count. | `homology` 90 passed in 0.55 s; clippy and rustdoc exit 0 |
| 2026-09-06 | **Item 5, wave four** (lane `cas-matgroup`, merged `5685a328a`): finite matrix groups over 𝔽ₚ as certified permutation groups through the vector and projective actions, so every permutation-group certificate applies unchanged: the classical orders of GL and SL at small p and n, PSL(2,5) ≅ A₅ and PSL(2,7) ≅ GL(3,2) found by the isomorphism search, SL(2,3) ≇ D₁₂ by invariants, and honest declines at the enumeration bounds. Mutation in a snapshot closed five test gaps, one of them the only guard on an empty generator list. **What remains for item 5**: matrix groups over ℚ and ℤ, characteristic-polynomial invariants, representation theory, and the bounds. | `matgroup` 26 passed, slowest 0.96 s; clippy and rustdoc exit 0 |
| 2026-09-06 | **Item 8, the guard sweep the previous row owed** (lane `homology-guards`, merged `3cc85756e`): all eleven guards now kill exactly one test each; four had killed nothing and got adversarial fixtures (a forged relative boundary matrix, the H_k(K) and H_k(L) exactness nodes, and the graded-commutativity check called directly with a wrong sign, which is unreachable through a certificate forgery once earlier guards hold and is kept as a guard against an implementation bug). No guard was dead. | `homology` 96 passed; clippy, rustdoc, registry floor 145 unchanged |
| 2026-09-06 | **Item 1, wave three** (lane `cas-witness-2`, merged `ffb32f715`): the cross-family radical case was three more wrong refutations (`root_6(4) = ∛2`, `root_4(4) = √2`, `root_6(8) = √2`), fixed by a k-free canonicalization that reduces the root index by the exponent gcd and merges a monomial's constant radicals over the lcm of their indices; a parity guard no test covered was found by mutation (without it `√(−4) = −2` certified true). The `exp` decomposition was built, converted every tower law at overflow scale, and was NOT shipped on measurement: an opaque `exp` atom alone costs 20× on variation of parameters because the fallback cannot tell an i128 overflow from an out-of-fragment expression, so exp-bearing inputs enter it and spend its budget; ADR-1670 records this as wave four's first job. The ADR's degree wall was off by one from a probe that stepped by two; corrected with the method. **What remains for item 1**: a fallback entry condition that distinguishes overflow from out-of-fragment (then `exp`), radicands past the trial-division bound, root indices past 64. | crate sweep 1926 passed, doctests 168, parity 121 agree; clippy, rustdoc, workspace check, hygiene, links, registry green in the lane |
| 2026-09-06 | **Item 9, wave four** (lane `cas-sum-gaps-2`, merged `de37ceb08`): the last two probability declines fall — Geometric with symbolic p (mass, mean, variance, mgf) under the recorded hypotheses 0 < p < 1, and Normal with a symbolic variance (now a `CasExpr`) under σ² > 0 through a proved Gaussian antiderivative that carries √σ² symbolically; decidable conditions are decided rather than recorded and a false one is refused. One of ten mutants survives and is documented with a direct seam. **Zero-test defect found**: `exp(−((1/2)/s)·u²)` and `exp(−u²/(2s))` are keyed as different atoms and refuted, the same class as the radical fixes; the next zero-test lane owns it. **What remains for item 9**: Gaussians with a linear term or finite bounds, geometric summands with the index in a denominator, symbolic-parameter convolution. | crate sweep 1937 passed; parity 126 entries, 124 agree; clippy, rustdoc, workspace check, hygiene green in the lane |
| 2026-09-06 | **Item 1, `axeyum-arith` slice 3** (lane `arith-slice-3`, merged `35e4aff98`): `ZPoly`/`QPoly` with a fraction-free layer, one Sturm chain, a Bézout certificate that re-multiplies its identity and a Sturm count a reader re-derives; four of the five CAS copies (`qe_big`, `fps_analytic`, `numberfield`, the ℚ[x] layer of `qe_fibre`) now route through the crate with no call-site or `None`-condition change, and nine orphaned helpers are gone. The i128 `sturm.rs` stays on purpose: its differential tests compare it against the bignum chain, which is now the shared one, so migrating it would make them vacuous; its oracle grew instead. Thirteen guard mutants killed; one decorative guard found and rewritten. Cost within 1.14x per family at a 15-run minimum (a 5-run minimum reported a 2.14x regression that did not exist). **What remains for slice 4**: `ir/poly.rs`, `ratint.rs`, `qe_fibre`'s K[y] layer, and `ir/poly_big.rs` (a public IR change, `BigAlgebraic` is reachable from `axeyum_ir::Value`, so it needs its own ADR paragraph). | arith 59 passed; cas sweep 1965 passed; clippy, fmt, rustdoc, workspace check, parity, hygiene, registry green in the lane |
| 2026-09-06 | **Item 1, wave four** (lane `cas-witness-3`, merged `7278d6404`): the exp-spelling defect was three wrong refutations, not one. Transcendental atoms were keyed on the printed form of an unreduced rational function, so every one of the 34 unary heads could refute a true equality; the key is now a canonical form (polynomial factors cancelled by the existing multivariate gcd, then both sides divided by the denominator's signed content), `normalize_exp` canonicalizes before choosing its route, `ln(x/2) = ln((1/2)·x)` certifies, and a content beyond i128 now declines instead of refuting. Five mutants registered as `cas-atom-key`, all killed. The parity README's counts had drifted from `corpus.json` (119 recorded against 123 present) and were corrected. **What remains for item 1**: the fallback's overflow-versus-fragment entry condition (then `exp`), and a checker that fails when the README's counts drift from the corpus. | crate sweep 1861 passed in the lane; parity +2 agree; clippy, rustdoc, workspace check, registry, hygiene green in the lane |
| 2026-09-06 | **Item 10, checker for the parity ledgers** (lane `cas-corpus-counts`, merged `06e18899c`): the parity corpus has three copies of the truth (`corpus.json`, the Rust harness, the README's counts) and twice this week the README drifted with nothing failing. `scripts/check-cas-parity-corpus.py` now fails on a stale README block (which `--write` regenerates), an id or tier that differs between `corpus.json` and the harness, a duplicate or unparseable corpus, or a `core` entry with no ground-truth family. Eleven fixture controls; seven guard mutants each killed exactly one. Registered in both `check.sh` and the `justfile`, and shown to examine the real corpus by corrupting the README and watching the registered step exit 1. At merge it found the README block stale against the two probability and atom-key merges of the same hour and was regenerated to 128 entries, 117 core, 11 decline_expected. | 11 controls pass; checker PASS on main at merge |
| 2026-09-06 | **Item 6, wave four** (lane `cas-geometry-4`, merged `e176b2f06`): Pascal and Desargues, declined by every earlier wave, both certify, and what moved is the statement. Pascal's six vertices are parabola points (t, t²) at free parameters, so "on a common conic" holds by construction; the diagonal points are cross products of side lines and the collinearity settles by linear elimination with a zero residue in 1.8 ms and **no non-degeneracy condition**. Desargues in the affine chart makes the perspectivity a construction and asserts the three meets on their side lines; three conditions, each with a replayed counterexample, 603 ms. Two measured findings: affine Pascal is provably outside the rules (its saturating conditions are unfalsifiable, which `geometry_check` refuses), and a projective Pascal with free diagonal points leaves a 14-term residue and a 366 s Gröbner decline. Six mutants killed; all thirteen prior artifacts byte-identical. **What remains for item 6**: Pascal for an arbitrary conic, projective Desargues with points at infinity, and `BlockScope::Joint` sharing one decomposition across every conclusion (a theorem whose conclusions want different decompositions loses all of them). | geometry 82 passed, artifact suite 14; clippy, rustdoc, workspace check, registry green in the lane |
| 2026-09-06 | **Items 1 and 4, `axeyum-arith` slice 2** (lane `arith-slice-2`, merged `750bde174`): enclosure rounding moves onto `Dyadic` (absolute-grid floor/ceil), and `sqrt`/`ln` round their arguments outward onto the order grid before any series or Newton step, with an exact-argument fallback so nothing that answered now declines. A 552-operation differential corpus shows the new route is never narrower than the old one and equal to it. The regulator of ℚ(√61) at precision 100 went from not finishing in 6.5 minutes to 58 ms; all three regulator tests reach thirty digits in debug. One cheap cell (d=2, precision 32) is 2.5× slower for climbing an extra rung. Six mutants killed; one survived its first test form (a bound, not grid membership) and the test was strengthened. **Residue**: the six mutants are not registered in `mutation_controls.py`; the next arith lane registers them. | arith 37 passed; enclosure 138, numberfield_real 43; clippy, rustdoc, workspace check, fmt, registry green in the lane |

## How to re-measure

```sh
# size and surface
find crates/axeyum-cas/src -name '*.rs' | xargs cat | wc -l
ls crates/axeyum-cas/src/*.rs | wc -l; ls -d crates/axeyum-cas/src/*/ | wc -l
grep -rhE '^\s*pub fn ' crates/axeyum-cas/src --include=*.rs | wc -l
grep -rh '#\[test\]' crates/axeyum-cas/src crates/axeyum-cas/tests | wc -l

# the trust split (the number file 11 and the roadmap also quote)
python3 scripts/check-cas-internal-residue.py --report

# the core arithmetic and evalf claims
grep -n 'i128' crates/axeyum-cas/src/lib.rs | head -3
grep -n 'pub fn evalf' crates/axeyum-cas/src/lib.rs

# absence probes -- file counts, case-insensitive; a hit must be READ, not
# counted (two of the baseline hits were false), and every negative needs a
# positive control of the same query shape:
for p in 'number_field|class_group|dedekind' 'cornacchia|two_squares' \
         'elliptic' 'factor_multivariate|multivariate_factor' 'galois' \
         'schreier|sylow|coset' 'pub struct.*(PowerSeries|FormalSeries)' \
         'generating_function' 'holonomic|guess_recurrence' \
         'struct [A-Za-z]*Distribution' 'fourier_transform' \
         'pub fn .*conic|struct Point3' 'quantifier_elim|cylindrical_alg' \
         'simplicial|homology' 'entropy'; do
  printf '%-60s %s\n' "$p" "$(grep -rliE "$p" crates/axeyum-cas/src | wc -l)"
done
grep -rlE 'pub fn (smith_normal_form|laplace_transform|gosper_sum|count_real_roots)' \
  crates/axeyum-cas/src   # positive controls: four files
```

## Related

- [11-applied-and-computational.md](11-applied-and-computational.md) — the
  trust axis of the same crate: residue, producers, reconstruction
- [`docs/research/10-cas/README.md`](../research/10-cas/README.md) — the
  capability table and the July roadmap this file supersedes as a
  priority list
- [`docs/plan/cas-parity-handoff-2026-07-22.md`](../plan/cas-parity-handoff-2026-07-22.md)
  — the paused wave-24 checkpoint and the gap-probing method
- [ADR-0301](../research/09-decisions/adr-0301-cas-layer-reduce-to-decide.md),
  [ADR-0601](../research/09-decisions/adr-0601-three-producers-one-trust-anchor.md),
  [ADR-1400](../research/09-decisions/adr-1400-a-certificate-must-record-every-distinction-its-acceptance-depends-on.md)
