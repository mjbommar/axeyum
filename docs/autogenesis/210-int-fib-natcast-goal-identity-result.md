# `Int.fib_natCast` goal-identity result

Date: 2026-08-21

The preregistered tool read the sealed 374,550-byte stream exactly once and
emitted no theorem type, theorem value, or proof. It bound the canonical kernel
type hash of exact `Int.fib_natCast` to
`3a173a7b65ddf0fcf8c30c4ea3511780667bfff311a531ec477515ea731c490b`.

The declaration identity still matches the twice-reproduced construction, and
the theorem still has no direct theorem dependencies and an empty axiom
footprint. This closes the missing operation-registration input. It does not
itself admit the fact or authorize a ledger write.
