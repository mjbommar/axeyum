# Self-contained generic cancellation plan

The first generic cancellation source failed only because its local certificate
module was not installed in the clean Mathlib root. V2 imports the pinned
Mathlib gcd module and inlines the four-natural balanced-certificate definition.
The mathematical argument and explicit certificate parameter remain unchanged.

One source copy, one compilation, one root-selected export, and two fresh
imports are allowed. The imports must agree and have an empty footprint. This
packaging correction grants no official specialization or Fibonacci credit.

See
[`coprime-factor-cancellation-self-contained-plan-v1.json`](../../artifacts/autogenesis/coprime-factor-cancellation-self-contained-plan-v1.json).
