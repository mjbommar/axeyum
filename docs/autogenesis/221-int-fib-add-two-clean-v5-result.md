# Clean `Int.fib_add_two` V5 result

Date: 2026-08-21

The narrow Abel import makes the source compile, and two independent imports
produce byte-identical observations. The result is nevertheless rejected: the
exact theorem carries `propext`, quotient, classical-choice, and string/opaque
support assumptions. Compilation is not admission.

The sealed root stream and all 23 direct theorem dependencies are retained for
one non-rendering classification pass. No ledger write or theorem credit was
issued.
