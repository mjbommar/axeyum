# Clean `Int.fib_add_two` V4 plan

Date: 2026-08-21

V4 adds one deterministic `abel` normalization step after every substantive
constructor, parity, and Fibonacci rewrite has already completed. This is not
used to discover a proof route; it checks the final additive rearrangement in
the integer abelian group. The representation and premises do not change.

Compilation is still a single no-retry attempt. Even if it succeeds, an export
must independently show an empty kernel footprint before construction counts.
