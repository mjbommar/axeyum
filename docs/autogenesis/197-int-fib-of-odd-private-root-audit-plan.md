# Private `Int.fib_of_odd` root audit plan

Date: 2026-08-21

The measured public theorem has exactly one direct dependency, a generated
private theorem root. This plan freezes one non-rendering audit of that root in
the existing sealed stream. It grants no authority to compose the private root,
render its type or proof, submit a theorem, or mutate the ledger. The result may
only shape a target-owned parity/sign recurrence replacement.
