# Official coprime-factor cancellation exact-reuse plan

Date: 2026-08-21

The first official-cancellation run measured a zero-addition boundary: both
target-owned multiplication leaves already exist in the closed generic
balanced-Bézout kernel. A new bounded operation is preregistered before changing
the driver or rereading any proof-isolated stream.

For each leaf, the operation must require equal canonical declaration hashes
between the clean-leaf source and the closed kernel plus independently checked
`kernel-type-shape` compatibility. The leaves are reused, never composed. The
driver then composes only the residual cancellation theorem, all-Nat adapter,
and native positive-divisor cancellation after replaying the three-operation
official balanced-Bézout closure.

Two fresh complete invocations may read the eight streams once each. Across
both runs the ceiling is twelve compositions, ten specializations, two final
theorem submissions, and zero retries. Outputs must be byte-identical, the
final footprint empty, and every receipt must replay. Until that evidence
exists, official cancellation and every Fibonacci or ledger count remain zero.

```sh
python3 scripts/check-autogenesis-official-coprime-factor-cancellation-exact-reuse-plan.py
python3 -m unittest \
  scripts.tests.test_check_autogenesis_official_coprime_factor_cancellation_exact_reuse_plan
```
