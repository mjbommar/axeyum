# Clean divisibility antisymmetry cross-kernel decline

Date: 2026-08-21

The first bounded invocation imported both permitted streams, transferred the
four clean official leaves, and composed the target-owned clean divisor bound.
It then rejected the antisymmetry theorem before publication:

```text
UnknownConst { name: NameId(4) }
```

The mathematics was not the failure. The driver constructed a `NatPrelude` in
one native kernel and then used its numeric handles while building expressions
in the independently imported r091 kernel. `NameId` values are kernel-local;
their integers carry no cross-environment identity.

The second invocation was skipped, no support theorem or partial kernel was
published, and no target or ledger credit is due. The next operation must build
both support proofs wholly inside one native kernel and transport only checked
named theorem closures into r091, with explicit target leaves where existing
declarations are reused.

The sealed pack is
`/nas3/data/axeyum/autogenesis/reference-packs/53242139f-clean-dvd-antisymm-v1`
with manifest SHA-256
`5a99581d6a321f9f9c0951b1fa68e2946b75f953c1d68493e600ea567d8b1567`.

```sh
python3 scripts/check-autogenesis-nat-gcd-fib-add-self-clean-dvd-antisymm-result.py
python3 -m unittest \
  scripts.tests.test_check_autogenesis_nat_gcd_fib_add_self_clean_dvd_antisymm_result
```
