# `Int.fib` definition blocker-path audit plan

Date: 2026-08-21

A new reusable non-rendering tool reports declaration carriers between one root
and multiple blocked assumptions using only names, kinds, hashes, and closure
sizes. This plan binds one read of the sealed direct-`rfl` stream, root
`Int.fib`, and the nine distinct blockers observed there. It may not render
the definition or any proof and grants no reconstruction or ledger authority.

The result will identify the nearest shared carriers to replace in a clean
target-owned integer Fibonacci representation.
