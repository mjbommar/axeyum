# Multiplicative witness dependency audit plan

The clean-looking multiplicative witness still reaches `propext`. One exact,
non-rendering reread will classify its only three theorem dependencies in the
same residual stream: `Eq.trans`, `Nat.mul_assoc`, and `congrArg`.

If all three are clean, the footprint is a direct axiom edge introduced by
elaboration rather than a transitive theorem carrier. No proof changes or
theorem credit are authorized by this audit.
