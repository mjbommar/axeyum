# T5.4 — `NullstellensatzCertificate` + `recheck()` + `TrustId`

| | |
|---|---|
| Phase | 5 - lateral bridges |
| Size | M |
| Depends on | T5.3 |
| Blocks | T5.5 |

## What

Solver-side certificate struct with an independent `recheck()`, a new `TrustId::Nullstellensatz`,
wiring into the NIA/NRA equational decline path, and a regenerated golden trust ledger.

## Files touched

- `crates/axeyum-solver/src/trust.rs`, `certify.rs`, `nra*.rs`/`nia*.rs`
- `docs/research/08-planning/trust-ledger.md` (regenerated)

## Exit criteria

- Soundness-negative tests reject mutated cofactors
- `progress_frontier` and the `--lib` sweep green (pre-merge gates apply)
- Degenerate fuzz seed-class present: zero polynomial, empty generator set, non-monic leading terms

## Notes and traps

The bridge fires only on the *decline* path, so a Groebner blowup can never slow the funnel -- it can
only fail to help. That framing keeps it safe to land.

Lean route: ring normalization, mirroring `linear_combination`, composable with the existing kernel.

Back to [phase README](README.md) · [track index](../README.md)
