# T5.2 — `reduce_with_quotients` in `groebner.rs`

| | |
|---|---|
| Phase | 5 - lateral bridges |
| Size | M |
| Depends on | T5.1 |
| Blocks | T5.3 |

## What

Multivariate division that retains the quotients. `reduce()` (`groebner.rs:321`) currently discards
them, which is why the single most valuable lateral certificate does not exist yet.

## Files touched

- `crates/axeyum-cas/src/groebner.rs`

## Exit criteria

- For all tests, `p = sum(q_i * b_i) + r` re-multiplies exactly
- Existing `reduce` behavior unchanged

## Notes and traps

i128 rational coefficients will hit the overflow-`None` path early under coefficient swell. Honest
`Unknown` is the house style -- keep it; do not silently truncate.

Back to [phase README](README.md) · [track index](../README.md)
