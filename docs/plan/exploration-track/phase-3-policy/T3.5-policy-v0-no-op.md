# T3.5 — policy-v0 = legacy dispatch differential gate

| | |
|---|---|
| Phase | 3 - searched policy |
| Size | M |
| Depends on | T2.1, T3.3, T3.4 |
| Blocks | T3.6, T3.7, T3.8 |

## What

Encode today's fixed dispatch order as `policy-v0.json` (bucket = the current if-chain conditions),
put policy-driven dispatch behind a flag, and prove equivalence.

## Files touched

- new `crates/axeyum-solver/src/policy.rs`
- policy JSON under `artifacts/`
- differential tests

## Exit criteria

- **Byte-identical verdicts and traces vs legacy dispatch on all committed corpora**

## Notes and traps

If you can only do one thing in this track, do this. It converts 8,616 lines of implicit ordering
into a reviewable artifact with standalone value even if no learning ever happens, and it is the only
safe way to touch `auto.rs`.

Do not learn anything until the no-op refactor is proven.

Back to [phase README](README.md) · [track index](../README.md)
