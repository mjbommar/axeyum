# T4.3 — Reusable `TermId`-to-`ENodeId` bridge

| | |
|---|---|
| Phase | 4 - eqsat and walk-back |
| Size | M |
| Depends on | none |
| Blocks | T4.5 |

## What

Factor the fourfold-duplicated bridge into one shared module and migrate at least two consumers.

## Files touched

- new `crates/axeyum-solver/src/egraph_bridge.rs`
- `euf_egraph.rs`, `qinst_egraph.rs`

## Exit criteria

- No behavior change; `corpus_regression` and `progress_frontier` green

## Notes and traps

Per ADR-0001, promote to a crate only after the boundary is proven by a second consumer *crate* --
a shared module inside `axeyum-solver` first.

Keep `decl: u32` caller-assigned; the egraph stays theory-free.

Back to [phase README](README.md) · [track index](../README.md)
