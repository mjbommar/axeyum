# T4.9 — `Bridge` walk-back contract retrofitted onto the 14 reductions

| | |
|---|---|
| Phase | 4 - eqsat and walk-back |
| Size | M |
| Depends on | T4.1 (ADR) |
| Blocks | T4.10 |

## What

Declare `lift_sat` / `lift_unsat` mode / `lift_unknown` for every existing reduction. No behavior
change -- this is a formalization of what `trust.rs` and the projection code already do.

## Files touched

- `crates/axeyum-solver/src/{trust,evidence}.rs` plus a new module

## Exit criteria

- Every TrustId maps to declared modes
- The golden trust-ledger test stays green
- The crossing table in the phase README is generated from code, not hand-maintained

## Notes and traps

The six NONE cells are already enumerated by `trust.rs`; the contract's value is that every **new**
bridge must declare its mode at registration, with NONE allowed but ledger-visible.

Back to [phase README](README.md) · [track index](../README.md)
