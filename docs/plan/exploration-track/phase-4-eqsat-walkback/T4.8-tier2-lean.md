# T4.8 — Tier-2 Lean lemma library keyed by `RewriteRuleId`

| | |
|---|---|
| Phase | 4 - eqsat and walk-back |
| Size | L |
| Depends on | T4.7 |
| Blocks | none |

## What

A lemma library in the `axeyum-lean-kernel` environment keyed by rule id; the chain becomes an
`Eq.trans` / `eq_congruent` spine. Bool rules first (kernel-decidable), then BV.

## Files touched

- `crates/axeyum-lean-kernel` prelude/env
- `crates/axeyum-solver/src/reconstruct/`

## Exit criteria

- At least the bool-rule subset reconstructs to kernel-checked terms
- One lemma per `RewriteRuleId`

## Notes and traps

`reconstruct/equality.rs:315-367` already handles n-ary `eq_congruent`, and `eq_transitive` /
`eq_symmetric` live in the same module -- the spine is reconstructable today.

Open ADR question: may rule lemmas land as *axioms* (ledger-visible trust hole) before being proven?
Precedent exists both ways in the kernel preludes.

Back to [phase README](README.md) · [track index](../README.md)
