# T9.11 — Mathlib + ten-challenges Comparator run

| | |
|---|---|
| Phase | 9 - Lean evidence ladder |
| Size | L |
| Depends on | T9.10 |
| Blocks | none |

## What

Run `lake exe comparator` on at least one `openai/ten-proofs` challenge with `COMPARATOR_NANODA`
pointed at the axeyum binary.

## Files touched

- none (run plus report); possibly parallelism ADR work

## Exit criteria

- At least one challenge exits 0

## Notes and traps

**Explicitly optional, ADR-gated capstone.** 4-8 part-time months whose unique payoff is largely
reputational; the technical residue is captured earlier and more cheaply by the Arena ladder.

Two honesty constraints: the lineage caveat (axeyum ports nanoda's semantics per ADR-0036, and bug
#14576 showed lean4lean inheriting what nanoda rejects), and Comparator's own warning that
`definition_names` holes are only name/type-matched -- so a second kernel does not close the
statement-auditing hole.

Back to [phase README](README.md) · [track index](../README.md)
