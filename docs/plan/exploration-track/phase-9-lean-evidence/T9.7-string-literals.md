# T9.7 — String-literal typing/reduction

| | |
|---|---|
| Phase | 9 - Lean evidence ladder |
| Size | M |
| Depends on | T9.6 |
| Blocks | T9.10 |

## What

`Lit::Str` typing plus `String.mk`/`Char` reduction, matching nanoda's `string_extension`.

## Files touched

- `crates/axeyum-lean-kernel/src/tc.rs`, `string_prelude.rs`
- the importer's `strVal` arm (`lib.rs:582`)

## Exit criteria

- The blocker-census String closure (570,807-byte stream, 10,339 records, 290 declaration records)
  imports
- Differential tests against real `lean`

## Notes and traps

Degenerate fuzz: `Char` out-of-range, surrogate code points, empty strings. The existing fail-closed
rejection is the right default -- do not relax it until the semantics are differentially confirmed.

Back to [phase README](README.md) · [track index](../README.md)
