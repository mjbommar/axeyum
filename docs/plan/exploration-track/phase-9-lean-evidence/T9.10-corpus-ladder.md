# T9.10 — Corpus ladder: `Init` then `Std`

| | |
|---|---|
| Phase | 9 - Lean evidence ladder |
| Size | L |
| Depends on | T9.6, T9.7, T9.8, T9.9 |
| Blocks | T9.11 |

## What

Check the `Init` closure, then `Std`, with the performance work each requires.

## Files touched

- performance work across the kernel (whnf cache keying, allocation)
- bench artifacts

## Exit criteria

- Full `Init` accepted
- `Std` accepted within 10x the official kernel's time
- Measurements committed

## Notes and traps

The memory architecture already anticipates scale: a sharded 64-way `ExprInterner` and non-relocating
`SegmentedVec` chunks sized for "tens of millions of expression nodes" (`lib.rs:88-210`). What is
single-threaded is the checking itself (`&mut Kernel` everywhere) -- see deferred ADR C5.

Back to [phase README](README.md) · [track index](../README.md)
