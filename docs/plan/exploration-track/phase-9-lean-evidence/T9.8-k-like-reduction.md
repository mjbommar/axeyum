# T9.8 — K-like reduction

| | |
|---|---|
| Phase | 9 - Lean evidence ladder |
| Size | M |
| Depends on | none |
| Blocks | T9.10 |

## What

Store the recursor `k` flag on `Declaration::Recursor` and implement K-like reduction in
def-eq/iota.

## Files touched

- `crates/axeyum-lean-kernel/src/{env,inductive,tc}.rs`
- the importer

## Exit criteria

- Arena K-reduction conformance tests pass
- A soundness-negative test rejecting misuse

## Notes and traps

**Confirm the gap first.** The importer parses and constrains the `k` flag (`lib.rs:1396-1402`), but
`Declaration::Recursor` in `env.rs` appears to store no K flag and `tc.rs` appears to have no K-like
reduction. Verify before building.

Back to [phase README](README.md) · [track index](../README.md)
