# T2.10 — ADRs: reward semantics, chain trust composition, feature surface, variant policy

| | |
|---|---|
| Phase | 2 - evaluation harness |
| Size | S |
| Depends on | none |
| Blocks | T2.6, T2.7 |

## What

Close ADR-queue items B7 (reward scalarization and assurance modes), B8 (chain trust composition as
a meet), B9 (feature extraction as public surface), and the pack-variant policy question.

## Files touched

- `docs/research/09-decisions/`

## Exit criteria

- Four ADRs accepted

## Notes and traps

B8 is a prerequisite for the whole bridge-graph idea, not just this harness: "trust of a composed
chain = meet of its edges' trust classes" needs `axeyum-rewrite` manifest contracts and the atlas
`proof_routes.status` vocabulary mapped onto one lattice.

B9 matters more than it looks: if the feature vector is a public artifact, the determinism promise
applies to its exact values across releases.

Back to [phase README](README.md) · [track index](../README.md)
