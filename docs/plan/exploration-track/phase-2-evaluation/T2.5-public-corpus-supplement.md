# T2.5 — Public-corpus supplement per atlas row

| | |
|---|---|
| Phase | 2 - evaluation harness |
| Size | M |
| Depends on | none |
| Blocks | T2.8 |

## What

Pin hashed SMT-LIB slices for QF_LIA / QF_LRA / QF_UF / QF_NIA / QF_NRA (the QF_BV pipeline already
exists) and commit baseline bench JSON.

## Files touched

- `scripts/fetch-corpus.sh`, corpus manifests, `justfile`

## Exit criteria

- Pinned, hashed slices per logic; baseline bench artifacts committed

## Notes and traps

The existing `bench-results/baselines/` runs show real timeout/decide diversity only on public QF_BV
at n >= 1000. Other logics need their own slices before any per-logic claim is meaningful.

Back to [phase README](README.md) · [track index](../README.md)
