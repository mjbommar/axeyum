# T2.9 — `dispatch_frontier` ratchet gate

| | |
|---|---|
| Phase | 2 - evaluation harness |
| Size | M |
| Depends on | T2.4, T2.7 |
| Blocks | phase-3 landing |

## What

A new ratchet in the `progress_frontier` mold: PAR-2 on the committed slices >= static baseline minus
a pinned tolerance; zero wrong verdicts; per-family frontier `N` >= committed baseline.

## Files touched

- new `crates/axeyum-solver/tests/dispatch_frontier.rs`
- `justfile`

## Exit criteria

- In `just check`; enforces locally, report-only on CI

## Notes and traps

Inherit the hardware-relative caveat honestly -- `progress_frontier.rs` already documents that its
ratchet only holds on the dev box. Do not pretend otherwise.

`corpus_regression` stays authoritative for wrong verdicts; this gate delegates rather than duplicates.

Back to [phase README](README.md) · [track index](../README.md)
