# T0.7 — Measured cost model from bench artifacts

| | |
|---|---|
| Phase | 0 - bridge catalogue |
| Size | M |
| Depends on | T0.1, T0.3 |
| Blocks | phase-3 T3.7 |

## What

Join bench JSON artifacts (PAR-2, decisive route from persisted traces) into the catalogue's
`cost.measured` field via a generator script, in the atlas-benchmark style.

## Files touched

- new generator script under `scripts/`
- `artifacts/ontology/bridge-catalogue.json` (cost fields)

## Exit criteria

- Cost fields populated from committed bench results
- Validator checks provenance paths exist

## Notes and traps

Cost must be *measured*, not hand-assigned (the MachSMT/MedleySolver lesson). Inherit the discipline
in `docs/research/08-planning/benchmarking-and-performance-methodology.md`, which already gates
priority claims on measurement: record which corpus, which machine, and a staleness policy.

Back to [phase README](README.md) · [track index](../README.md)
