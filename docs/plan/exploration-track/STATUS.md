# Exploration-track status moved to the root plan

The exploration track's current state and dependency-correct next actions are
maintained in the root [`PLAN.md`](../../../PLAN.md), under **A7** and the
workstream table. This file is a compatibility pointer, not a separate live
tracker.

The proposal remains ADR-gated. Catalogue work must proceed in the order
T0.2 → T0.6 → T0.1/T2.5 → T2.3/G1 before phase-3 policy work such as T3.5.
The track may not preempt the root plan's active P0/P1 solver, measurement, and
evidence work.

The pre-consolidation 96-task status snapshot is preserved at Git revision
`803c08439` as blob `9d191f6538df9cb045154953264f755083235640`.
