# P4.4 — SMT-LIB command/API conformance

**Size:** L · **Depends on:** — (independent; `get-proof` ties to
[Track 3](../track-3-proof-lean/README.md))

## Goal
Turn the existing parser, incremental command stream, and single-query Rust
helpers into a precisely scoped SMT-LIB 2 protocol surface. The immediate gap
is no longer “add every helper”: it is preserving command-point ordering,
emitting a textual response for every output command, enforcing option and
session lifecycle semantics, and exposing already-implemented direct engines
without pretending a parser no-op is a command implementation.

## Why / leverage
Mechanical but broad — it is what lets external tools and benchmark drivers use
axeyum without special-casing. The checked
[SMT-LIB/API conformance matrix](../generated/smtlib-api-conformance.md) now
provides the source of truth: 30 high-value rows distinguish syntax acceptance,
execution, output representation, assurance, exact tests, and residual gaps.
Its central result is that many useful helpers already exist, but **zero rows
provide an ordered interactive textual session**.

The follow-up [ordered-session design](../smtlib-session-contract-design-2026-07-21.md)
pins SMT-LIB 2.7 and materially corrects the initial estimate. A conforming
runner must scope declarations/definitions as well as assertions, implement
`reset-assertions` under `:global-declarations`, preserve exact query snapshots,
and make continued errors state-atomic. The checked abstract
[transcript prototype](../generated/smtlib-session-contract.md) freezes 14
invariants and 20 fixtures / 107 commands before Rust behavior changes.

## Tasks
| id | task | key references | size | exit |
|---|---|---|---|---|
| T4.4.0 | Keep both machine-readable prototypes and generated matrices source/test-checked | [`../smtlib-api-conformance-v1.json`](../smtlib-api-conformance-v1.json); [`../smtlib-session-contract-v1.json`](../smtlib-session-contract-v1.json); proposed ADR-0342 | S | both `--check` generators reject drift; current-source assertions and 20 abstract transcripts remain exact |
| T4.4.1 | Capture a complete ordered command/event IR without switching public behavior | [session design S1](../smtlib-session-contract-design-2026-07-21.md#migration-plan); `crates/axeyum-smtlib/src/parse.rs` | M | every currently accepted command has stable command/epoch identity and exact order; existing helper outputs remain identical |
| T4.4.2 | Add transactional scope/signature state and reset epochs | SMT-LIB 2.7 sections 4.1.4–4.2.2; session design S3 | L | assertions, declarations, definitions, aliases, macros, and names obey default/global scope; pop errors are atomic; full reset creates a fresh arena epoch; popped names can be redeclared |
| T4.4.3 | Add exact query snapshots and one typed ordered session runner | session design S2; `crates/axeyum-solver/src/smtlib.rs` | M | every result/output names the exact preceding query; no helper reparses or re-solves final state; `echo` and `exit` act in order |
| T4.4.4 | Enforce option/lifecycle semantics and add canonical textual plus in-memory/WASM adapters | SMT-LIB 2.7; session design S4 | M | immediate output options, produce-* gates, modes, channels, and continued errors match exact transcripts; pure core performs no filesystem/process I/O |
| T4.4.5 | Expose the landed `get-proof` helper through the session runner ([P3.3](../track-3-proof-lean/P3.3-alethe-qfbv.md)) | `crates/axeyum-solver/src/smtlib.rs`; `crates/axeyum-solver/src/proof.rs` | S | selected QF_BV, QF_UF/QF_ABV, QF_LRA, and QF_LIA proof responses are emitted only when the option/session contract permits; internal-only rules and external-Carcara coverage are labeled separately |
| T4.4.6 | Logic detection + capability gating (route `(set-logic)` to the right strategy) | `references/z3/src/solver/smt_logics.h`; ties to [P1.8](../track-1-engine/P1.8-strategy-tactics.md) | S | unsupported logics reject cleanly; declared logic mismatches are diagnosed; supported declarations constrain routing rather than serving only as returned metadata |
| T4.4.7 | Add textual adapters for implemented interpolation, Horn, and abduction engines | [categorical depth audit](../categorical-engine-depth-audit-2026-07-21.md); P3.8/P4.6/P4.7 | M | `get-interpolant`, `declare-rel`/`rule`/`query`, and `get-abduct` have exact syntax, ordering, verify-before-return behavior, and negative fixtures; general `synth-fun` remains a separate demand-gated engine |
| T4.4.8 | Add `declare-sort-parameter` and complete `declare-sort` / `define-sort` beyond the landed arity-zero surface | SMT-LIB 2.7; `crates/axeyum-smtlib/src/parse.rs` | M | global sort parameters plus parametric declarations/aliases have substitution semantics, scoped lifetime where applicable, solving, and precise collision/arity errors |
| T4.4.9 | Decide recursive-definition scope independently of macro expansion | `define-fun-rec` / `define-funs-rec`; foundational DAG | M | either a checked recursion/termination model exists or both commands remain explicit, exact-fixture rejections; nonrecursive macro expansion is not reported as recursive-definition support |

## Phase exit criteria
- A generated matrix covers every admitted high-value command and records parse,
  execution, output, assurance, scope, exact tests, and residual behavior.
- One ordered session runner covers state changes and output commands without
  re-solving against the wrong query or reading final global state for an
  earlier request.
- Declarations, definitions, aliases, macros, and named terms obey default and
  global scope across push/pop/reset, with fresh deterministic identity after
  redeclaration or full reset.
- Full `reset`, option updates, `exit`, output channels, and error behavior have
  transcript fixtures. Unsupported commands fail visibly.
- Proof, interpolation, Horn, and abduction text surfaces expose only their
  measured direct-API fragments and preserve verify-before-return boundaries.
- Parametric sorts and recursive definitions are either implemented with their
  semantic obligations or retained as explicit, tested rejections.

## References

- [Ordered session design](../smtlib-session-contract-design-2026-07-21.md)
- [Generated session transcript prototype](../generated/smtlib-session-contract.md)
- `docs/plan/references/axeyum-current-state.md` (historical July 10 baseline)
