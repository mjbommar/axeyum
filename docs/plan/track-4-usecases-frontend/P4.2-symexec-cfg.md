# P4.2 — Symbolic-execution CFG frontend (angr/unicorn-class)

**Size:** XL · **Depends on:** [P4.1 warm lazy memory](P4.1-warm-lazy-memory.md) ·
**Capstone of the symbolic-execution use case**

## Goal
Build the binary/IR frontend that turns axeyum's solver-side symbolic-execution
primitives (`SymbolicExecutor`, BMC, k-induction, path-condition optimization)
into an angr/unicorn-class tool: lift a program to an IR, build a CFG, explore
paths with a real memory model, and cross-check against concrete emulation.

## Why / leverage
The solver side is built (path exploration, test-suite enumeration, optimization,
reachability, certified safety). What's missing is the *frontend*: a program
representation, a CFG-shaped explorer, and a memory model — the difference between
"a solver with symexec primitives" and "a symbolic-execution engine."

## Tasks
| id | task | key references | size | exit |
|---|---|---|---|---|
| T4.2.1 | An IR + lifter for a small target (a toy ISA or an existing IR) to axeyum terms | this repo `crates/axeyum-ir`; angr/unicorn design (external) | L | a small program lifts to symbolic terms |
| T4.2.2 | CFG construction + a path-explorer over `SymbolicExecutor` (states, fork on `branch`, prune on infeasible) | this repo `crates/axeyum-solver/src/symexec.rs` | L | the explorer walks the CFG, pruning infeasible paths |
| T4.2.3 | Memory model on warm lazy arrays ([P4.1](P4.1-warm-lazy-memory.md)) (loads/stores, symbolic addresses) | this repo `crates/axeyum-solver` (memory) | L | symbolic memory accesses decided warmly |
| T4.2.4 | Concrete-emulation cross-check (run a concrete input, confirm it follows the symbolic path) | this repo `axeyum-scenarios` pattern | M | every explored path's model concretely reproduces the path |
| T4.2.5 | Reachability/safety queries via BMC/k-induction over the CFG | this repo `crates/axeyum-solver/src/bmc.rs` | M | "is this block reachable / this assert safe" answered with a trace or a (certified) proof |

## Phase exit criteria
- A working CFG-shaped symbolic executor over a small target: lifts, explores with
  pruning, models memory warmly, generates test inputs, answers reachability/safety
  with witnessed traces or certified proofs, and cross-checks against concrete
  emulation. (Scope is intentionally a *small* target first; broadening ISAs is
  follow-on.)

## References
`docs/plan/references/axeyum-current-state.md` (symexec status: "the frontend is
the gap").
