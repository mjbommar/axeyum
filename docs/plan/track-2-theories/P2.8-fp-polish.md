# P2.8 — FP polish (already near parity)

**Size:** S–M · **Depends on:** — (independent)

## Goal
Close the small remaining FP corners. axeyum's FP (F16–F128, fp→bv, differentially
validated) is the closest-to-parity theory; this is cleanup, not a rebuild.

## Tasks
| id | task | key references | size | exit |
|---|---|---|---|---|
| T2.8.1 | **Unspecified-value handling** for `fp.to_real`/`fp.to_*bv` (the `nan_wrap` + `_I` internal-op model trick keeps unspecified branches consistent) | `references/z3/src/ast/fpa/{fpa2bv_converter,bv2fpa_converter}.cpp` | S–M | unspecified-result queries decided consistently; models valid |
| T2.8.2 | **min/max ±0** implementation-defined selector bit | `references/z3/src/ast/fpa/fpa2bv_converter.cpp` | S | min/max ±0 model-soundness corner closed |
| T2.8.3 | Lazy "eager-once-touched" conversion (wrap/unwrap UFs with `unwrap(wrap(n))=n`) — only matters once FP mixes with UF/quantifiers | `references/z3/src/ast/fpa/fpa2bv_converter.cpp` | S | FP+UF mixes handled without eager full blast |
| T2.8.4 | Broaden oracle cross-checking (FP `unsat` is replay-blind; widen the differential corpus) | this repo `crates/axeyum-fp`, `axeyum-bench` | S–M | more FP `unsat` cross-checked vs an oracle |

## Phase exit criteria
- Unspecified-value and min/max ±0 corners handled to spec; FP `unsat` assurance
  strengthened by broader oracle cross-checking (recorded).

## References
`docs/plan/references/z3-theories.md` (FP).
