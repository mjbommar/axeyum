# P2.8 — FP semantic repair and completeness

**Size:** S–M · **Depends on:** — (independent)

## Goal
Restore a sound SMT-LIB floating-point semantic boundary, then measure and close
the remaining completeness gaps. Axeyum has broad F16–F128 operator coverage,
but the 2026-07-22 audit found quotient-domain, rounding-mode, underspecified-
result, directed-overflow, and Float128 defects. This is a semantic repair and
assurance phase, not evidence of general solver parity.

## Tasks
| id | task | key references | size | exit |
|---|---|---|---|---|
| T2.8.1 | **Unspecified-value handling** for `fp.to_real`/`fp.to_*bv` (the `nan_wrap` + `_I` internal-op model trick keeps unspecified branches consistent) | `references/z3/src/ast/fpa/{fpa2bv_converter,bv2fpa_converter}.cpp` | S–M | unspecified-result queries decided consistently; models valid |
| T2.8.2 | **min/max ±0** implementation-defined selector bit | `references/z3/src/ast/fpa/fpa2bv_converter.cpp` | S | min/max ±0 model-soundness corner closed |
| T2.8.3 | Lazy "eager-once-touched" conversion (wrap/unwrap UFs with `unwrap(wrap(n))=n`) — only matters once FP mixes with UF/quantifiers | `references/z3/src/ast/fpa/fpa2bv_converter.cpp` | S | FP+UF mixes handled without eager full blast |
| T2.8.4 | Broaden oracle cross-checking (FP `unsat` is replay-blind; widen the differential corpus) | this repo `crates/axeyum-fp`, `axeyum-bench` | S–M | more FP `unsat` cross-checked vs an oracle |
| T2.8.5 | Represent SMT-LIB Float and RoundingMode as quotient/finite domains throughout equality, UF, arrays, and quantifiers | SMT-LIB FP theory; `axeyum-ir`, `axeyum-bv` | M | NaN and five-mode metamorphic regressions pass end to end |
| T2.8.6 | Remove narrowing host-float paths and complete exact binary128 conversion/remainder or decline explicitly | `rustc_apfloat`, pure-Rust bigint substrate | M | F128 all-mode oracle and low-significand-bit regressions pass |

## Phase exit criteria
- Unspecified-value and min/max ±0 corners handled congruently; Float and
  RoundingMode domains match SMT-LIB; directed overflow and F128 exact paths are
  oracle checked; all `Fpa2Bv` trust steps remain uncertified until a complete
  reduction checker exists; affected full-library slices return DISAGREE = 0.

## Current bounded result

The 2026-07-27 [finite-domain preprocessing repair](../fp-finite-domain-preprocess-result-2026-07-27.md)
restores the standard-F32 `Float-no-simp3-main.smt2` decision from an unbounded
construction path to 18.025 ms in the focused debug artifact. Float and
RoundingMode constant folds now preserve their source sorts, and ground scalar
definitions flow through the model-reconstructing preprocessor.

The 2026-07-27 [ground custom-format division increment](../fp-ground-custom-division-result-2026-07-27.md)
decides the sole unsupported row in the committed eight-file QF_BVFP Bitwuzla
regression slice. Exact ground `(4,12)` division is oracle checked without
admitting the unvalidated symbolic format. Combined with the preprocessing
repair above, the refreshed slice is 8/8 decided with DISAGREE=0 and zero replay
failures.

## References
`docs/plan/references/z3-theories.md` (FP).
