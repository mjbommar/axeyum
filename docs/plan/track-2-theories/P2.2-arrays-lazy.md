# P2.2 — Arrays: lazy axiom instantiation

**Size:** L · **Depends on:** [P1.4 e-graph](../track-1-engine/P1.4-egraph.md),
[P1.5 CDCL(T)](../track-1-engine/P1.5-cdcl-t-loop.md) · **The biggest array
scalability win**

## Goal
Move arrays from eager elimination (read-over-write + Ackermann, ADR-0010 — which
blows up on store chains and large index spaces) to **lazy axiom instantiation**
on the e-graph, matching Z3.

## Tasks
| id | task | key references | size | exit |
|---|---|---|---|---|
| T2.2.0 | **Abstraction-only base selects + canonical bus**: no eager pair construction; materialize candidate-violated select congruence with array-first/UF-second abstraction and replay | ADR-0071; `crates/axeyum-rewrite/src/arrays.rs`; `crates/axeyum-solver/src/ufbv_online.rs` | S–M | **DONE** — pure ABV/AUFBV routes live, 768 differentials + public corpora DISAGREE=0 |
| T2.2.1 | Axiom queue (dedup, tri-state new/delayed/applied) + e-graph class-merge hooks | `references/z3/src/sat/smt/array_axioms.cpp` | M | WIP prerequisite: candidate-batched base-select queue exists; add merge-triggered delayed/applied store/extensionality states |
| T2.2.2 | **Lazy read-over-write**: `store(a,i,v)[j]=a[j] ∨ i=j` instantiated when congruence puts a select and a store in one class (eager `store(a,i,v)[i]=v`) | `references/z3/src/sat/smt/array_solver.cpp` (`propagate_select_axioms`, `merge_eh`) | L | store-chain instances that blow up eager now scale; models replay |
| T2.2.3 | **Extensionality on disequality** via per-dimension diff skolem | `references/z3/src/sat/smt/array_axioms.cpp` (`array_ext`/`diff`) | M | array disequalities decided lazily |
| T2.2.4 | **`func_interp` array models** with majority-vote else value | `references/z3/src/sat/smt/array_model.cpp` | M | scalable array models; usable by theory combination |
| T2.2.5 | (beyond QF_ABV) `const`/`map`/`default`/`as-array`/lambda beta-reduction | `references/z3/src/sat/smt/array_solver.cpp` | M | extended array fragment decided |
| T2.2.6 | Retire/keep the eager path for small instances (portfolio via [P1.8](../track-1-engine/P1.8-strategy-tactics.md)) | this repo `crates/axeyum-solver/src/abv.rs` | S | eager kept as a fast path for tiny arrays |

## Phase exit criteria
- Lazy arrays decide store-chain / large-index instances that the eager path
  cannot within budget, with replay-checked models; warm path no longer refuses
  arrays (ties into [P4.1](../track-4-usecases-frontend/P4.1-warm-lazy-memory.md)).
- An [Alethe reduction proof](../track-3-proof-lean/P3.5-reduction-proofs.md) for
  read-over-write (`ARRAYS_ROW/IDX/EXT`) becomes feasible (arrays are cvc5's
  fully-checked case).

## Current Boundary (2026-07-09)

ADR-0071 lands the first canonical array slice. `abstract_arrays` performs the
existing exact read-over-write rewrite but does not construct the eager O(k²)
select-pair set. Canonical rounds start with independent base reads and batch
only equal-index/unequal-result candidate violations. Mixed QF_AUFBV composes
this with the dynamic UF interface path, projects functions before arrays, and
replays originals. The next work is T2.2.1/T2.2.2: move store reasoning itself
from preprocessing into an e-graph-triggered axiom queue. Wide disequality
extensionality and majority-default array models remain T2.2.3/T2.2.4.

## References
`docs/plan/references/z3-theories.md` (Arrays).
