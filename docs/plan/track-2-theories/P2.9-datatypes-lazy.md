# P2.9 — Datatypes lazy (e-graph splitting + occurs-check)

**Size:** M · **Depends on:** [P1.4 e-graph](../track-1-engine/P1.4-egraph.md),
[P1.5 CDCL(T)](../track-1-engine/P1.5-cdcl-t-loop.md) · **Lower priority**

## Goal
Move datatypes from eager tag/field expansion to lazy, e-graph-driven reasoning
(recognizer exactly-one propagation, non-recursive-constructor case-split bias,
iterative occurs-check for acyclicity), matching Z3's Oppen procedure.

## Tasks
| id | task | key references | size | exit |
|---|---|---|---|---|
| T2.9.1 | Eager accessor axioms (`acc_i(C(a))=a_i`) on the e-graph | `references/z3/src/sat/smt/dt_solver.cpp` | S | accessors propagate via congruence |
| T2.9.2 | Recognizer⇔constructor coupling + **exactly-one propagation** | `references/z3/src/sat/smt/dt_solver.cpp` (`propagate_recognizer`) | M | recognizer vectors decided correctly |
| T2.9.3 | **Case-split biased to the non-recursive constructor** (guarantees a finite witness) | `references/z3/src/sat/smt/dt_solver.cpp` | S | splitting terminates with a finite model |
| T2.9.4 | **Iterative occurs-check DFS** for acyclicity (extend through array/seq nesting) | `references/z3/src/sat/smt/dt_solver.cpp` (`occurs_check`) | M | cyclic constraints rejected; nested acyclicity handled |
| T2.9.5 | Keep eager expansion as a fast path / differential oracle | this repo `crates/axeyum-solver/src/datatype_native.rs` | S | lazy vs eager agree on a differential test |

## Phase exit criteria
- Datatypes decided lazily on the e-graph with non-recursive-constructor split
  bias and occurs-check acyclicity, agreeing with the eager path differentially,
  models replay-checked.

## References
`docs/plan/references/z3-theories.md` (Datatypes).
