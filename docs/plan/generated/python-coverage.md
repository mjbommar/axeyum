# Python binding coverage ledger

Generated by `scripts/gen-python-coverage.py`. Do not edit. Source of truth for plan 02's exit criterion (`docs/python-2026-08/02-python-api.md`); the slice plan that consumes it is [`docs/python-2026-08/09-coverage-plan.md`](../../python-2026-08/09-coverage-plan.md).

`PYTHON_COVERAGE|crates=24|public=4704|referenced=1034|inventoried=250|tier_r_unreferenced=0|deferred=6`

**`referenced` is an upper bound on `bound`.** It means the binding names the item with comments stripped -- not that a Python callable exists, is correct, or is tested. The backlog below is therefore a lower bound on what plan 02 still owes.

Binding: `crates/axeyum-py`, 47 files, 141 `#[pyclass]`, 273 `#[pyfunction]`, 1788 `fn` definitions.

## Per crate

| crate | public | referenced | inventoried | untiered | tier-R | tier-R unreferenced | deferred |
|---|--:|--:|--:|--:|--:|--:|--:|
| `axeyum-aig` | 32 | 0 | 0 | 0 | 0 | 0 | 0 |
| `axeyum-bench` | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| `axeyum-bv` | 55 | 9 | 0 | 0 | 0 | 0 | 0 |
| `axeyum-cas` | 777 | 471 | 87 | 8 | 19 | 0 | 2 |
| `axeyum-cnf` | 357 | 29 | 7 | 3 | 1 | 0 | 0 |
| `axeyum-egraph` | 37 | 0 | 0 | 0 | 0 | 0 | 0 |
| `axeyum-evm` | 39 | 0 | 0 | 0 | 0 | 0 | 0 |
| `axeyum-fp` | 73 | 27 | 0 | 0 | 0 | 0 | 0 |
| `axeyum-ir` | 346 | 242 | 20 | 18 | 2 | 0 | 0 |
| `axeyum-lean-import` | 148 | 9 | 13 | 0 | 7 | 0 | 2 |
| `axeyum-lean-kernel` | 237 | 99 | 44 | 7 | 16 | 0 | 0 |
| `axeyum-property` | 158 | 0 | 0 | 0 | 0 | 0 | 0 |
| `axeyum-property-macros` | 1 | 0 | 0 | 0 | 0 | 0 | 0 |
| `axeyum-py` | 193 | 0 | 0 | 0 | 0 | 0 | 0 |
| `axeyum-query` | 47 | 28 | 0 | 0 | 0 | 0 | 0 |
| `axeyum-rewrite` | 129 | 0 | 0 | 0 | 0 | 0 | 0 |
| `axeyum-scenarios` | 265 | 0 | 0 | 0 | 0 | 0 | 0 |
| `axeyum-search` | 151 | 0 | 0 | 0 | 0 | 0 | 0 |
| `axeyum-smtlib` | 38 | 9 | 8 | 2 | 6 | 0 | 1 |
| `axeyum-solver` | 1217 | 111 | 71 | 27 | 10 | 0 | 1 |
| `axeyum-strings` | 110 | 0 | 0 | 0 | 0 | 0 | 0 |
| `axeyum-verify` | 287 | 0 | 0 | 0 | 0 | 0 | 0 |
| `axeyum-verify-macros` | 5 | 0 | 0 | 0 | 0 | 0 | 0 |
| `axeyum-wasm` | 2 | 0 | 0 | 0 | 0 | 0 | 0 |
| **total** | **4704** | **1034** | **250** | **65** | **61** | **0** | **6** |

`untiered` is the honest hole in this join: 65 inventory rows sit in tables with no tier column and under a heading that names no tier, so they are NOT in the backlog even though some of them are read-only surface. Tiering them is an inventory edit, not a code change. 6 further rows are filed against `crates/axeyum-py` itself (the `Cargo.toml` feature matrix) and are excluded as double-counting.

## Backlog -- tier-R inventory rows neither referenced nor deferred

One bullet per inventory row. A row marked *(skip v1)* / *(later)* / *(v2)* in the inventory is flagged: the inventory recorded an intent, but plan 02's criterion asks for a recorded reason, which lives in [`artifacts/python-coverage-deferrals.json`](../../../artifacts/python-coverage-deferrals.json).

None. Every tier-R inventory row is referenced or carries a recorded deferral.

## Recorded deferrals

43 entries in [`artifacts/python-coverage-deferrals.json`](../../../artifacts/python-coverage-deferrals.json), covering 6 tier-R inventory rows and 176 public Rust items. `deferred` in the census line and in the table above is the FIRST of those two numbers -- the one plan 02's exit criterion is about.

`rows` counts matched inventory rows, `items` matched public Rust items. A deferral matching zero of both is not an error -- the inventories do not cover every crate -- but it is worth a look, because it usually means the item was renamed.

| key | rows | items | reason | slice |
|---|--:|--:|---|---|
| `axeyum-cas:BarrierProblem` | 0 | 1 | See `LyapunovProblem`: no public constructor path from Python, and the checker is bound at the artifact level. | -- |
| `axeyum-cas:LyapunovProblem` | 0 | 1 | `SosArtifact` has no public constructor from parts (recorded in inventories/cas.md), so the per-kind problem and certificate structs are reachable only INSIDE an artifact. The per-kind checkers are bound taking the artifact and refusing a mismatched kind, which is the whole capability; exposing the halves would let Python build a problem/certificate pair the crate never emits. | -- |
| `axeyum-cas:PsdNotSosProblem` | 0 | 1 | See `LyapunovProblem`: no public constructor path from Python, and the checker is bound at the artifact level. | -- |
| `axeyum-cas:RatFunc` | 1 | 1 | No reachable public constructor: `CasExpr` has no text parser and nothing public returns a `RatFunc` (crates/axeyum-cas/src/lib.rs:1580), so a Python wrapper would be a type a caller cannot make. Recorded as `Skip:` in inventories/cas.md. | -- |
| `axeyum-cas:UnaryFunc` | 1 | 1 | The 27 unary builders are projected individually as `Expr.ln` ... `Expr.ceiling` (plan 02-B). Exposing the tag enum would let Python build a `UnaryFunc` application the builders never construct, which is surface the Rust API does not have. | -- |
| `axeyum-cas:certifies_wz_sum` | 0 | 0 | Private in `lib.rs` (`fn certifies_wz_sum`, not `pub fn`), so the crate ships no independent public checker for a bare WZ multiplier. `cas.certify.moments.WzCertificate.check()` re-derives the telescoping identity and the exact finite base case through the PUBLIC decidable zero test instead, which is what makes a tampered multiplier rejectable from Python. | -- |
| `axeyum-cas:gf2_extension::BinaryExtensionConnectedAdamsTraceShardReport` | 0 | 1 | The report type of a deferred shard producer; see the long-cycle shard report. | S5 |
| `axeyum-cas:gf2_extension::BinaryExtensionLongCycleTraceShardReport` | 0 | 1 | The report type of a deferred shard producer; binding a report a Python caller cannot produce would be a type nobody can make (the `RatFunc` mistake). | S5 |
| `axeyum-cas:gf2_extension::binary_extension_connected_adams_trace_shard` | 0 | 1 | The sharded form of `binary_extension_connected_adams_trace`, which IS bound, with the same reason as the long-cycle shard. | S5 |
| `axeyum-cas:gf2_extension::binary_extension_long_cycle_trace_shard` | 0 | 1 | The sharded form of `binary_extension_long_cycle_trace`, which IS bound. A shard is only meaningful paired with `combine_*` across a distributed run, which is a job-scheduling concern, not a CAS call; a Python caller handed one shard has a partial sum it cannot interpret. Bind the pair together when a Python consumer schedules shards. | S5 |
| `axeyum-cas:gf2_extension::collapse_binary_extension_long_cycle_trace_subshards` | 0 | 1 | Subshard collapse for the same distributed route; two levels of a sharding scheme no Python consumer drives today. | S5 |
| `axeyum-cas:gf2_extension::combine_binary_extension_connected_adams_trace_shards` | 0 | 1 | The combine half of the connected-Adams shard pair. | S5 |
| `axeyum-cas:gf2_extension::combine_binary_extension_long_cycle_trace_shards` | 0 | 1 | See `binary_extension_long_cycle_trace_shard`: the combine half of the same pair, and useless without it. | S5 |
| `axeyum-cas:gf2_shard::*` | 0 | 13 | Every entry in this module reads or writes a shard file on disk (`gf2_shard.rs` is the only module in the crate deriving serde for a wire struct precisely because it persists). A Python binding would put filesystem side effects behind a CAS call, and the shard artifacts are already read by `axeyum.knowledge`. Slice S5 deliberately excluded it. | -- |
| `axeyum-cas:groebner::reduce` | 0 | 1 | Unbudgeted multivariate reduction with no cofactor record, so its result is not re-checkable -- `reduce_with_cofactors` returns the same normal form plus the `sum(cofactor*gen) + remainder == target` witness. | S5 |
| `axeyum-cas:groebner_basis` | 0 | 1 | Unbudgeted Buchberger: no `Limits`, no typed decline, so a Python caller cannot bound it and a runaway is indistinguishable from a hang. `groebner_cert::reduce_with_cofactors` (budgeted, certificate-producing) is bound instead -- plan 02-B says prefer `groebner_cert`. | S5 |
| `axeyum-cas:ideal_contains` | 1 | 1 | Same unbudgeted Buchberger call as `groebner_basis`; the checkable route is a cofactor certificate, which `groebner_cert` produces and Python can re-check. | S5 |
| `axeyum-cas:sos::check::check_system` | 0 | 0 | Private to the crate's checker (`sos/check.rs`); it takes an internal `System` value with no public constructor. The three PUBLIC per-kind checkers (`check_lyapunov`, `check_barrier`, `check_psd_not_sos`) are bound and each calls it. | -- |
| `axeyum-cnf:ProofSearchProgress` | 1 | 1 | An `mpsc::Sender` progress sink. Binding it means owning a Rust thread and draining a channel from Python; v1 omits the progress fields entirely (plan 02-A), which is also why `SolverConfig` can stay a plain `#[pyclass]`. | S8 |
| `axeyum-lean-import:CompletedStatementImport` | 1 | 1 | The consumed form of an import. It only exists between `into_parts` and its four outputs; the binding produces those outputs directly, so there is nothing for Python to hold. | -- |
| `axeyum-lean-import:into_parts` | 1 | 4 | Consuming API: it destroys the import, which a Python object referenced elsewhere cannot survive. The binding calls it internally (crates/axeyum-py/src/producers.rs:928) and projects the non-consuming accessors -- plan 02-D says skip `into_parts`. | -- |
| `axeyum-lean-import:verify_*` | 2 | 10 | Receipt verifiers are bound in the same slice as the `issue_*` producer they check, never before: a certificate must carry every distinction its producer makes, and a Python caller holding only the verifier can accept a receipt shape the issuer never emits. `issue_*` stays Rust-side until the receipt shapes freeze (plan 02-D). | S2 |
| `axeyum-lean-kernel:release_transient_tables_for_export` | 1 | 1 | One-way: it frees interning tables the kernel needs for everything except export, so a Python caller could poison a live `Kernel` handle and the failure would surface far from the call. Plan 02-C says document as one-way or omit; omitted. | -- |
| `axeyum-query:QueryBuilder` | 0 | 1 | `QueryBuilder<'a>` borrows the arena for the life of the builder, and a lifetime cannot cross the PyO3 edge. Python accumulates triples and builds the `Query` in one Rust call (plan 02-A). | -- |
| `axeyum-smtlib:Script::checked_flat_view` | 0 | 1 | Returns the raw assertion list, which is EMPTY on a word-first-fallback parse and would then solve as a vacuous `sat` -- a shipped P0. The binding projects `solvable_flat_view`, which returns `None` in exactly that case (plan 02-A). | -- |
| `axeyum-smtlib:sort_text` | 1 | 1 | Writer-internal sort formatting. Python writes scripts through `write_script`; a second text path is a second place for the writer to disagree with the parser, and nothing in the plan needs it. | -- |
| `axeyum-solver:CheckBudget` | 2 | 1 | `CheckBudget<'a>` borrows (crates/axeyum-solver/src/proof.rs:376) and a lifetime cannot become a `#[pyclass]`; the budgeted entry points take their budget at the call instead. Plan 02-A: not bound. | -- |
| `axeyum-solver:CheckProgress` | 2 | 1 | `mpsc::Sender` progress sink -- one of the two fields that make `SolverConfig` `Send` but `!Sync`. v1 exposes the 18 plain fields and omits both (plan 02-A); revisit as a background-thread iterator. | S8 |
| `axeyum-solver:CheckingProgress` | 1 | 1 | The payload of the `CheckProgress` channel; pointless without the sink. Marked *(v2)* in inventories/smt-solver.md. | S8 |
| `axeyum-solver:ProofProgress` | 0 | 1 | `mpsc::Sender` progress sink, the other `!Sync` field of `SolverConfig`. Same v1 omission and same v2 route as `CheckProgress`. | S8 |
| `axeyum-solver:confirm_bounded_string_verdict` | 2 | 1 | Takes `&mut axeyum_smtlib::Script`: it mutates a parse the caller owns, and it is already run inside `solve_smtlib` (ADR-0052 `StringGate`). Projecting it would build a second, weaker front door that a Python caller could invoke in the wrong order. | -- |
| `axeyum-solver:decide_word_only_script` | 1 | 1 | Internal word-only decision procedure over `&mut Script`; reachable from Python only through `solve_smtlib`, which is the route whose verdict the corpus gates measure. | -- |
| `axeyum-solver:length_lia_verdict` | 0 | 1 | Internal string route over `&mut Script`; see `word_route_verdict`. | -- |
| `axeyum-solver:lex_order_verdict` | 0 | 1 | Internal string route over `&mut Script`; see `word_route_verdict`. | -- |
| `axeyum-solver:membership_unsat_certificate` | 0 | 1 | The certificate half of `membership_unsat_lean_module`; deferred with it so producer and checker are projected together. | S2 |
| `axeyum-solver:membership_unsat_lean_module` | 0 | 1 | Regex-emptiness Lean module emitter; belongs with the rest of Lean reconstruction (S2) so that one slice owns the kernel round-trip and the footprint assertion. Marked *(later)* in inventories/smt-solver.md. | S2 |
| `axeyum-solver:membership_verdict` | 0 | 1 | Internal string route over `&mut Script`; see `word_route_verdict`. | -- |
| `axeyum-solver:online_string_verdict` | 0 | 1 | Internal string route over `&mut Script`; see `word_route_verdict`. | -- |
| `axeyum-solver:optimize_smtlib` | 1 | 1 | `config` is ignored upstream (`let _ = config;`, smtlib.rs:2091), so a bound version would advertise timeouts, node budgets and memory limits that do not apply -- a budget that cannot bite is worse than no budget. Bind once the Rust side honours it. | S9 |
| `axeyum-solver:optimize_smtlib_lexicographic` | 1 | 1 | Same ignored `config` (smtlib.rs:2119) as `optimize_smtlib`; same fix first, then bind both together. | S9 |
| `axeyum-solver:reconstruct::*` | 0 | 112 | Lean reconstruction is its own slice (09 S2), not a wrapper pass: every route has to round-trip through the kernel and assert an axiom footprint, and a binding that returned a `Proof` nobody checked would be the checker-that-cannot-fail defect with a Python front end. | S2 |
| `axeyum-solver:upgrade_bounded_string_unknown` | 1 | 1 | Same `&mut Script` shape and same reason as `confirm_bounded_string_verdict`; it is an upgrade-only companion of the gate the front door already runs. | -- |
| `axeyum-solver:word_route_verdict` | 0 | 1 | Internal string route over `&mut Script`. The front door picks the route; a Python caller choosing one directly can get an `unknown` the shipped route decides, which reads as a capability regression that is not one. | -- |
