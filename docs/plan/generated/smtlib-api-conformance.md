# SMT-LIB and Rust API conformance matrix

> **Generated; do not edit by hand.** Source: [`docs/plan/smtlib-api-conformance-v1.json`](../smtlib-api-conformance-v1.json). Regenerate with `python3 scripts/gen-smtlib-api-conformance.py`; use `--check` in validation.

This is a command/protocol matrix, not a theory-support matrix. It keeps syntax acceptance, execution semantics, returned representation, and assurance separate so a parser no-op or direct Rust API cannot be mistaken for an interactive SMT-LIB implementation.

## Snapshot

- 29 high-value command/API rows; 27 name exact tests.
- Parse states: absent 5, accepted/no-op 7, recorded globally 8, command-point 5, semantic 3, rejected 1.
- Execution states: none 8, direct API only 3, parse-time semantics 5, single-query helper 7, incremental query 5, optimization helper 1.
- Output states: none 15, Rust value 13, canonical text artifact 1.
- **Zero rows provide an interactive textual command session.** Existing helpers return Rust values or a proof string from a complete script.

## State definitions

- **Parse:** `semantic` changes declarations/terms immediately; `command-point` preserves ordering; `recorded globally` loses command-point ordering; `accepted/no-op` validates shape but retains no request; `rejected` fails explicitly; `absent` falls through to unsupported-command handling.
- **Execution:** a direct API is not textual command execution; a single-query helper cannot preserve a multi-query transcript; incremental query execution does preserve assertion-stack behavior at each check.
- **Output:** `Rust value` means typed library data, while `canonical text artifact` means a standalone artifact such as Alethe. Neither is a complete SMT-LIB stdout transcript.

## Matrix

| Surface | Syntax | Parse | Execution | Output | Assurance | Scope | Evidence | Residual |
|---|---|---|---|---|---|---|---|---|
| Logic declaration | `(set-logic L)` | recorded globally | none | none | none | The logic name is retained in SmtLibOutcome; solver dispatch is inferred from terms rather than gated or selected by the declared logic. | API `solve_smtlib`; tests `parses_core_benchmark_shape` | No declared-logic capability gate, strategy selection, or mismatch diagnostic; set-logic is metadata today. |
| Script metadata | `(set-info :key value)` | recorded globally | parse-time semantics | none | none | All key/value pairs are retained in a map; :status is separately exposed as benchmark ground truth and never consulted while solving. | API `solve_smtlib`; tests `records_info_options_and_get_info_queries` | Global map storage does not preserve repeated updates at their command points. |
| Option updates | `(set-option :key value)` | recorded globally | parse-time semantics | none | none | Values are retained and observable through get-option; most standard options do not alter solver behavior. | API `solve_smtlib_get_option`; tests `get_option_returns_recorded_values_and_defaults` | No command-point option history and no full semantic enforcement for produce-* flags, channels, print-success, seeds, or resource limits. |
| Satisfiability query | `(check-sat)` | command-point | incremental query | Rust value | model-replay, internal-proof-check | One result per recorded query through solve_smtlib_incremental; the single-result facade rejects multiple effective queries. | API `solve_smtlib`, `solve_smtlib_incremental`; tests `incremental_push_pop_multiple_check_sats`, `flat_front_door_rejects_multiple_check_sats` | No interactive stdout response stream, success tokens, or command-local diagnostics. |
| Assumption query | `(check-sat-assuming (l ...))` | command-point | incremental query | Rust value | model-replay, internal-proof-check | Assumptions are applied to one query and discarded; arbitrary parsed Boolean terms are accepted rather than a separately validated literal grammar. | API `solve_smtlib_incremental`; tests `check_sat_assuming_does_not_retain_assumptions` | No get-unsat-assumptions response and no interactive ordering with subsequent output commands. |
| Assertion scopes | `(push n)`, `(pop n)` | command-point | incremental query | none | model-replay, internal-proof-check | Assertion scopes are semantically honored; declarations remain global and queries are re-solved from scratch rather than requiring retained backend state. | API `solve_smtlib_incremental`; tests `incremental_nested_scopes_restore_on_pop`, `flat_front_door_honors_single_push_pop_scope` | Protocol compatibility is semantic but not necessarily warm-state performance compatibility; pop-underflow/error behavior needs a conformance fixture. |
| Assertion reset | `(reset-assertions)` | command-point | incremental query | none | model-replay, internal-proof-check | Clears assertions and open scopes while retaining declarations in both single-query and incremental facades. | API `solve_smtlib`, `solve_smtlib_incremental`; tests `reset_assertions_clears_assertions_not_a_no_op`, `flat_front_door_honors_single_reset_assertions` | No interactive success response or option-controlled print-success behavior. |
| Full solver reset | `(reset)` | rejected | none | none | none | Explicitly unsupported because the parse-then-execute model interns declarations into one shared arena. | tests `full_reset_is_rejected_not_silently_ignored` | A conformant session runner needs a fresh declaration/option environment boundary without stale arena state. |
| Model query | `(get-model)` | recorded globally | single-query helper | Rust value | model-replay | Returns declared constants and finite function interpretations for one SAT query; definitions and parser-internal symbols are omitted. | API `solve_smtlib_get_model`, `solve_smtlib_model`; tests `get_model_returns_declared_constant_values`, `records_model_symbols_and_functions` | No canonical SMT-LIB model rendering and no command-point association after multiple checks. |
| Term-value query | `(get-value (t ...))` | recorded globally | single-query helper | Rust value | model-replay, evaluator | Requested terms are accumulated globally and evaluated against the replay-checked model of one SAT query. | API `solve_smtlib_get_value`; tests `get_value_reads_the_model` | Multiple get-value commands lose command grouping and model chronology; values are not rendered as SMT-LIB responses. |
| Named-assignment query | `(get-assignment)` | accepted/no-op | single-query helper | Rust value | model-replay, evaluator | The helper evaluates active top-level :named assertions for one SAT query; the command itself is not recorded. | API `solve_smtlib_get_assignment`; tests `get_assignment_uses_scoped_active_names` | The helper cannot tell whether or where get-assignment was requested, and nested named subterms are outside its reported subset. |
| Assertion-stack query | `(get-assertions)` | command-point | incremental query | Rust value | none | Returns one vector of rendered active assertions per request after push, pop, and reset-assertions; assumptions are intentionally excluded. | API `solve_smtlib_get_assertions`; tests `get_assertions_returns_scoped_snapshots`, `get_assertions_ignores_check_sat_assuming_literals` | Snapshots are returned as a Rust aggregate, not interleaved with check responses in a textual transcript. |
| Information query | `(get-info :key)` | recorded globally | single-query helper | Rust value | none | Recorded metadata, name/version defaults, and computed :reason-unknown are supported; unknown keys return an unsupported marker. | API `solve_smtlib_get_info`; tests `get_info_has_defaults_and_unsupported_marker` | Requests are globally accumulated; command-point state and standard textual response/error conventions are missing. |
| Option query | `(get-option :key)` | recorded globally | single-query helper | Rust value | none | Returns the final recorded value, a conservative default, or unsupported for each accumulated key. | API `solve_smtlib_get_option`; tests `get_option_returns_recorded_values_and_defaults` | A request observes final map state rather than necessarily the option value at its command point. |
| Unsatisfiable-core query | `(get-unsat-core)` | accepted/no-op | single-query helper | Rust value | solver-recheck | Deletion-minimized active assertion subset, using :named labels when present; the helper does not require or locate the command. | API `solve_smtlib_unsat_core`; tests `unsat_core_uses_scoped_active_assertion_names` | No produce-unsat-cores precondition, command-point response, or SMT-LIB textual list. |
| Unsatisfiable-assumption query | `(get-unsat-assumptions)` | accepted/no-op | none | none | none | Syntax and arity only. | tests `accepts_standard_output_commands` | No result surface exists despite parser acceptance. |
| Proof query | `(get-proof)` | accepted/no-op | single-query helper | canonical text artifact | internal-proof-check, external-proof-check | Alethe for selected QF_BV, QF_UF/QF_ABV, QF_LRA, and QF_LIA shapes; every returned proof is rechecked internally, while external Carcara coverage excludes internal-only rules and LIA. | API `solve_smtlib_get_proof`; tests `get_proof_returns_a_checkable_alethe_proof`, `get_proof_serves_the_array_row_same_fragment` | The parser does not retain the request, option preconditions are not enforced, and no session runner emits the proof at its command point. |
| Echo output | `(echo "text")` | accepted/no-op | none | none | none | Syntax and arity only; the string is discarded. | tests `accepts_standard_output_commands` | A drop-in command session must emit the provided string in order. |
| Session termination | `(exit)` | accepted/no-op | none | none | none | Syntax and arity only; later parsed commands are not suppressed. | tests `accepts_standard_output_commands` | No session lifecycle exists, so exit does not terminate command processing. |
| Uninterpreted sort declaration | `(declare-sort U arity)` | semantic | parse-time semantics | none | model-replay | Arity-zero uninterpreted sorts are first-class and many-sorted; arity one and higher are explicitly rejected. | tests `uninterpreted_sort_is_first_class`, `uninterpreted_sort_collisions_and_arity_are_errors` | Parametric sort constructors remain unsupported. |
| Sort alias definition | `(define-sort S (P ...) body)` | semantic | parse-time semantics | none | none | Arity-zero aliases and alias chains resolve through compound sorts; nonempty parameter lists are explicitly rejected. | tests `define_sort_alias_inside_array`, `define_sort_rejects_parametric` | Parameterized aliases and sort-family substitution are absent. |
| Nonrecursive definitions | `(define-fun f ((x S) ...) R body)`, `(define-const c S body)` | semantic | parse-time semantics | none | evaluator, model-replay | Nullary aliases and parameterized hygienic macros expand with arity and sort checks; define-const reuses the nullary path. | tests `parameterized_define_fun_macros_expand_hygienically`, `define_const_is_nullary_define_fun` | Definitions are macro-expanded rather than represented as ordered environment declarations; recursive definitions are separate and absent. |
| Recursive definitions | `(define-fun-rec ...)`, `(define-funs-rec ...)` | absent | none | none | none | Command scanners recognize the keywords for preprocessing boundaries, but parse_command has no implementation arm. | source assertions only | No exact negative fixture, recursive-definition IR, termination/recursion semantics, or solver route. |
| Optimization objectives | `(maximize t)`, `(minimize t)` | recorded globally | optimization helper | Rust value | solver-recheck | Int and unsigned BitVec objectives; boxed and lexicographic helpers exist, while Pareto behavior lives outside a textual command session. | API `optimize_smtlib`, `optimize_smtlib_lexicographic`; tests `optimize_integer_objective`, `lexicographic_optimization_differs_from_boxed` | No option-driven priority protocol, incremental objective lifecycle, textual optimum/model response, or portable optimality certificate. |
| Objective query | `(get-objectives)` | accepted/no-op | none | none | none | Syntax and arity only; objective values are returned only by separate optimization helpers. | tests `optimize_integer_objective` | No command-faithful objective rendering or response ordering. |
| Interpolation command | `(get-interpolant ...)` | absent | direct API only | Rust value | verify-before-return, external-proof-check | Direct APIs cover selected QF_BV, QF_UF, LIA, LRA, UFLIA, and UFLRA families; there is no textual command contract. | API `qf_bv_interpolant`, `qf_uf_interpolant`, `lia_interpolant`, `lra_interpolant`; tests `certified_single_predicate_interpolant_matches_and_refutes` | Add exact command syntax/partition semantics and response ordering without misclassifying the already-implemented engines as absent. |
| CHC/Horn command family | `(declare-rel ...)`, `(rule ...)`, `(query ...)` | absent | direct API only | Rust value | verify-before-return | HornSystem direct API supports selected Real and Bool/BV systems, stratification, and compatible mutual SCCs; no fixedpoint session syntax exists. | API `solve_horn`; tests `safe_lra_horn_is_sat_and_rechecks` | No declare-var/relation/rule/query parser, fixedpoint option model, certificate serialization, or Spacer-compatible transcript. |
| Abduction command | `(get-abduct ...)` | absent | direct API only | Rust value | verify-before-return | The direct abduct API searches bounded one- and two-literal shared-vocabulary hypotheses and rechecks consistency and sufficiency. | API `abduct`; tests `lra_non_entailed_finds_verified_abduct` | No textual grammar/name contract, command response, minimality guarantee, or broader candidate language. |
| General SyGuS synthesis | `(synth-fun ...)`, `(constraint ...)`, `(check-synth)` | absent | none | none | none | No general SyGuS grammar IR or function-synthesis result surface exists. | source assertions only | This is the genuinely absent synthesis engine and remains demand-gated rather than inheriting credit from bounded abduction. |

## Negative controls

These rows are deliberately kept visible. Their source assertions fail the generator when the named parser command appears, forcing the matrix and its evidence to be updated together.

- **Recursive definitions:** No exact negative fixture, recursive-definition IR, termination/recursion semantics, or solver route.
- **Interpolation command:** Add exact command syntax/partition semantics and response ordering without misclassifying the already-implemented engines as absent.
- **CHC/Horn command family:** No declare-var/relation/rule/query parser, fixedpoint option model, certificate serialization, or Spacer-compatible transcript.
- **Abduction command:** No textual grammar/name contract, command response, minimality guarantee, or broader candidate language.
- **General SyGuS synthesis:** This is the genuinely absent synthesis engine and remains demand-gated rather than inheriting credit from bounded abduction.

## Accepted-but-not-executed controls

- **Named-assignment query:** The helper cannot tell whether or where get-assignment was requested, and nested named subterms are outside its reported subset.
- **Unsatisfiable-core query:** No produce-unsat-cores precondition, command-point response, or SMT-LIB textual list.
- **Unsatisfiable-assumption query:** No result surface exists despite parser acceptance.
- **Proof query:** The parser does not retain the request, option preconditions are not enforced, and no session runner emits the proof at its command point.
- **Echo output:** A drop-in command session must emit the provided string in order.
- **Session termination:** No session lifecycle exists, so exit does not terminate command processing.
- **Objective query:** No command-faithful objective rendering or response ordering.

## Planning consequence

The immediate compatibility project is an ordered textual session runner, not another theory engine. It should consume one command stream, emit one response per output command at the exact command point, enforce option preconditions, and reuse the existing helpers without re-solving or losing scope. Textual interpolation, Horn, and abduction commands should expose the existing verify-before-return direct APIs only after that runner gives their responses a precise ordering and error contract.
