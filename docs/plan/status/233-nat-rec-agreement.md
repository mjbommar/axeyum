# Lane: nat-rec-agreement — prove two `Nat.rec`-defined functions agree

<!-- plan-section: lane-status -->

**Your lane's block (`WIP`, nat-rec-agreement, 2026-08-29).** Building the
machinery to relate two independently-built `Nat.rec` instances, and using it
on `Nat.bitwise and_fn` vs `Nat.land`. IN PROGRESS — this is the early
commit the lane protocol requires; nothing is admitted through the kernel yet.

<!-- plan-section: landed-changes -->

| 2026-08-29 | nat-rec-agreement | lane opened: fuel-generalized agreement induction + `mod 2 ∈ {0,1}` case split |
