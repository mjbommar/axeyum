# Lane: pi — `CReal.pi` via cosine's first zero (Spivak ch. 15)

<!-- plan-section: lane-status -->

**Status: IN PROGRESS (pi, 2026-08-27).** Target: `CReal.pi := mul (ofNat 2) c`
where `c` is the exact root of `cosFnWide` on a sub-interval of `[0, 8/5]`,
via `CReal.ivt_exact_root` applied to `fun x => neg (cosFnWide x)` (whose
derivative is `sinFn`, positive on `[1, 8/5]`).

Three numeric rungs, being worked in order:
1. `cos 1 > 0` from `cosOne_alternating_lower`.
2. `cos (8/5) < 0` — an alternating-series bound at `8/5`, not at 1.
3. `sin z >= 1/(k+1)` uniformly on `[1, 8/5]`.

This block is a placeholder committed early per the lane protocol; it will be
rewritten with exact statements, timings and rejections before the lane ends.

<!-- plan-section: landed-changes -->

| 2026-08-27 | pi | lane opened: `CReal.pi` via `ivt_exact_root` at `cosFnWide` |
