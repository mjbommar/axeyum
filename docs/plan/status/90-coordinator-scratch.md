# Lane: coordinator — snapshots stop going into RAM

<!-- plan-section: landed-changes -->

| 2026-08-15 | `pending` | `scripts/lane-snapshot.sh`: the "build from your own `git archive HEAD` snapshot" recipe as a script, because as prose it did not take — of ~60 `git archive` recipes in tracked files, **one** used `tar --touch`. Extracts to `/data0` with correct mtimes, prints only the path, refuses a tmpfs scratch root, and stamps `.lane-owner`/`.lane-ref`. Motivated by measurement: `/tmp` here is a **62 G tmpfs (RAM)**, found at 81% / 50 G with `Shmem` 45.1 G of a 123 G box and **9.3 GB in fifteen abandoned axeyum snapshots**, four 15–20 h old and none held open — a standing contributor to this box's OOM kills; 2.7 GB reclaimed. Ten controls, two of which caught the script itself: ownership is stamped *before* extraction (a 5-min timeout killed a 127-s extract and left an unattributable orphan — the exact problem it exists to end), and reuse is gated on a `.lane-complete` sentinel (the first draft handed back *any* existing directory, so a truncated checkout would be built against and measured as the commit — a wrong number with no symptom). (coordinator) |
| 2026-08-15 | `pending` | Amended [`88-import-scale.md`](88-import-scale.md): its headline "`String` literals: 52% of `Init`+`Std`, 79% of Mathlib" has since been cashed out by `import-strings` and does not mean what it looks like — clean rate went 43.8% → 50.8% and 19.5% → 34.8%, so strings bought **7 and 15 points**, not 52 and 79. What they bought is reach: records arriving at the kernel rose **18.6×** and **86×**. One conclusion flips — "not one Mathlib-specific root blocker" was true only because strings were hiding the Mathlib. (coordinator) |
