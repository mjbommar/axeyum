# Lane: coordinator — snapshots stop going into RAM

<!-- plan-section: landed-changes -->

| 2026-08-15 | `pending` | `scripts/lane-snapshot.sh`: the "build from your own `git archive HEAD` snapshot" recipe as a script, because as prose it did not take — of ~60 `git archive` recipes in tracked files, **one** used `tar --touch`. Extracts to `/data0` with correct mtimes, prints only the path, refuses a tmpfs scratch root, and stamps `.lane-owner`/`.lane-ref`. Motivated by measurement: `/tmp` here is a **62 G tmpfs (RAM)**, found at 81% / 50 G with `Shmem` 45.1 G of a 123 G box and **9.3 GB in fifteen abandoned axeyum snapshots**, four 15–20 h old and none held open — a standing contributor to this box's OOM kills; 2.7 GB reclaimed. Ten controls, two of which caught the script itself: ownership is stamped *before* extraction (a 5-min timeout killed a 127-s extract and left an unattributable orphan — the exact problem it exists to end), and reuse is gated on a `.lane-complete` sentinel (the first draft handed back *any* existing directory, so a truncated checkout would be built against and measured as the commit — a wrong number with no symptom). (coordinator) |
| 2026-08-15 | `pending` | Amended [`88-import-scale.md`](88-import-scale.md): its headline "`String` literals: 52% of `Init`+`Std`, 79% of Mathlib" has since been cashed out by `import-strings` and does not mean what it looks like — clean rate went 43.8% → 50.8% and 19.5% → 34.8%, so strings bought **7 and 15 points**, not 52 and 79. What they bought is reach: records arriving at the kernel rose **18.6×** and **86×**. One conclusion flips — "not one Mathlib-specific root blocker" was true only because strings were hiding the Mathlib. (coordinator) |
| 2026-08-15 | `pending` | Audited whether the fact ledger's checkers can fail: **40 of 162 checker runs, across 36 settled facts, exit 0 on completion alone**. Not all are defects, but the biggest family is — `nat_theorem_inventory -- this_theorem_does_not_exist` prints `0 theorems` and exits 0, which is the exact shape of `F:nat-add-comm`'s checker, and `nat_axiom_inventory` prints `nat: axiom=0` and exits 0 *whatever the number is*, so `axiom_footprint: []` on 31 kernel-lean facts is asserted by nothing. All three inventory examples are plain `fn main()` with no `ExitCode`/`exit`/`assert`/panic. Recorded as finding 8 in [`docs/refactor-2026-08/README.md`](../../refactor-2026-08/README.md); the fix is with the `kernel-reuse` lane, which owns those files. (coordinator) |
| 2026-08-15 | `b94b56425` | First instance of finding 8, fixed: `F:schedule-critical-chain-infeasible` recorded **30** axioms while the code produced **26**, drifted unnoticed because the checker exited 0 without comparing. Benign in direction — `False`/`Eq` became `inductive` with `Eq.rec` the recursor, a real shrink of the trusted surface, and the module still references both — but the same silence would have hidden growth. `infeasibility_farkas_lean` gains `--dump-axioms` (sorted names, so a ledger diff is a diff) and `--expect-axioms N` (fails on drift either way, and fails when the kernel route never produced a module rather than treating an unreached check as a pass). Ledger corrected and the checker now carries the pin. (coordinator) |
| 2026-08-15 | `pending` | **The private-index remedy leaves a staged revert of your own commit behind it** — seventh clobbering incident, second one caused by the fix. Committing from a private index advances `HEAD` while the *shared* `.git/index` still holds the pre-commit blobs, which relative to the new `HEAD` reads as a staged revert (a staged **deletion**, for a newly added file). Hit twice within an hour: `quant-pins` found a staged `−138` revert of the golden-pin fix it had just landed; the coordinator's was `−430` across ten files including deletion of a 130-line script committed minutes earlier. Every file was byte-identical to `HEAD` on disk — content never at risk, only the index, which is why it is invisible to `ls` and `git show`. Remedy recorded in CLAUDE.md: after committing, `unset GIT_INDEX_FILE` and `git add` exactly your committed paths, then confirm `git diff --cached --stat HEAD` is empty. Never `git read-tree HEAD` the shared index to fix it — that drops other lanes' legitimate staging. (coordinator) |
| 2026-08-15 | `pending` | Finding 8 closed for its worst family. Wired the assertion flags `kernel-reuse` landed into **22 facts** (17 `nat_axiom_inventory`, 3 `nat_theorem_inventory`, 5 `int_theorem_inventory`), so `axiom_footprint: []` on the kernel-lean facts is now asserted by something rather than by nothing. Two defects caught only by *running* the rewritten commands: cargo eats a flag with no `--` separator (`--require-axiom-free` became "unexpected argument"), and the theorem filter is a **substring** match — `add_neg` returns four theorems, so a pinned count would have been both wrong now and brittle as the library grows. Used the name-anchored `grep` idiom eight int rows already had. 17/17 rewritten commands pass; five negative controls (including the original `nat_theorem_inventory -- <nonexistent>` case) all exit non-zero. (coordinator) |
| 2026-08-15 | `pending` | **n=7 sorting-network lower bound: two thirds established, and the remaining third was mis-provisioned rather than hard.** Shards 0/3 and 1/3 are COMPLETE — 2011 and 2010 of 6031 cubes, every one UNSAT and backward-checked, in 13.4 h and 14.1 h. Shard 2/3 was healthy but running **4 workers on a 16-core box at load 4.1** while three other hosts sat at load 0.00, giving 83 s/cube against the others' 24 and an ETA of ~31 h. Split it three ways: `sorting_network.rs:913` filters `i % shards == shard`, so `{2/9, 5/9, 8/9}` covers exactly `2/3` (`i%9 ∈ {2,5,8} ⟺ i%3 == 2`) — verified by source *and* empirically, the three headers summing to 2010. Launched as memory-bounded `systemd-run --user` units on s4/s6/s7, never `nohup`. **Provisioning measurement worth keeping: 14 workers gave HALF the throughput of 8.** s4 (14 jobs) 0.020 cubes/s vs s6 (10) and s7 (8) both 0.040, on identical 16-core hosts with no competing load and no memory pressure (114 G free on s4). Per-cube DRAT generation plus backward checking is bandwidth-bound well before core count; provision future campaigns at ~half the cores, not all of them. Two misreadings on the way: shard 2 logs progress to **stderr**, so its 146-byte stdout looked like a wedged job until 34 KB of `[663/2010] ok` turned up; and its "no output in 15 h" was my own arithmetic — it was 3 minutes in. (coordinator) |
| 2026-08-15 | `pending` | `fact-frontier.py` was under-reporting our own capability, and the fix for that re-introduced the overstatement the file exists to prevent. Its `DECIDABLE` set was authored, and `QF_FP` was missing from it while `F:fp8-add-monotone-rne` — same fragment — sat in the ledger **proved** on `smt-clausal`, so the queue printed `NO ROUTE` for work that was dispatchable and a lane reading it would have skipped `F:fp16-add-monotone-rne`. Replaced the authored list with a seed **augmented by demonstration**: a fragment settled on a terminating route is decidable by us, and the report now names the fact that demonstrates each one (same rule ADR-0465 just adopted for the axiom ledger — derive from measurement, do not author). `kernel-lean` is deliberately excluded from the terminating routes, or any Nat theorem would make Goldbach's fragment "decidable". First run of that rule printed **`F:collatz-reaches-one … DECIDABLE — dispatch it`**, because a conjunctive-query fact carries `fragment: "none"` and was settled by `search-certificate` — the exact Goldbach-class overstatement this file's header was written about, re-created within minutes by the fix for a different one. Sentinel fragments are now excluded by name; both controls verified (Collatz back to `NO ROUTE`, fp16 still dispatchable). (coordinator) |
| 2026-08-15 | `pending` | **Session wrap-up.** Four lanes (`import-projrec`, `solver-decomp`, `numerics`, `nat-numbertheory`) stopped mid-step on a spend limit. Assessed rather than swept: the worktree *compiled*, and two lanes had left complete, coherent slices, landed on their behalf with gates re-run here rather than inherited — `import-projrec`'s structure-eta rule (276 lib + all suites, the new `structure_eta_recursor_major` 6 and real-Lean crosscheck 1, and `k_like_reduction` back to 7/7, which an earlier lane had correctly reported failing as *not theirs*), and `solver-decomp`'s certificate-data extraction (1155 solver tests, clippy `-D warnings` clean after one missing pair of backticks, and the module-graph script re-run to confirm the **65 modules / 115,840 lines → 24 / 58,215** claim rather than trusting it). Resolved a concurrent **ADR-0466 collision** — two lanes picked the next free number at once; `import-projrec`'s kept it since its code already referenced it, `numerics`' became ADR-0467. **Deliberately NOT committed: the five `bench-results/frontier/*.json`.** They record a run on 24 unpinned cores at load ~23 which the harness itself marked `comparable: false, ratchetable: false`, against committed baselines from 8 pinned cores at load 5.7 marked `true`. Committing them would have lowered the `lia_cuts` ratchet 35 → 26 on a contended run — the phantom regression `frontier-ratchet-reference-frame.md` exists to prevent. (coordinator) |
