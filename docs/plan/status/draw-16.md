# Lane: draw-16

Status: IN PROGRESS — 2026-08-31

Task: author nursery draw 16 (layout RP, indices 0 and 3 held-out), which
requires performing R11's authorable disclosure review for the two newly
opened held-out families `natural-primitive-recursion` (ADR-1240) and
`natural-integer-root` (ADR-1245), and re-screening the layout with the real
`select()`/`assign_partitions()`/`guard()` machinery against a freshly built
`shape_search --release`.

## Landed changes

(none yet — this is the first commit, made early per the lane protocol)
