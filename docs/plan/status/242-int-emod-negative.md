# Lane: int-emod-negative — `Int.emod_natAbs_bound` and `Int.gcd_div`'s three named lemmas

<!-- plan-section: lane-status -->

**Your lane's block (`WIP`, int-emod-negative, 2026-08-29).** Starting from
`docs/plan/status/236-int-gcd-div-2.md`'s handoff: building
`Int.emod_natAbs_bound` (the sign-general remainder bound), then
`Int.ediv_emod_unique_general`, then `Int.gcd_div` itself, in
`crates/axeyum-lean-kernel/src/int_prelude/division.rs` (emod/ediv bounds) and
`gcd.rs` (`gcd_div`). This commit is a placeholder recording intent before any
kernel-checked code lands — no declarations added yet.

<!-- plan-section: landed-changes -->

| 2026-08-29 | int-emod-negative | WIP placeholder, no code yet |
