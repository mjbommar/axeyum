# Lane: divergence-bitwise — register Nat.land/lor/ldiff/bitwise in the divergence registry

<!-- plan-section: lane-status -->

**Your lane's block (`WIP`, divergence-bitwise, 2026-09-02).** Starting work
on the `testbit-codomain` lane's named next action: `Nat.land`, `Nat.lor`,
`Nat.ldiff` (and `Nat.bitwise`) diverge from Mathlib by the same standard the
registry applies to `Nat.minFac`, and none has a registry row yet. In
progress.

<!-- plan-section: landed-changes -->

| 2026-09-02 | divergence-bitwise | what landed, in one line |
