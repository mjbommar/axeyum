# Lane: simp-1 — the third producer: rewrite-chain `simp` for the kernel

<!-- plan-section: lane-status -->

**Your lane's block (`WIP`, simp-1, 2026-09-03).** Building the `simp` rewrite-
chain producer in `crates/axeyum-lean-kernel/src/simp/`, modeled on `linarith`
and `ring`: an oriented rewrite set (identity/annihilator/defining laws per
carrier), outermost-first first-order matching to a fixed point with a
`MAX_STEPS` budget, each step emitted as a congr-lifted `Eq.trans`/`Eq.symm`
chain and kernel-checked. Starting with ℕ, then ℤ, then `List` (the payoff:
first producer over a non-numeric carrier). Retiring ten hand-proofs in
`nat_prelude` whose bodies are pure rewrite chains, then five in ℤ, three in
`List`. Just started; no code landed yet.

<!-- plan-section: landed-changes -->

| 2026-09-03 | simp-1 | status stub opened, starting research pass |
