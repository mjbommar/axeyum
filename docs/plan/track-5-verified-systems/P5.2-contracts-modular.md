# P5.2 — Contracts & modular verification

## Goal

`#[requires(..)]` / `#[ensures(..)]` attributes on Rust functions that compile
into hypotheses/goals over the reflected terms, and **modular verification**:
each function is verified against its contract *assuming its callees'
contracts*, making function calls a composition rule instead of an inlining
problem.

## Why / leverage

This is the architectural unlock, not a convenience. ADR-0296/0297 now cross
one exact scalar LLVM boundary, but the general reflector still cannot compose
the checksum module without body-derived functional summaries (its earlier MIR
result relied on the inliner). SPARK and Verus both won adoption with exactly
this contract shape. Contracts keep every obligation **finite and decidable** —
the callee's `ensures` is just another hypothesis — which is what separates
this from ghost-code deductive systems. It also gives `#[verify]` its graduation
path: today's "no-annotation panic-freedom" stays the zero-config default;
contracts are the opt-in for cross-function claims.

## Dependencies

- P5.1 (reflected functions to attach contracts to; T5.1.1's API).
- The existing `axeyum-verify` proc-macro surface
  (`crates/axeyum-verify-macros/`) — extend, don't fork.

## Tasks

| ID | Task | Key references | Size | Exit |
|---|---|---|---|---|
| T5.2.1 | **Contract surface + lowering**: `#[requires]`/`#[ensures]` (expressions over params/return in the supported scalar/array fragment) parsed by `axeyum-verify-macros` and lowered to terms over the reflected function's symbols | `crates/axeyum-verify-macros/src/`, `crates/axeyum-verify/src/verify.rs` | M | a contracted function verifies (ensures proved under requires) and a violated contract yields a replayed witness |
| T5.2.2 | **WIP (ADR-0296--0299 accepted) — Modular calls**: checked LLVM and MIR scalar calls use body-verified exact or relational contracts after discarding the body. Nontrivial loop-call `requires` enters `trans` only after its exact path-conditioned complement becomes a replayable, source-attributed bad state. Both checksum callers receive distinct fresh results plus separately verified `ensures`; weak contracts expose genuine havoc. The MIR resolver independently proves the checked callee panic predicate false before following the normal-return edge | P5.1 executors; `reflect/{llvm,mir}` contract routes | M–L | modular checksum re-proves on both checked IRs without an inliner or conflated panic/postcondition |
| T5.2.3 | **PROPOSED (ADR-0315) — Panic contracts**: existing constructors retain `panic_when = false`; an explicit checked-MIR callee panic summary propagates into the caller and guards the normal-return relation before source annotations | `reflect_mir_into_checked` panic terms; ADR-0315 | S–M | a two-function chain's panic predicate matches the inlined proof, with exhaustive replay and a concrete callee-panic witness |
| T5.2.4 | **WIP (ADR-0296--0299 accepted) — Contract soundness gate**: exact modular/inlined `leaf` formulas and 100,000 tuples agree; the nontrivial-requirement gate adds 100,000 classified rows and replayed bad states. Each checksum IR classifies 100,000 valid plus 100,000 violating choices and replays a weak-contract countermodel. Separate checked-MIR panic-freedom and cross-IR modular equivalence pass; the standing gate is 81 variants / 17 groups / ten binaries / 114 tests | `tests/llvm_direct_calls.rs`; `tests/checksum_module.rs`; checked MIR contract tests | M | committed gate, DISAGREE=0 between modular and inlined routes on both IRs |

## Phase exit criteria

- A multi-function example (the checksum module is the canonical one) verifies
  modularly on both IRs, with contract violations producing replayed witnesses.
- The modular-vs-inlined differential gate is committed and green.
- The annotation story is documented: zero-annotation panic-freedom remains the
  default; contracts are additive.

ADR-0315 deliberately establishes the runtime panic-composition rule before
annotation lowering. It does not let source syntax silently define unwind or
postcondition semantics.

## References

- The `#[verify]` UX baseline: [`docs/consumer-track/verify/PLAN.md`](../../consumer-track/verify/PLAN.md)
- Precedents: SPARK (`GNATprove`) contract flow; Verus `requires/ensures`
  (their deductive lane is explicitly *not* copied — ADR-0056 §Decision-3).
