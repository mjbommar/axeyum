# P5.3 — Kernel-shaped obligations: memory regions, 2-safety, FSM refinement

## Goal

The three obligation families a seL4-inspired Rust kernel/network stack
actually needs, each chosen because it is **finite, BV/array-shaped, and
already inside axeyum's strong fragment**: (a) bounded memory regions and
page-table-style index math, (b) 2-safety hyperproperties by self-composition
(constant-time / secret-independence), (c) protocol-FSM refinement (spec
machine ≃ reflected implementation).

## Why / leverage

This is where Track 5 differentiates on *substance*, not just trust: the
ecosystem's own first verification target (Asterinas/vostd) is the page-table
module; constant-time verification of kernel/crypto primitives is a high-value
niche that is decidable and badly served; and the FSM refinement rung is the
network-stack story (the spec-side toolkit already ships —
`protocol_toolkit.rs`, PDR/k-induction). Self-composition is nearly free in our
architecture: the cross-IR suites already lower two functions over shared
symbols in one arena; 2-safety is the same trick with one function twice.

## Dependencies

- P5.1 (front end; T5.1.5 memory for (a)).
- Shipped: `Sort::Array`/QF_ABV, `TransitionSystem` + `prove_safety_pdr` /
  `prove_safety_k_induction` / `bounded_model_check`, the protocol-FSM toolkit.
- (b) and (c) do not depend on T5.1.5 and can start immediately after T5.1.1.

## Tasks

| ID | Task | Key references | Size | Exit |
|---|---|---|---|---|
| T5.3.1 | ✅ **DONE (branch leakage)** (`ac7494f0`). **2-safety by self-composition**: the MIR reflector records control-flow leakage (`switchInt` scrutinees) via `reflect::mir::reflect_mir_params_with_leaks`; `reflect::hyper::control_flow_ct_goal` reflects a function twice over shared-public / distinct-secret inputs and conjoins the pairwise equalities of the leaked branch decisions. `constant_time.rs` (4 tests): public-predicated PROVED constant-time while its output is refuted secret-independent (the crisp distinction); secret-predicated REFUTED with a replay-checked distinguishing witness; branch-free trivially CT. **Residual:** memory-access index leakage (cache timing) — only branch leakage today; LLVM-side leakage; the `(pub,sec)/(pub,sec')` shape on both IRs. | `reflect/{mir,hyper}.rs`; `tests/constant_time.rs` | M | ✅ constant-time proved + leaky refuted w/ witness (MIR); memory-index leakage + LLVM side are the residual |
| T5.3.2 | **WIP (ADR-0318 rejected; ADR-0319 accepted) — Bounded memory regions + page-table math**: the operation/block audit fit ADR-0288, but the owning-Cargo gate exposed compiler-emitted nested `scope` metadata for named locals. ADR-0319 now accepts only bare numeric nested scopes containing typed locals/debug metadata, with depth/brace/content fail-closed gates, and the exact walk fixture reaches the existing checked-memory profile. This closes the metadata prerequisite only: no raw walk artifact or invariant is admitted, and any fresh walk attempt requires a separately preregistered successor | T5.1.5; ADR-0287--0289; `crates/axeyum-fp`-style bit-field patterns; ADR-0010 arrays | L | a walk implementation's invariants proved (aligned, in-range, permission-monotone) with replayed witnesses for unmasked index, unaligned frame, and permission escalation |
| T5.3.3 | **FSM refinement**: prove a reflected implementation step function refines a spec-side `TransitionSystem` (simulation relation as a BV predicate; per-transition obligation, discharged per event) | `tests/protocol_toolkit.rs`, `protocol_unbounded.rs`; [`protocol-state-machines.md`](../../consumer-track/verify/protocol-state-machines.md) | L | one protocol (the shipped handshake) with spec FSM ⟵ refinement ⟶ reflected Rust step function, safety transported across the refinement |
| T5.3.4 | **Obligation catalog doc**: one page per obligation family — the exact goal shape, fragment, evidence route, and a worked example — the track's user-facing "what can it prove" | this folder | S | committed; linked from the track README and the verify SCOREBOARD |

## Phase exit criteria

- Each family has ≥1 committed, green, replayed/certified example on reflected
  code (not hand-built terms), with wall-times recorded.
- The refuted variants exist for each family (a leaky compare, a broken walk, a
  skipping FSM) — the prover must be demonstrably discriminating here too.

## References

- Application charter §ladder: [`verified-systems-and-protocols.md`](../../consumer-track/verify/verified-systems-and-protocols.md)
- Market precedent for (a): Asterinas `vostd` (Verus on OSTD page tables).
- Unbounded engines: [`unbounded-protocol-safety.md`](../../consumer-track/verify/unbounded-protocol-safety.md)
