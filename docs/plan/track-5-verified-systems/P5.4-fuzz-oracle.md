# P5.4 — The fuzzing loop: reflections as oracles, witnesses as seeds

## Goal

Productize the verification↔fuzzing hybrid the prototypes exercised by hand:
**reflected terms as differential-fuzzing oracles** (real execution vs
reflection, DISAGREE=0), **solver countermodels as seed corpora** aimed at
exact boundary conditions, and an honest **`unknown` → directed-fuzz handoff**
for whatever the prover declines.

## Why / leverage

The goal statement is "formal verification *and fuzzing* in a natural way" —
and the two are one loop here, not two tools. Every prototype suite already
closes it manually: solver finds the witness → `catch_unwind` replays it →
neighbors confirmed clean. No current Rust tool offers "proofs where decidable,
solver-directed fuzz where not, one harness"; this phase is mostly *packaging*
of demonstrated capability, which is why it is M-sized and parallelizable with
P5.2.

## Dependencies

- P5.1 (the reflection API; T5.1.1 suffices to start).
- Shipped: witness replay patterns (`checked_*` suites), the deterministic
  differential fuzz (`cross_ir_equivalence.rs`), `axeyum-verify`'s
  witness-to-`#[test]` rendering (`reproduce`).

## Tasks

| ID | Task | Key references | Size | Exit |
|---|---|---|---|---|
| T5.4.1 | ✅ **DONE** (`2423eaeb`). **Oracle harness**: `reflect::oracle::DiffFuzz` runs deterministic differential fuzz (seeded LCG + width corners + masks) in both shapes — `check_agree` (reflection ≡ reflection) and `check_against` (reflection ≡ real `fn`, oracle gets sampled inputs in symbol order); `FuzzReport`/`assert_agreed` give the DISAGREE=0 one-liner. First two suites collapsed onto it (cross-IR differential fuzz, checksum module oracle); the remaining `llvm_reflection` buffer/mixed-width loops are the follow-up. | `reflect/oracle.rs`; `tests/cross_ir_equivalence.rs`, `tests/checksum_module.rs` | S–M | ✅ harness landed + 2 suites converted, green; full-suite conversion is the residual |
| T5.4.2 | ✅ **DONE (`873c671e`, `75971d1d`, `1efa7f25`; ADR-0339). Witness corpus**: only replay-confirmed countermodels enter typed seeds; panic, source-postcondition, and raw QF_BV equivalence witnesses form one lexically ordered canonical JSON corpus plus generated `#[test]`s through the existing renderers. Full-width signed interpretation and all native signed/unsigned scalar/array boundary literals are regression-owned | `witness_corpus.rs`; `tests/witness_corpus.rs`; `tests/fixtures/witness-seed-corpus/`; `tests/reproduce_render.rs`; ADR-0339 | M | ✅ three independently obtained classes replay before admission; exact 1,404-byte JSON and 712-byte generated source are committed, included, compiled, executed, reverse-order reproduced, and mutation-tested |
| T5.4.3 | **WIP — implementation pushed at `3d75d407`, proposed ADR-0340. Reason-preserving `Unknown` handoff**: one fail-closed hybrid entry point retains checked proof and replayed-refutation outcomes; only a structured solver `Unknown` emits a deterministic guarded QF_BV target and sampled `fuzzed-only` report. Errors, replay failures, guard drops, violations, and oracle disagreements stay separate. The three branches, callback isolation, replay/disagreement errors, deterministic reruns, width-128 sampling, JSON controls, and independently parsed two-input violation semantics are tested; exact fixtures, the complete rejection matrix, mutation identities, and full capped acceptance gates remain | `axeyum-verify::directed_fuzz`; `SolverConfig` budgets; `UnknownReason`; T5.4.1 oracle conventions; ADR-0340 | M | an artificially hard two-input guarded goal yields exact target/report artifacts and a runnable source-oracle harness; proved/refuted+replayed/fuzzed-only branches and callback isolation are tested |
| T5.4.4 | **Coverage accounting**: the report separates proof-covered input space from fuzz-sampled space (never conflated) — the honesty layer the scoreboard discipline requires | bench-results conventions | S | report format documented + one committed example |

## Phase exit criteria

- One harness call gives: proofs where decided, replayed witnesses where
  refuted, directed fuzz where unknown — with the three outcomes reported
  distinctly and DISAGREE=0 between reflection and execution everywhere
  sampled.

## References

- Charter §fuzzing: [`fuzzing-and-multi-peer.md`](../../consumer-track/verify/fuzzing-and-multi-peer.md)
- The replay discipline: every prototype `checked_*` suite in
  `crates/axeyum-verify/tests/`.
