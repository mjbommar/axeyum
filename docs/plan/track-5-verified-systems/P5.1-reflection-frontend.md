# P5.1 — The reflection front end (crate-ify MIR + LLVM reflection)

## Goal

Turn the prototype reflectors in `crates/axeyum-verify/tests/reflect_common/`
into a real, reusable front end: parse the **actual artifacts of a `cargo`
build** (rustc MIR text, LLVM `.ll`) with full-fidelity parsers, reflect a
documented Rust/LLVM fragment into `axeyum-ir` terms plus **panic-condition
terms**, and expose the entry points the rest of the track builds on.

## Why / leverage

Everything in Track 5 flows through this: contracts (P5.2) attach to reflected
functions, kernel obligations (P5.3) are goals over reflected terms, the fuzz
loop (P5.4) uses reflections as oracles, and the external target (P5.5) is
"point the front end at their code." The prototypes proved the *semantics*
work — 34 green test binaries, millisecond proofs; what's missing is
robustness (real compiler output, not hand-curated fixtures), coverage
(calls, loops, memory), and a consumable API.

## Dependencies

- The prototype scaffolds (done, rounds Q–U — see the
  [track README](README.md#where-it-starts-from-2026-07-03-prototype-rounds-qu)).
- **Crate creation is ADR-gated** (ADR-0001 discipline): the `axeyum-reflect`
  boundary gets its own ADR when T5.1.1 lands, with the test-scaffold usage as
  the "proven by use" evidence.
- No Track 1 keystone dependency — the eager pipeline suffices for v1.

## Tasks

| ID | Task | Key references | Size | Exit |
|---|---|---|---|---|
| T5.1.1 | ✅ **DONE** (`cc695925`, ADR-0057). **Module, not crate**: `tests/reflect_common/{mod,mir,llvm}.rs` promoted to `axeyum_verify::reflect` (submodules `reflect::mir`/`reflect::llvm`) — a real library API (`reflect_mir_*`, `reflect_ll`/`reflect_into`, checked variants returning `(value, panic)`, the shared `binop`/`compare`/`width_of` vocabulary + proof/eval harness). 8 test binaries (62 tests) rewired to `use axeyum_verify::reflect::…` and green; `missing_docs` + `implicit_hasher` API-hardened; clippy/rustdoc `-D warnings` clean. Crate split deferred (ADR-0057: one consumer today). | `crates/axeyum-verify/src/reflect/` | M | ✅ 8 reflection test binaries green against the module API; ADR-0057 accepted |
| T5.1.2 | **WIP (ADR-0279--0284) — structured `.ll` parser**: ADR-0280 adds the non-panicking function boundary and first compatibility migration. ADR-0281 accepts typed straight-line scalar instructions plus checked value+definedness reflection. ADR-0282 accepts typed PHIs/terminators and whole-function predecessor/successor validation on real compiler diamonds. ADR-0283 accepts bounded checked acyclic execution with explicit definedness. ADR-0284 accepts canonical scalar CFG rendering and exact LLVM identifier escapes: the scalar parse→print→parse fixpoint, structural projection, `llvm-as`, and checked semantic gates pass. Memory, `freeze`/`undef`, and module-wide resolution remain later slices. Do not create a shared parser crate until a second implemented consumer proves the boundary. | `src/reflect/llvm.rs`; `src/reflect/llvm/{checked,syntax}.rs`; LLVM LangRef; ADR-0279--0284 | M–L | parses unmodified `rustc -O --emit=llvm-ir` / `clang -S -emit-llvm` output for the supported fragment; malformed/unsupported input returns diagnostics rather than panicking; property: parse→print→parse fixpoint on the corpus |
| T5.1.3 | **WIP (ADR-0287--0289 accepted) — MIR extraction pipeline**: one ordinary Rust source fixture is bound to exact compiler identity/argv, raw stdout, hashes, stable-CI drift checks, and exact-toolchain byte replay. The authenticated module feeds a named, located, non-panicking typed reflection path. `axeyum-mir-build` now selects an explicit locked Cargo manifest/package/lib-or-bin/function under the registered toolchain, checks before atomic raw retention, emits deterministic typed/term JSON, and reproduces one standalone target package's 1,438 bytes and store/load contract in two runs. General target semantics and `stable_mir` remain open. | `docs/consumer-track/verify/real-rust-frontend.md`; ADR-0287--0289 | M | a target crate's function reflects from its *own build's* MIR with one command; fixture drift is detected, not silently stale |
| T5.1.4 | **Loops → `TransitionSystem` bridge**: detect reducible loops in either IR and auto-construct the transition system (the manual construction exists in `tests/llvm_reflection.rs`); bounded unroll (`#[unwind(K)]`) as the fallback | `crates/axeyum-solver` PDR/k-induction/BMC; `tests/llvm_reflection.rs` loop reflector | L | the existing manual loop proofs reproduce through the automatic bridge; cyclic-CFG panic replaced by routing |
| T5.1.5 | **WIP (ADR-0286--0289 accepted) — memory beyond byte-array prototypes**: LLVM, authenticated direct-rustc MIR, and Cargo-owned MIR each carry one checked initialized byte object with explicit access safety, stores, final-memory joins, source replay, and the same proved four-byte store/load contract. MIR access panic is independent of compiler `assert`; its conditional write is path-sensitive. Wide/endian-sensitive accesses, aliases/provenance, and general places remain later. | `tests/reflect_common/llvm.rs`, buffer reflectors in `tests/llvm_reflection.rs`; ADR-0010; ADR-0286--0289 | L | the straight-line buffer proofs reproduce inside the CFG executor; a store/load roundtrip proof lands on both IRs |
| T5.1.6 | **Semantics gate**: promote the cross-IR differential fuzz + refutation corpus to the front end's own CI gate; every new op/instruction lands with (a) an equivalence or spec proof and (b) fuzz coverage | `tests/cross_ir_equivalence.rs`, `tests/cross_ir_refutation.rs` | S | gate documented + wired; DISAGREE=0 standing |

## Phase exit criteria

- A named public API reflects the supported fragment of **both** IRs from real
  build artifacts, returning `(value, panic)` term pairs.
- The prototype capability set (CFG, checked arithmetic, bounds, division,
  casts, multi-param, byte arrays) is preserved under the migration with all
  suites green, plus loops and bounded memory landed.
- The supported fragment, the UB stance, and the unsupported-construct behavior
  (loud decline, never silent misreflection) are documented in one place.

## References

- Prototype design log: [`reflect-common-abstraction.md`](../../consumer-track/verify/reflect-common-abstraction.md)
- Feasibility notes: [`real-rust-frontend.md`](../../consumer-track/verify/real-rust-frontend.md),
  [`llvm-ir-frontend.md`](../../consumer-track/verify/llvm-ir-frontend.md)
- Design precedent: Hyperkernel (SOSP '17) — LLVM-level, SMT-backed, finitized
  interfaces; our deltas are certificates + no external solver + dual-IR.
