# P5.5 — A real external target, measured

## Goal

Point the front end at a **real, external Rust systems project** and commit a
measured result: a module verified (with evidence) or a real bug found and
reproduced — under the same scoreboard discipline as every other measured
claim (DISAGREE=0, committed baselines, wall-times, no seeding).

## Why / leverage

The standing course-correction applies to Track 5 doubly: **measure, don't
seed**. Fixtures we wrote can only prove the machinery works on what we chose
to write. One verified module of someone else's kernel — or one reproduced bug
— is the difference between a demo and a capability, and it is what makes the
positioning claim ("Verus-but-checkable / Hyperkernel-but-Rust") defensible in
public.

## Dependencies

- P5.1 (must reflect real build artifacts, not curated fixtures).
- At least one of P5.2 (if the module has internal calls) / P5.3 (if the
  obligation is memory/2-safety/FSM-shaped).
- Target selection should happen **early** (during P5.1) because it fixes the
  fragment-coverage priorities.

## Candidate targets (from the 2026-07-06 landscape survey; pick by smallest viable)

| Candidate | Why | Risk |
|---|---|---|
| **Maestro** (Linux-compatible Rust kernel, early-stage) | small, monolithic, plausibly bug-rich; a found+reproduced panic is a headline | young codebase churns; MIR fixture drift |
| **Hubris / Tock components** (production embedded Rust) | small, real, safety-critical, stable | teams may have stronger invariants already; fewer easy finds |
| **Asterinas OSTD slice** (the vostd/Verus target) | direct, benchmarkable head-to-head vs the Verus approach on the *same module* (page tables) | largest fragment demands (memory model); highest bar |
| **A `rust-sel4` userspace task** | composes with the seL4 story directly (QSOE-style) | smaller audience; toolchain setup |

Selection is recorded as a short decision note in this folder (not a full ADR
unless it forces new public surface).

T5.5.1 selects Maestro's three scalar device-number encode/decode functions at
an exact upstream revision. The [selection note](P5.5-target-selection-maestro-device-id.md)
records the candidate comparison, owning-build feasibility observation,
universal obligation shape, external-capture gaps, and the no-vendoring license
gate. No external artifact or result is admitted by selection alone.

ADR-0326 closes that owning-build route negative. The replacement
[selection note](P5.5-target-selection-tock-log2.md) chooses Tock's public
32/64-bit integer-log helpers at exact revision `ac5d597d`. ADR-0327 accepts
their checked LLVM-22 `range`/`ctlz` prerequisite. ADR-0328 now freezes the
zero-row authenticated build/extraction protocol. Its producer, ten focused
mutation/cleanup tests, and exact registration were pushed before invocation;
v1 then closes at the locked-offline cache preflight because `ghash 0.4.4` is
absent. Zero builds/target bytes exist. A dedicated-cache v2 requires a fresh
zero-result decision before preparation or builds. ADR-0329 freezes and runs
that input-only protocol, but preparation v2 closes before download because the
namespace omits the runtime target of the host resolver symlink. A fresh v3
must authenticate the minimal resolver file and a real DNS probe while keeping
the offline/inventory/no-compilation gates unchanged. ADR-0330 now freezes that
one-file network-only bind and pinned IPv4 probe. Its thin producer proves DNS
and completes the fetch, then v3 closes because the inventory rejects a
legitimate Cargo/libgit firmware pack-index hard link. V4 needs a fresh
canonical hard-link-row decision; no cache survives.

ADR-0331 now freezes one lexicographic owner plus explicit alias rows with
shared mode/size/hash/link count and outside-root rejection. Every non-inventory
preparation gate remains unchanged. Pushed v4 proves that inventory, then
closes because the inherited probe wrongly equates 162 active metadata packages
with all 169 lock entries. V5 needs structural package-ID/lock authentication.

ADR-0332 now freezes that structural validator and makes the active count/digest
result-only. Pushed v5 accepts an independently replayed 3,077-row / 41.18 MB
distinct-byte cache (`fd6ee33d`) and a 162-node/814-edge active graph
(`da6971e4`) against 169 lock entries, with zero OOM deltas. No build exists.

ADR-0333 now freezes capture v2: exact cache/result replay and read-only virtual
mount, structural offline metadata replay, then ADR-0328's unchanged two-root
raw-module/LLVM-22 extraction/admission gates. The outer-atomic wrapper,
registration, eight focused plus 33 inherited protocol tests,
local-result/inventory replay, and physical/virtual cache-path checks pass
pre-build. Pushed `9bff9d2e` then closes v2 before Cargo/build: structural replay
receives the merged capture registration without cache-only
`expected_lock_packages` and raises `KeyError`. Cleanup leaves no output/OOM
delta. Preregister v3 changing only the replay argument to the pinned full cache
registration.

ADR-0334 now freezes that one-object correction and exact v2
registration/negative lineage. Pushed v3 accepts two raw-identical
2,651,673-byte modules (`f9a1e155...`), admits both selected helpers, records
1,105/1,033 ms and 289,104/288,312 KiB, and has zero path leaks/partial/OOM
deltas. Generated bytes stay ignored and metadata identity is
`9ec0a0c3...84b9`. T5.5.2 closes; preregister T5.5.3 before any query.

ADR-0335 now freezes T5.5.3: exact authenticated canonicals, eight checked
defined/zero/floor-log/MSB proofs, six reflected/native-replayed controls,
proof-producing pure-Rust QF_BV with only certified trust steps, explicit
limits, pushed-HEAD archive isolation, and atomic scoreboard. Commit/push the
zero-row ADR precedes implementation. The ignored runner/producer/registration,
five focused producer tests, independent-spec test, strict Clippy, and full
package suite now pass with the authenticated test skipped. Commit/push this
checkpoint before archive preflight. Pushed preflight rejects HEAD's unrelated
sole absolute corpus symlink before Cargo; the corrected registration skips
exactly that link and retains regular-input hashes. Commit/push and repeat
preflight before one invocation. The pushed-HEAD preflight passes, but the sole
v1 invocation is frozen negative before compilation because archived HEAD's
committed lock is stale under `--locked --offline`. Zero queries or rows run;
cleanup leaves no output. Preregister v2 changing only to a matching committed
lock snapshot and a new output path.

ADR-0336 now freezes that v2 correction. It pins v1's exact negative and the
corrected committed lock, inherits every semantic/solver/trust/replay/resource
gate, versions the schemas/output, and requires a pushed-HEAD locked-offline
compilation of only the non-authenticated independent-spec test before one
official invocation. No v2 producer, query, or row exists yet.

The v2 wrapper and registration now pass five focused plus five inherited
producer tests and live lineage/capture validation without creating output.
Commit/push these exact bytes before the non-authenticated archived compilation
preflight; no target query or scoreboard row has run.

Pushed producer `07b22549` passes that fresh archived-HEAD compilation under the
cap: one independent-spec test passes, the authenticated test is filtered out,
and `proof-v2` remains absent. Commit/push this zero-query gate before one
official v2 invocation.

The sole v2 invocation is accepted as a negative. The first authenticated
`log_base_two` definedness query returns `Proved`, but the evidence ledger marks
BitBlast uncertified while Tseitin and SatRefutation are certified. The frozen
gate grants no proof credit and stops before controls or later properties;
cleanup leaves no output. Do not rerun or weaken v2. Audit the existing lowering
checker/evidence path before deciding whether a new proof-infrastructure ADR is
warranted.

## Tasks

| ID | Task | Key references | Size | Exit |
|---|---|---|---|---|
| T5.5.1 | ✅ **DONE — replacement selected after bounded negative**: Maestro `major`/`minor`/`makedev` closes at the isolated build boundary; Tock `log_base_two`/`log_base_two_u64` at revision `ac5d597d` now bind exact source/toolchain/license identities, real kernel uses, independent obligation shape, and the checked `range`/`ctlz` gap | [Maestro note](P5.5-target-selection-maestro-device-id.md); [Tock note](P5.5-target-selection-tock-log2.md); ADR-0323--0327 | S | committed replacement note; no external result admitted |
| T5.5.2 | ✅ **DONE (ADR-0327--0334)**: accepted cache `fd6ee33d`/active graph `da6971e4`; pushed v3 builds two independent raw-identical 2,651,673-byte modules (`f9a1e155...`), admits both selected helpers, records 1,105/1,033 ms and 289,104/288,312 KiB, and has zero path leaks/partial/OOM deltas. Generated bytes remain ignored; committed metadata identity is `9ec0a0c3...84b9`. Maestro gets no exception | T5.1.2–3 pipeline; [Tock selection](P5.5-target-selection-tock-log2.md); ADR-0327--0334 | S–M | authenticated external capture/parser admission complete; no query/scoreboard credit |
| T5.5.3 | **WIP — v2 measured trust-boundary negative (ADR-0335/0336)**: v1 closes pre-query on stale lock. V2 compiles; first target definedness returns `Proved`, but BitBlast is uncertified while Tseitin/SatRefutation are certified. Frozen all-certified gate credits zero rows and stops; no output survives. V2 is closed | ADR-0335/0336; capture `9ec0a0c3...84b9`; `proof-v1-negative.json`; `proof-v2-negative.json` | M | audit existing lowering checker/evidence; only then preregister certified bit-blast lifting or close the target proof cell honestly |
| T5.5.4 | **Upstream + write-up**: report reproduced bugs upstream (responsibly); a write-up doc comparing effort/coverage/trust vs the target's existing tooling (e.g. vostd/Verus for Asterinas) | `docs/consumer-track/verify/` | S–M | upstream issue links (if bugs) + committed comparison note with measured numbers only |

## Phase exit criteria

- A committed, regenerable result on an external target with the scoreboard
  discipline, plus the honest write-up. If the result is "the fragment was too
  small for this target," that is *recorded as the measured result* and drives
  the next P5.1 iteration — the phase does not silently move goalposts.

## References

- Scoreboard discipline: [`bench-results/SCOREBOARD.md`](../../../bench-results/SCOREBOARD.md)
  conventions; the measured-not-seeded course correction (2026-06-23, PLAN.md).
- Landscape survey: ADR-0056 §Context.
