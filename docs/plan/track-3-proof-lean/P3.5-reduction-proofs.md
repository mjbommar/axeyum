# P3.5 — Alethe for the reductions (retire trust-ledger entries)

**Size:** M per theory · **Depends on:** [P3.2 Alethe IR](P3.2-alethe-ir.md),
[P3.0 ledger](P3.0-trust-ledger.md), and the lazy/checkable reductions from
Track 2 · **Front:** reduction certificates (load-bearing #2)

## Goal
Emit Alethe proof steps for the non-bit-blast reductions so each becomes
independently checkable, retiring its [trust-ledger](P3.0-trust-ledger.md) entry.
Priority order follows cvc5's maturity: **arrays (fully checked) → Ackermann
(congruence) → int-blast (rewrites)**, keeping honest holes for fpa2bv and the
int-blast core (matching cvc5's own boundary).

## Tasks
| id | task | key references | size | exit |
|---|---|---|---|---|
| T3.5.1 | **Arrays**: `ARRAYS_ROW/IDX/ROW_CONTRA/EXT` steps (one per resolved read; checker re-derives `select(store…)`) | `references/cvc5/src/theory/arrays/proof_checker.cpp`; `references/cvc5/proofs/eo/cpc/rules/Arrays.eo` | M | array `unsat` carries a checkable reduction proof; retires the array `TrustId` |
| T3.5.2 | **Ackermann (UF)**: each congruence constraint as `eq_congruent`; the fresh-var introduction is the remaining seam (matches cvc5) | `references/cvc5/src/proof/` (PREPROCESS_ACKERMANN) | M | UF reduction congruence steps check; residual seam ledgered |
| T3.5.3 | **Int-blast**: term-rewrite steps (`INT_TO_BV_ELIM`-style) checkable; the blasting pass core stays a ledgered hole (matches cvc5) | `references/cvc5/src/theory/bv/int_blaster.cpp` | M | int-blast rewrites check; core hole ledgered |
| T3.5.4 | **Datatypes**: constructor/recognizer reduction steps | `references/cvc5/src/theory/datatypes/` | M | datatype reduction proof checks |
| T3.5.5 | **FP (fpa2bv)**: keep the miter-style faithfulness as the interim cert; ledger the rest | this repo `bitblast_miter.rs` pattern for fp; `references/cvc5` FP proofs | M | fpa2bv has a faithfulness cert or an explicit ledgered hole |
| T3.5.6 | Wire each into `produce_evidence` so a combined `unsat` carries clausal + reduction proofs; update the trust-ledger report | this repo `crates/axeyum-solver/src/evidence.rs` | S–M | the trusted-base report shrinks as theories are certified |

## Phase exit criteria
- Arrays, Ackermann, and int-blast reductions emit Alethe steps that Carcara
  (P3.3) / the in-tree checker (P3.4) validate; their trust-ledger entries are
  retired or reduced to a precise residual seam; the aggregate trusted base
  measurably shrinks (golden-tested report).

## References
`docs/plan/references/proof-and-lean.md` (per-reduction obligations table; the
seam principle).
