# Notes: top-three-focus

Detail moved out of
[`../status/126-top-three-focus.md`](../status/126-top-three-focus.md) so the
generated operational authority stays within its per-lane size bound.

## 2026-08-25 progress

The lane established the top-three plan, a production-only episode gate, a
kernel-authoritative lemma search index, and held-out-safe neighborhood and
candidate tools. It repaired Python CI dependency coverage, a pinned-nightly
MIR fixture, dependency assurance, and a broken CReal Rustdoc link. The live
audit compared the code—not only documentation—with proof assistants, SMT
solvers, proof agents, CAS systems, and program analyzers.

The autonomous loop admitted `Nat.ModEq.symm`, `Nat.ModEq.trans`, and then the
newly dependency-ready `Nat.ModEq.comm` through the same reusable producer.
Multi-target production credit rose from 8 to 11. This proved that durable
ledger state changes later scheduling, while the zero-theorem-dependency comm
proof showed it was not yet proof-term compounding.

The graph-to-kernel bridge then resolved the open `Nat.fib_mono` fact's authored
dependency to the exact axiom-free kernel theorem `Nat.fib_le_succ`. A native
reference construction settled `Nat.fib_mono`; its kernel dependency closure
records the adjacent-step theorem. That pattern was subsequently extracted as
the target-agnostic `Nat.monotone_of_le_succ` combinator. Production provenance
still classifies the fact as `no_operation`, honestly separating checked
mathematical compounding from autonomous production.
