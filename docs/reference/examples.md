# Runnable Examples

This page inventories every Cargo example checked into the workspace. It keeps
three different things separate:

- **learning examples** are safe starting points and print to standard output;
- **artifact generators** intentionally create or refresh reports and require a
  reviewed output path or clean-tree check; and
- **maintainer diagnostics** are narrow measurement probes, not stable product
  interfaces.

An example being present under `examples/` means Cargo builds it under the
appropriate all-target gate. It does not make its arguments, output schema, or
performance a public compatibility promise. For library APIs, use
[Public API](public-api.md); for the supported SMT-LIB boundary, use
[SMT-LIB support](smtlib-support.md).

## Start with these

These commands have no external solver requirement and do not intentionally
write tracked files:

```sh
cargo run -p axeyum-solver --features full --example first_smtlib_query
cargo run -p axeyum-cas --example cas_tour
cargo run -p axeyum-cas --example certified_calculus
cargo run -p axeyum-bench --example curriculum_demo
```

| Example | Crate | What it demonstrates |
|---|---|---|
| [`first_smtlib_query`](../../crates/axeyum-solver/examples/first_smtlib_query.rs) | `axeyum-solver` | Parses one QF_BV script, obtains a typed model, and prints the wrapped 8-bit witness. Requires `--features full`. |
| [`geometry_portfolio`](../../crates/axeyum-solver/examples/geometry_portfolio.rs) | `axeyum-solver` | Compares the specialized LRA and incomplete NRA routes on the same small geometry goals. Requires `--features full`; timings are measurements, not API guarantees. |
| [`cas_tour`](../../crates/axeyum-cas/examples/cas_tour.rs) | `axeyum-cas` | Broad tour of exact and certified computer-algebra operations. |
| [`certified_calculus`](../../crates/axeyum-cas/examples/certified_calculus.rs) | `axeyum-cas` | Focused differentiate/integrate examples with explicit certification labels. |
| [`curriculum_demo`](../../crates/axeyum-bench/examples/curriculum_demo.rs) | `axeyum-bench` | Connects the concept DAG, rendered exercises, sound grading, checked QF_BV evidence, and curriculum coverage. |
| [`scenario_pipeline_report`](../../crates/axeyum-bench/examples/scenario_pipeline_report.rs) | `axeyum-bench` | Runs the self-checking scenario catalog through the BV pipeline and reports deterministic sizes plus informational timings. |
| [`scenario_scaling`](../../crates/axeyum-bench/examples/scenario_scaling.rs) | `axeyum-bench` | Sweeps a self-checking mixing workload to show AIG/CNF and solve-time scaling. |

Run the last three benchmark-oriented examples in release mode when the timing
numbers matter:

```sh
cargo run --release -p axeyum-solver --features full --example geometry_portfolio
cargo run --release -p axeyum-bench --example scenario_pipeline_report
cargo run --release -p axeyum-bench --example scenario_scaling
```

## Import and trust-boundary tools

| Example | Invocation after `--example <name> --` | Output and boundary |
|---|---|---|
| [`lean4export_import`](../../crates/axeyum-lean-import/examples/lean4export_import.rs) | `<export.ndjson\|-> [theorem]` | Imports one format-3.1 stream and prints an assurance-separated inventory. With a theorem name, it additionally derives the exact declaration identity, axiom footprint, and direct theorem dependencies from the imported kernel. Admission does not authenticate the producer or imply complete Lean compatibility. |
| [`lean4export_composition`](../../crates/axeyum-lean-import/examples/lean4export_composition.rs) | `<source.ndjson> <target.ndjson> <root>+` | Imports two proof-isolated streams, composes the selected theorem closure into a private target clone, replays the V5 receipt, and rejects any added theorem with a non-empty kernel footprint. It prints the exact receipt and performs no ledger write. |
| [`nat_mod_invariant_specialization`](../../crates/axeyum-lean-import/examples/nat_mod_invariant_specialization.rs) | `<proof.ndjson> <target.ndjson> [--probe-dvd-gcd]` | Composes the constructive remainder invariant and checked native arithmetic helpers, specializes their named declarations into target `Nat.dvd_mod_iff`, replays all receipts, requires an empty footprint, and checks the native theorem type shape. The optional probe measures one- and two-leaf `Nat.dvd_gcd` closures and fail-closed admission frontiers without publishing a failed clone or writing the ledger. |
| [`nat_fib_iterate_recurrence`](../../crates/axeyum-lean-import/examples/nat_fib_iterate_recurrence.rs) | `--native-composition --stream <r080.ndjson>` | Reconstructs the fixed admitted `Nat.fib_add_two` candidate without search, composes its exact proof closure into the axiom-free native Nat kernel in dependency order, replays the receipt, and audits an empty footprint and direct theorem-dependency set. |
| [`nat_fib_iterate_recurrence`](../../crates/axeyum-lean-import/examples/nat_fib_iterate_recurrence.rs) | `--native-coprime-control --stream <r080.ndjson>` | Reconstructs the bounded Fibonacci-neighbor coprimality induction twice in the completed native kernel, requires exact theorem identities, an empty footprint, and the frozen eight-theorem dependency set. It issues no r082 receipt because official/native gcd semantics remain unbridged. |
| [`nat_fib_native_definition_probe`](../../crates/axeyum-lean-import/examples/nat_fib_native_definition_probe.rs) | `<r082.ndjson>` | Composes the exact target-stream `Nat.fib` definition into the native Nat kernel, replays the checked receipt, audits the added control theorem, and leaves the caller and ledger unchanged. |
| [`nat_gcd_succ_specialization`](../../crates/axeyum-lean-import/examples/nat_gcd_succ_specialization.rs) | `<nat-mod-invariant.ndjson> <r082.ndjson> <nat-gcd-bridge.ndjson> [--all-support]` | Reconstructs an axiom-free official `Nat.gcd_succ` by checked specialization and retries either `Nat.dvd_gcd` alone or all seven planned Fibonacci support roots with explicit target-owned leaves. |
| [`lean4export_census`](../../crates/axeyum-lean-import/examples/lean4export_census.rs) | `<export.ndjson>...` | Walks every declaration in one or more streams through the same fail-closed gate `lean4export_import` uses, but never stops at the first decline. `lean4export_import` reports only its FIRST blocker per stream, so a single run's decline is a sample, not a distribution — a prior "27 declines in 4 clusters" handoff was 27 first-blocker samples; the census over the same data found 10 distinct roots and 61 of 93 declines were `UnknownConst` cascades from an earlier skip, not independent blockers. |
| [`nat_add_reduction_probe`](../../crates/axeyum-lean-import/examples/nat_add_reduction_probe.rs) | `<export.ndjson>` | Reduces `Nat.add n (succ m)` to whnf inside a kernel loaded from a real import and narrows to the smallest head mismatch. Used to tell a *reducer* problem from a *def-eq* problem when a `DeclarationValueMismatch` decline gave only "does not reduce" — the fix for the case that motivated this probe turned out to be `Proj`/`Proj` congruence in `def_eq`, not the reducer. |
| [`string_literal_reduction_probe`](../../crates/axeyum-lean-import/examples/string_literal_reduction_probe.rs) | `<export.ndjson\|-> [declaration]` | Reduces a named definition in a real stream to a `String` literal and requires the kernel to identify it with `String.ofList` over its Unicode scalars, built from the **imported** `String`/`List`/`Char.ofNat` rather than a fixture shaped like them. A census answers "did the gate refuse anything"; this answers "does the literal mean what Lean means by it". The reordered-scalar control runs in the same invocation, because agreement without it measures nothing. |
| [`wf_recursion_decline_probe`](../../crates/axeyum-lean-import/examples/wf_recursion_decline_probe.rs) | `<export.ndjson>` | Reduces a `TypeMismatch` decline to the smallest pair of terms the kernel refuses to identify, *inside the staging environment that refused them*. `lean4export_census` can only report two arena indices, and those cannot be printed after the fact because the staging kernel is dropped with the census; this re-runs the same records through the same gate and walks the two terms down — whnf, spine, congruence, binders — printing the first pair at each depth the kernel calls not definitionally equal. It admits nothing: the refused declaration is absent from the kernel the inspector sees. `KernelError` carries only the two `ExprId`s, so reduction happens in a fresh empty `LocalContext` and a bare `_fvar.N` in the output means "ask the local context", not "the kernel is stuck here". Written for `Nat.bitwise._unary`, which now prints `PROBE\|no kernel decline`; use `Nat.Linear.Poly.denote_reverse` for a stream that still declines. |
| [`statement_adapter_import`](../../crates/axeyum-lean-import/examples/statement_adapter_import.rs) | `<export.ndjson> <target-definition>` | Imports one stream through the proof-isolated *statement* adapter (`import_statement_ndjson`) and prints the checked goal identity: the target's content SHA-256, the rendered goal and its SHA-256, and the declaration and axiom counts. The adapter admits exactly one `definition : Prop := statement` target and refuses a stream carrying a trusted declaration, a non-`Prop` goal, universe parameters on the target, or any other target cardinality — so what it prints is a *statement* that a kernel checked, never a proof of one. |
| [`prelude_axiom_inventory`](../../crates/axeyum-lean-kernel/examples/prelude_axiom_inventory.rs) | no arguments | Prints the deterministic reconstruction-prelude axiom inventory as tab-separated, hex-delimited data. Covers the `real`, `integer` and `string` preludes **only** — it never builds `nat` or `logic`, so zero rows for those means "not enumerated", not "axiom-free". |
| [`probe_add_structure`](../../crates/axeyum-lean-kernel/examples/probe_add_structure.rs) | no arguments | Dumps the structural `Declaration::Definition` value of `Nat.add` beside its rendered text. Built to separate a *printer* defect from a *kernel defeq divergence* when real Lean rejected an exported module — read the structure, never the printed text, when the printer is the thing under suspicion. |
| [`nat_axiom_inventory`](../../crates/axeyum-lean-kernel/examples/nat_axiom_inventory.rs) | no arguments | The same inventory for `nat` and `logic`, and over the FULL trusted surface (`Axiom`, `Opaque`, `Quotient`) rather than `Axiom` alone. Per-prelude counts go to stderr because an empty stdout is the expected result for an axiom-free prelude. |
| [`nat_theorem_inventory`](../../crates/axeyum-lean-kernel/examples/nat_theorem_inventory.rs) | `[name-substring]` | Every theorem the Nat prelude admits, with its canonical `render_lean` type — the paste-into-a-fact form. Declarations go through a helper taking an interned `NameId`, so this is the only way to read the inventory without building the environment. |
| [`theorem_axiom_footprint`](../../crates/axeyum-lean-kernel/examples/theorem_axiom_footprint.rs) | `[name-substring]` | Per-declaration axiom footprints (`Kernel::axiom_footprint`) for the `nat`, `integer` and `real` preludes — this kernel's `#print axioms`. Reports axioms alongside theorems, because `integer` and `real` declare no substantive theorems. |
| [`creal_shape_probe`](../../crates/axeyum-lean-kernel/examples/creal_shape_probe.rs) | no arguments | Prices ℝ before building it (ADR-0512). Admits the constructive-real carrier, its recursor, the representative projection and the setoid relation over the CONSTRUCTED `Rat`, and reports the trusted surface that costs — measured 0, because equality is a defined `CReal.Equiv` rather than `Eq`, so no quotient and therefore no `Quot.sound`. Carries a `funext` negative control in a second kernel whose footprint comes back non-empty, so the zero discriminates rather than merely being printed. |
| [`creal_setoid_witness`](../../crates/axeyum-lean-kernel/examples/creal_setoid_witness.rs) | no arguments | ℝ **constructed**, and **all 22 ordered-commutative-ring laws proved over it** at a measured trusted surface of **0** — a Bishop setoid of regular ℚ-sequences (ADR-0512 phase R2, complete). Where [`creal_shape_probe`](../../crates/axeyum-lean-kernel/examples/creal_shape_probe.rs) priced the carrier parametrically before ℚ had an order, this is the real thing over the constructed `Rat`. The exit status depends on eight findings, and the last six are what stop a green light being vacuous: every declaration checked and footprint-empty; the whole trusted surface still empty; `CReal.ofRat` present, so `CReal.Regular` has a **solution** and the carrier is not the empty type (every law would otherwise hold, footprint-free, of nothing); `CReal.Equiv.not_zero_one` present, so `Equiv` is not the **total** relation, which is also an equivalence relation; `CReal.not_le_one_zero`, without which all three `le` laws hold footprint-free of the relation that relates every pair; `CReal.zero_lt_one` **plus** `CReal.lt_irrefl`, because six of the seven strict-order laws only CONSUME a `lt` and so all hold, footprint-free, of the EMPTY relation; `CReal.ofRat_mul` **plus** `CReal.not_equiv_mul_one_one_zero`, because `mul_zero`, `mul_comm` and `sq_nonneg` all hold, footprint-free, of the product `fun _ _ => zero` — `ofRat_mul` pins the OPERATION on the embedded ℚ rather than asserting a property of it; and finally `CRealPrelude::ordered_ring_laws` naming **22 distinct** footprint-empty theorems, which is where the headline count is read from and nowhere else. Verified by mutation: deleting any one witness, or any one law, leaves every other row green and still exits 1. Thirteen of the 22 are the `Real` package's statements **verbatim**; the other nine mention `Eq` there and are stated over `CReal.Equiv` here, because `Eq CReal` is not the equality of real numbers — making that substitution good is phase R4, which this example does not claim. |
| [`complex_ring_witness`](../../crates/axeyum-lean-kernel/examples/complex_ring_witness.rs) | no arguments | ℂ **constructed** over the constructed ℝ, and **all 9 commutative-ring laws proved over it** at a measured trusted surface of **0** — a pair of `CReal`s under the componentwise *defined* equality `Complex.Equiv`, so no quotient is taken at either level and `Quot.sound` is never needed (ADR-0521). The exit status depends on seven findings, and the last three are what stop a green light being vacuous or degenerate: every named declaration checked and footprint-empty; the whole trusted surface still empty; `ComplexPrelude::ring_laws` naming **9 distinct** footprint-empty theorems, which is where the headline count is read from and nowhere else; `Complex.ofReal` present, so the carrier is not the empty type; `Complex.Equiv.not_zero_one` **plus** `Complex.Equiv.not_zero_I`, because the first refuses the total relation and only the second refuses one that ignores the imaginary component entirely — under which ℂ would be ℝ; `Complex.ofReal_mul` **plus** `Complex.I_sq`, because `mul_comm`, `mul_zero` and `left_distrib` all hold, footprint-free, of `fun _ _ => zero`, and `ofReal_mul` cannot pin `I` since `I` is not in the image of `ofReal`; and finally `Complex.no_compatible_order` present **and** `Complex.le`/`Complex.lt` absent from the environment. That last pair is the point: the 13 order laws of the `Real` package are missing here because they are **refuted**, not deferred — the theorem quantifies over both relations and derives `False` from seven of them with `I` as the witness — and an omission looks identical to a refutation unless the witness checks both halves. |
| [`lean_hypothesis_binding_dump`](../../crates/axeyum-solver/examples/lean_hypothesis_binding_dump.rs) | `<file.smt2>` | Deliberately dumb: parses a query, solves it, renders the Lean module, and prints the source assertions beside the module's `axeyum.reconstruct.*.hyp._N` axioms — making no judgement about whether they correspond. `scripts/check-lra-hypothesis-binding.py` re-parses and re-normalizes BOTH sides in Python, sharing no code with this dumper or with `axeyum-smtlib`, so the renderer's own notion of "same constraint" cannot launder a mis-transcription. Splitting it this way is the point: the thing being checked must not also be the judge. |
| [`characterization_status`](../../crates/axeyum-lean-kernel/examples/characterization_status.rs) | no arguments | Prints whether our `Nat` and `Int` are provably *the* naturals and integers, rather than merely axiom-free inductive types with those names. Peano categoricity for ℕ (every model of the Peano axioms is in structure-preserving bijection with ours, so it is pinned up to unique isomorphism); for ℤ, no-junk, generation by 1, and discreteness — properties **proved to hold of** `Int`, which is weaker: they are not shown to determine it. Exits non-zero on a non-`Theorem` entry, a non-empty axiom footprint, or a population change, so it is a checker rather than a report. |
| [`arith_model_witness`](../../crates/axeyum-lean-kernel/examples/arith_model_witness.rs) | no arguments | Prints a machine-checked model of the `Real` axiom package inside the axiom-free `Int` development: each `Real` law's `witness` is a theorem whose type is that law with the eight carrier/operation constants substituted, proved by the corresponding `Int` theorem and admitted by the kernel. Answers whether the 30 trusted `Real` declarations are *satisfiable*, which `nat_axiom_inventory`'s count alone does not — but it is relative consistency, **not** a discharge: `Int` is not `Real`. |
| [`creal_model_witness`](../../crates/axeyum-lean-kernel/examples/creal_model_witness.rs) | no arguments | The same table as [`arith_model_witness`](../../crates/axeyum-lean-kernel/examples/arith_model_witness.rs), at a carrier that **is** ℝ (ADR-0512 phase R4). Each witness `Real.CRealModel.<law>` is a theorem whose type is the `Real` axiom **as it stands in the environment** with the eight carrier/operation constants substituted and the partial application `Eq Real` read as `CReal.Equiv`, proved by the corresponding `CReal` theorem and type-checked by the kernel at admission — so an axiom `CReal` does not satisfy makes the build FAIL rather than dropping a row. Measured: 22/22 witnesses footprint-empty, 22/22 syntactically the `CReal` law up to binder names, **9/22** restated over `CReal.Equiv` (ADR-0512 Measurement 2, read out of the kernel). The exit status also depends on the population coming from the environment (a 31st `Real` axiom is a shortfall, not a tidier count) and on the **seven discrimination witnesses**, because 22 empty footprints look identical over an empty carrier, a total relation, an empty relation or a constant-zero product; presence is asserted before any footprint is read, since `axiom_footprint` of an interned-but-undeclared name is empty. Verified by mutation: disabling the `Eq Real` rewrite makes the kernel refuse the model outright (`DeclarationValueMismatch`), dropping one law fails the coverage check, an undeclared guard name fails the vacuity check, and falsifying the restated flag kills **exactly one** unit test. This discharges ADR-0456's "`Int` is not ℝ" caveat; it does **not** give `Eq CReal` as real-number equality, which is what would cost `Quot.sound`. |
| [`reconstruct_lean_certificate`](../../crates/axeyum-solver/examples/reconstruct_lean_certificate.rs) | `<file.cnf>` | DIMACS → DRAT → LRAT → Alethe → compact resolution reconstruction, emitting an externally checkable Lean certificate of a refutation. |
| [`reconstruct_ceiling_probe`](../../crates/axeyum-solver/examples/reconstruct_ceiling_probe.rs) | `<file.cnf>` | Characterises the ceiling of inlined resolution reconstruction on one instance. A measurement of where the route stops, not a verdict. |
| [`reconstruct_differential_probe`](../../crates/axeyum-solver/examples/reconstruct_differential_probe.rs) | `<file.cnf>` | Runs one instance through both inlined and compact clausal reconstruction and reports the difference. |
| [`kernel_equivalence`](../../crates/axeyum-fp/examples/kernel_equivalence.rs) | `[claim-name]` | Decides floating-point kernel-equivalence claims two independent ways — axeyum's SMT front door and an exhaustive `rustc_apfloat` enumeration sharing no code with the bit-blaster, CNF encoder or SAT core. Exhaustive at fp8/binary16 and (for binary32) 2^32 in ~51 s; ternary claims are already at the wall at 8 bits. |
| [`emit_telescoping_certificates`](../../crates/axeyum-cas/examples/emit_telescoping_certificates.rs) | no arguments | Writes `artifacts/cas-certificates/*.json` — Zeilberger certificates, emitted only through the checker so an unverifiable one cannot be published. |
| [`emit_geometry_certificates`](../../crates/axeyum-cas/examples/emit_geometry_certificates.rs) | no arguments | Writes `artifacts/geometry-certificates/*.json` — Nullstellensatz cofactor certificates with their non-degeneracy conditions and degenerate counterexamples. |
| [`emit_sos_certificates`](../../crates/axeyum-cas/examples/emit_sos_certificates.rs) | `[<id> ...] [--check]` | Writes `artifacts/sos-certificates/*.json` — sum-of-squares certificates, each re-derived and checked *before* it is written, so a certificate this binary could not itself re-derive never reaches disk looking like evidence. `--check` writes nothing and treats a differing file as an error, which is how a gate asserts the committed artifacts still match the corpus they came from. |
| [`sos_certify`](../../crates/axeyum-cas/examples/sos_certify.rs) | `<file.json> [--expect-kind KIND] [--expect-id ID] [--expect-checks N] [--expect-rate NUM/DEN] [--show]` | Re-derives one committed sum-of-squares certificate from its artifact file, rebuilding the Lie derivatives, norm, monomial basis and moment matrix here rather than reading them — only the system, the candidate functions and the squares come from the file. Every `--expect-*` flag is an assertion about content and a mismatch fails the run; a run that discharges no obligation is a failure, not a pass, so `--expect-checks` catches a certificate quietly shrinking. |
| [`telescoping_search_cost`](../../crates/axeyum-cas/examples/telescoping_search_cost.rs) | no arguments | Search-versus-check cost per identity. Reproduces the derived-degree-bound speedup (Chu-Vandermonde 18.5 s to 48.9 ms) and shows which sums still decline. |
| [`geometry_probe`](../../crates/axeyum-cas/examples/geometry_probe.rs) | `[budget-scale] [theorem-name]` | Per-condition-subset cost of a geometry certification, tracked and untracked, so you can see where the Gröbner route stops. Skips the theorems it does not return on (`euler-line`, `pappus-hexagon` — both reached by the linear route instead) and prints how many it skipped, since an instrument that silently examines nothing reads exactly like one that found nothing; pass an explicit id to probe one anyway. `AXEYUM_MONOMIAL_ORDER=grevlex` selects the order. |
| [`geometry_order_audit`](../../crates/axeyum-cas/examples/geometry_order_audit.rs) | `[theorem-name]...` | Runs **every** non-degeneracy condition subset under both monomial orders, then compares the two certificates byte for byte. Answers the only question that makes changing the default order dangerous: whether a faster order lands a certificate on a *different* condition set, since those conditions are hypotheses in the facts' `formal.statement`. Also reports whether every subset was *decided*, which is what makes a reported condition set minimal absolutely rather than relative to the budget. Minutes, not seconds — `lex` pays the full cost of the theorems it cannot reach. |
| [`geometry_obstruction`](../../crates/axeyum-cas/examples/geometry_obstruction.rs) | `<theorem-name> [max-pairs]` | Why a reduction does not return, as a growth curve rather than a duration. Runs a ladder of S-pair ceilings and reports pairs processed and still queued, pairs that reduced to zero, pairs the product criterion would skip, basis size and widest intermediate polynomial. Use a theorem that *finishes* as the control — the numbers only mean something in comparison. |
| [`geometry_linear_route`](../../crates/axeyum-cas/examples/geometry_linear_route.rs) | `[theorem-id]...` | The like-for-like counters (S-pairs processed/queued, basis size) for the linear-elimination route against `geometry_obstruction`'s Gröbner numbers, plus the block decomposition, determinant, and non-degeneracy power the elimination found. This is the route that certifies `euler-line` in 4–6 ms where Buchberger does not return in 27 minutes; a zero residue is short-circuited rather than handed to a reduction, since reducing the zero polynomial still computes a full Gröbner basis and hangs on `euler-line`. |
| [`geometry_cofactor_routes`](../../crates/axeyum-cas/examples/geometry_cofactor_routes.rs) | `[--buchberger] [--patient] [theorem-id]...` | Bounded-degree linear algebra against Buchberger on the residues the linear-elimination route hands over, per condition subset, together with which blocks each subset **licenses**. The Buchberger column is opt-in because on `pappus-hexagon` it does not come back: the same 48-term residue that `cofactor_ansatz` settles at cofactor degree 2 in ~25 ms with `±1` coefficients was killed at 7.5 minutes on the Gröbner route without returning. |
| [`pappus_condition_subsets`](../../crates/axeyum-cas/examples/pappus_condition_subsets.rs) | `[prime]... [--full]` | Decides exhaustively over `F_p`, for Pappus's hexagon theorem, which non-degeneracy condition subsets admit a **refuting** configuration — the question a certificate cannot answer, since a certificate proves only that its condition set suffices. Reads the hypothesis, condition and conclusion polynomials out of the committed corpus and reduces them mod `p`, so it cannot agree with a formula the corpus does not hold; enumerates the first carrier triple up to the affine group, with `--full` as the control on that reduction. |
| [`int_theorem_inventory`](../../crates/axeyum-lean-kernel/examples/int_theorem_inventory.rs) | `[name-substring]` | Every theorem the Int prelude admits with its canonical type — the Int counterpart of `nat_theorem_inventory`, and how you check which of the original 34 integer axioms are now derived. |
| [`theorem_dependency_inventory`](../../crates/axeyum-lean-kernel/examples/theorem_dependency_inventory.rs) | `[name-substring]` | Per admitted theorem, the theorems its statement and proof DIRECTLY use — the half of the constant closure `axiom_footprint` discards. Makes a kernel-route fact's `depends_on` derivable rather than transcribed; `scripts/check-fact-depends-derived.py` requires the ledger to agree with it. |
| [`theorem_knowledge_audit`](../../crates/axeyum-lean-kernel/examples/theorem_knowledge_audit.rs) | `ROOT [--fixture chain-clean\|chain-premise-leak\|chain-direct-leak\|chain-indirect-leak] [--same-type-as NAME] [--require NAME] [--deny NAME] [--expect-axiom-free]` | Fail-closed leakage audit over the kernel-derived **transitive** declaration closure of an admitted theorem. A run that hides theorems from proof search while keeping them in the environment can still have a proposed proof reach a withheld theorem, directly or through a helper; this builds the fixture, admits the candidate, and then requires every `--deny` name absent from its closure, every `--require` name present, the canonical type equal to `--same-type-as`'s, and — under `--expect-axiom-free` — an empty footprint. An unknown root, an unknown policy name, or any miss exits non-zero. The three leak fixtures are what make a clean pass mean something: `chain-premise-leak` proves the premise by `Nat.zero_add` instead of by induction, `chain-direct-leak` proves the candidate by `Nat.mul_one`, and `chain-indirect-leak` reaches `Nat.mul_one` through a helper. An audit only ever run against `chain-clean` has never been shown to fire. |
| [`infeasibility_iis`](../../crates/axeyum-solver/examples/infeasibility_iis.rs) | `<instance.smt2> [--expect-core N]` | Extracts an unsat core and MEASURES irreducibility by re-solving every leave-one-out subset, replaying each returned model through the IR evaluator. `unknown` is treated as failure, never as a pass. |
| [`infeasibility_farkas_lean`](../../crates/axeyum-solver/examples/infeasibility_farkas_lean.rs) | `<instance.smt2>` | Takes an LRA core to a Farkas certificate and on into the Lean kernel. Since 2026-08-15 `prove_unsat_to_lean_module` DOES reach the genuine reconstructor for a conjunctive real core (it used to emit a structural attestation containing no arithmetic); the example keeps two independent shim detectors and fails if they disagree, because a detector only exercised while it reports the bad case is one nobody notices going blind. |
| [`ordered_ring_refutation`](../../crates/axeyum-solver/examples/ordered_ring_refutation.rs) | `[--require-empty\|--dump-statement\|--footprint-table\|--constructed-reals]`, requires `--features full` | Generalizes an LRA/Farkas or SOS refutation over the ordered-ring interface, λ-abstracting the 30 `Real` constants out of the proof term so the generalized theorem's `Kernel::axiom_footprint` is measured **empty** — the laws become the theorem's own hypotheses rather than assumptions — and re-applies it to the `Real` constants to recover the original, kernel-rechecked statement. Prints the non-empty original footprint alongside the empty generalized one as a negative control in the same output. Also reconstructs each fixture a second time with equality routed through the **equality slot** (ADR-0512 phase R3), generalizes it over **39** binders instead of 30, and specializes that back at `Eq`: the 30 non-slot binder types must come back node for node, and the setoid proof must mention no kernel `Eq` constant. `--require-empty` fails on any of those. `--constructed-reals` runs every fixture a second time over the **constructed** reals (`CRealPrelude`, ADR-0512) with the equality slot *adopted* from `CReal.Equiv` rather than axiomatized — measured 0 declarations added against 18 for the `Real` route — and prints the carrier axioms the closed `False` rests on each way: 30 over `Real`, **0** over `CReal`. |
| [`front_door_carrier`](../../crates/axeyum-solver/examples/front_door_carrier.rs) | `[--require-axiom-free]`, requires `--features full` | Measures the SHIPPED front door rather than a bespoke context: runs three infeasible queries through `prove_unsat_to_lean_module` and reports which carrier the emitted module names, then reconstructs the same queries both ways and prints `Kernel::axiom_footprint` of the closed `False`. Footprint / of which CARRIER: **15/12, 22/17, 10/8** over the axiomatized `Real` package against **3/0, 5/0, 2/0** over the constructed reals (`CReal`, ADR-0512) — the residue being the query's own variable and hypothesis axioms. Both carriers are measured every run, because "zero carrier axioms over `CReal`" is only a result if the `Real` column is non-empty; an empty one means the measurement broke, and `--require-axiom-free` fails on it. Also checks the module's `axiom` lines equal the kernel footprint, so Lean's `#print axioms` and `Kernel::axiom_footprint` cannot disagree. Reports module size as the price: 2.4-41 kB to ~2.6 MB. |
| [`shared_prelude_module`](../../crates/axeyum-solver/examples/shared_prelude_module.rs) | requires `--features full` | Measures the **split module layout** against the shipped front door's own fixtures. A refutation over the constructed reals renders to ~1.3 MB where the axiomatized `Real` package renders ~8 KB — and the refutation's own theorem term is **4,193 bytes of that, 0.16%**. The rest is the ℕ/ℤ/ℚ/setoid development, identical for every query over that carrier, so emitting it once and `import`ing it takes the per-query half to 5,056 / 14,567 / 1,954 bytes (**257x / 91x / 738x**). Reports both halves and the Lean check time for each. ADR-0511. |
| [`proof_keyword_cost`](../../crates/axeyum-solver/examples/proof_keyword_cost.rs) | requires `--features full` | Renders every shipped artefact **both** ways — proofs spelled `theorem` and spelled `def` — so the cost and the benefit of `Kernel::set_render_proofs_as_def` are read off one run. Lean's two checkers disagree about a proof's opacity (ADR-0517): its *kernel* accepts the whole constructed-real carrier, while its *elaborator* refuses to unfold a `theorem` while reducing and so rejects four `CReal` declarations whose type-checking must compute through `Nat.gcd`. The measurement behind ADR-0518's decision NOT to flip the default: 1.36-1.69x elaboration for nothing on the shipped surface, since both shipped artefacts already elaborate clean. |
| [`ring_interface_pin`](../../crates/axeyum-solver/examples/ring_interface_pin.rs) | `[--require-identical]`, requires `--features full` | Pins the ordered-ring interface as a **30-binder telescope** instead of 30 axioms, and measures that the two say the same thing. `real: axiom=30` is the whole remaining trusted surface, and [ADR-0509](../research/09-decisions/adr-0509-the-trusted-surface-is-measured-as-reached-not-only-declared.md) records the two reasons the package is retained: it is the *specification*, and it is the *negative control* for every axiom-freedom measurement here. This example discharges the first. `ring_interface_telescope` computes the same 30-`∀` prefix `generalize_over_ordered_ring` produces — from the signature's declaration types alone, with no refutation — read off the **axiom-free** `Int` development, so the interface is stated in the kernel while assuming nothing. `--require-identical` makes the exit status depend on the two telescopes being the same statements, which is the only thing that turns it into a discharge rather than a coincidence. |
| [`proof_gap_shape_census`](../../crates/axeyum-smtlib/examples/proof_gap_shape_census.rs) | `<file.smt2>...` | Emits source-syntax and reachable parsed-IR censuses. This is diagnostic data, not a solver verdict. |
| [`probe_selected_evidence_lean`](../../crates/axeyum-bench/examples/probe_selected_evidence_lean.rs) | `<file.smt2>...` | Tests whether already-selected evidence reconstructs through an existing Lean route, avoiding query-only proof re-derivation. |
| [`cq_containment_certify`](../../crates/axeyum-bench/examples/cq_containment_certify.rs) | `<file.cq> [--expect-checks N] [--show-certificates] [--verify-formal <file.smt2>] [--timeout SECONDS]` | Decides conjunctive-query containment `Q₁ ⊆ Q₂` — the reasoning behind view reuse and answering queries using views — by three routes that share no search: backtracking over the frozen domain, the solver on a one-hot Boolean encoding, and complete unpruned evaluation of both queries over the frozen database. Nothing is reported without a Chandra–Merlin homomorphism that `check_homomorphism` has already replayed, and a `sat` model is additionally replayed against its encoding by the IR evaluator. The routes must agree; disagreement is a failure, not a tie-break. The complete-evaluation leg is what makes a *negative* answer an exhibited counterexample rather than a search that gave up. |
| [`db_design_certify`](../../crates/axeyum-bench/examples/db_design_certify.rs) | `<file.dbd> [--expect-checks N] [--verify-formal <file.smt2>] [--timeout SECONDS]` | Executes the `expect …` lines a committed relational-schema instance pins — functional-dependency implication, keys, BCNF/3NF, lossless join, dependency preservation — and refuses to report any answer without a certificate an independent checker has already replayed: an Armstrong derivation, a decoded two-row counterexample relation, or a replayed chase trace. Implication is decided twice by routes sharing no code (the attribute-closure fixpoint and the solver on a Boolean Horn encoding) and they must agree. It fails if the file pins nothing, if an expectation is unmet, or if fewer obligations ran than the file declares — a checker that exits `0` on completion alone checks nothing. |

For example:

```sh
cargo run -p axeyum-lean-import --example lean4export_import -- export.ndjson
cargo run -p axeyum-lean-kernel --example prelude_axiom_inventory
cargo run -p axeyum-smtlib --example proof_gap_shape_census -- query.smt2
```

## Artifact generators

These examples can write JSON, Markdown, DIMACS, or committed consumer reports.
Use scratch output paths first. If a command intentionally refreshes a tracked
artifact, start from a clean tree and review the complete diff.

| Example | Invocation contract | Purpose / mutation boundary |
|---|---|---|
| [`audit_dominance`](../../crates/axeyum-bench/examples/audit_dominance.rs) | `<baseline.json> [timeout_ms] [limit] [out.json]` | Re-runs baseline-decided rows through evidence and Lean reconstruction. Omitting `out.json` prints the report. |
| [`cnf_core_bench`](../../crates/axeyum-bench/examples/cnf_core_bench.rs) | `<cnf-dir> <out.json> [repetitions] [kissat]` | Compares byte-identical DIMACS inputs across fresh SAT cores; the optional external solver includes process startup. |
| [`cnf_stream_bench`](../../crates/axeyum-bench/examples/cnf_stream_bench.rs) | `<profile.jsonl> <snapshot-dir> <out.json> [repetitions] [timeout_ms]` | Replays captured append-only CNF streams through persistent BatSat and Z3. Requires `--features z3` and a usable Z3 link. |
| [`cvc5_qfbv_timeout_sweep`](../../crates/axeyum-bench/examples/cvc5_qfbv_timeout_sweep.rs) | `<corpus-root> <manifest> <cvc5> <out.json> [repetitions] [timeouts_csv]` | Runs a hash-bound QF_BV manifest through fresh cvc5 processes. |
| [`cvc5_smt_stream_bench`](../../crates/axeyum-bench/examples/cvc5_smt_stream_bench.rs) | `<trace-dir> <cvc5> <out.json> [repetitions] [timeout_ms] [cold-reset\|retained-lcp]` | Replays an ordered, hash-bound SMT stream through one cvc5 process. |
| [`dump_dimacs`](../../crates/axeyum-bench/examples/dump_dimacs.rs) | `<file.smt2> <out.cnf>` | Applies the benchmark preprocessing/lowering path and writes DIMACS for an external SAT diagnostic. |
| [`measure_corpus`](../../crates/axeyum-bench/examples/measure_corpus.rs) | `<dir> [timeout_ms] [out.json]` | Head-to-head Axeyum/system-Z3 corpus measurement. Requires a `z3` executable; use release mode. |
| [`measure_graduated`](../../crates/axeyum-bench/examples/measure_graduated.rs) | `<dir> [timeout_ms] [out.json]` | Measures a construction-labeled graduated corpus against Axeyum and system Z3. Requires a `z3` executable; use release mode. |
| [`property_corpus_scoreboard`](../../crates/axeyum-property/examples/property_corpus_scoreboard.rs) | `<json\|markdown> [out]` | Regenerates or prints the property consumer scoreboard. Omit `out` for a non-mutating preview. |
| [`measure_evm`](../../crates/axeyum-evm/examples/measure_evm.rs) | no arguments | Regenerates the committed EVM consumer scoreboard and corpus. It is not a read-only tour. |
| [`measure_verify`](../../crates/axeyum-verify/examples/measure_verify.rs) | no arguments | Regenerates the committed Rust-verification scoreboard and corpus. It is not a read-only tour. |
| [`recertify_rado`](../../crates/axeyum-search/examples/recertify_rado.rs) | `<claim-id> [artifact-root]` | Rechecks one committed Rado claim from its hash-bound witness or refutation artifact; it does not discover or strengthen claims. |
| [`sorting_network`](../../crates/axeyum-cnf/examples/sorting_network.rs) | `--n N --size K [--sym none\|first\|commute\|full\|max] [--drat] [--model]`, or `--sweep`, `--verify`, `--dimacs PATH`, `--cubes DIR [--depth D] [--jobs J] [--cube-sym none\|full\|subsume] [--keep-proofs]` | Decides `S(n)`, the optimal sorting-network size, as sat/unsat — no optimizer and no MaxSAT. Writes files only under `--dimacs` (one CNF) and `--cubes`, which streams one DRAT per branch and **deletes each after the backward checker accepts it** unless `--keep-proofs` is given (at `n = 7` a single branch's certificate reaches a gigabyte). `--verify` re-derives the committed `F:sorting-network-optimal-size-n{3,4,5,6}` facts; its `n = 6` leg is the monolithic 20-minute route, so prefer `--cubes` for a quick recheck. `--sweep` is the negative control against the published `S(n)`. Every `--sym`/`--cube-sym` setting weaker than the default must give the same verdict — that is the control against a wrong UNSAT. |
| [`auto_param_closure_probe`](../../crates/axeyum-lean-import/examples/auto_param_closure_probe.rs) | `<stream.ndjson> <root> [out.ndjson]` | Prints the normalized checked-closure report for one imported root and then lists the closure members that are trusted (`axiom`, `theorem`, `opaque`, `quotient`) rather than derived. The optional third argument re-renders that root closure back out as a lean4export NDJSON stream and writes it; omit the path and the run touches no file. |
| [`int_gcd_contract_residualization`](../../crates/axeyum-lean-import/examples/int_gcd_contract_residualization.rs) | `--stream <r018.ndjson> --output <observation.json>` | Writes one ADR-0489 residualization control observation for Mathlib `Int.gcd`. Pins the source toolchain (Lean 4.30.0 and its githash), the exact `Int.gcd` declaration SHA-256 and an empty imported axiom set; residualizes the body over `Nat.gcd` while retaining `Int` and `Int.natAbs`; admits a bounded reflexivity witness for the resulting source equation; and records that witness's axiom footprint and direct and transitive theorem dependencies. Each pin is checked before anything is written. It admits no contract and writes no fact-ledger row. |
| [`int_gcd_source_delta_trace`](../../crates/axeyum-lean-import/examples/int_gcd_source_delta_trace.rs) | `--stream <r018.ndjson> --output <observation.json>` | Writes the ADR-0490 one-source delta control over the same pinned `Int.gcd`: a single `selected-transparent-definition-delta-v1` step with its before/after expression digests, beside the proof-free residual template. Requires the one-step body constants to be exactly `Int`, `Int.natAbs` and `Nat.gcd`, and requires that neither `Int.gcd` nor `Nat.gcd` survives among the template's direct constants — a template still naming the function it generalized would make the contract vacuous. No contract admitted, no ledger write. |
| [`int_gcd_trace_contract_receipt`](../../crates/axeyum-lean-import/examples/int_gcd_trace_contract_receipt.rs) | `--stream <r018.ndjson> --output <observation.json>` | Issues one trace-backed **source-contract** receipt for the pinned `Int.gcd`, re-verifies it against the same kernel and instances in the same run, and writes it inside an observation envelope. The exit status depends on the receipt's content — schema and policy version, source/residual/retained names, function arity, contract binders, consulted declarations, an empty source axiom footprint, and a valid self-digest — rather than on the run completing. It proves no theorem and writes no ledger row. |
| [`int_gcd_contract_theorem_control`](../../crates/axeyum-lean-import/examples/int_gcd_contract_theorem_control.rs) | `--stream <r018.ndjson> --output <observation.json>` | The next seam along: re-issues the source-contract receipt whose SHA-256 is preregistered in the file, then issues and verifies a trace-backed **semantic theorem** receipt for `Axeyum.Autogenesis.IntGcdDef` through the `trace-contract-reflexivity-v1` producer, and writes it. Pins the producer's exact shape (2 binders, 5 constructed nodes), an empty theorem axiom footprint, and a valid digest. One preregistered calibration invocation: it establishes neither premise of `Int.gcd_fib` and grants no evaluation or ledger credit. |
| [`nat_prelude_composition_probe`](../../crates/axeyum-lean-import/examples/nat_prelude_composition_probe.rs) | `<stream.ndjson> [out.json]` | Measures whether the axiom-free native `Nat` library composes with a real import; prints the observation and writes it when the second argument is given. Classifies every native/imported name overlap as exact, alpha-type-compatible, kernel-type-shape-compatible or type-mismatched, and runs positive controls composing `Nat.add_comm`, `Nat.eq_one_of_dvd_one`, `Acc.inv` and a singleton-inductive `Exists` theorem into the imported kernel. The negative control on `Nat.dvd_gcd` must decline **and** leave the caller's environment SHA-256 unchanged: a failed composition that mutates the caller is worse than one that succeeds, and only the digest comparison can tell them apart. |
| [`semantic_abstraction_census`](../../crates/axeyum-lean-import/examples/semantic_abstraction_census.rs) | `--archive <dir> --observation <producer.json> [--output <census.json>]` | Kernel-backed census of the exact definition identities a type-slice producer abstracted. Re-imports each cited stream from `<archive>/streams/`, re-derives every definition's content digest, instantiated type and normalized trusted closure, and classifies its contract shape; the producer's own file supplies only the population to check. Fails unless exactly the registered 32 identities are found and every one is described. `--output` is opt-in — without it the run prints its digest and writes nothing. Generates no contract and no ledger row. |
| [`semantic_contract_target_census`](../../crates/axeyum-lean-import/examples/semantic_contract_target_census.rs) | `--archive <dir> --producer <producer.json> --semantic <census.json> --output <targets.json>` | Joins the `pointwise-function-equation` identities out of [`semantic_abstraction_census`](../../crates/axeyum-lean-import/examples/semantic_abstraction_census.rs) to statement-side contract demand and writes the row set, re-importing each stream to re-derive the identity rather than trusting the input file. Fails unless exactly 15 identities and 50 bindings are found. The eligibility it reports is narrow and the observation says so: every nonrecursive constant named directly by the transparent body is already retained in the proof-free slice — not that the equation is minimal, target-useful, or sufficient to prove the statement. |
| [`type_slice_replay`](../../crates/axeyum-lean-import/examples/type_slice_replay.rs) | `--archive <dir> --mapping <mapping.json> [--output <observation.json>] [--auto-param-v2 \| --auto-param-binders-v3 [--reflexivity-v1]]` | Checked replay of ADR-0484 type slices over the train and development partitions. Each mapped row is re-imported, put through the selected abstraction policy, and issued a type-slice receipt; `--reflexivity-v1` additionally runs the bounded reflexivity producer against a fresh goal. Every row is recorded with its outcome — accepted receipt, proof outcome, or the stage that declined — so a decline is counted rather than dropped, which is what makes the coverage totals mean anything. The two autoParam policies are mutually exclusive, `--reflexivity-v1` requires `--auto-param-binders-v3`, and `--output` is opt-in. |
| [`autogenesis_induction_plan_check`](../../crates/axeyum-lean-kernel/examples/autogenesis_induction_plan_check.rs) | `--plans <plans.tsv> --candidate NAME --budget N --expect proved\|no-proof --bundle-sha256 SHA --catalog-sha256 SHA [--evidence-output <evidence.tsv>]` | Interprets catalog-only Nat induction proposals in a fresh kernel process: the proposer runs outside and never sees proof bodies, and this rebuilds the prelude and gives each ranked plan a real admission attempt. The plan file's header must match the registered bundle, catalog and phase. `--expect` is mandatory and the exit status depends on it, so exhausting the budget without a proof is a failure when a proof was expected. `--evidence-output` requires `--expect proved` and writes a TSV record — candidate, canonical type, bundle/catalog digests, attempts, budget, accepted plan rank, axiom footprint, retained answer dependencies — opened `create_new`, so it never overwrites an existing evidence file. |
| [`autogenesis_apply_plan_check`](../../crates/axeyum-lean-kernel/examples/autogenesis_apply_plan_check.rs) | `--plans <plans.tsv> --phase pre_b\|pre_a\|post_b --candidate NAME --budget N --expect proved\|no-proof --bundle-sha256 SHA --catalog-sha256 SHA [--premise-candidate NAME --premise-plans <file> --premise-budget N --premise-bundle-sha256 SHA --premise-catalog-sha256 SHA] [--evidence-output <evidence.tsv>]` | The same contract for `apply-visible-theorem` proposals. `post_b` requires the complete premise plan identity and budget and the pre phases must not receive any of it, so a two-episode run cannot be reported as one. `pre_a` is the registered negative control, where **finding** a proof is the failure — which is why `--expect` is mandatory rather than a hint. Evidence is written only under `--phase post_b --expect proved`, opened `create_new`, and only after checking that the accepted proof's dependency closure uses exactly the episode premise. |

The generic release-mode form is:

```sh
cargo run --release -p axeyum-bench --example <name> -- <arguments>
```

Do not retain a measurement merely because the command exited successfully.
Apply the [benchmark artifact contract](../contributor-guide/benchmark-artifacts.md):
bind the corpus and solver revision, keep limits/configuration, check verdict
agreement and replay, and separate diagnostics from credited results.

## Maintainer diagnostics

The remaining examples answer one narrow implementation question. They may
encode frozen paths, schemas, budgets, or experiment assumptions and should not
be presented as general solver CLIs.

| Example | Arguments | Question answered |
|---|---|---|
| [`clause_estimate_attribution`](../../crates/axeyum-bench/examples/clause_estimate_attribution.rs) | `<frozen-target.smt2>...` | Which operators and demanded multiplier bits account for the frozen QF_NIA pre-lowering clause estimate? |
| [`diagnose_evidence`](../../crates/axeyum-bench/examples/diagnose_evidence.rs) | `<file.smt2> [timeout_ms]` | Did evidence production spend time in decision search or post-decision certificate attempts? |
| [`explain_corpus`](../../crates/axeyum-bench/examples/explain_corpus.rs) | `<dir> [timeout_ms] [--json]` or `--list <file> [timeout_ms] [--json]` | Which route decided or declined each file, with persistable route traces in JSON mode? |
| [`pbls_probe`](../../crates/axeyum-bench/examples/pbls_probe.rs) | `<file.smt2> [timeout_s]` | Can pure-Rust word-level local search find a witness for a SAT-search-bound QF_BV case? It does not prove UNSAT. |
| [`preprocess_timing`](../../crates/axeyum-bench/examples/preprocess_timing.rs) | `<file.smt2>` | Which word-level preprocessing pass dominates one large QF_BV input? |
| [`qf_abv_probe`](../../crates/axeyum-bench/examples/qf_abv_probe.rs) | `<file.smt2>...` | How much does eager array elimination grow the reachable DAG? |
| [`replay_refine_profile`](../../crates/axeyum-bench/examples/replay_refine_profile.rs) | `<file.smt2>` | What operator mix and AIG/CNF counters result from one replay-refined BV plan? Environment variables select diagnostic policies. |
| [`smtcomp_cli`](../../crates/axeyum-bench/examples/smtcomp_cli.rs) | `<benchmark.smt2> [--timeout-ms N]` | Exercises the competition-style single-query wrapper. It is an example harness, not the finished interactive SMT-LIB product surface. |
| [`uf_pair_profile`](../../crates/axeyum-bench/examples/uf_pair_profile.rs) | `<file.smt2> [sample_limit]` | What same-function application-pair shapes would a lazy Ackermann pre-seed see? |
| [`uf_unknown_probe`](../../crates/axeyum-bench/examples/uf_unknown_probe.rs) | `<file.smt2> [timeout_ms]` | What full typed `UnknownReason` is hidden by the competition wrapper's one-word output? |
| [`uflia_online_probe`](../../crates/axeyum-bench/examples/uflia_online_probe.rs) | `<file.smt2> [timeout_ms]` | How does the online EUF+LIA combination route classify one query? |
| [`xor_cdcl_probe`](../../crates/axeyum-bench/examples/xor_cdcl_probe.rs) | `<file.cnf>` | Does the in-tree CDCL(XOR) core move a small-CNF SAT-search case? |
| [`drat_memory_probe`](../../crates/axeyum-cnf/examples/drat_memory_probe.rs) | `<cnf> <proof>` | What is the peak resident size of each DRAT checking route on a real certificate (ADR-0426)? |
| [`prelude_build_timing`](../../crates/axeyum-lean-kernel/examples/prelude_build_timing.rs) | `[iterations]` (default 5) | How long does a fresh kernel take to build each prelude (`nat`, `logic`, `integer`, `real`, `string`)? Prints deterministic tab-separated `prelude/iteration/elapsed-micros` rows with no aggregation of its own — the prelude build is the kernel's own critical-path workload, exercising every reduction cache in the type checker, so a WHNF-cache-key change is priced here rather than estimated. |
| [`relation_goal_probe`](../../crates/axeyum-lean-import/examples/relation_goal_probe.rs) | `<stream.ndjson> <target-definition>` | What relation does a proof-isolated Mathlib goal actually terminate in? Strips the target definition's `Pi` binders, reduces the body to whnf, and prints one JSON line carrying the binder count, the head constant before and after reduction, and the rendered reduced goal. Refuses a stream that carries axioms or a target that is not a monomorphic definition; submits nothing to the kernel and writes nothing. |
| [`euclidean_dependency_footprint_audit`](../../crates/axeyum-lean-import/examples/euclidean_dependency_footprint_audit.rs) | `<stream.ndjson>` | Which declarations does the FAILED Euclidean proof actually rest on? Reads the proof-bearing stream once and prints declaration identities, the preregistered direct theorem dependencies, and axiom footprints. Deliberately narrow: it never renders an expression or a theorem value, so it cannot be used to read the proof out of the stream. Prints only; writes nothing. |
| [`euclidean_bounded_dependency_footprint_audit`](../../crates/axeyum-lean-import/examples/euclidean_bounded_dependency_footprint_audit.rs) | `<stream.ndjson>` | The same audit for the bounded-induction Euclidean proof, over its own sealed stream. Same restriction — identities, dependencies and axiom footprints only, no expressions, no theorem values. Prints only; writes nothing. |
| [`theorem_footprint_batch_audit`](../../crates/axeyum-lean-import/examples/theorem_footprint_batch_audit.rs) | `<stream.ndjson> <root>…` | Audits an ordered batch of theorem roots from one Lean export stream. Imports once and emits declaration identities, direct theorem dependencies and axiom footprints. Read-only by construction: no expressions, no theorem values, nothing submitted to the kernel. |
| [`coprime_target_support_audit`](../../crates/axeyum-lean-import/examples/coprime_target_support_audit.rs) | `<stream.ndjson>` | The same audit narrowed to the three preregistered target-side coprime-cancellation roots. Prints only; writes nothing. |
| [`euclidean_public_equation_carrier_audit`](../../crates/axeyum-lean-import/examples/euclidean_public_equation_carrier_audit.rs) | `<stream.ndjson>` | Audits one dependency layer beneath the two public Euclidean equations, over the sealed stream. Prints only; writes nothing. |
| [`official_gcd_balanced_bezout_composition`](../../crates/axeyum-lean-import/examples/official_gcd_balanced_bezout_composition.rs) | `<stream.ndjson>` | Closes the generic official-gcd balanced-Bézout theorem from independently checked pieces, without crossing the measured native/official `WellFounded` representation boundary. Submits to the kernel and reports what it admitted; writes no file. |
| [`official_coprime_factor_cancellation_composition`](../../crates/axeyum-lean-import/examples/official_coprime_factor_cancellation_composition.rs) | `<stream.ndjson>` | Closes official coprime-factor cancellation from independently checked pieces. `include!`s the balanced-Bézout example above rather than duplicating it, so both are Cargo targets and that shared body is compiled twice — read the two rows together. |
| [`statement_reflexivity_operation`](../../crates/axeyum-lean-import/examples/statement_reflexivity_operation.rs) | `<export.ndjson> <target-definition>` | Does the bounded definitional-reflexivity producer settle one imported statement, and does an independent kernel accept the result? Proposes a candidate under the shared 8-binder / 16-node budget, admits it as a theorem, and then audits it: a candidate whose dependency closure reaches the target definition, or whose axiom footprint or theorem dependencies are non-empty, exits non-zero. Proving the target *from* the target is exactly what that audit exists to catch. |
| [`statement_reflexivity_coverage`](../../crates/axeyum-lean-import/examples/statement_reflexivity_coverage.rs) | `<artifact-directory> <mapping.json>` | Across a mapped population, where does that same route stop? Runs all 138 mapped targets through the statement adapter and the producer, classifying each as an adapter rejection, a producer decline, a kernel rejection, an assurance rejection or an admissible proof, each with a stable reason string, and prints the JSON observation. Refuses a mapping of any other size, a row outside the train/development partitions, or an artifact path that is not a bare file name. Prints only; it grants no ledger credit. |

### Rado-family campaign drivers

These seven encode a specific research campaign — `R_k(a(x-y) = bz)` — and carry
frozen instance parameters, output paths, and budget assumptions from it. They
are the most experiment-specific examples in the tree; read the file's own header
before running one, and do not treat any of them as a general search CLI.

| Example | Arguments | Question answered |
|---|---|---|
| [`akb2_frontier`](../../crates/axeyum-search/examples/akb2_frontier.rs) | see file header | Frontier driver for `R_k(a(x-y) = bz)` at `k >= 4` — the `a^k` line of Chang, De Loera and Wesley (ISSAC 2022, arXiv:2210.03262). |
| [`rado_adaptive_cover`](../../crates/axeyum-search/examples/rado_adaptive_cover.rs) | see file header | Adaptive cube-cover driver, built for `F_741`, for instances where a flat cover stops paying. |
| [`rado_certify_tree_cover`](../../crates/axeyum-search/examples/rado_certify_tree_cover.rs) | see file header | Offline certification of a dumped tree cover: reads every proof the search run deferred. |
| [`rado_cover_gaps`](../../crates/axeyum-search/examples/rado_cover_gaps.rs) | see file header | What a partial tree cover has NOT covered, written as a resumable pending file. |
| [`rado_dump_cnf`](../../crates/axeyum-search/examples/rado_dump_cnf.rs) | see file header | Writes the deciding CNF from the encoder the cover actually used, so a ledger's `unsat` can be re-derived rather than believed. |
| [`rado_replay_tree_cover`](../../crates/axeyum-search/examples/rado_replay_tree_cover.rs) | see file header | Independent re-validation of a tree cover from its ledger alone, for runs that checked inline and kept no DRAT bytes. |
| [`rado_sat_probe`](../../crates/axeyum-search/examples/rado_sat_probe.rs) | see file header | Bounded satisfiable-side probe. At `n = 741, k = 4` the expected outcome is that nothing is found. |

### Shared example-local modules

These four files are tracked under `examples/` but Cargo does not build them as
example targets: they are `mod`-included by the examples beside them, so there is
nothing to run. They are listed here because the catalogue inventories every
checked-in file under `examples/`, not only the runnable targets.

| File | Included by | What it provides |
|---|---|---|
| [`statement_reflexivity_support/mod.rs`](../../crates/axeyum-lean-import/examples/statement_reflexivity_support/mod.rs) | `statement_reflexivity_operation`, `statement_reflexivity_coverage`, `type_slice_replay`, `int_gcd_contract_residualization`, and two adversarial test suites | The untrusted bounded reflexivity proposer `propose_reflexivity` and its two budgets, `MAX_BINDERS = 8` and `MAX_CONSTRUCTED_NODES = 16`. It proposes and the kernel decides: it is deliberately outside the trusted surface, which is why the adversarial tests share this exact code rather than a copy of it. |
| [`autogenesis_support/mod.rs`](../../crates/axeyum-lean-kernel/examples/autogenesis_support/mod.rs) | `autogenesis_induction_plan_check`, `autogenesis_apply_plan_check` | Fixture-local parsers and search loops for the Autogenesis proposal TSVs (`parse_induction_plans`, `search_induction`). Deliberately kept below a public proof-plan IR: it exercises one small grammar without adding a crate API or claiming the Phase-3 design decision. |
| [`support/fib_coprime.rs`](../../crates/axeyum-lean-import/examples/support/fib_coprime.rs) | `nat_gcd_succ_specialization` | The bounded Fibonacci-neighbour coprimality proof-term constructor. It builds a candidate term; the kernel decides it. Untrusted by construction, like the reflexivity proposer above. |
| [`support/fib_gcd_shift.rs`](../../crates/axeyum-lean-import/examples/support/fib_gcd_shift.rs) | `nat_gcd_succ_specialization` | Fixed support constructors for the Fibonacci gcd-shift operation, plus the canonical declaration/expression SHA-256 pins that bind them to their source. |

## Inventory and validation

The authoritative target list comes from Cargo metadata, including required
features:

```sh
cargo metadata --no-deps --format-version 1 |
  jq -r '.packages[] as $p | $p.targets[] |
    select(.kind | index("example")) |
    [$p.name, .name, ((.["required-features"] // []) | join(","))] | @tsv' |
  sort
```

`cargo test --workspace --all-targets --all-features --no-run` compiles the
complete target population without executing artifact-producing `main`
functions. The repository's full pre-merge and hosted CI gates include
all-target compilation; a focused documentation edit should not rerun every
measurement example.
